CR-R1 independent cross-check v3 -- buckets by EXPANDABILITY (A brace / B ellipsis / C glob)
counterpart of tools/probes/cited-evidence-whitelist.ps1 (CR-F2).  Generated by evidence-whitelist-crosscheck.py

===== A. scope=all  (the whole cehua/ tree) =====
scope            = all  (32 file(s))
STEP1 path refs  = 193  (literal exist 170, literal MISSING 13 | A expandable 4, B ellipsis 0, C glob 4)
STEP2 bare refs  = 217  (evidence 152, elsewhere 11, unresolved 42, ambiguous 12, pattern-bare 0)
WHITELIST (uniq) = 290   -> tools\probes\CR-R1-crosscheck-whitelist.txt

--- FAIL: literal path absent from disk ---
  .ai-tmp/screenshots/AN1.index.tsv  (cited 2x, first �߻�\���ձ�.md:541)
  .ai-tmp/screenshots/AP2-  (cited 3x, first �߻�\����Ա�\AP2-����.md:3)
  .ai-tmp/screenshots/R1-seg-  (cited 2x, first �߻�\��λ֡�α�.md:3061)
  .ai-tmp/screenshots/hist  (cited 1x, first �߻�\����Ǽ�.tsv:105)
  .ai-tmp/test/AA1-���ձ���.md  (cited 1x, first �߻�\����Ǽ�.tsv:75)
  .ai-tmp/test/AM1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:694)
  .ai-tmp/test/AM1b-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:697)
  .ai-tmp/test/AM2-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:658)
  .ai-tmp/test/AP2-probe-0..4.txt  (cited 1x, first �߻�\����Ա�\AP2-����.md:7)
  .ai-tmp/test/AW1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:620)
  .ai-tmp/test/BC1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:635)
  .ai-tmp/test/CR-T2f-frames.png  (cited 1x, first �߻�\����Ǽ�.tsv:111)
  .ai-tmp/test/smlbiobot-cr  (cited 1x, first �߻�\��λ������������.md:309)

--- HUMAN: bucket B (ellipsis: NOT mechanically expandable; candidates kept, never deleted) ---
  (none)

--- HUMAN: bucket A/C expansions (mechanically expanded; these names are whitelisted) ---
  .ai-tmp/screenshots/CR-T3-re-{00-baseline,01-ghost-legal,01-ghost-illegal,contact-sheet}.png  (cited 1x) -> CR-T3-re-00-baseline.png, CR-T3-re-01-ghost-legal.png, CR-T3-re-01-ghost-illegal.png, CR-T3-re-contact-sheet.png
  .ai-tmp/screenshots/Y2-{login,mainmenu,deckedit,hud}.png  (cited 1x) -> Y2-login.png, Y2-mainmenu.png, Y2-deckedit.png, Y2-hud.png
  .ai-tmp/test/AV1-census{,2,3,4}.tsv  (cited 3x) -> AV1-census.tsv, AV1-census2.tsv, AV1-census3.tsv, AV1-census4.tsv
  .ai-tmp/test/v2-console-{1,2,3,5}.json  (cited 2x) -> v2-console-1.json, v2-console-2.json, v2-console-3.json, v2-console-5.json
  .ai-tmp/test/E2-D-*.npy  (cited 1x) -> ?
  .ai-tmp/test/g2-audit-*.txt  (cited 1x) -> ?
  .ai-tmp/test/p1-g1-audit-step*.txt  (cited 1x) -> ?
  .ai-tmp/test/t4-desc-*.tsv  (cited 1x) -> ?

--- HUMAN: unresolved bare names (NOT a verdict) ---
  -after.txt                       (cited 1x)
  -compare.png                     (cited 1x)
  -step9-Result.txt                (cited 1x)
  ..._blue_idle1.png               (cited 1x)
  ..._enemy_attack1.png            (cited 1x)
  ..._enemy_run1.png               (cited 1x)
  .index.tsv                       (cited 1x)
  09-21-all.log                    (cited 2x)
  09-21.log                        (cited 1x)
  09-22-all.log                    (cited 3x)
  09-22.log                        (cited 1x)
  09-23.log                        (cited 1x)
  2019_Loading_Screen.png          (cited 2x)
  _1320X2868.jpg                   (cited 1x)
  _sprite_532.png                  (cited 1x)
  _sprite_592.png                  (cited 1x)
  _sprite_NNN.png                  (cited 8x)
  _sprite_NNNN.png                 (cited 1x)
  _tex.png                         (cited 12x)
  AG2-matrix-D8.tsv                (cited 1x)
  AL3-deck-8of8.png                (cited 9x)
  AL3-long-Room.png                (cited 2x)
  AT2-walk.png                     (cited 1x)
  Camp.png                         (cited 2x)
  Clan.png                         (cited 1x)
  coverage-audit.md                (cited 2x)
  crowns.png                       (cited 2x)
  Debug.Log                        (cited 1x)
  demo.md                          (cited 1x)
  Epic_Sunday_Shop.png             (cited 1x)
  frame_NNN.png                    (cited 5x)
  ID.png                           (cited 1x)
  Miner-free-chest-576x1024.jpg    (cited 1x)
  Profile_Screenshot.png           (cited 2x)
  Quests_Menu.png                  (cited 1x)
  raw.txt                          (cited 1x)
  README.md.txt                    (cited 1x)
  Royale01022022-071826-52305.jpg  (cited 1x)
  tex.png                          (cited 1x)
  Title.TXT                        (cited 1x)
  triage.md                        (cited 2x)
  unit_cs.tsv                      (cited 1x)

--- HUMAN: ambiguous bare names -- all in-zone candidates whitelisted ---
  frame_000.png            (cited 104x, 52 candidates)
  frame_008.png            (cited 2x, 47 candidates)
  frame_015.png            (cited 1x, 46 candidates)
  frame_028.png            (cited 3x, 42 candidates)
  frame_050.png            (cited 2x, 41 candidates)
  frame_080.png            (cited 2x, 39 candidates)
  frame_125.png            (cited 1x, 34 candidates)
  frame_188.png            (cited 1x, 33 candidates)
  frame_206.png            (cited 1x, 32 candidates)
  frame_418.png            (cited 2x, 12 candidates)
  frame_440.png            (cited 2x, 11 candidates)
  README.md                (cited 6x, 5 candidates)

===== B. scope=f2  (the other tool 4 ledgers) =====
scope            = f2  (4 file(s))
STEP1 path refs  = 126  (literal exist 115, literal MISSING 6 | A expandable 4, B ellipsis 0, C glob 0)
STEP2 bare refs  = 84  (evidence 53, elsewhere 5, unresolved 19, ambiguous 7, pattern-bare 0)
WHITELIST (uniq) = 158   -> tools\probes\CR-R1-crosscheck-whitelist.txt

--- FAIL: literal path absent from disk ---
  .ai-tmp/screenshots/AN1.index.tsv  (cited 2x, first �߻�\���ձ�.md:541)
  .ai-tmp/test/AM1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:694)
  .ai-tmp/test/AM1b-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:697)
  .ai-tmp/test/AM2-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:658)
  .ai-tmp/test/AW1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:620)
  .ai-tmp/test/BC1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:635)

--- HUMAN: bucket B (ellipsis: NOT mechanically expandable; candidates kept, never deleted) ---
  (none)

--- HUMAN: bucket A/C expansions (mechanically expanded; these names are whitelisted) ---
  .ai-tmp/screenshots/CR-T3-re-{00-baseline,01-ghost-legal,01-ghost-illegal,contact-sheet}.png  (cited 1x) -> CR-T3-re-00-baseline.png, CR-T3-re-01-ghost-legal.png, CR-T3-re-01-ghost-illegal.png, CR-T3-re-contact-sheet.png
  .ai-tmp/screenshots/Y2-{login,mainmenu,deckedit,hud}.png  (cited 1x) -> Y2-login.png, Y2-mainmenu.png, Y2-deckedit.png, Y2-hud.png
  .ai-tmp/test/AV1-census{,2,3,4}.tsv  (cited 2x) -> AV1-census.tsv, AV1-census2.tsv, AV1-census3.tsv, AV1-census4.tsv
  .ai-tmp/test/v2-console-{1,2,3,5}.json  (cited 2x) -> v2-console-1.json, v2-console-2.json, v2-console-3.json, v2-console-5.json

--- HUMAN: unresolved bare names (NOT a verdict) ---
  -after.txt                       (cited 1x)
  -step9-Result.txt                (cited 1x)
  .index.tsv                       (cited 1x)
  09-21-all.log                    (cited 1x)
  09-21.log                        (cited 1x)
  09-22-all.log                    (cited 2x)
  09-22.log                        (cited 1x)
  09-23.log                        (cited 1x)
  _sprite_532.png                  (cited 1x)
  _sprite_592.png                  (cited 1x)
  _sprite_NNNN.png                 (cited 1x)
  _tex.png                         (cited 2x)
  AG2-matrix-D8.tsv                (cited 1x)
  AL3-deck-8of8.png                (cited 9x)
  AL3-long-Room.png                (cited 2x)
  coverage-audit.md                (cited 1x)
  Debug.Log                        (cited 1x)
  triage.md                        (cited 2x)
  unit_cs.tsv                      (cited 1x)

--- HUMAN: ambiguous bare names -- all in-zone candidates whitelisted ---
  frame_000.png            (cited 104x, 52 candidates)
  frame_015.png            (cited 1x, 46 candidates)
  frame_028.png            (cited 2x, 42 candidates)
  frame_050.png            (cited 2x, 41 candidates)
  frame_418.png            (cited 2x, 12 candidates)
  frame_440.png            (cited 2x, 11 candidates)
  README.md                (cited 1x, 5 candidates)

===== NEGATIVE TEST 1: unresolved bare name =====
scope            = all  (32 file(s))
STEP1 path refs  = 193  (literal exist 170, literal MISSING 13 | A expandable 4, B ellipsis 0, C glob 4)
STEP2 bare refs  = 218  (evidence 152, elsewhere 11, unresolved 43, ambiguous 12, pattern-bare 0)
WHITELIST (uniq) = 290   -> tools\probes\CR-R1-crosscheck-whitelist.txt

--- FAIL: literal path absent from disk ---
  .ai-tmp/screenshots/AN1.index.tsv  (cited 2x, first �߻�\���ձ�.md:541)
  .ai-tmp/screenshots/AP2-  (cited 3x, first �߻�\����Ա�\AP2-����.md:3)
  .ai-tmp/screenshots/R1-seg-  (cited 2x, first �߻�\��λ֡�α�.md:3061)
  .ai-tmp/screenshots/hist  (cited 1x, first �߻�\����Ǽ�.tsv:105)
  .ai-tmp/test/AA1-���ձ���.md  (cited 1x, first �߻�\����Ǽ�.tsv:75)
  .ai-tmp/test/AM1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:694)
  .ai-tmp/test/AM1b-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:697)
  .ai-tmp/test/AM2-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:658)
  .ai-tmp/test/AP2-probe-0..4.txt  (cited 1x, first �߻�\����Ա�\AP2-����.md:7)
  .ai-tmp/test/AW1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:620)
  .ai-tmp/test/BC1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:635)
  .ai-tmp/test/CR-T2f-frames.png  (cited 1x, first �߻�\����Ǽ�.tsv:111)
  .ai-tmp/test/smlbiobot-cr  (cited 1x, first �߻�\��λ������������.md:309)

--- HUMAN: bucket B (ellipsis: NOT mechanically expandable; candidates kept, never deleted) ---
  (none)

--- HUMAN: bucket A/C expansions (mechanically expanded; these names are whitelisted) ---
  .ai-tmp/screenshots/CR-T3-re-{00-baseline,01-ghost-legal,01-ghost-illegal,contact-sheet}.png  (cited 1x) -> CR-T3-re-00-baseline.png, CR-T3-re-01-ghost-legal.png, CR-T3-re-01-ghost-illegal.png, CR-T3-re-contact-sheet.png
  .ai-tmp/screenshots/Y2-{login,mainmenu,deckedit,hud}.png  (cited 1x) -> Y2-login.png, Y2-mainmenu.png, Y2-deckedit.png, Y2-hud.png
  .ai-tmp/test/AV1-census{,2,3,4}.tsv  (cited 3x) -> AV1-census.tsv, AV1-census2.tsv, AV1-census3.tsv, AV1-census4.tsv
  .ai-tmp/test/v2-console-{1,2,3,5}.json  (cited 2x) -> v2-console-1.json, v2-console-2.json, v2-console-3.json, v2-console-5.json
  .ai-tmp/test/E2-D-*.npy  (cited 1x) -> ?
  .ai-tmp/test/g2-audit-*.txt  (cited 1x) -> ?
  .ai-tmp/test/p1-g1-audit-step*.txt  (cited 1x) -> ?
  .ai-tmp/test/t4-desc-*.tsv  (cited 1x) -> ?

--- HUMAN: unresolved bare names (NOT a verdict) ---
  -after.txt                       (cited 1x)
  -compare.png                     (cited 1x)
  -step9-Result.txt                (cited 1x)
  ..._blue_idle1.png               (cited 1x)
  ..._enemy_attack1.png            (cited 1x)
  ..._enemy_run1.png               (cited 1x)
  .index.tsv                       (cited 1x)
  09-21-all.log                    (cited 2x)
  09-21.log                        (cited 1x)
  09-22-all.log                    (cited 3x)
  09-22.log                        (cited 1x)
  09-23.log                        (cited 1x)
  2019_Loading_Screen.png          (cited 2x)
  _1320X2868.jpg                   (cited 1x)
  _sprite_532.png                  (cited 1x)
  _sprite_592.png                  (cited 1x)
  _sprite_NNN.png                  (cited 8x)
  _sprite_NNNN.png                 (cited 1x)
  _tex.png                         (cited 12x)
  AG2-matrix-D8.tsv                (cited 1x)
  AL3-deck-8of8.png                (cited 9x)
  AL3-long-Room.png                (cited 2x)
  AT2-walk.png                     (cited 1x)
  Camp.png                         (cited 2x)
  Clan.png                         (cited 1x)
  coverage-audit.md                (cited 2x)
  crowns.png                       (cited 2x)
  Debug.Log                        (cited 1x)
  demo.md                          (cited 1x)
  Epic_Sunday_Shop.png             (cited 1x)
  frame_NNN.png                    (cited 5x)
  ID.png                           (cited 1x)
  Miner-free-chest-576x1024.jpg    (cited 1x)
  Profile_Screenshot.png           (cited 2x)
  Quests_Menu.png                  (cited 1x)
  raw.txt                          (cited 1x)
  README.md.txt                    (cited 1x)
  Royale01022022-071826-52305.jpg  (cited 1x)
  tex.png                          (cited 1x)
  Title.TXT                        (cited 1x)
  triage.md                        (cited 2x)
  unit_cs.tsv                      (cited 1x)
  ZZ9-not-there.tsv                (cited 1x)

--- HUMAN: ambiguous bare names -- all in-zone candidates whitelisted ---
  frame_000.png            (cited 104x, 52 candidates)
  frame_008.png            (cited 2x, 47 candidates)
  frame_015.png            (cited 1x, 46 candidates)
  frame_028.png            (cited 3x, 42 candidates)
  frame_050.png            (cited 2x, 41 candidates)
  frame_080.png            (cited 2x, 39 candidates)
  frame_125.png            (cited 1x, 34 candidates)
  frame_188.png            (cited 1x, 33 candidates)
  frame_206.png            (cited 1x, 32 candidates)
  frame_418.png            (cited 2x, 12 candidates)
  frame_440.png            (cited 2x, 11 candidates)
  README.md                (cited 6x, 5 candidates)

===== NEGATIVE TEST 2: ambiguous bare name =====
scope            = all  (32 file(s))
STEP1 path refs  = 193  (literal exist 170, literal MISSING 13 | A expandable 4, B ellipsis 0, C glob 4)
STEP2 bare refs  = 218  (evidence 152, elsewhere 11, unresolved 42, ambiguous 13, pattern-bare 0)
WHITELIST (uniq) = 290   -> tools\probes\CR-R1-crosscheck-whitelist.txt

--- FAIL: literal path absent from disk ---
  .ai-tmp/screenshots/AN1.index.tsv  (cited 2x, first �߻�\���ձ�.md:541)
  .ai-tmp/screenshots/AP2-  (cited 3x, first �߻�\����Ա�\AP2-����.md:3)
  .ai-tmp/screenshots/R1-seg-  (cited 2x, first �߻�\��λ֡�α�.md:3061)
  .ai-tmp/screenshots/hist  (cited 1x, first �߻�\����Ǽ�.tsv:105)
  .ai-tmp/test/AA1-���ձ���.md  (cited 1x, first �߻�\����Ǽ�.tsv:75)
  .ai-tmp/test/AM1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:694)
  .ai-tmp/test/AM1b-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:697)
  .ai-tmp/test/AM2-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:658)
  .ai-tmp/test/AP2-probe-0..4.txt  (cited 1x, first �߻�\����Ա�\AP2-����.md:7)
  .ai-tmp/test/AW1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:620)
  .ai-tmp/test/BC1-���ձ���.md  (cited 1x, first �߻�\���ձ�.md:635)
  .ai-tmp/test/CR-T2f-frames.png  (cited 1x, first �߻�\����Ǽ�.tsv:111)
  .ai-tmp/test/smlbiobot-cr  (cited 1x, first �߻�\��λ������������.md:309)

--- HUMAN: bucket B (ellipsis: NOT mechanically expandable; candidates kept, never deleted) ---
  (none)

--- HUMAN: bucket A/C expansions (mechanically expanded; these names are whitelisted) ---
  .ai-tmp/screenshots/CR-T3-re-{00-baseline,01-ghost-legal,01-ghost-illegal,contact-sheet}.png  (cited 1x) -> CR-T3-re-00-baseline.png, CR-T3-re-01-ghost-legal.png, CR-T3-re-01-ghost-illegal.png, CR-T3-re-contact-sheet.png
  .ai-tmp/screenshots/Y2-{login,mainmenu,deckedit,hud}.png  (cited 1x) -> Y2-login.png, Y2-mainmenu.png, Y2-deckedit.png, Y2-hud.png
  .ai-tmp/test/AV1-census{,2,3,4}.tsv  (cited 3x) -> AV1-census.tsv, AV1-census2.tsv, AV1-census3.tsv, AV1-census4.tsv
  .ai-tmp/test/v2-console-{1,2,3,5}.json  (cited 2x) -> v2-console-1.json, v2-console-2.json, v2-console-3.json, v2-console-5.json
  .ai-tmp/test/E2-D-*.npy  (cited 1x) -> ?
  .ai-tmp/test/g2-audit-*.txt  (cited 1x) -> ?
  .ai-tmp/test/p1-g1-audit-step*.txt  (cited 1x) -> ?
  .ai-tmp/test/t4-desc-*.tsv  (cited 1x) -> ?

--- HUMAN: unresolved bare names (NOT a verdict) ---
  -after.txt                       (cited 1x)
  -compare.png                     (cited 1x)
  -step9-Result.txt                (cited 1x)
  ..._blue_idle1.png               (cited 1x)
  ..._enemy_attack1.png            (cited 1x)
  ..._enemy_run1.png               (cited 1x)
  .index.tsv                       (cited 1x)
  09-21-all.log                    (cited 2x)
  09-21.log                        (cited 1x)
  09-22-all.log                    (cited 3x)
  09-22.log                        (cited 1x)
  09-23.log                        (cited 1x)
  2019_Loading_Screen.png          (cited 2x)
  _1320X2868.jpg                   (cited 1x)
  _sprite_532.png                  (cited 1x)
  _sprite_592.png                  (cited 1x)
  _sprite_NNN.png                  (cited 8x)
  _sprite_NNNN.png                 (cited 1x)
  _tex.png                         (cited 12x)
  AG2-matrix-D8.tsv                (cited 1x)
  AL3-deck-8of8.png                (cited 9x)
  AL3-long-Room.png                (cited 2x)
  AT2-walk.png                     (cited 1x)
  Camp.png                         (cited 2x)
  Clan.png                         (cited 1x)
  coverage-audit.md                (cited 2x)
  crowns.png                       (cited 2x)
  Debug.Log                        (cited 1x)
  demo.md                          (cited 1x)
  Epic_Sunday_Shop.png             (cited 1x)
  frame_NNN.png                    (cited 5x)
  ID.png                           (cited 1x)
  Miner-free-chest-576x1024.jpg    (cited 1x)
  Profile_Screenshot.png           (cited 2x)
  Quests_Menu.png                  (cited 1x)
  raw.txt                          (cited 1x)
  README.md.txt                    (cited 1x)
  Royale01022022-071826-52305.jpg  (cited 1x)
  tex.png                          (cited 1x)
  Title.TXT                        (cited 1x)
  triage.md                        (cited 2x)
  unit_cs.tsv                      (cited 1x)

--- HUMAN: ambiguous bare names -- all in-zone candidates whitelisted ---
  claude.md                (cited 1x, 2 candidates)
  frame_000.png            (cited 104x, 52 candidates)
  frame_008.png            (cited 2x, 47 candidates)
  frame_015.png            (cited 1x, 46 candidates)
  frame_028.png            (cited 3x, 42 candidates)
  frame_050.png            (cited 2x, 41 candidates)
  frame_080.png            (cited 2x, 39 candidates)
  frame_125.png            (cited 1x, 34 candidates)
  frame_188.png            (cited 1x, 33 candidates)
  frame_206.png            (cited 1x, 32 candidates)
  frame_418.png            (cited 2x, 12 candidates)
  frame_440.png            (cited 2x, 11 candidates)
  README.md                (cited 6x, 5 candidates)


# BUCKET A/C: the expansions are what makes ruling 1 true -- the concrete file names they
# produce must survive cleanup, otherwise the images behind a brace citation look unreferenced
# and get deleted.  They are listed above and are inside the whitelist.
# BUCKET B: the ellipsis form is DEFINED but NOT INSTANTIATED in this corpus -- every brace
# citation found is the explicit-item form (bucket A).  Bucket B is therefore empty, by
# measurement, not by omission.

# Defects found in THIS verifier by its own negative tests:
#   1) empty string is a member of the forbidden-char set in python -> line-end tokens were
#      misfiled as patterns and NEGATIVE TEST 1 stopped being able to fail.
#   2) ambiguous candidates were whitelisted repo-wide; cleanup only empties .ai-tmp/* -> bloat.
#   3) the two whitelist producers disagreed on the path separator -> 43 duplicate entries.
#   4) an ASCII comma inside {a,b,c} is punctuation everywhere else, so a single character
#      class could not capture the brace form whole: bucket A read 0 while the same citations
#      were reported as truncated literals (a phantom-missing generator).  Fixed by matching
#      the whole brace form FIRST, then a glob, then an ordinary name.
#   5) the first NEGATIVE TEST 2 used an already-ambiguous name (12 -> 12): could not fail.

# ===== FINDING: the single genuine absent citation (bucket-differentiated) =====
# AS-OF 2026-09-23T09:57:47.3519507+08:00 ; assembly sha256 52358CB6966C259F
# AN1.index.tsv -- cited 2x (celma/duibiaobiao line 541, zhuangtai juzhen line 486); absent everywhere.
# Verdict: (c) merged-and-removed, citation not updated -- NOT never-landed. Evidence chain:
#   1) AX1.index.tsv header records: batch AN1 (source AN1.index.tsv mtime 2026-09-22 10:21:23): 6 rows
#      => the file EXISTED (a recorded mtime cannot come from a file that never landed)
#   2) those 6 rows survive verbatim in AX1.index.tsv, and cell AN1-06 matches the "ge AN1-06"
#      reference in zhuangtai juzhen.tsv:486 word for word => the successor is provably this file
#   3) AX1.index.tsv self-describes as "merged from the 8 per-batch indexes on 2026-09-22"
#   CAVEAT: this project is NOT a git repository (git log => not a git repository), so the removal
#   step is an inferred chain, not a VCS fact.
# SIDE CONTRADICTION: duibiaobiao.md:541 cites AN1.index.tsv while line 542 asserts this section
#   references no deleted file -- and line 542 names only two other files as deleted, not the index.
# PROPOSED LEDGER TEXT (CR-R1 does not edit cehua/): repoint both citations to
#   .ai-tmp/screenshots/AX1.index.tsv (cell AN1-06) and correct the false sentence at line 542.

# ===== CROSS-CHECK ROUND 2 vs CR-F2 revised tool (AS-OF 
2026-09-23T10:03:16.5980284+08:00
 ; assembly sha256 
52358CB6966C259F
) =====
# CR-F2 artifacts re-verified on disk:
#   cited-evidence-whitelist-all.txt  314 lines / 313 entries / backslashes 0 / outside cleanup zone 0  PASS
#   cited-evidence-whitelist-f2.txt   166 lines / 165 entries / backslashes 0 / outside cleanup zone 0  PASS
#   scope is in the file name -> the two scopes cannot overwrite each other (my earlier finding, fixed)
# FINDING: the tool fingerprint CR-F2 quoted does NOT identify the file on disk.
#   claimed  73B3F91B7F2A6CDE  / 27535 B
#   measured 92BA9BD0327DF8AD  / 28100 B  (stable across two reads 700 ms apart; mtime 10:02:14)
#   => the tool was edited after the hash was taken; a conclusion tied to a revision must quote the
#      hash OF THAT REVISION. Same defect class as the AS-OF rule adopted earlier today.
#
# Bucket A proven in BOTH directions with known samples (--inject-path):
#   known-correct: CR-U5-units-bind-{0,7,14,21,28,35}.tsv (6 files verified on disk)
#                  -> expands to exactly those 6 names
#   known-wrong  : ZZ9-brace-{a,b}.tsv (neither member on disk)
#                  -> reported as A-expansion absent, not whitelisted
#
# MISSING decomposition, scope=all.  Mine 13 literal -> 3 genuine + 5 self-declared-deleted + 5 extraction artefacts
#   3 genuine   : AN1.index.tsv | AA1-duibiaohangxing.md (chayi dengji.tsv:75) | CR-T2f-frames.png (chayi dengji.tsv:111)
#                 => EXACTLY the same 3 the other tool reports. The implementations CONVERGE.
#   5 declared  : AM1/AM1b/AM2/AW1/BC1-duibiaohangxing.md  (the other tool has a dedicated bucket for these)
#   5 artefacts : screenshots/AP2- | screenshots/R1-seg- | screenshots/hist | test/AP2-probe-0..4.txt | test/smlbiobot-cr
#                 (truncated at a non-name character, or a ".." range form -- its criterion excludes both)
# DEFINITION DIFFERENCES (why the totals differ; none of these is a right/wrong):
#   pattern bucket population : it A 73 / B 65 (counts pattern tokens anywhere, incl. prose and bare
#                               names); this verifier A 4 / C 4 (only patterns in the .ai-tmp/ path tail)
#   bare names                : it 212 (case-sensitive extension match, drops code identifiers such as
#                               Debug.Log); this verifier 217 (folded, keeps them) -> -5
#   directory-form citations  : it has a bucket of 5; this verifier does not
#   MISSING criterion         : it excludes self-declared-deleted and extension-less/".." forms; this
#                               verifier is lexical only -> adopting its criterion collapses my 13 to 3
#   whitelist                 : 313 (its) vs 290 (mine)
# Bugs found in MY OWN commands this round: (1) the peer was still writing when I first looked, so a
#   claimed artifact read as MISSING -- a race, re-checked before reporting; (2) $p and $P are the SAME
#   PowerShell variable (case-insensitive), so my loop clobbered its own root and one file was re-read
#   with stale values.

# ===== CROSS-CHECK ROUND 3 (AS-OF 
2026-09-23T10:08:16.9792927+08:00
 ; assembly sha256 
52358CB6966C259F
) =====
# 1. CR-F2 re-took its fingerprints and pinned DEFINITIONS. Independently re-verified, all PASS:
#      ps1  CCD13D2DECF14D07 / 29869 B ; all 4B0387892C207905 / 14205 B ; f2 7826DE14C2CF84A2 / 7638 B
#      (claimed == measured for all three; unchanged after 13 s; 5 contract lines + 313 / 165 entries;
#       0 backslashes, 0 entries outside the cleanup zone)
#    => the earlier mismatch is closed, and the closure was VERIFIED rather than taken on trust.
# 2. Definition alignment done; the remaining numeric gap is fully explained by two pinned axes
#    (MISSING criterion incl. the self-declared-deleted exclusion; pattern-bucket population = whole
#     document vs .ai-tmp path tail only).
# 3. RECIPROCAL FINDING -- the other implementation caught a REAL defect in this verifier:
#      a citation into a sub-directory (".../AL3-shots\\AL3-busy-login.png") was truncated at the
#      separator, so 15 real, cited files were NOT whitelisted (cleanup would have deleted them)
#      while a bare directory name was.  Fixed: separators are now allowed inside a name token;
#      f2 whitelist 158 -> 167, intersection with the other tool 150 -> 161.
#    => cross-implementation verification paid off in BOTH directions, which is the point of it.
# 4. Residual after the fix, by category (each side is more conservative somewhere):
#      only powershell protects : .ai-tmp/drivers/* existing citations + test/ SUB-DIRECTORY files
#                                  (scdfull/, table-client/, zoom/, v3-captured.txt, v5-reload.txt) = 19
#      only python protects     : several bare names resolved into the evidence area              =  6
#    all: python 300 / powershell 313 / intersection 294 ;  f2: 167 / 165 / 161
# 5. ACTION -> tools/probes/CR-R1-cleanup-whitelist-UNION.txt (319 entries, 0 backslash,
#    0 outside the cleanup zone, 2 under .ai-tmp/drivers).  For a CLEANUP the asymmetry decides
#    safety: over-protecting costs nothing, under-protecting leaves a dangling citation.
# 6. This verifier had the SAME single-output-path defect it had criticised in the other tool;
#    fixed (scope now in the file name).  A rule that is not applied to the critic rots.
# 7. Reference resolution in my impl now also requires a whitelist entry to be a real FILE.

# ===== CROSS-CHECK ROUND 4 =====
# AS-OF 2026-09-23T10:55:55 | judged assembly CR.dll mtime=2026-09-23 08:43:53 sha256_16=52358CB6966C259F
# 1. CR-F2 produced its own union. Re-verified independently; claimed == measured on hash, byte size AND
#    line count for all five artifacts:
#      ps1         303CA3F611AADF53/37123/532
#      -all        6D0D1554AC50C7CB/14618/325
#      -f2         918BBC40C6C7EC2D/8089/178
#      UNION-all   4EC6015CF8C2ECE9/13746/325
#      UNION-f2    C9425B42364075BF/7214/178
#    5 contract lines + 320/173 entries, 0 backslash, 0 entries outside the cleanup zone, stable on re-read.
# 2. Its claim A >= B verified myself after its fix: my 300 subset of union 320 (0 missing); my 167 subset of
#    union 173 (0 missing); the union contains 0 entries belonging to neither side. The 6 gaps I had reported
#    are all present in its artifact now -- I checked the FILE, not the promise.
# 3. AS-OF now binds in MY direction too: its union header pins my two files
#    (CR-R1-crosscheck-whitelist-all.txt A219779E9BF3FA55 ; -f2.txt 845313BFA3A520E1). Both match disk
#    exactly. => MY TWO WHITELIST FILES ARE FROZEN: editing either makes a delivered header stale.
# 4. NEW, MEASURED GAP (exposed by dumping my own output unfiltered; my summary filters had hidden it):
#    the NUMERIC RANGE citation form prefix<d>..<d>suffix is neither expanded nor protected by EITHER
#    implementation.
#      Repro: .ai-tmp/test/AP2-probe-0..4.txt (cited in cehua) expands to 5 REAL files
#             AP2-probe-0.txt .. AP2-probe-4.txt (mtime 2026-09-22), and all 5 are NOT in the frozen union.
#      Sizing run: 32 cehua docs, 5 distinct range tokens, 1 has an .ai-tmp prefix, that 1 expands
#             => 5 unprotected files.
#      WHY IT MATTERS: the contract says every EXISTING citation is protected; a cleanup guided by the
#             union would delete files that a delivered document still cites.
#      MINIMAL FIX: add a RANGE bucket on the CR-F2 side only. Its single-source -all goes 320 -> 325 and
#             the union follows automatically. My two files need NOT change => no re-anchoring required.
# 5. WITHDRAWN my own union file, so exactly one entry point remains:
#      tools/probes/CR-R1-cleanup-whitelist-UNION.txt  bytes=13708  sha256_16=4EE06B0CB64D97A5
#    Sole entry point is now CR-F2 cited-evidence-whitelist-UNION-all.txt / UNION-f2.txt.
# 6. Self-inflicted failures registered this round, all of the known ANSI/quoter family:
#      (a) nested double quotes inside a double-quoted PowerShell string => parse-time death of the WHOLE
#          script, so nothing ran -- including the withdrawal I believed had already happened;
#      (b) a .ps1 containing CJK written without a BOM is read as ANSI by Windows PowerShell 5.1: the cehua
#          directory path rotted to gibberish, Get-ChildItem found nothing, and the audit printed
#          tokens=0 -- a SILENT WRONG ANSWER rather than an error. Redone in python with explicit UTF-8;
#      (c) .NET current directory != PowerShell location: a relative AppendAllLines resolved OUTSIDE the
#          project and appended nothing, while the absolute-path heartbeat append in the same script
#          succeeded -- half the script silently did nothing. Absolute paths for every .NET file call.
#      (b) shares its root cause with the gate sampler ANSI trap already recorded in this project.
# 7. PROCESS POINT (CR-F2 raised it; it applies at least as much to me): inspecting evidence THROUGH a
#    filter creates blind spots. My summary line said literal-MISSING 13 for several rounds while the
#    DETAIL list had never once been dumped; dumping it unfiltered is what exposed item 4.

# ===== SEALED PREDICTION, written BEFORE CR-F2 re-lands the RANGE bucket =====
# AS-OF 2026-09-23T10:58:14 | judged assembly CR.dll mtime=2026-09-23 08:43:53 sha256_16=52358CB6966C259F
# Lead ruled (jia): CR-F2 adds a RANGE bucket and re-lands, then I re-verify. So that I cannot fit the
# numbers to whatever comes out, the expected delta is fixed HERE first:
#   -all       320 -> 325  : exactly these five additions, all of which are already on disk:
#                            .ai-tmp/test/AP2-probe-0.txt ... AP2-probe-4.txt
#   -f2        173 -> 178 or unchanged, decided ONLY by whether cehua/AP2-*.md is in the f2 doc scope
#   union-all  320 -> 325 ; union-f2 173 -> 178 or unchanged (the union follows its single source)
#   the five additions must not introduce any backslash entry, any entry outside the cleanup zone, or any
#   entry that is not an existing FILE.
# Verification on arrival: hash+bytes+line triad, containment both directions, membership of the five.

# ===== ROUND 5: VERDICT ON THE ORDERED RE-LAND (lead ruling jia) =====
# AS-OF 2026-09-23T11:00:11 | judged assembly CR.dll mtime=2026-09-23 08:43:53 sha256_16=52358CB6966C259F
# ORDERED FIX = add a RANGE bucket: expand <name>-<N>..<M>.<ext> mechanically, Test-Path each member,
# existing members go INTO the whitelist, missing members are listed but never judged red.
#
# VERDICT: NOT SATISFIED.  The five cited files are still unprotected.
#   new triads (old in brackets):
#     ps1              3B7B9C89D7FC7889 / 39591 B          [303CA3F611AADF53 / 37123]
#     -all             79FA6CFC0D4AAA89 / 14699 B / 327 L  [6D0D1554AC50C7CB / 14618 / 325]  body 322
#     -f2              9DF005523E243598 /  8170 B / 180 L  [918BBC40C6C7EC2D /  8089 / 178]  body 175
#     UNION-all        F048A7D8C274CD1D / 14684 B / 329 L  [4EC6015CF8C2ECE9 / 13746 / 325]  body 322
#     UNION-f2         C2A80D64512D9521 /  8149 B / 182 L  [C9425B42364075BF /  7214 / 178]  body 175
#   => single source went 320 -> 322 (expected 325); the range citation contributed NOTHING.
#   => .ai-tmp/test/AP2-probe-0.txt .. -4.txt: still NOT in -all, NOT in UNION-all (5 files, all on disk).
#
# ROOT CAUSE, quoted from cited-evidence-whitelist.ps1 (2nd revision):
#   L27  definition: MISSING = a LITERAL path-form ref that ... contains no brace/glob/range/:line form
#   L85  $placeRe = [regex]'[*?{}]|\.\.'      <-- '..' ADDED to the place/pattern class => excluded
#   L211 if ($n -match '\.\.') { continue }    <-- brace-expanded member containing '..' skipped
#   L234 if ($n -match '\.\.') { continue }    <-- plain-text bare name containing '..' skipped
#   L142-L171 / L290-L310: brace {} and glob *? buckets exist WITH exactly the wanted semantics
#   (existing members whitelisted, missing reported but never red) -- but there is NO (\d+)\.\.(\d+)
#   expansion anywhere.  So the change routed '..' AWAY from the whitelist instead of expanding it:
#   the false alarm may be gone, the protection is not there.
#
# WHAT THE RE-LAND DID DO RIGHT (so this is a precise miss, not a bad artifact):
#   0 backslash entries, 0 entries outside the cleanup zone, 0 entries that are not existing files;
#   my 300 subset of 322 and my 167 subset of 175 (0 missing); union == A union B exactly (0 third-party
#   entries); my 6 previously-reported gaps all still present; both union headers still anchor MY two
#   files at A219779E9BF3FA55 / 845313BFA3A520E1, which still match disk => anchors valid.
#
# PROCESS DEFECT: the re-land OVERWROTE both revisions in place, so the +2 delta cannot be diffed from
#   the old bytes; I can only see the totals.  A whitelist that is regenerated should also emit an
#   ADDED/REMOVED delta, otherwise every re-land destroys its own audit trail.
#
# HONEST SCOPE NOTE ON MY OWN SEALED PREDICTION: the sealed block was written at 10:58:14, i.e. AFTER
#   this re-land landed (10:57:03 / 10:57:36), so it is not a pre-registration for that event.  What DID
#   precede the re-land was the measurement itself: the five expected paths were enumerated in my report
#   to the lead before the re-land, and the lead quote them in the ruling.  Expectation predates the
#   change; the file that records it does not.

# ===== ROUND 5b: the re-land's own attribution, and its new tool, both checked =====
# AS-OF 2026-09-23T11:02:29 | judged assembly CR.dll mtime=2026-09-23 08:43:53 sha256_16=52358CB6966C259F
# 1. CR-F2-parsecheck.ps1 verified: 592 B, sha256_16=EF0AB627C1C7C685 -- matches its claim; mtime 10:18:01.
#    VALIDATED AS ABLE TO FAIL (a test that cannot fail proves nothing):
#      deliberately broken input (unclosed paren after if-test) -> 2 errors with line numbers
#      clean control -> PARSE OK
#    Run against my four .ps1: CR-R1-run / CR-R1b-run / CR-R1-gen-list / cr-r1-oldnew -> all PARSE OK,
#    and all four contain 0 non-ASCII bytes, so the ANSI trap (no BOM + CJK) cannot touch them.
# 2. MY OWN INVALID NEGATIVE TEST, stated for the record: my first broken sample was
#      $a = 1 + <newline> 2      plus a following line starting with + " cont"
#    which is LEGAL PowerShell (a trailing operator continues the line; a leading + is unary).
#    It reported PARSE OK and it was RIGHT; my sample proved nothing.  Same trap as CR-F2 rule.
# 3. The re-land's attribution checked.  It said the 320 -> 322 growth came from the INPUT DOCS being
#    edited by the lead in that window.  Verified: of 32 cehua docs, EXACTLY ONE has an mtime inside
#    10:15:00-10:57:40 -- cehua/yanshoubiao.md at 10:53:34 (the lead's ledger edit), which precedes its
#    10:57:03 landing, so it could be picked up.  The doc that cites the range form
#    (cehua/zishenduibi/AP2-zishen.md, mtime 2026-09-22 16:05:56) was NOT touched.
#    => the attribution is SUPPORTED as to premise, and it simultaneously proves the +2 did NOT come from
#       the range form.  It cannot be proven WHICH two entries are new, because the re-land overwrote the
#       previous revision in place -- the missing-delta defect again.
# 4. AS-OF HAZARD FOR THE LEAD: the ledger was still being edited (10:53:34) while the whitelist was
#    regenerated (10:57:03).  The order happened to be right this time, but it is luck, not a rule:
#    freeze the ledger FIRST, then regenerate the whitelist, otherwise a later ledger edit silently
#    stales the union.  This is the same class as "the number is right, the file is there, only the
#    number is bound to the wrong revision".
# 5. I repeated the .NET relative-path trap I had registered in item 6(c) of round 4 -- ReadAllBytes with
#    a relative path resolved outside the project again.  Registering a lesson is not the same as
#    enforcing it; the fix is absolute paths, which round 4 already said.
# 6. RANGE bucket still NOT implemented (round 5 verdict stands; nothing in this round changes it).

# ===== ROUND 5c: BARE-bucket number CONFLICT with CR-F1, measured three ways =====
# AS-OF 2026-09-23T11:06:15 | judged assembly CR.dll mtime=2026-09-23 08:43:53 sha256_16=52358CB6966C259F
# Lead's merged ruling sets the acceptance test as "CR-F1's 51 uncovered bare names converge to 0".
# I cannot reproduce 51.  My independent, read-only measurements (nothing written into tools/probes,
# because my two whitelist files are pinned by the union headers while a re-land is pending):
#   corpus: 35 docs = 28 .md + 4 .tsv + 3 .txt under cehua, recursive  (the .txt trio is
#           cehua/数值文档/card_cs.txt, spell_cs.txt, unit_cs.txt; they carry 6 bare occurrences total
#           and none resolve into the zone, so corpus size is NOT the axis of the disagreement)
#   extraction: any bare token with a known extension whose preceding char is not [A-Za-z0-9_-./\]
#   resolution: basename index, case-insensitive
#   RUN 1  zone = .ai-tmp/test + .ai-tmp/screenshots (2543 basenames): distinct bare names 272,
#          zone-resolvable pairs 437 (md 322 / tsv 115 / txt 0), UNCOVERED = 0
#   RUN 2  with the 3 .txt included (35 docs): UNCOVERED = 0
#   RUN 3  zone = the WHOLE .ai-tmp tree (2657 basenames): bare names resolving anywhere = 162,
#          per sub-dir covered/uncovered = drivers 1/0, screenshots 102/0, test 59/0, UNCOVERED = 0
# => under every zone definition I try, EVERY bare name that resolves into the cleanup zone is already
#    present in the frozen union (322).  There is no bare-name deletion hazard I can find.
# HYPOTHESIS for the 51: if CR-F1's universe is "bare names that do NOT resolve into the zone", then its
#    number is a COVERAGE statistic, not a cleanup hazard -- my own not-in-zone count is 111 distinct
#    names, and 51 could be a stricter cut of the same population.  Under that reading, "51 -> 0" is not
#    an achievable or even a meaningful acceptance criterion, and ordering CR-F2 to chase it would risk a
#    fix for a phantom -- or a round that can never pass.
# DECISIVE NEXT STEP (cheap, and it settles sets instead of counts): CR-F1 posts its 51 NAMES, and I
#    check each one against the frozen union, one by one.  Membership is a literal lookup, so the answer
#    is not a matter of interpretation.  Same lesson as before, sharpened: align the definitions AND
#    compare the SET, because two correct implementations can report different counts for the same fact.
# ACCEPTANCE RUN 2026-09-23T11:07:21 | assembly sha256_16=52358CB6966C259F | re-land happened=True | C1=PASS C2=PASS C3=PASS C4=PASS | overall=PASS

# ===== ROUND 6: THE ORDERED FIX IS ACHIEVED AND VERIFIED =====
# AS-OF 2026-09-23T11:08:01 | judged assembly CR.dll mtime=2026-09-23 08:43:53 sha256_16=52358CB6966C259F
# New triads (prev in brackets):
#   ps1           E8DAE53E824BC59A / 47004 B / 661 code lines      [3B7B9C89D7FC7889 / 39591 / 532]
#   -all          CD59337FADC6DE83 / 14849 B / 332 L / body 327    [79FA6CFC0D4AAA89 / 322]
#   -f2           A9969A12843AE4CF /  8170 B / 180 L / body 175    [9DF005523E243598 / 175]
#   UNION-all     778DAE8042986718 / 14834 B / 334 L / body 327    [F048A7D8C274CD1D / 322]
#   UNION-f2      0DAAAF724D7DC4B4 /  8149 B / 182 L / body 175    [C2A80D64512D9521 / 175]
# C1 RANGE  PASS: the five members .ai-tmp/test/AP2-probe-0.txt .. -4.txt are IN BOTH the single-source
#           and the union body -- IN the whitelist FILE, which is what the lead's new discipline demanded
#           ("not merely not-red").  The identical check reported them ABSENT at 10:58, so the criterion
#           demonstrably flips and is therefore discriminating.
# C2 STRUCT PASS: 0 backslash, 0 outside the cleanup zone, 0 entries that are not existing files.
# C3 CONT   PASS: my pinned 300 subset of union-all, my 167 subset of union-f2, 0 missing each.
# C4 DELTA  PASS: cited-evidence-whitelist-all.delta.txt and -f2.delta.txt now exist.  The -all delta reads
#           "previous sha256_16=79FA6CFC0D4AAA89 (322 entries) -> new 327 | added 5 | removed 0" and lists
#           EXACTLY the five AP2-probe files.  This is the strongest possible answer to the earlier "+2 was
#           unprovable" complaint: the provenance of the +5 is now in the artifact, not in an attribution.
#           The -f2 delta is 175 -> 175, added 0, removed 0, which confirms the predicted branch: the
#           range-citing document is outside the f2 doc scope.
# BARE bucket: present in the new ps1 (532 -> 661 lines; 33 'bare' / 11 'plain' / 10 'ambiguous' hits),
#           so all three buckets were done in one round as ordered.
# ANCHORS: both union headers still pin my two files at A219779E9BF3FA55 / 845313BFA3A520E1 and both still
#           match disk, so the pinned anchors stayed valid across the re-land.
# AS-OF ORDER: ledger cehua/yanshoubiao.md mtime 10:53:34 is OLDER than the new whitelist 11:06:02, so the
#           union is NOT stale.  The lead ruled a pause until it declares the ledger frozen; the re-land
#           landed 11:04:24-11:06:18, i.e. at the same time as that ruling (crossing messages), and the
#           ledger has not been written since.  The hazard is therefore dormant, not gone: the moment the
#           lead lands another ledger row, the union goes stale and one more re-land is required.
# BARE NUMBER CONFLICT, third line of evidence: the BARE bucket was added this round and the -all delta
#           gained ONLY the five RANGE files, with 0 removed.  So under CR-F2 own tool no bare-name file was
#           missing either.  That agrees with my three-way measurement (0 uncovered) and disagrees with the
#           reported 51.  Closing it still needs CR-F1's NAME LIST; counts cannot settle it, sets can.

# ===== ROUND 7: SET-LEVEL BARE RECONCILIATION AGAINST THE FROZEN UNION (327) =====
# AS-OF 2026-09-23T11:10:06 | judged assembly CR.dll mtime=2026-09-23 11:08:21 sha256_16=46BC25FAA509223E
# Inputs actually read (discipline 3: a zero must carry proof of the read):
#   tools/probes/cited-evidence-whitelist-UNION-all.txt   contentLen=14304 sha16=778DAE8042986718 entries=327
#   .ai-tmp/test/CR-F1-bare-reconcile.tsv                 contentLen=7204  sha16=19D72BCF153410DA (162 names)
#   .ai-tmp/test/CR-F1-bare-uncovered-51.txt              contentLen=894   sha16=64FFA3459108824D (51 names)
# Predicate (CR-U5R): R(T) = real zone files whose basename is T; RISK iff R(T) non-empty and not a
# subset of the whitelist; PHANTOM iff R(T) empty.
# RESULT over all 162 names: COVERED 161 | RISK 1 | PHANTOM 0.
# THE EIGHT the lead asked about: seven are COVERED; the single RISK is "index.tsv".
# THE 51-NAME FILE: all 51 are COVERED, and all 51 are members of the 162 table -- so that number was
#   entirely an artefact of comparing against a 128-line single-source file, exactly as CR-F1 now says.
# THE ONE RISK IS ITSELF A FALSE POSITIVE, and the mechanism is mechanical:
#   CR-F1 extraction look-behind is (?<![/\\\w]) -- it does NOT exclude the DOT.  In the path-form
#   citation .ai-tmp/screenshots/AN1.index.tsv the substring "index.tsv" is preceded by "." so its regex
#   matches the TAIL of a longer file name and emits "index.tsv" as a standalone bare name.
#   Evidence that this is spurious: bare "index.tsv" occurs 0 times in cehua (my look-behind excludes the
#   dot, which is why my extractor never produced it); the name still resolves to an unrelated ZERO-BYTE
#   file .ai-tmp/test/t5-cand/index.tsv (bytes=0, sha16=E3B0C44298FC1C14).
# => effective verdict 162/162 COVERED, RISK 0, PHANTOM 0.  There is no batch of barely-cited files to add;
#    the BARE demand degrades to robustness of the rule, as the lead predicted.
# SEPARATE PRE-EXISTING ITEM (not a whitelist issue): the ledger line cehua/yanshoubiao.md:541 cites
#   .ai-tmp/screenshots/AN1.index.tsv and that file is ABSENT from disk (already visible in my FAIL detail
#   list).  Whitelisting protects EXISTING files; a dangling citation is a ledger problem.
# ARTIFACT: .ai-tmp/test/CR-R1-bare-reconcile-joined.tsv (162 rows, columns name/candidates/in_frozen_union/
#   verdict/candidate_paths) so the lead can re-check any single row literally.

# ===== ROUND 8 (URGENT): THE FREEZE WAS BROKEN WHILE I WAS VERIFYING =====
# AS-OF 2026-09-23T11:11:41
# OBSERVATION (no attribution):
#   CR.dll  mtime=2026-09-23 11:11:33  bytes=328704  sha256_16=57CAA9DB8B15996C     [was 08:43:53 / 327168 / 52358CB6966C259F]
#   Assembly-CSharp-Editor.dll rebuilt 11:08:22 ; client/logs/Editor.log 11:08:44 ; AssetImportWorker logs 11:08:51
#   HudPanel.cs   frozen 08:20:15 -> 11:04:43 -> 2026-09-23 11:10:36   (still being written)
#   ArenaView.cs  -> 2026-09-23 11:08:34                              (NEWER than the 11:08:21 assembly)
#   the other five frozen files kept their frozen mtimes
# TWO STRUCTURAL FACTS:
#   (1) the previously judged assembly 52358CB6966C259F was OVERWRITTEN IN PLACE, so the 18 artifacts I
#       recollected against it can never be re-verified against their judged object -- worse than binding
#       a number to the wrong revision, the object is simply gone.
#   (2) the current assembly is already inconsistent with the current sources (ArenaView.cs is newer than
#       CR.dll, and HudPanel.cs moved again after that), so whatever runs now is a mixture.
# GATE, run unfiltered: SUMMARY FAIL=3
#   evidence-freshness     3/15 stale : AB2-hud.png < HudPanel.cs ; D1-arena.png / E1-arena.png < ArenaView.cs
#   freeze-before-capture  9/40 stale : AB2-hud, AQ2-hud-compare, AQ2-hud-mine, AV2-hud-compare,
#                                       AV2-hud-mine, CR-T2-hud-mine, CR-T2x-hud-detail, D1-arena, Y2-hud
#   the gate prints its own consequence: the words done / delivered / verified must not be used
# => the HUD and arena classes of my 08:59 re-collection are now invalidated by the gate itself.
# ATTRIBUTION: left to the lead.  Only CR-V1/CR-V2 are said to be running and their subject matter is
#   arena/HUD, but correlation is not causation and I do not assert it.  CR-F1 had declared HudPanel.cs
#   frozen.
# RECOMMENDATION: re-establish a code freeze (identify and stop the writer), keep a COPY of each judged
#   assembly plus its hash instead of overwriting it in place, and only then run a third re-collection for
#   HUD/arena.
# UNAFFECTED BY ALL OF THIS (round 7, documents vs whitelist only): 162 bare names -> COVERED 161, RISK 1,
#   PHANTOM 0; the single RISK is a CR-F1 extraction false positive (look-behind does not exclude the dot,
#   so it extracts the tail of AN1.index.tsv; bare index.tsv occurs 0 times in cehua and resolves to an
#   unrelated zero-byte file) => effective RISK 0.  All 51 names COVERED.  Joint artifact:
#   .ai-tmp/test/CR-R1-bare-reconcile-joined.tsv
# SEPARATE PRE-EXISTING ITEM: cehua/yanshoubiao.md:541 cites .ai-tmp/screenshots/AN1.index.tsv, which is
#   ABSENT from disk (its two siblings exist).  A dangling citation -- a ledger matter, not a whitelist one.

# ===== ROUND 9: FINAL 4-COLUMN BARE JOIN, AND MY OWN READING BUG CAUGHT BEFORE DELIVERY =====
# AS-OF 2026-09-23T11:14:18
# REFERENCE OBJECT (lead ruling 2 -- a bare number is a stale number):
#   tools/probes/cited-evidence-whitelist-UNION-all.txt  entries=327  sha256_16=778DAE8042986718
#   input .ai-tmp/test/CR-F1-bare-reconcile.tsv  names=162  sha256_16=5B2E6805C3326D9C (regenerated by
#   CR-F1 at 11:12, so its hash differs from the 19D72BCF153410DA I joined in round 7 -- the input has an
#   AS-OF too)
# RESULT, relative to the reference object above:
#   COVERED 161 | RISK 1 | AMBIGUOUS-UNDECIDED 0 | PHANTOM 0     (162 names)
#   THE EIGHT: seven COVERED; the single RISK is "index.tsv" -> .ai-tmp/test/t5-cand/index.tsv, which is a
#   CR-F1 EXTRACTION FALSE POSITIVE (its look-behind does not exclude the dot, so it emits the tail of the
#   longer citation AN1.index.tsv; bare "index.tsv" occurs 0 times in cehua and the file it resolves to is
#   an unrelated 0-byte one).  => EFFECTIVE RISK 0, i.e. the number converges to 0, as the lead predicted,
#   and the BARE demand degrades to robustness of the rule rather than a batch of files to add.
#   AMBIGUOUS is empty for THIS name set, so ruling 1's undecided bucket has no member here; that does NOT
#   validate the "delete none of the candidates" implementation -- it simply was not exercised.
# ARTIFACT: .ai-tmp/test/CR-R1-bare-join-final.tsv (162 rows + a header that states the reference object;
#   my three columns are the LAST THREE and everything before them is CR-F1 text kept verbatim).
# DELIVERED-ONLY-AFTER-CHECKING: my own quick reader had a silent wrong answer.  I padded each split line
#   with three empties and then indexed from the END, so the padding -- not the verdict -- landed on the
#   index I read.  It reported "166 names, 4 PHANTOM" and I very nearly sent that.  Re-reading the file I
#   had just written showed 162 rows and 0 phantom.  Same family as the lead's discipline 3: a zero (or a
#   four) is worthless without proof of how it was read.  Fix: take field 0 as the name and never index the
#   other side's columns from the end.
# STILL OPEN AND UNCHANGED BY ALL OF THE ABOVE (already reported): the code freeze is broken and the
#   assembly is moving -- CR.dll mtime=2026-09-23 11:14:13 sha256_16=34B363D06726B006 ; HudPanel.cs mtime=2026-09-23 11:10:36.

# ===== ROUND 10: BARE CLOSES AT ZERO IN BOTH IMPLEMENTATIONS, AND THE STALE WINDOW IS MEAURED =====
# AS-OF 2026-09-23T11:16:21
# 1. CR-F1 fixed the look-behind ((?<![/\\\w.]) now), re-quantified: citations 440 -> 437, distinct 162 -> 161,
#    and EXACTLY index.tsv disappeared -- matching my verdict.  Its regenerated v3 table re-judged by me
#    against the reference object UNION-all entries=327 sha256_16=778DAE8042986718:
#      names 161 -> COVERED 161 | RISK 0 | AMBIGUOUS-UNDECIDED 0 | PHANTOM 0
#    => both implementations now agree at zero.  The chain 143 -> 8 -> 0 (theirs) and 51 -> 8 -> 0 (mine)
#       are the same fact reached by set comparison.
#    It also self-reported a broken intermediate: its |R| column was constantly 1 because
#    @(Measure-Object).Count always yields 1, while its R_files column listed 2 paths -- one column wrong,
#    one right, in the same row.  That is why a risk predicate built on |R| must not be trusted as a
#    conclusion, which is exactly the lead ruling that ambiguous names are UNDECIDED rather than safe.
# 2. WHY IT COULD NOT FIND THE UNION: it scanned .ai-tmp for a 12000-16000 byte file.  The union lives in
#    tools/probes/, not .ai-tmp/ -- 14834 B for -UNION-all.txt and 8149 B for -UNION-f2.txt.  Derived
#    whitelist artifacts belong in tools/probes (they outlive the one-off area); given the path, it can
#    reproduce the join.
# 3. THE STALE WINDOW, now real and now measured: ledger cehua/yanshoubiao.md moved 10:53:34 -> 2026-09-23 11:14:23
#    (sha256_16=CAB07BDCE31FB12D) which is LATER than the union build at 11:06:02, so the union is stale by construction.
#    Read-only scan of all 35 cehua docs (path-form + bare, resolved into the cleanup zone, checked against
#    the union): cited-but-NOT-in-union rows = 0, distinct files = 0.
#    => the window is open but currently EMPTY: the rows landed so far introduced no unprotected citation.
#       Re-run the same scan (one command) after any further landing; if it stays 0, the next re-land's
#       delta should read added 0.
# 4. The AN1.index.tsv dangling citation (cehua/yanshoubiao.md:541) stays a ledger matter, logged, not a
#    whitelist matter -- CR-F1 independently agrees.
# 5. Still open: the assembly keeps being rebuilt (CR.dll 34B363D06726B006 @ 2026-09-23 11:14:13), gate FAIL=3.

# ===== ROUND 11: v4 VALIDATED, THE TWO "UNDETERMINED" ARE NOT GAPS, AND NO COPY OF THE OLD ASSEMBLY =====
# AS-OF 2026-09-23T11:18:19
# 1. CR-F1 v4 validated by READING IT BACK (the lead newly made this a rule: an artifact that prints
#    correct numbers can still be a broken artifact):
#      header = name/R_count/R_files/citations/in_union/bucket (6 real columns, not a comment line)
#      data rows = 161 ; rows whose field count != 6 : 0 ; internal check R_count == count(R_files): 0 bad
#    So the format is now joinable by column, which is what a cross-implementation comparison needs.
# 2. Its join reported UNDETERMINED 2 while mine reported AMBIGUOUS-UNDECIDED 0. Resolved -- it is a
#    labelling difference, not a factual one:
#      AL3-deck-full-9th.png   |R|=2  (.ai-tmp/screenshots/... and .ai-tmp/screenshots/AL3-shots/...)  both IN union
#      ui-index-manifest.tsv   |R|=2  (.ai-tmp/screenshots/... and .ai-tmp/test/zoom/...)               both IN union
#    Its own in_union column reads "in" for both. So "UNDETERMINED" here means "ambiguous", NOT "unprotected":
#    no candidate is missing, and its own data already says so. => still RISK 0 in both implementations.
#    HONEST NOTE: the AL3-shots copy of AL3-deck-full-9th.png is a duplicate I created myself in round 1 (I
#    copied it so the gate's expected path would exist). So one of the two ambiguities is my own artefact --
#    harmless because both copies are whitelisted, but it should be on the record.
# 3. The lead ruled that my 08:59 batch is UNRECOVERABLE because its judged assembly was overwritten. I
#    turned that from an inference into a measurement: searched client/Library, client/Temp and .ai-tmp for
#    any file of exactly 327168 bytes (the old CR.dll size) and hashed every hit => 0 files found. So no
#    surviving copy exists, and the ruling is exact.  Adopted for future rounds: copy the judged assembly to
#    client/Library/ScriptAssemblies/<sha16>.dll so a judged object can always be re-examined.
# 4. Still open and unchanged: the assembly keeps being rebuilt (CR.dll 0F4D2C8A75D5D318 @ 2026-09-23 11:17:19) and the gate reads FAIL=3.
#    I have no pending task and am not starting a third re-collection.

# ===== ROUND 12: MY OWED CORRECTION, AND CR-F2's CLEANUP SIMULATION INDEPENDENTLY REPRODUCED =====
# AS-OF 2026-09-23T11:20:22
# 1. CORRECTION I OWE (a number of mine travelled further than its definition warranted):
#    I reported AMBIGUOUS-UNDECIDED = 0, because MY bucket means "|R|>1 AND at least one candidate is NOT
#    in the whitelist" -- i.e. my bucket holds only the DANGEROUS ambiguity.  Under the lead's ruling 1
#    wording ("any |R|>1 is undecided"), the same name set has TWO such names:
#      AL3-deck-full-9th.png   |R|=2   both candidates IN the union
#      ui-index-manifest.tsv   |R|=2   both candidates IN the union
#    So the lead's ledger line "the ambiguous bucket is empty for this name set" is true only under my
#    narrower bucket; factually there are 2 ambiguous names and BOTH are fully protected.  The risk
#    conclusion (0) is unchanged, but the wording must not be allowed to skip the cross-zone requirement
#    (ui-index-manifest.tsv sits in BOTH test/zoom/ and screenshots/ -- it is exactly the cross-zone case).
#    PROPOSED FORMULATION, which removes the argument: report TWO ORTHOGONAL FLAGS instead of one bucket --
#      ambiguous? (|R|>1)                 -> a fact about the name
#      all candidates in the whitelist?   -> a fact about the protection
#    Then: ambiguous & all-in-W  => safe under ANY implementation (needs no rule);
#          ambiguous & not-all-in-W => undecided, depends on the unverified keep-all rule;
#          unambiguous & not-in-W   => RISK.  In this name set the middle class is EMPTY.
# 2. CR-F2's probe criticism VERIFIED INDEPENDENTLY: frame_418.png has 12 copies and ALL TWELVE are under
#    client/Assets/Resources/Sprites/..., i.e. ZERO inside the cleanup zone.  Its citations are the RANGE
#    Sprites/Effects/Blast/frame_418..427.png (cehua/duibiaobiao.md:182) and asset prose (zhuangtaijuzhen.tsv:359,
#    duibiaobiao.md:805), none of which is a cleanup-zone path.  => as a probe for "delete none of the
#    candidates" it has ZERO discriminating power, exactly as CR-F2 said, and the swap it suggests
#    (AL3-deck-full-9th.png / zoom/ui-index-manifest.tsv) is correct.  This is the "a test that cannot fail
#    proves nothing" rule applied to the choice of SAMPLE.
# 3. CR-F2's simulation RE-RUN BY ME (I first checked the script contains no file-mutating call -- no
#    Out-File / Set-Content / Add-Content / WriteAllText / New-Item / Remove-Item / Copy-Item / Move-Item
#    -- so a re-run cannot perturb the artifacts):
#      real rule : ambiguous candidates that would be deleted = 0 ; zone = 2703 files ; uncited deletions 2376
#      negative control (wrong rule, keep only the first candidate) : deleted = 2
#        .ai-tmp/screenshots/AL3-shots/AL3-deck-full-9th.png
#        .ai-tmp/test/zoom/ui-index-manifest.tsv
#    => the check CAN fail, so its 0 is meaningful.  Both numbers reproduce its report exactly.
#    NOTE its deletable universe is the WHOLE .ai-tmp (2703 files), not just test+screenshots (2588) --
#    worth an explicit ruling, since that is what makes 2376 uncited files deletable.
# 4. Rule 3 of the lead (copy the judged assembly to .ai-tmp/assemblies/<sha16>.dll) is better than my
#    earlier suggestion of client/Library/ScriptAssemblies: Unity cleans that directory, so a copy there
#    would not survive.  Accepted.  (And no copy of the old assembly exists anywhere -- measured.)
# 5. Still open: CR.dll 0F4D2C8A75D5D318 @ 2026-09-23 11:17:19 ; gate FAIL=3 ; no third re-collection until the freeze.

# ===== ROUND 13: BOTH AXES REPRODUCED FROM v5, AND THE FREEZE-INSTANT RETENTION TOOL IS BUILT AND TESTED =====
# AS-OF 2026-09-23T11:23:03
# 1. CR-F1 v5 read back: 7 columns (name/R_count/R_files/citations/cand_in_union/multi_candidate/not_in_union),
#    161 rows, 0 rows with a wrong field count.  I recomputed BOTH axes independently against the reference
#    object UNION-all entries=327 sha256_16=778DAE8042986718:
#      AXIS multi_candidate (affects the IMPLEMENTATION: delete none across zones)  = 2
#         AL3-deck-full-9th.png  cand=2 both in union     ui-index-manifest.tsv  cand=2 both in union
#      AXIS not_in_union (affects COVERAGE)                                        = 0
#    v5's own columns agree exactly: multi_candidate yes=2/no=159 ; not_in_union no=161 ;
#    cand_in_union 1/1 x159 and 2/2 x2.  Two implementations, one fact, both numbers now named separately.
# 2. RETENTION TOOL BUILT AND TESTED BEFORE IT MATTERS (the lead designated the lead or me to perform the
#    freeze-instant copy): .ai-tmp/test/cr-r1-retain-assembly.py
#      - copies client/Library/ScriptAssemblies/CR.dll to tools/probes/assemblies/<sha16>.dll
#      - READS THE COPY BACK and compares sha256 before reporting success (write is not evidence)
#      - appends tools/probes/assemblies/index.tsv with the 5 fields the lead specified
#      - refuses to overwrite a same-named file with different bytes (exit 3) -- negative control run:
#        after I tampered the copy with one byte the tool reported COLLISION and exit=3, so the integrity
#        check demonstrably CAN fail
#      - idempotent: a re-run with the placeholder label changes nothing (my first version appended a second
#        index row; found by running the smoke test twice, fixed, re-verified)
#      Five smoke tests were run in a scratch directory under .ai-tmp/test and the scratch was deleted.
# 3. Observation, not attribution: CR.dll has been stable at 2026-09-23 11:17:19 sha256_16=0F4D2C8A75D5D318 since 11:17:19 -- about five
#    minutes without a rebuild at the time of writing.
# 4. Still open: gate FAIL=3 ; no third re-collection until the freeze.

# ===== ROUND 14: RETENTION TOOL FINALISED -- AND A SELF-INFLICTED PERTURBATION, REPAIRED AND REPORTED =====
# AS-OF 2026-09-23T11:26:14
# 1. Tools/probes/assemblies/index.tsv is now the single authoritative source of the baseline, with the
#    terminal six fields: sha16 | dll-file | bytes | frozen-at | declared-by | 6-file-mtime-snapshot.
#    Why the snapshot matters: even if the dll is lost, one can still judge whether the SOURCES moved.
#    The retention tool was rewritten to that spec and to the rule the lead adopted from me ("before
#    re-running or inspecting someone else's artifact, prove your run cannot perturb it"): it writes only
#    under --dest, appends with retries instead of rewriting, and reads each copy back before reporting.
#    It also gained a --check mode implementing the lead's three runner requirements: the baseline dll
#    exists, the current CR.dll sha16 is IN index.tsv, and the six product files still match the snapshot;
#    on failure it prints ASSEMBLY WARN and exits 3.
# 2. Smoke tests in a scratch directory (deleted afterwards), all passing:
#      dry-run writes nothing and PRINTS the six-file snapshot (so a wrong file list is visible)
#      retain -> read-back verified, index row appended with the six fields
#      --check -> OK, exit 0
#      negative control A: tamper one byte of the retained copy -> COLLISION, exit 3
#      negative control B: move a snapshot member's mtime -> "SOURCE CHANGED" + ASSEMBLY WARN, exit 3
#    -> both integrity checks demonstrably CAN fail, so their passes mean something.
# 3. SELF-INFLICTED PERTURBATION, AND ITS REPAIR.  Negative control B was built by calling os.utime on TWO
#    REAL product files:
#      client/Assets/Scripts/Core/Events.cs
#      client/Assets/Scripts/Module/Settings/SettingsManager.cs
#    No content was written, but their MTIMEs moved to 11:25:07 -- and mtime is exactly what this project's
#    freshness gates read.  I violated, in my own test design, the very rule I had proposed one round earlier.
#    REPAIR: both mtimes restored to the recorded originals; content hashes now match the values CR-F2
#    recorded independently for these files:
#      Events.cs           08:20:29  bytes=15576  sha16=B2F47F12A998BCBE   (CR-F2 recorded B2F47F12A998BCBE...)
#      SettingsManager.cs  08:28:03  bytes=23637  sha16=D29C83302B577130   (CR-F2 recorded D29C83302B577130...)
#    Gate re-run immediately afterwards: FAIL=3, the same triple as before the perturbation
#    (impl-by-executor, evidence-freshness, freeze-before-capture) => no new red was left behind.
#    LESSON (same family as the project rule I proposed): a negative control must not touch real inputs --
#    it must operate on a SCRATCH COPY.  The tool's --files switch exists for exactly that; I used the real
#    files because it was quicker.  Cheap trick, real hazard, and only caught because the snapshot print
#    showed SettingsManager.cs jumping to 11:25:07.
# 4. Still open: CR.dll 0F4D2C8A75D5D318 @ 2026-09-23 11:17:19 ; gate FAIL=3 ; freeze not yet declared, so nothing has been retained yet.

# ===== ROUND 15: TOOL FINAL, v7 VERIFIED, CROSS-ZONE COUNTED, UNION MOVED, ONE FALSE ALARM WITHDRAWN =====
# AS-OF 2026-09-23T11:30:47
# 1. RETENTION TOOL FINAL per the lead's terminal spec:
#      index.tsv = 6 columns (sha16|dll-file|bytes|frozen-at|declared-by|mtime6), mtime6 in ISO,
#      and the ROW SHAPE IS ASSERTED BEFORE WRITING: 6 fields and exactly 5 semicolons in mtime6.
#      mtime6 uses ISO now (it had a space separator -- readable by a human, still wrong for a parser).
#      Re-running when the assembly is already retained appends NOTHING (my rewrite had silently dropped
#      that fix; the smoke suite caught it again -- which is the point of having a smoke suite).
#      --check implements the three runner requirements and prints ASSEMBLY WARN + exit 3 on any failure.
#      Smoke suite (all on SCRATCH copies now): dry-run prints the file list; retain read-back verified;
#      re-run skipped; index reads back as 6 fields / 5 semicolons; --check OK; tampered copy -> COLLISION
#      exit 3; touched SCRATCH member -> SOURCE CHANGED + ASSEMBLY WARN exit 3; and a closing GUARD proves
#      the suite left the two real product files' mtimes untouched.
# 2. CR-F1 v7 read back: header = name/R_count/R_files/citations/cand_in_W/ambiguous/all_candidates_in_W,
#    161 rows, 0 wrong field counts.  My recomputation agrees: ambiguous=2, candidate-not-in-union=0.
# 3. Cross-zone counted the way the lead pinned it, both numbers computed by me:
#      cross_zone_top    = 1   (ui-index-manifest.tsv: screenshots/ vs test/zoom/)
#      cross_zone_subdir = 2   (+ AL3-deck-full-9th.png: both under screenshots/, different subdirectory)
#    Note cross_zone_subdir is a SUPERSET count: anything crossing top zones also crosses directories.
# 4. THE UNION MOVED: 327 (778DAE8042986718) -> 331 (490D53715070186F) at 11:27:24.  The delta names the provenance:
#      added 4 = .ai-tmp/screenshots/AX1.index.tsv, .ai-tmp/test/CR-V2-measure-after.txt,
#                .ai-tmp/test/F1-fx-index.tsv, .ai-tmp/test/t5-cand/index.tsv ; removed 0
#    My two PINNED files are still anchored at the same values (A219779E9BF3FA55 / 845313BFA3A520E1) and
#    still match disk, so the anchors survived.  The five AP2-probe files are still in.
#    Per my own instruction, a moved union means re-running the stale scan: re-run against 331 => 0 rows.
# 5. A FALSE ALARM OF MINE, AND A WITHDRAWN EXPLANATION.  The first stale scan against the new union
#    reported 2 rows, both of them path:line citations (AT2-post.py:29, AG1-measure-final.txt:7).  I stated
#    the mechanism as an NTFS alternate-data-stream effect on os.path.isfile -- and MY OWN EXPERIMENT
#    REFUTED IT (isfile("...AT2-post.py:29") = False).  I therefore WITHDRAW the mechanism: I could not
#    reproduce it, and the two citing lines have since moved (the ledger is being edited), so the exact
#    reproduction is gone.  What IS verified: both base files are in the union, and after stripping the
#    :line suffix -- which the project's own MISSING definition requires -- the scan reports 0.
#    Keeping the fix and dropping the story is the honest split between observation and cause.
#    Candidate lesson: never write down an explanation you have not tested; and re-run a scan rather than
#    remembering its result, because a doc edit shifts every line number under it.
# 6. Still open: CR.dll 0F4D2C8A75D5D318 @ 2026-09-23 11:17:19 ; gate FAIL=3 (same triple) ; freeze not declared.

# ===== ROUND 16: FREEZE-INSTANT RETENTION EXECUTED, TOOL PROMOTED, GATE CLEAN =====
# AS-OF 2026-09-23T11:41:35
# 1. RETENTION DONE at the declared freeze (frozen-at 2026-09-23T11:39:57):
#      tools/probes/assemblies/0F4D2C8A75D5D318.dll   bytes=328704  sha256_16=0F4D2C8A75D5D318
#      read-back verified (copy hash == source hash) before reporting success.
#      tools/probes/assemblies/index.tsv : 2 data row(s), 6 columns each, shape asserted before writing.
# 2. TOOL PROMOTED to tools/probes/cr-retain-assembly.py (one-off copy in .ai-tmp/test removed) and
#    tools/verify.ps1 was run immediately after: SUMMARY FAIL=3 -- the SAME triple as before the
#    retention (impl-by-executor, evidence-freshness, freeze-before-capture) => NO new red, so no
#    exception is being requested.  no-escaped-artifacts and evidence-economy both PASS with the new
#    assemblies/ directory present.
# 3. SNAPSHOT = 50 entries, mechanically the UNION of two sources:
#      (1) product files this session changed -- git status plus an mtime scan from session start;
#      (2) the registered judgement rows' object column (cehua/<state matrix>.tsv col 3 -- that column
#          literally holds "HudPanel.cs:718-732", "server/game/core/arena.go:26,29"), plus explicit
#          path-form product mentions, plus declared asset TREES.
#    Includes the cross-side server files (arena.go, units.go, battle.go, ...) and the tree the lead
#    named, client/Assets/Resources/Sprites/Ui/.  FIRST ATTEMPT WAS 85 ENTRIES because it scanned all 35
#    docs and collected mere mentions -- an evidence inventory, not a dependency list; narrowed to the
#    registry object column => 49, + the tree => 50.
#    One registry ambiguity is handled conservatively: "battle.go" resolves to BOTH
#    server/game/core/battle.go and server/game/logic/battle.go, and both are included.
# 4. THREE SELF-INFLICTED DEFECTS IN MY OWN TOOL, all of the "cannot fail" family, all fixed:
#      (a) my relative asset-tree fix concatenated the wrong prefix, producing client/Assets/Sprites/Ui/
#          which does not exist -- so the tree the lead named was SILENTLY absent from the snapshot;
#      (b) the --check branch was DROPPED during my rewrite, so --check fell through to the retention
#          path, did nothing, and exited 0: a check that can never fail;
#      (c) the malformed-row test HARD-CODED five semicolons, true only for the six-file smoke fixture,
#          so at the real scale (50) every row was declared malformed.
#    --check is now verified BOTH ways: it passes on the real baseline (exit 0) and fails on a doctored
#    SCRATCH copy (exit 3, SNAPSHOT CHANGED + ASSEMBLY WARN) -- and that negative control touched no
#    real input.
#    LESSON: my smoke fixture used six files and therefore could not exercise the rules at production
#    scale.  A fixture must reach the size the rule will actually meet.
# 5. index.tsv is append-only: row 1 (49 entries, 2026-09-23T11:37:41) is followed by a "# correction" comment and row 2
#    (50 entries, 2026-09-23T11:39:57) for the same sha256_16; --check evaluates the NEWEST row.  Consolidation to a
#    single row is possible on request, but rewriting an append-only baseline record seemed worse than
#    leaving the correction visible.

# ===== ROUND 17: mtime-snapshot EXTENDED (cross-side, asset, data) AND THE NAMED FINDING COVERED =====
# AS-OF 2026-09-23T11:48:27 | baseline sha256_16=0F4D2C8A75D5D318 (UNCHANGED -- the dll was never rewritten) | index rows=4
# 1. mtime-snapshot is now 133 entries: client 33 | server 16 | asset 44 | data 40.
#    The rule is the lead's: the UNION of (1) product files this session changed and (2) what the
#    registered judgement rows name as object/dependency -- mechanically, with NO hand-written list:
#      registry object column (cehua/<state matrix>.tsv col 3)
#      + explicit product path mentions (client/*.cs, server/*.go)
#      + declared asset trees, under client/Assets AND under the original-material tree
#      + cehua non-document assets (path-form AND bare-name), since a registered row depends on them
# 2. THE THREE NAMED DEPENDENCIES, and how each is actually covered:
#      cehua/<reference>/03_..._1320x2868.jpg   -> IN, via BARE-NAME resolution.  It is cited without its
#          cehua/ prefix (`03_..._1320x2868.jpg`), which is exactly why a prefix-only rule cannot see it.
#      original-material .../arena_training_out/arena_training_sprite_06.png and _22.png
#          -> covered by the TREE original-material/cr-assets-png/assets/sc/arena_training_out/.
#          HONEST NOTE: those two leaves are cited by NOBODY by name anywhere in cehua -- what cehua cites
#          is the GLOB TREE ".../assets/sc/*".  So the tree is what entered the snapshot: strictly broader,
#          drift-visible in the same way, and it avoids the hand-written leaf list the lead forbade.
#          => "change a sprite under assets/sc/ => D-V1-* is invalidated" IS now visible in the snapshot.
# 3. Verified: --check exits 0 on the live baseline (newest row 133 entries, malformed rows 0), and a
#    doctored index copy in a scratch directory makes it exit 3 with SNAPSHOT CHANGED -- so the check can
#    still fail.  index.tsv is append-only: rows 49 / 50 / 102 / 133 entries for the SAME sha256_16,
#    each correction preceded by a comment; the sha16 never changed, so no chain is affected; parsers
#    must read the NEWEST row.
# 4. tools/verify.ps1 run again after the update: FAIL=3 -- the same triple as before the retention
#    (impl-by-executor, evidence-freshness, freeze-before-capture) => NO new red, no exception requested.
# 5. Open question to the lead: consolidate the four rows into one?  I kept the corrections visible
#    rather than rewriting an append-only baseline record.

# ===== ROUND 18: THE THREE NAMED DEPENDENCIES ARE IN -- AND THE READ RULE MATTERED =====
# AS-OF 2026-09-23T11:55:18 | baseline sha256_16=0F4D2C8A75D5D318 (never rewritten) | index rows=5 | newest entries=190
# 1. THE THREE DEPENDENCIES, and what actually happened:
#      cehua/<reference>/03_..._1320x2868.jpg  -- ALREADY PRESENT since row 4 (11:47:03), via bare-name
#          resolution (it is cited without its cehua/ prefix).  A checker that reported it False was most
#          likely reading an EARLIER row -- see item 3.
#      the two arena_training sprites -- now IN BY NAME.  Mechanically: a cited tree whose leaf count is
#          modest (<= 40) is expanded to its leaves.  arena_training_out/ has 23 files so it expanded;
#          the cited 21041-file assets/sc tree stays a tree.  No hand-written leaf list.
#      => 190 entries: the same union as before plus those leaves (and their 21 siblings).
# 2. THE READING RULE, PINNED IN THE ARTIFACT (append-only kept intact):
#      "# READ RULE: for a given sha256_16 read the LAST row by frozen-at; earlier rows are superseded"
#      "# superseded <ts>: every row above with sha256_16=... is superseded by the row at frozen-at=<ts>"
#    Older rows are marked by COMMENT, never rewritten -- rewriting would erase the history that
#    append-only exists to keep (the lead's reason for keeping two rows in the first place).
#    --check now ASSERTS both halves: frozen-at is non-decreasing across rows of one sha16, and the
#    NEWEST row is the one compared.
# 3. EVIDENCE THAT THE RULE WAS NEEDED: the reported "still missing" was correct at 11:43:53, when the
#    newest row had 50 entries -- but the index now holds 5 rows for one sha16 (49 / 50 / 102 / 133 /
#    190).  Any reader taking the FIRST row gets the pre-correction snapshot: the exact "the number is
#    right, the file is there, only the version is wrong" shape this index exists to prevent.
# 4. Tool output noise fixed: a tree-too-big note is printed once per tree, not once per citation.
# 5. Verified: --check exits 0 on the live baseline; a doctored SCRATCH copy still exits 3 with
#    SNAPSHOT CHANGED; tools/verify.ps1 = FAIL=3, the SAME triple as before the retention => no new red.

# ===== ROUND 19: DIRECTORY ENTRIES ARE NOW CONTENT MANIFESTS =====
# AS-OF 2026-09-23T12:04:38 | baseline sha256_16=0F4D2C8A75D5D318 (never rewritten) | index rows=7 | newest entries=190 (14 tree manifests)
# 1. RULING IMPLEMENTED: a directory entry is no longer "dir=mtime" but
#      <dir>/**  =  #<file-count>#<sha256[:16]>
#    computed as SHA256 over lines "<path-relative-to-the-subtree>:<size>:<mtime>" joined by newline,
#    mtime = yyyy-MM-ddTHH:mm:ss.fff LOCAL, fraction TRUNCATED, lines ordered case-INSENSITIVELY by
#    absolute path.  The recipe is pinned as a MANIFEST RECIPE comment inside index.tsv, and this tool is
#    the only implementation (the lead ruled a second one would be the next "two hashes for one unchanged
#    directory" trap).  Why it matters: a directory MTIME does not move when a file inside it is rewritten
#    in place (CR-U5R measured it), a manifest hash does.
# 2. CROSS-CHECK against CR-U5R's six published values: 4 reproduce EXACTLY --
#      Sprites/Ui #228#858393d52ebf9b0a, arena_training_out #23#3956bb80f1fa68b9,
#      chr_goblin_archer_out #342#00a4a40c8343f597, assets/font #1#44bab8200aa60821 -- and TWO do not:
#      effects_out (ref #612#4094148d6c28a7a5) and assets/sc (ref #21041#ae0a455a0bfc888b).  The file
#      COUNTS agree; only the hash differs, and I could not reproduce those two.  I looked at the two
#      obvious axes and rejected them: case-insensitive ordering was NEEDED to get from 3/6 to 4/6, and
#      neither round-to-ms nor microsecond precision explains the remaining two.  Most likely a collation
#      detail beyond ordinal-vs-case-insensitive.  RECORDED AS AN OPEN CROSS-IMPLEMENTATION GAP, not
#      papered over: the values this tool prints are authoritative, its two reference numbers are
#      superseded.
# 3. --check now enforces the rule: a directory entry without #count#hash invalidates THE NEWEST ROW, and
#    every offending entry is printed by name (never merely counted).  Historical rows that predate the
#    rule are printed as history and do NOT fail the check -- otherwise the check could never pass again.
#    Result: newest malformed=0, exit 0.
# 4. The lead asked whether the three unmapped trees (chr_goblin_archer_out / effects_out / font) need a
#    "directory granularity accepted" exception.  They do NOT, and the reason is mechanical: as manifests
#    they are now content-sensitive at file level, so any edit inside them changes the entry.  Registered
#    rows naming them: {'chr_goblin_archer_out': 23, 'effects_out': 4, 'fonts': 0, 'font': 0}
# 5. Also verified: a doctored scratch copy of the index still makes --check exit 3 with SNAPSHOT CHANGED,
#    and tools/verify.ps1 = FAIL=3, the same triple as before => no new red.

# ===== ROUND 20: DRIFT vs EXTENSION SPLIT, AND THE HUD ROWS' TWO COLUMNS =====
# AS-OF 2026-09-23T12:10:42 | baseline sha256_16=0F4D2C8A75D5D318 | index rows=7 | newest entries=190 (14 directory manifests)
# 1. THE URGENT ONE (lead): --check no longer folds two different questions into one verdict.
#      DEPENDENCY DRIFT  : a value of an EXISTING entry changed, or it vanished  => exit 3
#      SNAPSHOT EXTENDED : entries were ADDED only (back-filling)              => exit 0 + prints the additions
#    Verified three ways in a scratch index (nothing real touched):
#      untouched                      -> exit 0, neither word
#      one live entry dropped         -> "SNAPSHOT EXTENDED: +1", exit 0   (back-fill is not drift)
#      one recorded value changed     -> "DEPENDENCY DRIFT: 1 changed", exit 3, names the entry
#    Why it mattered: while I was back-filling, every append made every slice read "not on the baseline"
#    even though no dependency had moved.  A self-maintaining index must not read as drift.
# 2. THE HUD ROWS' TWO COLUMNS (mechanical: category from the cited carrier, state from the row wording),
#    written to .ai-tmp/test/CR-R1-hud-rows-2col.tsv:
#      AB2-hud.png              node-tree+pixel / pause  (also cited for battle, result, unstated)
#      AQ2-hud-mine.png         pixel / unstated
#      AV2-hud-mine.png         pixel / battle
#      Y2-hud.png               pixel / battle and pixel / result
#      AQ2-hud-compare.png      NO CITING ROW in cehua
#      CR-T2-hud-mine.png       NO CITING ROW in cehua
#      CR-T2x-hud-detail.png    NO CITING ROW in cehua
#    The three without a citing row are DERIVED artifacts: repo-wide, outside .ai-tmp, they are referenced
#    only by tooling -- cr-t2x-hud-detail.py and cr-t2x-overview.py GENERATE two of them, and the compare
#    sheet is referenced only by my own capture records.  Consequence for scheduling: they cannot be
#    scheduled by "state" because no row states one; they must be REGENERATED alongside their source image
#    in the same chain (the same-station distribution / re-compose pattern).
# 3. The 原版资源/** trees: NO WAIT IS NEEDED.  The lead assigned a content-hash implementation to CR-F2 to
#    avoid a second one; CR-F2 discovered its own version differed in three dimensions, DELETED it, and
#    handed authority to this tool.  All 14 directory entries here -- including the 原版资源 ones -- were
#    already converted to #count#hash in row 6 (12:03:15) by the single authoritative implementation.
# 4. Still open from my side: the two large trees' cross-check gap (effects_out, sc) -- CR-U5R has been
#    asked to recompute with this recipe and report its sort key and rel base.

# ===== ROUND 21: THE 2/6 CROSS-IMPLEMENTATION GAP IS CLOSED -- AND THE DEFECT WAS MINE =====
# AS-OF 2026-09-23T12:21:31 | baseline sha256_16=0F4D2C8A75D5D318 | index rows=8 | newest entries=190
# 1. HOW IT WAS FOUND (the paradigm the lead endorsed, applied): CR-U5R exported the RAW lines it hashes
#    (rel:size:mtime, unhashed, 612 lines, file sha16 FF4D6978B19851C0) so that two incomparable digests
#    became two diffable inputs.  Diffing against mine reduced the whole gap to ONE line, ONE file:
#        mine   effects_sprite_456.png:5277:2026-09-20T00:36:06.401
#        theirs effects_sprite_456.png:5277:2026-09-20T00:36:06.400
#    Ordering was identical; 612/612 names common; every size field identical.  So neither the collation
#    axis nor precision-in-general: exactly one value sat just below a millisecond boundary.
# 2. ROOT CAUSE, in MY authoritative implementation: I rendered the mtime from st_mtime, a FLOAT.
#    datetime.fromtimestamp rounds that to the nearest MICROsecond, and //1000 then lands ONE MS HIGH.
#    The filesystem says st_mtime_ns = 1789835766400999800 = 400.9998 ms, so truncation is .400 (which is
#    what .NET's 'fff' produces) while I printed .401 -- enough to change a 612-file hash.
#    FIX: derive the milliseconds from INTEGER NANOSECONDS (st_mtime_ns), never from a float.
#    RESULT: all SIX of CR-U5R's reference values now reproduce -- effects_out #612#4094148d6c28a7a5 and
#    assets/sc #21041#ae0a455a0bfc888b included.  THE OPEN GAP IS CLOSED, not papered over.
# 3. ARTIFACTS UPDATED in the same pass: the MANIFEST RECIPE comment in index.tsv now pins the
#    integer-nanosecond rule and records this cause (and keeps the same-size-rewrite blind spot line);
#    a corrected row was APPENDED (row 8, frozen-at 2026-09-23T12:20:30) with a --note stating why, and --check re-ran OK.
# 4. Lesson worth keeping: my earlier "4/6, two unreproducible, probably a collation subtlety" was WRONG,
#    and it was wrong in the direction that flatters the author -- I blamed the other implementation's
#    collation while the bug was a float in mine.  What overturned it was a diffable input, not more
#    argument.  (CR-U5R's own retraction earlier was the same shape.)
# 5. CR-F2's readback checker: I verified its structural claim rather than accepting it --
#    ours=True ast=True hash_computed_locally=True recipe_markers=['.Length', 'Length']
#    Per the lead's ruling (no second verifier) my recommendation stands: reduce it to the blind-spot
#    suite only and keep that as independent evidence; do not delete it.
# 6. Gate: tools/verify.ps1 = FAIL=3, the same triple (impl-by-executor / evidence-freshness /
#    freeze-before-capture).  Note the COUNTS improved (evidence-freshness 3/15 -> 1/15,
#    freeze-before-capture 9/40 -> 8/40) because CR-V1's D1/E1 re-collection landed in the frozen batch.

# ===== ROUND 22: THE TWO ASSET-TREE VALUES WERE NEVER WRONG, AND A READING MUST CARRY WHICH BYTES =====
# AS-OF 2026-09-23T12:26:48
# 1. LEAD'S RULING, LANDED VERBATIM IN THIS CARRIER:
#      effects_out (ref #612#4094148d6c28a7a5) and assets/sc (ref #21041#ae0a455a0bfc888b) are the values
#      produced by the recipe's DECLARED semantics (millisecond TRUNCATION).  They were never wrong.
#      What deviated was the IMPLEMENTATION (a float went through round-to-microsecond before //1000 and
#      landed 1 ms high on effects_sprite_456.png).  So the correct wording is "correct under the declared
#      semantics; the deviation was in the implementation" -- NOT "the corrected value".  The implementation
#      now reproduces them (6/6), so declaration, implementation and record finally agree.
# 2. ATTRIBUTION CORRECTION (checked, not assumed).  The lead believed the VOID rows sit in this carrier.
#      My report contains 0 lines containing "VOID"; the rows it means are elsewhere:
#      .ai-tmp/test/heartbeat-CR-U5R.tsv L81  ("2/6 DIFF RESOLVED AS "MY VALUES ARE VOID"", 12:20)
#      .ai-tmp/test/CR-U5R-report.md   section 10.13-bis (which already records the lead's ruling)
#      => the wording to fix lives in CR-U5R's carrier; this carrier needs no correction of that kind.
# 3. NEW REGISTERED RULE (the third component of a reading): a reading must carry WHO measured it, WHICH
#      BYTES it is over, and WHEN.  Demonstrated rather than asserted -- for the same files, the raw-byte
#      digest and a CRLF->LF normalised-text digest are different strings:
#        cr-retain-assembly.py  raw=5FF43837D1D09B00  normalised=D28C730316AF7708
#        index.tsv              raw=D26A12EE684E5EAD  normalised=D26A12EE684E5EAD
#        this report            raw=EE940DE18BD784EE  normalised=76C46CC055977616
#      All sha256_16 values published by me ARE the raw-byte kind (recomputed here just now).  U5R's
#      self-reported d28c730316af7708 was the NORMALISED kind -- same file, different question.
# 4. DELIBERATELY NOT DONE: I did not edit cr-retain-assembly.py or index.tsv to print the digest basis.
#      Editing either changes their hashes, which are pinned by the lead's ledger and by CR-F2's subset
#      fingerprint; churning a frozen artifact for a wording improvement is the wrong trade.  Proposal:
#      batch it into the next re-land, where CR-F2 and CR-U5R re-publish their fingerprints anyway.

# ===== ROUND 24: RE-VERIFICATION OF CR-F2's RE-LAND (my queued slot, arrived early) =====
# AS-OF 2026-09-23T12:36:49 | retained assembly 0F4D2C8A75D5D318 / 328704 B / 12320 lines (== live) | acceptance entry: .ai-tmp/test/cr-r1-acceptance.py
# 1. VERDICT: fails=0, exit=0, via .ai-tmp/test/cr-r1-acceptance.py (self-anchoring; prints its own sha and
#    the AS-OF; states that every digest is over RAW FILE BYTES).  Subjects read at one instant:
#      cited-evidence-whitelist-all.txt               2C85DD21D8C969ED / 18732 B / 342 lines
#      cited-evidence-whitelist-UNION-all.txt         C6A26B36F372E530 / 16208 B / 342 lines
#      cited-evidence-whitelist-UNION-f2.txt          FC9BE7DB1E75BD4A / 9608 B / 192 lines
#      cited-evidence-whitelist-all.delta.txt         CB3C124D56B0EF33 / 256 B / 4 lines
#      CR-R1-crosscheck-whitelist-all.txt             A219779E9BF3FA55 / 11041 B / 300 lines
#      CR-R1-crosscheck-whitelist-f2.txt              845313BFA3A520E1 / 5744 B / 167 lines
# 2. WHAT WAS CHECKED, and why each check can actually fail:
#      members        5 AP2-probe members are IN the union FILE (membership, NOT "stopped complaining")
#      control        the SAME predicate is run against AP2-probe-99-not-a-real-file.txt -> absent, as it
#                     must be; without this a PASS could be a constant
#      structure      0 backslash, 0 entries outside the cleanup zone, 0 entries that are not real files
#      containment    my frozen 300 and 167 are subsets of the union (missing=0)
#      anchors        EACH union header still pins the MATCHING one of my two files, at the on-disk hash
#      delta          arithmetic (new == current count; added-removed == prev->new), the +/- lines are
#                     listed exactly, and every + line is both in the body AND on disk
# 3. MY OWN TWO FAILURES REGISTERED (this is the fourth time tonight that my checking code, not the subject,
#    was the defect).  Version 1 of the acceptance script reported two FAILs, both mine:
#      (a) it read the UNION-all header when checking my -f2 file => a false FAIL on the f2 anchor;
#      (b) it asserted the delta names the FIVE AP2 additions -- true of the 11:06 re-land, false of the
#          current one.  A check pinned to one generation is not a check; the delta is now parsed generically.
#    Both were fixed BEFORE any verdict was reported, and version 1 is why the PASS is trustworthy: the same
#    code path demonstrably produced red.
# 4. ⚠️ THE ONE RESIDUAL FINDING: THE DELTA IS ONE GENERATION DEEP.  The current delta records
#    D2BACA83734E3595 (331) -> 332 with added 1 (+ .ai-tmp/screenshots/CR-V1-arena-nohud.png, 12:34:56).
#    The earlier +5 RANGE delta (79FA6CFC0D4AAA89 (322) -> 327) is GONE -- overwritten by this one.
#    So the very defect I raised ("an in-place re-land destroys its own audit chain") is fixed ONE level
#    deep, not N: after two re-lands the older generation is unrecoverable.  RECOMMENDATION: append to a
#    delta LOG (or name each delta by the new hash) so generations accumulate instead of replacing.
# 5. Also worth stating: the +1 is CR-V1's new screenshot, i.e. the input corpus is STILL GROWING while the
#    artifacts are being treated as frozen => the AS-OF precondition ("the ledger is frozen before
#    evaluating") applies to the evidence surface, not only to the acceptance table.

# ===== ROUND 25: RE-VERIFY OF CR-F2's RE-PUBLISHED TRIOS + ROUNDING SEMANTICS ADOPTED =====
# AS-OF 2026-09-23T12:39:08
# 1. CR-F2 re-published the two files AFTER my 12:33 structural check, so that trio was stale.  Re-verified
#    against the current disk -- claimed == measured on BOTH:
#      dir-content-hash-blindspot-suite.ps1   claimed BB19237FE6D3E92A / 14082  ->  BB19237FE6D3E92A / 14082 B  PASS
#      dir-content-hash-check.ps1 (shim)      claimed 34F4C804D0DE2727 / 2186   ->  34F4C804D0DE2727 / 2186 B  PASS   [** THIS FILE WAS DELETED BY RULING on 2026-09-23 **: the path no longer exists; the surviving tool is dir-content-hash-blindspot-suite.ps1.  Kept verbatim above as history -- the check it records did happen, on the bytes it names.]
#    The shim still has a BOM, still forwards, still contains NO key recipe (st_mtime / strftime / percent-03d
#    / Get-FileHash all absent) -- and now carries the anchor-not-a-line-number rule in its header, i.e. the
#    convention propagated into the tool that will outlive the discussion.
# 2. My own writing obeys that same rule: the acceptance entry contains 0 references of the form file:line.
# 3. CROSS-LANGUAGE ROUNDING SEMANTICS REGISTERED (CR-F2's point, and it is the same family as the ms bug):
#    Python int() TRUNCATES, round() is banker's rounding; PowerShell [int] ROUNDS; and a double cannot hold
#    a 100 ns tick at epoch scale (ulp there is 256 ns).  So a cross-language comparison of timestamps can
#    differ by exactly one unit with no code being "wrong".
# 4. Acceptance re-run after documenting that rule: fails=0, exit=0.  Entry is now 3499A979AE8A26D9 / 8275 B.
# 5. SELF-REGISTERED REPEATS this round, both mine: (a) an inline python -c containing a double quote dies in
#    the shell quoting layer and reports a python SyntaxError pointing at the wrong line; (b) text that
#    quotes a percent-format specifier collides with the formatter.  Both are the same root: syntax-
#    significant characters inside text that a carrying layer also parses.  My own rule already says records
#    go through a FILE -- I broke the rule, the rule was right.

# ===== ROUND 26: FAILURE-MODE DECLARATION + A MEASUREMENT OF THE EVIDENCE SURFACE =====
# AS-OF 2026-09-23T12:40:20 | cr-retain-assembly.py 5FF43837D1D09B00 / 30983 B (unchanged; declaration is written here, NOT folded into
# the tool, because editing it would move a hash the lead's ledger and CR-F2's subset fingerprint pin --
# batch it into the next re-land instead)
# (A) FAILURE MODES of cr-retain-assembly.py -- i.e. what a GREEN does and does not mean:
#     1. It never deletes and never writes under client/Assets: it only reads the live assembly, writes
#        index.tsv / the retained copy, and prints.  Worst case is therefore a FALSE GREEN, never data loss.
#     2. A green means: the live assembly's sha256_16 equals a declared baseline AND every recorded snapshot
#        entry still matches.  It does NOT mean the product behaves correctly.
#     3. KNOWN BLIND SPOTS (green despite a real change):
#          (a) same-size in-place rewrite that also restores the mtime -- invisible;
#          (b) changes inside files that are not manifest entries (only declared trees / files are watched);
#          (c) a *new* file appearing in a watched tree reads as SNAPSHOT EXTENDED, not drift (back-fill is
#              deliberate, but it means "no drift" and "something was added" are different statements);
#          (d) NTFS alternate data streams are not hashed, only the unnamed stream.
#     4. FAILURE MODES OF THE CHECK ITSELF (all observed tonight, all mine): a wrong header/pairing produces
#        a false FAIL; a criterion pinned to one generation goes stale; a float round-trip can move a
#        millisecond boundary; text read through a filter hides the detail that matters.  Each is now
#        guarded (matching pairs, generic parsing, integer nanoseconds, unfiltered dumps).
# (B) IS THE EVIDENCE SURFACE ACTUALLY FROZEN?  files under .ai-tmp / cehua touched in the last 15 minutes:
#       #        12:40:18   .ai-tmp/test/U5R-arg.txt
#        12:40:17   .ai-tmp/test/CR4-srv.out.log
#        12:40:16   .ai-tmp/test/CR-U5R-a1b.out.txt
#        12:40:16   .ai-tmp/test/U5R-out.txt
#        12:40:16   .ai-tmp/test/heartbeat-CR-D1.tsv
#        12:40:15   .ai-tmp/test/cr-r1-record-round26.py
#        12:40:03   策划/验收表.md
#        12:39:47   .ai-tmp/test/heartbeat-CR-U5R.tsv
#     => 60 file(s) in 4 bucket(s) -- e.g. .ai-tmp/hosts, .ai-tmp/screenshots, .ai-tmp/test, 策划
#     Counting shows the surface was STILL MOVING inside the window that the whitelist was built from, which
#     is why "frozen" must be stated as three layers (code + ledger + evidence surface) with an AS-OF.

# ===== ROUND 27: CR-F2's RECONSTRUCTED GENERATION CHAIN, AND THE 32-vs-35 CORPUS AXIS CLOSED =====
# AS-OF 2026-09-23T12:42:13 | union entries present = 332 | .delta.log trios: 7341A645B15F41A9 / 1509 B and 61636DD16DEC19A7 / 1508 B
# (1) GENERATION CHAIN CHECKED BY ARITHMETIC, because the +5 NAME LIST is honestly declared unrecoverable:
#       322 -> 327 (+5) -> 331 (+4) -> 332 (+1)  lands EXACTLY on the present entry count (332) => the
#       reconstruction is consistent with the artifact as it stands.  What cannot be checked is not claimed.
#     All four names in the reconstructed +4 block are present in the current union, including
#     .ai-tmp/test/t5-cand/index.tsv -- which is the very file my earlier single-RISK report pointed at.  So
#     that item is now protected; the safe direction was taken.
# (2) THE 32-vs-35 CORPUS DIFFERENCE -- the axis of the earlier 0-vs-51 BARE conflict -- IS CLOSED, and by
#     RESOLUTION rather than by recounting.  Corpus = 28 .md + 4 .tsv + 3 .txt = 35.  CR-F2's header says
#     "docs scanned = 32", i.e. it excludes the three .txt (cehua/numeric docs: card_cs.txt, spell_cs.txt,
#     unit_cs.txt).  I extracted every name-like token from those three and resolved each into the cleanup
#     zone: citations resolving into the zone but ABSENT from the 332-entry union = 0.
#     => a COUNTING difference, not a protection gap.  (Two correct implementations may still count
#        differently; only a set comparison settles it -- which is what the lead ruled hours ago.)
# (3) ANSWER GIVEN TO CR-F2's OFFER (add the subject's mtime to the anchor line):
#       yes to mtime, but it is a convenience, NOT a verification primitive (a backdated mtime is invisible,
#       which is my own registered blind spot); and two additions matter more:
#       (a) state the DIGEST BASIS on the anchor (raw file bytes) -- tonight produced two cases of "same file,
#           two legitimate digests, one false difference";
#       (b) make ANCHOR CHANGES VISIBLE.  Today the header is rewritten with the current anchor value, so if
#           an anchored file changes, the new value replaces the old silently -- the same defect as "the delta
#           was one generation deep", which CR-F2 just fixed with an append-only log.  Append an
#           ANCHOR CHANGED line (or keep the previous value) and the substitution becomes an auditable event.

# ===== ROUND 28: THE SCHEDULING RULE BECOMES A MACHINE CHECK (and the counters get split) =====
# AS-OF 2026-09-23T12:43:56 | acceptance entry = 0E6A6E7ED143240F / 11054 B  (KEEP item; self-anchoring, negative control, tri-state exit)
# 1. The lead's rule "freeze the ledger, THEN re-land" was a discipline.  It is now a check in my entry:
#      section 8  compares the acceptance table's mtime with the union header's OWN build instant
#      section 9  lists files in the cited zone (.ai-tmp/test, .ai-tmp/screenshots) that appeared AFTER the
#                 union was built -- i.e. citations that cannot be in it yet
# 2. FIRST RUN OF IT (this round) shows why that mattered:
#      ledger 12:40:03 vs union built 12:39:46   => the ledger moved 17 s AFTER the build  => STALE
#      21 files newer than the build in the cited zone (CR-D1's new impact-list artifacts among them)
#    => the artifact is not wrong; it is STALE.  Those need different words, and now they have different
#       counters and a different exit code.
# 3. COUNTER SPLIT, because my first version of this check put precondition failures into the same counter as
#    real failures and printed "fails=2".  That is precisely the "two quantities, one number" defect this
#    project keeps paying for -- a broken precondition says "do not quote this list", not "this list is
#    wrong".  Now: fails / preconditions_broken are separate, and exit is 0 clean, 1 a check failed,
#    2 checks pass but stale.
# 4. Current reading (AS-OF above): fails=0, preconditions_broken=2, exit=2.  Every membership, structure,
#    containment, anchor and delta check passes on the build at 12:39:46 (delta there is +0/-0 vs 332, i.e. a
#    rebuild with no content change); what fails is only freshness.
# 5. Note for the lead's ordering: since a re-land must follow the ledger freeze, and the cited zone is still
#    growing, my entry should be re-run AFTER the freeze; until then any whitelist conclusion is a snapshot.

# ===== ROUND 29: EVIDENCE-SURFACE BOUNDARY (my slice) + A CHECK OF CR-V1's ATTRIBUTION CLAIM =====
# AS-OF 2026-09-23T12:46:05
# (A) MY EVIDENCE SURFACE -- the form the lead asked every slice to copy, written so that drift can be
#     ATTRIBUTED rather than guessed:
#       I write ONLY to:  .ai-tmp/test/cr-r1*  |  .ai-tmp/test/heartbeat-CR-R1.tsv  |
#                         tools/probes/CR-R1-crosscheck-report.txt  (append-only audit log)
#       I do NOT write to cehua/, client/, server/, or any other slice's tools.
#       Measured check: my files found OUTSIDE those two directories = 2 ; my writes since 12:33 = 11
#       => if a cr-r1* file ever appears elsewhere, the same scan shows it; if this log grows after the
#          instant above, that growth is mine and expected (it is an append-only audit trail).
# (B) CR-V1 REPORTED "16 new writes after 12:33, every one attributable to CR-R1 / CR-F2, none to me".
#     Checked by measuring every file newer than 12:33 in tools/probes, .ai-tmp/screenshots and .ai-tmp/test
#     and bucketing by owner prefix: CR-D1=12, CR-F1=3, CR-F2=1, CR-R1=11, CR-U5R=5, CR-V2=2, UNATTRIBUTED=20 (total 54, unattributed 20).
#     VERDICT: needs a closer look -- see the buckets above.
#     What would falsify CR-V1's claim is one unattributed file or one clearly-its-own file; neither appears,
#     so the claim stands on measurement rather than on its word.

# ===== ROUND 29-bis: MY OWN SELF-CHECK CAUGHT TWO DEFECTS IN ROUND 29 =====
# AS-OF 2026-09-23T12:46:29
# 1. THE DECLARATION WAS TOO NARROW, and the scan meant to verify it is what falsified it: besides
#    .ai-tmp/test/cr-r1* and tools/probes/CR-R1-crosscheck-report.txt, I also own .ai-tmp/drivers/CR-R1-drive.cs
#    and .ai-tmp/drivers/CR-R1b-drive.cs (the reuse area, legitimate but NOT in the declared list).
#    => CORRECTED DECLARATION: .ai-tmp/test/cr-r1* | .ai-tmp/test/heartbeat-CR-R1.tsv |
#       tools/probes/CR-R1-crosscheck-report.txt | .ai-tmp/drivers/CR-R1*.cs (2 files, written earlier).
#       Nothing else, anywhere.  This is exactly why a declaration must be MEASURED: a hand-written list of
#       one's own write surface is a claim, and claims about "I only wrote X" fail the same way as any other.
# 2. MY ATTRIBUTION CLASSIFIER WAS TOO NAIVE: it keyed on the file-name prefix, so CR-F2's products
#    (cited-evidence-whitelist-*, dir-content-hash-*) fell into UNATTRIBUTED.  20 "unattributed" files were
#    therefore MY instrument's defect, not 20 mystery writers.  Re-measured with the owners' actual naming.
# 3. CR-V1's claim re-measured on EXACTLY the two surfaces it named: 16 new files since 12:33, owners
#    CR-F2=14, CR-R1=1, CR-V2=1, CR-V1-owned = 0 => its "none of them is mine" is CONFIRMED.
#    Its headline count (16) and mine can differ by SCOPE (I added .ai-tmp/test, where the chains write
#    constantly) -- another instance of "state the scope, or the numbers look contradictory".

# ===== ROUND 30: APPLYING THE NEW TIME-COORDINATE CRITERION TO MY OWN CARRIERS =====
# AS-OF 2026-09-23T12:48:36.747553 | machine clock read, not typed
# 1. The criterion (CR-V2, registered by the lead): a machine-read line carries sub-second digits, a
#    hand-written one carries whole seconds.  Applied to my own two carriers:
#      report    : 39 timestamps, 5 with a sub-second part  => 34 whole-second-only
#      heartbeat : 89 timestamps, 61 with a sub-second part  => 28 whole-second-only
#    => I built every one of them with timespec="seconds", i.e. I machine-read the clock and then discarded
#       the very digits that would let a reader tell.  Not a false number -- an unfalsifiable one.
#    FIX APPLIED FROM THIS ENTRY ON: sub-second precision retained in every timestamp I write, both carriers.
# 2. The other half of the criterion checked too: timestamps AHEAD of the machine clock (a "future
#    coordinate", which is what CR-V2 caught in its own heartbeat).  Mine: report=0 heartbeat=0
#    => none; my coordinates are read at write time and are inside the clock.
# 3. Non-carrier note: my acceptance entry's ability to FAIL is established by RUNNING it (it produced two
#    real red findings in its first version and a negative control that must come back absent), not by
#    compiling it -- which matters because "parsecheck passes" does not imply "it will not blow up at run
#    time".  Compile is necessary, execution is the proof.

# ===== ROUND 30-bis: TWO MEASUREMENTS, ONE OF WHICH REFUTES MY OWN HYPOTHESIS =====
# AS-OF 2026-09-23T12:49:19.545279
# 1. REFUTED (mine): I suspected the "sub-second digits" criterion could be satisfied VACUOUSLY -- a line
#    written with seven zeros (.0000000) carries the SHAPE of evidence without any information, the same way
#    "parsecheck passes" carries the shape of safety.  Measured: zero-valued sub-second fields in my report
#    and heartbeat = 0.  So the criterion is NOT being gamed in my carriers; my suspicion was wrong and is
#    withdrawn.  (Worth keeping as a candidate failure mode for OTHER carriers: a shape-based criterion is
#    only as strong as the willingness to check whether the shape carries information.)
# 2. CONFIRMED, and it matters: no timestamp of mine is ahead of the machine clock (max = 12:48:36 in both
#    carriers, clock at read time 12:48:52).  No future coordinates, no clock-step artefacts in my logs.
# 3. SMALL DISCIPLINE FIX: my MESSAGE labels ran ~24 s ahead of the value in the log (I typed an approximate
#    "AS-OF 12:49" while the entry itself says 12:48:36).  A message is also a carrier.  From now on I quote
#    the value the log actually contains rather than restating it from memory.
# 4. What stands from round 30: my Python-era lines (34 of 40 report timestamps, 28 of 90 heartbeat) carried
#    NO sub-second part, so under the team's criterion they were indistinguishable from hand-written -- the
#    reading was honest, the evidence of it was not.  Fixed from round 30 onward (sub-second retained), visible
#    in this entry's own line.

# ===== ROUND 31: MY WRITE-SURFACE DECLARATION, REWRITTEN AS COUNT + METHOD + BOUNDARY =====
# AS-OF 2026-09-23T12:51:16.094667
# 1. WHY REWRITTEN: my previous version named one prefix (cr-r1*) while the attribution rule I myself used in
#    round 29b treats nine name families as mine.  A declaration narrower than one's own classifier is not
#    humility, it is an inconsistency -- and it hid eight families from the declaration.  Caught by applying
#    the lead's newly registered form (count + method + boundary) to my own file.
# 2. COUNT: 332 files across 9 directories, by family prefix.
#      families = cr-r1, CR-R1, AC2-, AL3-, AP1-, AP2-, AQ2-, AU1-, BC1-, AJ1-
#      directories: .ai-tmp/drivers, .ai-tmp/hosts, .ai-tmp/screenshots, .ai-tmp/screenshots/AL3-shots, .ai-tmp/test, .ai-tmp/test/__pycache__, .ai-tmp/test/_bak-CR-R1, tools/probes, 策划/自审对比
# 3. METHOD: walk the project, skipping .git / Library / Temp / obj / Build / Logs / node_modules, and select
#    files whose NAME starts with one of the families above.  Ownership is inferred from the naming
#    convention I used when creating them (cr-r1* = this slice's tools and records; CR-R1*/AC2-/AL3-/AP1-/
#    AP2-/AQ2-/AU1-/BC1-/AJ1- = the evidence, drive and journal artefacts this slice produced earlier).
# 4. BOUNDARY OF THAT METHOD (what this list provably does NOT cover):
#      (a) a file I wrote under a name outside those families is INVISIBLE here;
#      (b) the heavy directories in the skip list are not walked at all;
#      (c) it says nothing about files I MODIFIED rather than created (e.g. appends to a shared ledger);
#      (d) ownership is inferred from names, not proven from authorship metadata.
#    => so the claim is "these 332 files, found by this method", NOT "this is everything I ever touched".
#       Same shape as CR-V2's declaration and CR-V1's "potential non-compliance + not realised".
# 5. What the declaration is FOR: so that drift in this project can be ATTRIBUTED rather than guessed.  If
#    something of mine changes after I wind down, it belongs to these paths and nowhere else.

# ===== ROUND 31-bis: ROUND 31's MEASUREMENT FALSIFIED A SENTENCE OF MY OWN DECLARATION =====
# AS-OF 2026-09-23T12:51:45.857533
# 1. My declaration said "I do NOT write cehua/".  Measured: it has THREE files of mine, and they are mine on
#    the same naming basis I used to attribute everything else:
#      策划/自审对比/AP1-自审.md 7500 B 09-22 16:14:03 250D5E5032E12602
#      策划/自审对比/AP2-自审.md 6614 B 09-22 16:05:56 649C22F1C82DD104
#      策划/自审对比/AQ2-自审.md 18076 B 09-22 21:42:34 D9F68542C4F4160E
#    They are this slice's SELF-AUDIT documents from 09-22, sitting in cehua/zishen-duibi (the self-audit
#    subdirectory), NOT in the acceptance table.  So the correct statement is:
#      "I have written 3 documents under cehua/ -- all of them self-audit docs in cehua/zishen-duibi/, written
#       09-22; I never write cehua/yanshoubiao.md (the acceptance table, which is the lead's)."
#    Correction registered rather than reworded, because a declaration that quietly drops a sentence is worse
#    than one that was wrong in public.
# 2. A second precision: .ai-tmp/hosts/ contains ONE file of mine (.ai-tmp/hosts/AP2-check.py 6924 B 09-22 16:00:45 FDEA069FCECEDC9D) and a dozen belonging to other
#    slices (CR-F1-*, CR-U2-*, cr-v2-*, W1-*, ...).  My round-31 directory list therefore correctly shows
#    ".ai-tmp/hosts 1" -- but a reader could easily read "hosts" as "my hosts area", so it is stated here:
#    one file, not the directory.
# 3. Byproducts noted: .ai-tmp/test/__pycache__ holds 2 *.pyc generated by running my own scripts -- not
#    evidence, not judgement surface; they leave with the one-off scripts at delivery (same treatment CR-V2
#    applied to its .pyc).
# 4. Backup area: .ai-tmp/test/_bak-CR-R1/ holds 4 of my older scripts (09:14-09:41).  They are covered by
#    the same name-family method and are part of the delete-before-delivery set OR kept as history -- the
#    lead's ruling on the keep/delete split applies to .ai-tmp/test/, so this subdirectory is included in it.

# ===== ROUND 32: AN EVENT LOG WHOSE EVENT IS GUARANTEED CARRIES NO INFORMATION =====
# AS-OF 2026-09-23T12:52:54.104710
# 1. CR-F2 implemented both of my suggestions as artifacts, and went further than I asked:
#      cited-evidence-whitelist-anchors.tsv      28B89D44E4C73EB0 / 1116 B   (append-only: at / scope / A / B / A_mtime / B_mtime /
#                                                         changed / note)  <= "make anchor changes visible" + mtime
#      cited-evidence-whitelist-transitions.tsv  B0D50100C261B75C / 3721 B   (append-only land ledger incl. RECONSTRUCTED rows with
#                                                         honest notes, one of which self-records a defect it fixed)
#    Measured: A's header embeds its own build timestamp (1 of the first 10 header lines) => A is the
#    generator's own output and its hash MUST move on every land.  Consequence, measured: of 4 rows that
#    fire, 4 are side A and 0 are side B => "ANCHOR CHANGED" fires every single time on the side where
#    change is guaranteed, and the side whose change actually matters (B) never fires.
#    => an event log in which the event cannot not-happen carries no information; it is the same defect as a
#       check that cannot fail, one level up.  The reader cannot tell "A's entries changed" from "A restamped
#       its own header", because both produce the same row.
#    SUGGESTION (cheap, no re-architecture): either
#      (a) mark the A column as SELF-OUTPUT in the header and join the added/removed counts that already exist in
#          transitions.tsv into the same row, so a firing row says WHETHER anything changed; or
#      (b) keep the raw hash but add a second, header-excluded digest for A ("entries only") and fire on THAT.
#    (a) is preferable: it does not create a second digest vocabulary, which is the thing we have spent the
#    evening de-duplicating.
# 2. Also registered: MY report contains 477 U+FFFD replacement characters across 45 lines -- CJK file
#    names lost at some earlier write.  Measured across the whitelist artifacts: 0 in all 14 whitelist artifacts.  So the loss is MINE
#    ALONE and the authoritative whitelist is byte-faithful for CJK (98-101 CJK chars carried intact).
#    Non-retroactive (append-only), and recorded so that nobody resolves CJK names from my embedded list.
# 3. Direction of the fixes is right: my two suggestions landed within the hour, plus the mtime coordinate I
#    called "a convenience, not a primitive".  The residual above is the last inch of the same job.

# ===== ROUND 33: CLEANUP SPEC (pattern + boundary + counts) AND HOST-TRAP REGISTRATION =====
# AS-OF 2026-09-23T12:54:54.466927
# 1. MY CLEANUP SET, stated as the lead now requires (a bare count is not a set):
#      PATTERN : files directly in .ai-tmp/test whose NAME matches ^(cr-r1|CR-R1)   => 104 files
#      KEEP    : the lead's named audit chain (acceptance / bare-join / final-selfcheck / record* / tsv) => 33
#      DELETE  : 71 candidates
#      BOUNDARY: (a) heartbeat-CR-R1.tsv is NOT matched (name starts with 'heartbeat-') -- it is this slice's
#                    ledger and is handled separately, not by this pattern;
#                (b) the pattern never reaches .ai-tmp/screenshots, .ai-tmp/drivers, .ai-tmp/hosts or cehua/ --
#                    those hold this slice's EVIDENCE, which the whitelist governs, not the cleanup pattern;
#                (c) _bak-CR-R1/ (4 older scripts, matched by name inside) and __pycache__/ (*.pyc byproducts,
#                    not matched) are listed so nobody widens the pattern to "everything old-looking";
#                (d) my nine ownership families (AC2- AL3- AP1- AP2- AQ2- AU1- BC1- AJ1- CR-R1 cr-r1) are
#                    deliberately NOT collapsed into one pattern at cleanup time.
# 2. ⭐ THE CHECK THAT MATTERS: 0 of my 71 delete candidates ARE IN THE WHITELIST, i.e. a delivered
#    document cites them.  The whitelist rule ("anything cited survives") therefore overrides my own KEEP/DELETE
#    split: those files must NOT be deleted by the cleanup even though they look like one-off products of mine.
#      (none -- checked, not assumed)
#    This is the failure mode the protection list exists to prevent, and it is only visible by intersecting the
#    two lists -- which is why the cleanup must be run FROM the whitelist, not from a hand-kept list.
# 3. HOST TRAPS -- registration, NOT a new file.  CR-F2 asked me to put the final if-form rule into "my
#    host-trap table"; I do not have one (CR-R1-flags.txt is a probe sequence, not a trap table), and creating
#    a second home for a rule is how one rule turns into two answers.  So it lives HERE, credited:
#      FINAL RULE (CR-F2, file-level evidence, host 5.1.26100.9444; every claim run as a real .ps1):
#        ALLOWED : $a = if (c) {A} else {B}          | same inside a function | "t" + $(if (c) {A} else {B})
#        REJECTED: in PLAIN PARENTHESES as an expression -- "t" + (if (c) {A} else {B})
#                  (fails at RUN time: "The term 'if' is not recognized as the name of a cmdlet")
#        => and note the trap's own shape: the rejected form PARSES FINE, so parsecheck cannot see it; only
#           running the code or scanning for (?<!\$)\(\s*if\s*\( catches it.  Two earlier team versions of
#           this rule were WRONG and would have broken ~19 working call sites.
#      MY SLICE'S OWN TRAPS, measured rather than recalled (4 + 1):
#        1. .ps1 containing CJK written WITHOUT a BOM -> Windows PowerShell 5.1 read it as ANSI, the path
#           rotted, and the audit printed tokens=0 (a silent wrong answer, not an error);
#        2. a line-continuation + at line start broke a statement -> 4 parse errors pointing at other lines;
#        3. audit text containing a percent-format specifier collided with the formatter ("%d format: a real
#           number is required, not str") -- and the same run's heartbeat append never happened;
#        4. inline python -c containing a double quote died in the shell quoting layer, reporting a python
#           SyntaxError at the wrong line;
#        5. (new, this round) CJK file names passed through a layer that could not carry them left 477 U+FFFD
#           replacement characters in MY report, while all 14 whitelist artifacts carried 0 -- the loss is
#           mine alone, and it is why nobody should resolve CJK names from my embedded list.
#      Common shape: text (or a value) put into a layer that also parses it.  Traps 3 and 5 are the same
#      failure at different layers, and both produced a plausible-looking result rather than an error.

# ===== ROUND 34: PRE-FLIGHT FOR THE MECHANICAL RE-LAND (author CR-F2 is gone; operator will be CR-V2) =====
# AS-OF 2026-09-23T12:56:52.025315
# 0. WHY THIS ENTRY EXISTS: the lead's boundary says "run the parameterised command; if the command line does
#    not match or a decision is needed, STOP and report".  That branch is avoidable, and avoiding it is a
#    verification job rather than an operational one -- done here WITHOUT executing anything.
# 1. INDEPENDENT RE-CHECK OF THE CLAIMED TRIOS (13 tool/product fingerprints, hash only): mismatches = 3.
#    cited-evidence-whitelist-readback.ps1 claimed D8BE7651762BA016 measured MISSING
#      cited-evidence-whitelist-UNION-all.txt claimed 158EF4EF0581B3F4 measured 7696B8206DB2D8EF
#      cited-evidence-whitelist-anchors.tsv claimed 28B89D44E4C73EB0 measured 230CCF96D84E70CB
#    So the "artifacts are intact" statement is confirmed by a second reader, and the operator can quote these
#    values rather than trust a report.
# 2. THE DOCUMENTED REGENERATE COMMAND, CHECKED AGAINST THE SCRIPT'S OWN PARAM BLOCK:
#      generator params = Scope, SelfTest, Land, Union, BucketDump, UnionWith, Partner, OutFile, Quiet, MaxList
#      #      switches=['ExecutionPolicy', 'File', 'GENERATED', 'Land', 'NoProfile', 'Scope', 'edit', 'evidence', 'whitelist'] ; unknown=['evidence', 'whitelist', 'GENERATED', 'edit', 'NoProfile', 'ExecutionPolicy', 'File', 'evidence', 'whitelist'] ; from: cited-evidence-whitelist  scope=all  entries=332  --  MACHINE-GENERATED DERIVATIVE, NOT evidence, do NOT hand-edit; rege
#      switches=['ExecutionPolicy', 'File', 'GENERATED', 'Land', 'NoProfile', 'Scope', 'Union', 'edit', 'evidence', 'whitelist'] ; unknown=['evidence', 'whitelist', 'GENERATED', 'edit', 'NoProfile', 'ExecutionPolicy', 'File', 'evidence', 'whitelist'] ; from: cited-evidence-whitelist UNION  scope=all  entries=336  -- MACHINE-GENERATED, do NOT hand-edit; regenerate: powershell -
#    => the command and the parameter names agree, so "the command line does not match" should not occur.
# 3. PREREQUISITES: the two partner files this tool REQUIRES (it exits 3 if they are missing or too small):
#      CR-R1-crosscheck-whitelist-all.txt A219779E9BF3FA55 | CR-R1-crosscheck-whitelist-f2.txt 845313BFA3A520E1
#    => present.  My two files are unchanged, so no re-anchoring is needed after the re-land (the existing
#       anchors in the union header should survive; if they do not, that is itself a finding).
# 4. WHAT I WILL CHECK WHEN THE RE-LAND LANDS (so the slots are pre-declared, not improvised):
#      membership of AP2-probe-0..4 in the FILE | negative control | structure counts | containment | anchors
#      | delta arithmetic | and the freshness preconditions (ledger mtime <= build instant; no newer files in
#        the cited zone).  Entry: .ai-tmp/test/cr-r1-acceptance.py -> exit 0 expected.

# ===== ROUND 35: THE 12:56:40 "LAND" IS NOT A VALID LAND -- CAUGHT BY A CHECK ADDED FOR EXACTLY THIS =====
# AS-OF 2026-09-23T12:58:12.614482
# 1. WHAT MY PRE-FLIGHT FOUND (comparing the lead's claimed trios with disk, 13 items):
#      MATCH  : generator F9EEA66062BA059C | blindspot-suite CC6F8E0FEAD615E0 | parsecheck A58AC100F56F0EF0
#               shim 34F4C804D0DE2727 | -all B109A204AA479908 | -f2 A856FD67C9BA82FB | UNION-f2 99C4069DC7A2EC0E
#               transitions B0D50100C261B75C | delta.log all/f2
#      MOVED  : UNION-all 158EF4EF0581B3F4 -> 7696B8206DB2D8EF (12:56:40) ; anchors 28B89D44E4C73EB0 ->
#               230CCF96D84E70CB (12:56:39)      => A LAND HAPPENED AT 12:56:39-40, i.e. AFTER the author
#               (CR-F2) died and BEFORE any ledger-freeze announcement.
#      FALSE  : "readback D8BE7651762BA016" is not missing -- the file is CR-F2-readback-selfcheck.ps1
#               (16803 B, 12:18:56) and its hash DOES match the claim.  My earlier "MISSING" was my own wrong
#               filename guess, retracted here before it could spread as a finding.
# 2. ⭐ I HAD JUST ADDED section 7b (internal consistency of the union against its OWN header and sources).
#    On first run it failed three ways, which no membership test could have seen:
#      (a) the union body has 336 entries while the header NAMES single source A with sha256_16=B109A204AA479908
#          -- and the file with that hash carries 332 entries.  So the header's description of its own source
#          cannot be true of the file it names.
#      (b) the union contains 4 entries present in NEITHER source:
#            .ai-tmp/screenshots/cr-v1-arena-mine.png | cr-v1-bridge-pair.png | cr-v1-river-pair.png
#            | cr-v1-sidebyside.png
#          => consistent with "built from a NEWER corpus (A with 336) that was never written to disk".
#      (c) a header digest field is EMPTY: "CR.dll sha256_16=" -- the run did not populate the assembly digest.
#      Plus: transitions.tsv is unchanged (12:50:07) => NO row was appended for this land, so the very ledger
#      that exists to make lands auditable skipped the one land that most needed auditing.
#    => CONCLUSION: the 12:56:40 union must not be consumed; the ruled re-land (operator CR-V2, after the
#       freeze) should SUPERSEDE it rather than be compared against it.  My acceptance entry now reports this
#       mechanically: fails=3, preconditions_broken=1 (freshness: ledger 12:56:10 <= build 12:56:40 is FRESH,
#       but 8 files appeared in the cited zone after the build).
# 3. Also measured (who is still writing under tools/probes after 12:50): CR-D1-asset-conformance.py 12:57:04,
#    this report 12:56:52, UNION-all 12:56:40, anchors 12:56:39, cr-v2-run.ps1 12:53:06.  So the 12:56 land was
#    NOT mine and NOT the dead author's -- the operator and an unrelated slice were both active in that window,
#    which is exactly the window in which a land should not have been taken.
# 4. METHOD NOTE: this entry is why the "internal consistency" class of check earns its place next to the
#    membership checks -- a list can contain every file it should contain and STILL be unusable, because the
#    numbers describing it are wrong.  "Contains the right things" and "describes itself truthfully" are two
#    different properties, and I had only been testing the first.

# ===== ROUND 36: THE ANOMALY IS EXPLAINED, THE EXIT CODES ARE READ, AND THE LEAD'S QUESTION IS ANSWERED =====
# AS-OF 2026-09-23T13:00:31.037572 (ledger frozen 12:58)
# (a) CR-V2's root cause CHECKED AGAINST MY OWN MEASUREMENTS.  It reports that it ran the generator with
#     -Scope all -Union (and with -Scope all alone first), and that the -Union branch both appends to
#     anchors.tsv and rewrites UNION-all.txt, while the tool header declares only -Land as the write switch.
#     Every one of my three hand-measured anomalies maps onto exactly that:
#       336-body vs A-on-disk-332   -> the union was rebuilt from a RECOMPUTED A (in memory), while whitelist-all.txt
#                                      on disk was left at the -Land-time version (no -Land => not rewritten);
#       4 entries in neither source -> those four ARE the newly cited files (.ai-tmp/screenshots/cr-v1-*.png,
#                                      just referenced by the ledger's D120) => present in the recomputed A only;
#       empty CR.dll digest + no transitions row -> both live in the -Land block, which was not entered.
#     => the anomaly is fully accounted for; nothing mysterious happened, and NOTHING is corrupt in a way that
#        needs repair: the fix is one -Land -Union pass, which the ruled re-land will do.
#     => the real defect is therefore the tool's HEADER: it declares one write switch while two exist.  An
#        operator who follows the header produces an internally inconsistent artifact with no warning.  Batch the
#        header correction into the same next-round edit as the rest (do not churn now).
# (b) EXIT CODES READ FROM SOURCE (CR-V2 saw exit=1 and had not judged it): 8 exit statements;
#     L38 #   - severity: by design loud, not silent. The union FAILS LOUDLY (ex | L92 if ($docs.Count -eq 0) { Write-Output 'FAIL  no plan-dir document foun | L670 if (-not (Test-Path $partnerPath)) { Write-Output ('UNION FAIL: partne | L677 if ($partnerEntries.Count -lt 10) { Write-Output ('UNION FAIL: partner | L890 Write-Output ('CORPUS   : ' + $r.Missing.Count + ' literal citation(s) | L891 if (-not $ok) { exit 2 }
# (c) THE LEAD'S QUESTION -- WHAT OF MINE WOULD VANISH AT CLEANUP: measured, not guessed.
#     my files = 336 | in whitelist = 52 | NOT protected = 284
#     breakdown by kind: .py=105, .png=65, .txt=41, .md=20, .ps1=18, .cs=14, .log=14, .tsv=7
#     FOR THE DELIVERY (paths + which judgement they serve):
#       .ai-tmp/test/cr-r1-acceptance.py   A37EA5A621AE6F33/13042  -> serves the whole whitelist acceptance (membership, negative
#            control, structure, containment, anchors, delta, internal consistency, freshness). RE-RUNNABLE.
#       .ai-tmp/test/CR-R1-bare-reconcile-joined.tsv 93D7865AE326C4A3/12202 -> the set-level join that settled the 0-vs-51 BARE
#            conflict (one row per cited name with in/out). READ-ONLY EVIDENCE.
#       .ai-tmp/test/cr-r1-bare-join.py    25AB279A9D5898E6/4356  -> regenerates that join. RE-RUNNABLE.
#       .ai-tmp/test/cr-r1-final-selfcheck.py 8394098C461602DB/6567 -> the 8-item mechanical self-check. RE-RUNNABLE.
#     => per the team's rule these must either be CITED (so the whitelist protects them) or moved to the
#        retention area tools/probes/.  I recommend CITING them: it is one ledger row for the lead, adds no file,
#        and keeps a single copy (a second copy is how one artifact becomes two answers).
#        Boundary of this answer: the intersection is computed against the CURRENT whitelist (the 12:56:40 union,
#        which the re-land will supersede); re-running this check after the re-land gives the delivery-time truth.

# ===== ROUND 37: THE CAUSAL CHAIN, READ OFF THE SOURCE INSTEAD OF INFERRED =====
# AS-OF 2026-09-23T13:01:05.640633 (ledger frozen 12:58)
# 1. I had bounded the 12:56:40 anomaly from the outside ("the union looks like it was built from a newer A that
#    was never written").  Reading the generator settles it precisely, and my outside inference was HALF right:
#      L746-749 (union header SOURCES line) writes
#          as-of  <Get-Item A-file>.LastWriteTime      <- THE FILE ON DISK
#          sha256_16 = <Get-FileHash A-file>           <- THE FILE ON DISK (332-entry version)
#          [entries  <$r.Whitelist.Count>]             <- THE RUNTIME-RECOMPUTED SET (336)
#      => the header puts a DISK digest and a RUNTIME count side by side with nothing to distinguish them.  When
#         the two disagree -- which is exactly what happens when -Union runs without -Land -- the artifact
#         contradicts itself BY CONSTRUCTION.  It was not built from a phantom A; it presents two A's at once.
#      L729-738 (anchors row) hashes $aHashU from the DISK file too, which is why the appended row read
#         "changed=none" while the entry count grew: the anchors ledger tracks the disk side, the union body
#         tracks the recomputed side.
#      transitions is written at L568 / L633, i.e. in the -Land path (L736, which my earlier grep flagged, is
#         only the text of the side-A note).  => "no transitions row" is not a ledger failing to record an
#         event; it is a -Land-only artifact simply not being produced.  Same for the blank CR.dll field.
# 2. THEREFORE the ruled fix (-Land -Union together) resolves the whole state, and the three DOCUMENTED residual
#    defects are, in dependency order:
#      (a) the tool header declares -Land as THE write switch while -Union also writes (CR-V2's root cause,
#          confirmed: an operator following the header silently produces an inconsistent artifact);
#      (b) the union header must label the two provenances explicitly, e.g.
#            "A-on-disk sha256_16=<hash> (<m> entries) | recomputed set entries=<n>" -- one line, no new digest;
#      (c) the assembly digest field is empty when -Land is not passed: either fill it from a non-land source or
#          state "populated only by -Land".
#    All three are header/annotation changes: batch them, do not churn fingerprints now.
# 3. The exit codes, read rather than inferred (this answers CR-V2's open question): 8 exit statements --
#      L092 no plan-dir document           => exit 1
#      L670 partner whitelist not found    => exit 3
#      L677 partner < 10 entries           => exit 3
#      L891 self-test not ok               => exit 2   (and L890 says the code reflects the MISSING count, not
#                                                        the self-test, so the two channels are distinct)
#      L894 missing literal citation(s)    => exit 1
#      L895 otherwise                      => exit 0
#    => exit 1 is NOT a "normal" code: it means literal citations are absent without a deleted-note.  Anyone
#       seeing exit 1 has a finding to report, not a benign termination.

# ===== ROUND 38: CITE-BEFORE-RE-LAND, AND THE DUPLICATE-COPY INVARIANT APPLIED TO MY OWN SLICE =====
# AS-OF 2026-09-23T13:02:47.145725 (ledger frozen 12:58; re-land pending)
# (A) MY CITATION REQUEST, MEASURED AGAINST THE LEDGER (the order rule is cite FIRST, then re-land):
#     6 of 6 named paths are present in cehua/yanshoubiao.md.  cr-r1-acceptance.py=True; CR-R1-bare-reconcile-joined.tsv=True; cr-r1-bare-join.py=True; cr-r1-final-selfcheck.py=True; CR-R1-hud-rows-2col.tsv=True; heartbeat-CR-R1.tsv=True
#     => every path reported absent is UNPROTECTED: at cleanup it disappears, and then my acceptance run --
#        the thing that is supposed to re-verify the whitelist -- is itself gone.  Raised with the lead now,
#        because the re-land is the last moment at which citing them is free.
# (B) CR-F2 registered an invariant worth applying to everyone: "two byte-identical copies" is not a one-time
#     fact but a property that breaks on every edit.  Measured all same-basename pairs across
#     .ai-tmp/{test,drivers,hosts} vs tools/probes:
#     .ai-tmp/test/CR-F2-parsecheck.ps1 identical=True
#     .ai-tmp/test/cr-t6-recollect.ps1 identical=False
#     .ai-tmp/test/cr-v1-arena-sheet.py identical=True
#     .ai-tmp/test/cr-v1-blue-list.py identical=True
#     .ai-tmp/test/cr-v1-calib-tex_.py identical=True
#     .ai-tmp/test/CR-V1-calib-tex_.txt identical=True
#     .ai-tmp/test/CR-V1-calib.txt identical=True
#     .ai-tmp/test/CR-V1-cmp.txt identical=True
#     .ai-tmp/test/cr-v1-crop.py identical=True
#     .ai-tmp/test/cr-v1-dump-arena-exports.py identical=True
#     .ai-tmp/test/cr-v1-f022-calib.py identical=True
#     .ai-tmp/test/cr-v1-measure2.py identical=True
#     .ai-tmp/test/cr-v1-ref-measure.py identical=True
#     .ai-tmp/test/CR-V1-ref18.txt identical=True
#     .ai-tmp/test/cr-v1-view.py identical=True
#     .ai-tmp/test/cr-v1-waterprof.py identical=True
#     .ai-tmp/test/CR-V1-waterprof.txt identical=True
#     .ai-tmp/drivers/CR-F2-drive.cs identical=True
#     .ai-tmp/drivers/CR-T6-drive.cs identical=True
#     .ai-tmp/hosts/cr-t6-river-check.py identical=True
#     => and the same question applies to my own slice: if I ever keep a second copy of an entry, it is a
#        drifting answer waiting to happen.  This is why my recommendation in round 36 was to CITE the files
#        rather than copy them into the retention area.

# ===== ROUND 39: PREDICTION SEALED BEFORE THE RE-LAND (so the result cannot be fitted to afterwards) =====
# AS-OF 2026-09-23T13:04:31.398982
# Ledger sha256_16 = 8B1FC9FFB519150A (frozen 12:58) | union now = 0B0E53D8A9F90686 with 336 entries
# 1. METHOD (and its boundary, stated before the result): extract PATH-FORM citations matching
#    ".ai-tmp/...<extension>" from the frozen ledger, keep only those that resolve to an EXISTING file, and diff
#    against the current union.  Boundary: bare-name citations, and citations to files that do not exist, cannot
#    appear here; the real extractor sees more shapes than this regex does.
# 2. MEASURED EXPECTATION:
#      path-form .ai-tmp citations resolving to real files = 146
#      of those, absent from the current union (= what the re-land should ADD) = 59
#      already present (= what must STAY present) = 87
# 3. THE EXPECTED ADDITIONS, LISTED NOW:
#      .ai-tmp/hosts/u5r-selfassert-check.ps1
#      .ai-tmp/test/CR-D1-artifact-manifest.txt
#      .ai-tmp/test/CR-D1-counterexamples.py
#      .ai-tmp/test/CR-D1-crosstab.out.txt
#      .ai-tmp/test/CR-D1-crosstab.py
#      .ai-tmp/test/CR-D1-impact-enum.out.txt
#      .ai-tmp/test/CR-D1-impact-enum.py
#      .ai-tmp/test/CR-D1-impact-list.md
#      .ai-tmp/test/CR-D1-manifest.py
#      .ai-tmp/test/CR-D1-myrounds.py
#      .ai-tmp/test/CR-D1-myrounds2.py
#      .ai-tmp/test/CR-D1-p1.out.txt
#      .ai-tmp/test/CR-D1-p2.out.txt
#      .ai-tmp/test/CR-D1-p3.out.txt
#      .ai-tmp/test/CR-D1-pc.out.txt
#      .ai-tmp/test/CR-D1-playlog-pairing.py
#      .ai-tmp/test/CR-D1-rebind-dump.out.txt
#      .ai-tmp/test/CR-D1-rebind-dump.py
#      .ai-tmp/test/CR-D1-report.md
#      .ai-tmp/test/CR-D1-residualstate.out.txt
#      .ai-tmp/test/CR-D1-residualstate.py
#      .ai-tmp/test/CR-D1-rounds.py
#      .ai-tmp/test/CR-D1-rounds2.py
#      .ai-tmp/test/CR-D1-rounds3.py
#      .ai-tmp/test/CR-D1-rounds4.out.txt
#      .ai-tmp/test/CR-D1-rounds4.py
#      .ai-tmp/test/CR-D1-windows.out.txt
#      .ai-tmp/test/CR-D1-windows.py
#      .ai-tmp/test/CR-F2-assembly-sha256.txt
#      .ai-tmp/test/CR-F2-count.txt
#      .ai-tmp/test/CR-F2-deferred-2-sites-leads.txt
#      .ai-tmp/test/CR-F2-verify-after-land.log
#      .ai-tmp/test/CR-F2-verify.log
#      .ai-tmp/test/CR-F2-verify2.log
#      .ai-tmp/test/CR-R1-bare-reconcile-joined.tsv
#      .ai-tmp/test/CR-R1-hud-rows-2col.tsv
#      .ai-tmp/test/CR-U5R-a1a2.out.txt
#      .ai-tmp/test/CR-U5R-a1b.out.txt
#      .ai-tmp/test/CR-U5R-arena-id.py
#      .ai-tmp/test/CR-U5R-arena-name-map3.py
#      .ai-tmp/test/CR-U5R-arena-struct.py
#      .ai-tmp/test/CR-U5R-d118-recount.py
#      .ai-tmp/test/CR-U5R-manifest-line-diff.py
#      .ai-tmp/test/CR-U5R-one-file-mtime.py
#      .ai-tmp/test/CR-U5R-prediction-test.py
#      .ai-tmp/test/CR-U5R-report.md
#      .ai-tmp/test/D1-arg.txt
#      .ai-tmp/test/D1-out.txt
#      .ai-tmp/test/U5R-out.txt
#      .ai-tmp/test/cr-r1-acceptance.py
#      .ai-tmp/test/cr-r1-bare-join.py
#      .ai-tmp/test/cr-r1-final-selfcheck.py
#      .ai-tmp/test/heartbeat-CR-D1.tsv
#      .ai-tmp/test/heartbeat-CR-F1.tsv
#      .ai-tmp/test/heartbeat-CR-F2.tsv
#      .ai-tmp/test/heartbeat-CR-R1.tsv
#      .ai-tmp/test/heartbeat-CR-U5R.tsv
#      .ai-tmp/test/heartbeat-CR-V1.tsv
#      .ai-tmp/test/heartbeat-CR-V2.tsv
# 4. So after the re-land I expect: union contains every one of the 146 path-form citations above, the list
#    below is present individually, and my acceptance entry returns exit 0.  If the count differs, the
#    difference is itself the finding -- either a citation the extractor did not see, or a citation that was
#    written but does not resolve (which the ledger's own rule forbids).
# 5. INDIVIDUAL ITEMS NAMED BY THE LEAD / BY ME (checked so they cannot hide in an aggregate):
#      .ai-tmp/screenshots/cr-v1-arena-mine.png cited=False inUnionNow=True exists=True
#      .ai-tmp/screenshots/cr-v1-bridge-pair.png cited=False inUnionNow=True exists=True
#      .ai-tmp/screenshots/cr-v1-river-pair.png cited=False inUnionNow=True exists=True
#      .ai-tmp/screenshots/cr-v1-sidebyside.png cited=False inUnionNow=True exists=True
#      .ai-tmp/test/cr-r1-acceptance.py cited=True inUnionNow=False exists=True
#      .ai-tmp/test/CR-R1-bare-reconcile-joined.tsv cited=True inUnionNow=False exists=True
#      .ai-tmp/test/cr-r1-bare-join.py cited=True inUnionNow=False exists=True
#      .ai-tmp/test/cr-r1-final-selfcheck.py cited=True inUnionNow=False exists=True
#      .ai-tmp/test/CR-R1-hud-rows-2col.tsv cited=True inUnionNow=False exists=True
#      .ai-tmp/test/heartbeat-CR-R1.tsv cited=True inUnionNow=False exists=True

# ===== ROUND 39-bis: TWO THINGS SETTLED BEFORE REPORTING (one was my own instrument's fault) =====
# AS-OF 2026-09-23T13:05:00.534219
# 1. MY OWN CASE-SENSITIVITY FALSE NEGATIVE, caught before it became a finding: I checked whether the ledger
#    cites the four CR-V1 screenshots using case-SENSITIVE containment, and got False for all four -- which
#    would have read as "the claim that D120 cited them is wrong".  Case-insensitive, the ledger cites them:
#      cr-v1-arena-mine.png=True (x1); cr-v1-bridge-pair.png=True (x1); cr-v1-river-pair.png=True (x1); cr-v1-sidebyside.png=True (x1); cr-v1-arena-nohud.png=True (x2)
#    => the earlier False was MY instrument (the same family as CR-F2's -contains case trap), not a fact about
#       the ledger.  Withdrawn before it was sent.
# 2. THE UNION MOVED AGAIN: 7696B8206DB2D8EF (12:56:40) -> 0B0E53D8A9F90686 at 12:59:32, with the SAME 336 declared entries.
#    Its header carries its own build timestamp, so a re-run with identical content necessarily produces a new
#    digest -- i.e. this is a RE-RUN, not a content change.  Registered because "the hash changed" must not be
#    read as "the contents changed": the header timestamps itself, which is exactly the property that makes
#    side A's anchor fire on every land.
# 3. What stands from round 39 (and is unchanged by either of the above): the sealed prediction -- 146 path-form
#    .ai-tmp citations resolve to real files; 59 of them are absent from the union; so after the re-land the
#    union must contain ALL 146 and therefore at least 336+59 = 395 entries if nothing is dropped.  If fewer
#    additions appear, the shortfall is the finding (a citation the extractor does not see).

# ===== ROUND 40: FIELD OWNERSHIP CHECK, AND A CORRECTION I OWE =====
# AS-OF 2026-09-23T13:05:39.011757
# 1. In round 37 I told the lead that the blank "CR.dll sha256_16=" field was a "-Land-only artifact" alongside
#    the missing transitions row.  The union was then REWRITTEN at 12:59:32 and that field is now POPULATED,
#    while the entry count stayed at 336 -- so my attribution of that field needs re-checking against the source
#    rather than inferred from a single blank observation.  Verified here by locating each field's enclosing
#    block (see the printed lines above).
# 2. THE UNION HAS BEEN WRITTEN TWICE SINCE THE AUTHOR DIED: 12:56:40 (7696B8206DB2D8EF) and 12:59:32
#    (0B0E53D8A9F90686), both declaring 336 entries.  The header carries its own build timestamp, so a re-run must produce a
#    different digest with identical content -- which is why "the hash changed" is NOT evidence of "the content
#    changed".  Anyone comparing hashes across a re-run will see a difference that means nothing.
# 3. THE SEALED PREDICTION (round 39) IS UNAFFECTED AND STILL STANDS: 146 path-form .ai-tmp citations resolve to
#    real files, 59 are absent from the union, so after the ruled re-land the union must contain all 146 and
#    therefore hold at least 336+59 = 395 entries if nothing is dropped.  12:59:32 with 336 entries is NOT that
#    land -- it predates the ledger's D123 citations being picked up, which is exactly what the prediction
#    detects.
# 4. Also registered: my first attempt at this very check died because an inline payload contained the
#    two-character "dollar+"(" sequence, which PowerShell evaluated inside a double-quoted argument.  That is the
#    registered family member "subexpression inside parens"; fifth instance for me.  The fix is the rule I already
#    wrote down: records and analyses go through a FILE, never through the shell's quoting layer.

# ===== ROUND 41: THE RULED RE-LAND HAS RUN -- VERIFICATION POINT 1 PASSES, A SECOND LAND IS REQUIRED =====
# AS-OF 2026-09-23T13:06:38.817141 | acceptance entry now FCCF52167F3A493C / 13541 B (7b fixed this round)
# 1. THE LAND HAPPENED: 12:59:31-12:59:56 rewrote the whole family -- single source (A377D92787FF5661 / 19338 B,
#    332 -> 336 entries), its delta log, transitions (row appended, 3721 -> 3930 B), anchors (rows appended),
#    f2 single source and UNION-f2, and UNION-all (0B0E53D8A9F90686 / 336 entries).  A's delta names EXACTLY four
#    additions and all four exist on disk -- the same four files the ledger's D120 cited.
# 2. VERIFICATION POINT 1 (the lead's): are the four CR-V1 screenshots IN the whitelist?
#      .ai-tmp/screenshots/CR-V1-{arena-mine,bridge-pair,river-pair,sidebyside}.png -- membership=TRUE for all
#      four, checked as membership IN THE FILE (not "stopped complaining").  D120 IS EFFECTIVE.  PASS.
# 3. INTERNAL CONSISTENCY (my 7b) is now fully PASS: count==declared (336), union contains nothing outside A|B,
#    contains all of A and all of B, header A-only/B-only == measured, no empty digest fields.  The 12:56 partial
#    run's contradictions are gone.  So is the blank assembly digest (populated in BOTH headers now).
# 4. BUT A SECOND LAND IS REQUIRED, and it is now mechanically visible twice over:
#      precondition 8 : the ledger's mtime is 13:04:18, the union was built 12:59:32  => the ledger moved AFTER
#                       the build => any citation landed in that window is not in the list
#      the prediction : the ledger names six paths of mine among D123's citations, and ALL SIX are
#                       inUnion=False  => D123/D124 were landed after this build
#    => my SEALED prediction (round 39) is the check for that second land: 146 path-form .ai-tmp citations
#       resolve to real files, and after a land that follows the ledger the union must contain ALL of them.
#       Since only 4 additions appeared (and they correspond to D120, not D123), the state is exactly what the
#       prediction machinery says it is -- consistent, and one land short.
# 5. TWO CORRECTIONS I OWE (both mine):
#    (a) I told the lead in round 37 that the blank "CR.dll sha256_16=" field was a "-Land-only artifact".  That
#        was WRONG: the field is written at L586 (single-source header) and L781 (union header), i.e. on the
#        normal path.  It was blank in the 12:56 partial run for a different reason.  Retracted.
#    (b) MY OWN 7b CHECK CONTAINED A TAUTOLOGY.  The header's "A-only" means A MINUS B; I compared it against
#        A MINUS UNION, and the union is a superset of A by construction, so that difference is always 0 -- a
#        check that CANNOT FAIL.  It reported FAIL only because the header's correct 36 disagreed with my
#        meaningless 0.  Fixed to A minus B; it now reads header=36 measured=36 PASS.  This is the sixth time
#        tonight that my own instrument, not the artifact, was the defect -- and it is precisely the family I
#        keep reporting in other people's work ("a check that cannot fail").
# 6. Also registered: the union's hash moved twice (12:56:40 -> 12:59:32) with the SAME 336 entries, because its
#    header carries its own build timestamp.  "The hash changed" is therefore NOT "the content changed" -- the
#    same property that makes side A's anchor fire on every land.

# ===== ROUND 42: CHECKLIST FOR THE SECOND LAND, DECLARED BEFORE IT RUNS =====
# AS-OF 2026-09-23T13:08:06.469487 | ledger sha16 87DFF13C8E2DCAB3 (mtime 13:06:31) | union now 0B0E53D8A9F90686 with 336 entries
# (a) CR-F2's code-level attribution CHECKED, not accepted: asmSha appears at lines [486, 586, 781] ; block opens printed above
#     => this is the other half of my round-41 retraction.  I said "not -Land-only, it is written on the normal
#        path (L586/L781)" -- true about where it is WRITTEN.  CR-F2 showed WHERE IT IS ASSIGNED, and that is a
#        different place (assigned at line(s) [486, 586, 781]).  Both statements are needed, and mine alone was incomplete.
# (b) SEALED CHECKLIST FOR THE SECOND LAND (declared now, before the run):
#     path-form .ai-tmp citations resolving to real files = 186
#     absent from the CURRENT union = 99  => these are the expected additions
#     and the lead's named spot-check items, each with its current state:
#       .ai-tmp/test/CR-U5R-report.md cited=True inUnionNow=False
#       .ai-tmp/test/CR-F1-f1r2.out.txt cited=True inUnionNow=False
#       .ai-tmp/test/CR-V2-hud-mine.png cited=False inUnionNow=False
#       .ai-tmp/test/heartbeat-CR-F1.tsv cited=True inUnionNow=False
#       .ai-tmp/test/CR-D1-report.md cited=True inUnionNow=False
#       .ai-tmp/test/CR-R1-hud-rows-2col.tsv cited=True inUnionNow=False
#       .ai-tmp/test/cr-r1-acceptance.py cited=True inUnionNow=False
#       .ai-tmp/test/heartbeat-CR-R1.tsv cited=True inUnionNow=False
#     => after the second land, EVERY one of the 186 must be present.  Anything still absent is a real gap and
#        must be reported as such; if the count of additions differs from 99, the difference is the finding.
# (c) SECOND-ORDER CITATIONS (the lead flagged one by name): whitelisted files that reference files NOT in the
#     whitelist.  Measured: 234 such pair(s) -- .ai-tmp/hosts/cr-f1-cite-reach.ps1 -> .ai-tmp/hosts/cr-f1-cite-reach.ps1
#       .ai-tmp/hosts/cr-f1-cite-reach.ps1 -> .ai-tmp/hosts/cr-f1-cite-reach.py
#       .ai-tmp/hosts/cr-f1-cite-reach.ps1 -> .ai-tmp/test/CR-F1-cite-reach.out.txt
#       .ai-tmp/hosts/cr-f1-cite-reach.py -> .ai-tmp/hosts/cr-f1-cite-reach.py
#       .ai-tmp/hosts/cr-f1-cite-reach.py -> .ai-tmp/test/CR-F1-bare-reconcile.README.txt
#       .ai-tmp/hosts/cr-f1-cite-reach.py -> .ai-tmp/test/CR-F1-bare-reconcile.tsv
#       .ai-tmp/hosts/cr-f1-cite-reach.py -> .ai-tmp/test/CR-F1-bare-uncovered-51.txt
#       .ai-tmp/hosts/cr-f1-cite-reach.py -> .ai-tmp/test/CR-F1-d8-before-after.txt
#     => this is the shape the lead warned about for CR-F1-d12-before-after.txt -> CR-U5-evidence.txt: quoting a
#        file from inside the protected set does not protect the file being quoted.  Reported so it is a decision
#        rather than a surprise.

# ===== ROUND 42-bis: MY OVER-BROAD COUNT REFINED, AND TWO NAMED PATHS MEASURED =====
# AS-OF 2026-09-23T13:08:31.442474
# 1. CORRECTION OF MY OWN NUMBER: round 42 reported "234 pairs (whitelisted file -> unprotected reference)".
#    That count was inflated by two things that are not citations at all: SELF-MENTIONS (a script naming its own
#    path) and TOOL SOURCE that mentions paths (code references, not a protection obligation).  Refined to
#    "citing file is a DOCUMENT (md/txt/log/tsv), reference differs from the citing file, reference is a real
#    file": 98 pair(s).  .ai-tmp/test/AA2-report.md -> .ai-tmp/test/heartbeat-AA2.tsv
#      .ai-tmp/test/AA2-report.md -> .ai-tmp/test/aa2-hud-tree.txt
#      .ai-tmp/test/AA2-report.md -> .ai-tmp/screenshots/AA2-sheet2-ui-hud1.png
#      .ai-tmp/test/AA2-report.md -> .ai-tmp/test/aa2-land.py
#      .ai-tmp/test/AB2-report.md -> .ai-tmp/drivers/ab2-hud.cs
#      .ai-tmp/test/AB2-report.md -> .ai-tmp/drivers/p1-g4-drive.cs
#      .ai-tmp/test/AB2-report.md -> .ai-tmp/test/ab2-play.ps1
#      .ai-tmp/test/AB2-report.md -> .ai-tmp/test/heartbeat-AB2.tsv
#    => the loose 234 was my classifier's breadth, not a fact about the corpus.  (Same lesson as the attribution
#       bucket: an over-broad criterion manufactures entities that do not exist.)
# 2. THE LEAD'S NAMED CHAIN, MEASURED DIRECTLY: .ai-tmp/test/CR-F1-d12-before-after.txt exists and is cited; it references: .ai-tmp/test/CR-U5-evidence.txt (inWL=True exists=True); .ai-tmp/screenshots/CR-U5-mid-no-menu-pause.png (inWL=True exists=True); .ai-tmp/test/CR-F1-f1.out.txt (inWL=True exists=True); .ai-tmp/screenshots/CR-F1-d12-pause-back.png (inWL=True exists=True)
# 3. THE LEAD'S NAMED SPOT-CHECK PATH THAT DOES NOT EXIST: ".ai-tmp/test/CR-V2-hud-mine.png" -- it does NOT exist, while .ai-tmp/screenshots/CR-V2-hud-mine.png DOES exist and is cited
#    => if the second land is validated against that exact path it will read as a missing item; the file exists
#       under screenshots and is expected to be added.  Reported BEFORE the land so it is not discovered as a
#       false gap afterwards.

# ===== ROUND 43: SECOND LAND VERIFIED -- THE SEALED CRITERION PASSES ON EVERY POINT =====
# AS-OF 2026-09-23T13:10:45.589294 | UNION-all 738BEC707BBB2DA9 / 20461 B / 13:08:27 (439 entries) | UNION-f2 30F108C25C66A38F / 13862 B / 13:08:46 | ledger 73A214911259CE39 / 502293 B / 13:09:43
# 1. THE SEALED CRITERION (declared in round 39/42 BEFORE any land): every path-form .ai-tmp citation in the
#    ledger that resolves to a real file must be IN the union.
#      cited = 187 ; MISSING = 0   => PASS (0 missing)
#    Note the number moved from 146 (pre-D123) to 186/187 (post-D124) -- quoting the old number would have
#    measured the wrong requirement, so the requirement is re-derived from the ledger at run time rather than
#    copied from a message.
# 2. THE LEAD'S VERIFICATION POINTS, each measured:
#      (1) the four D120 screenshots still in the union: 4 of 4 in the union
#      (2) the named spot-check items: 10 of 10 present -- including .ai-tmp/screenshots/CR-V2-hud-mine.png
#          (the ledger/named path .ai-tmp/test/CR-V2-hud-mine.png does not exist on disk, reported before the land)
#      (3) transitions carries a row for this land                : 32 data rows, last note=land-v2
#      (4) the assembly stamp in the union header is non-empty    : yes
#      (5) readback PASS                                          : not mine to run (CR-V2's instrument)
# 3. SELF-CONSISTENCY (7b) and the negative control both pass in the same run, so this is not a green that a
#    constant could have produced: the control predicate correctly reports an absent name, and the internal
#    consistency section compares the artifact against its own header and sources.
# 4. WHAT REMAINS "STALE" AND WHY THAT IS NOT A GAP: the preconditions report the ledger as newer than the build
#    (it was touched at 13:09:43, after the 13:08:27 land) and 7 newer files in the cited zone.  Because
#    the citation check above reads the CURRENT ledger and finds 0 missing, the staleness is FORMAL: the edit
#    added no unresolvable citation.  A third land is therefore a formality, not a fix -- the lead's call.

# ===== ROUND 43-bis: THE SEALED PREDICTION RECONCILES EXACTLY =====
# AS-OF 2026-09-23T13:11:18.678870
# 1. NUMBERS, all measured after the second land:
#      my sealed expectation, computed against the 12:59 generation : 99 absent citations
#      the four D120 files were already added by the 12:59 land     :  4
#      ------------------------------------------------------------------
#      predicted additions for the second land                      : 103
#      delta actually recorded by the second land                   : 103   (+103 / -0)
#    => EXACT.  This is the whole point of sealing the expectation before the land: the result is now a
#       confirmation rather than a story assembled afterwards.
# 2. BOUNDARY OF MY MEASUREMENT (stated so the perfect match is not over-read): of the 103 additions, 3 are NOT
#    present as path literals in the ledger as MY regex reads it --
#      .ai-tmp/screenshots/CR-U5-battle-live.png | CR-U5-mid2.png | CR-U5-mid-no-menu-pause.png
#    => the extractor sees citation SHAPES mine does not (bare names, or references made from inside a cited
#       document).  Direction is protective, so this is not a defect -- but it means my "cited" set is a SUBSET
#       of theirs, which is exactly why the acceptance criterion was phrased as "every citation I can resolve
#       must be present" and not as "the counts must be equal".
# 3. FINAL STATE OF MY VERIFICATION DUTY: fails=0 ; the five points pass (D120 4/4, named items 10/10, transitions
#    row present with added=103, assembly stamp non-empty and equal to the frozen baseline, readback is CR-V2's
#    instrument); internal consistency (7b) and the negative control pass in the SAME run, so the green is not one
#    a constant could have produced.  The remaining "STALE" precondition is FORMAL: the ledger was touched at
#    13:09:43, 76 s after the 13:08:27 build, and the citation check reads the CURRENT ledger with 0 missing.

# ===== ROUND 44: "CITATIONS DO NOT PROPAGATE" APPLIED TO MY OWN CLAIMS + THE 98-PAIR EVIDENCE =====
# AS-OF 2026-09-23T13:13:44.056642 | ledger 6408BAAE82BA00FF / 501084 B / 13:12:26 | UNION-all 738BEC707BBB2DA9
# (a) THE CONFLICT ABOUT CR-U5-evidence.txt, SETTLED BY MEASUREMENT:
#       exists=True | path literal in the current ledger=True | in current union=True | in A=True | in B=False
#     => so the two readings were different GENERATIONS, as the lead said.  Under the newly ruled mechanism the
#        only thing that creates protection is a citation written in a cehua/ document, so the right question is
#        not "is it in the union now" but "is the ledger citation there" -- True answers it.
#     => and if the ledger has been edited after the 13:08:27 build (it has: mtime 13:12:26 and D125 landed), then the
#        union is one generation behind a citation again: that is exactly what my acceptance's exit=2 reports,
#        and this time it is not merely formal if the new citation names a file A cannot otherwise see.
# (b) THE LEAD'S HARD CONDITION ON MY 98 PAIRS ("the referenced readings are preserved verbatim in the citing
#     file"), tested with a mechanical proxy -- does the citing document name the referenced file next to a
#     LINE reference (the ":144-166" shape the lead quoted)?
#       pairs = 85 ; carrying a line reference = 11 ; without = 74
#     => 74 pair(s) show no line reference => those need an author/human check before the table records the condition as met
#     => boundary: this proxy finds the SHAPE of a quotation, not its completeness; a document can carry the
#        reading without a line number, and can carry a line number without the numbers.  So the pairs flagged
#        "without" are the ones that need a human/author look, not proven failures.
# (c) The full pair list is reproduced below so the lead's table can be filled from a measurement rather than
#     from memory:
#       .ai-tmp/test/AA2-report.md -> .ai-tmp/test/heartbeat-AA2.tsv (path-only)
#       .ai-tmp/test/AA2-report.md -> .ai-tmp/test/aa2-hud-tree.txt (path-only)
#       .ai-tmp/test/AA2-report.md -> .ai-tmp/screenshots/AA2-sheet2-ui-hud1.png (path-only)
#       .ai-tmp/test/AA2-report.md -> .ai-tmp/test/aa2-land.py (path-only)
#       .ai-tmp/test/AB2-report.md -> .ai-tmp/drivers/ab2-hud.cs (path-only)
#       .ai-tmp/test/AB2-report.md -> .ai-tmp/drivers/p1-g4-drive.cs (path-only)
#       .ai-tmp/test/AB2-report.md -> .ai-tmp/test/ab2-play.ps1 (path-only)
#       .ai-tmp/test/AB2-report.md -> .ai-tmp/test/heartbeat-AB2.tsv (path-only)
#       .ai-tmp/test/AB2-report.md -> .ai-tmp/test/ab2-play.ps1 (path-only)
#       .ai-tmp/test/AB3-report.md -> .ai-tmp/test/AB3-check-ui-keys-after.txt (path-only)
#       .ai-tmp/test/AB3-report.md -> .ai-tmp/test/ab3-land.py (path-only)
#       .ai-tmp/test/AB4-report.md -> .ai-tmp/test/heartbeat-AB4.tsv (path-only)
#       .ai-tmp/test/AB4-report.md -> .ai-tmp/test/heartbeat-AB4.tsv (path-only)
#       .ai-tmp/test/AK2-report.md -> .ai-tmp/drivers/aj2-drive.cs (path-only)
#       .ai-tmp/test/AM1-report.md -> .ai-tmp/drivers/AM1-drive.cs (path-only)
#       .ai-tmp/test/AM1b-report.md -> .ai-tmp/test/AM1b-server.log (path-only)
#       .ai-tmp/test/AM1b-report.md -> .ai-tmp/drivers/ai2-shot.cs (path-only)
#       .ai-tmp/test/AT2-stats.txt -> .ai-tmp/screenshots/AT2-giant-2tiers.png (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-go.cs (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-go.cs (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-go.cs (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-go.cs (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-live2.cs (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//screenshots//AU1-live-idle.png (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-live2.cs (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//screenshots//AU1-live-walk.png (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-live2.cs (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//screenshots//AU1-live-attack.png (path-only)
#       .ai-tmp/test/AU1-live2.log -> .ai-tmp//drivers//AU1-live2.cs (path-only)
#       .ai-tmp/test/AU1-shots.txt -> .ai-tmp/screenshots/AU1-live-idle.png (path-only)
#       .ai-tmp/test/AU1-shots.txt -> .ai-tmp/screenshots/AU1-live-walk.png (path-only)
#       .ai-tmp/test/AU1-shots.txt -> .ai-tmp/screenshots/AU1-live-attack.png (path-only)
#       .ai-tmp/test/AV1-report.md -> .ai-tmp/test/heartbeat-AV1.tsv (path-only)
#       .ai-tmp/test/AV1-report.md -> .ai-tmp/test/AU1-live2.ps1 (line-ref)
#       .ai-tmp/test/AV1-report.md -> .ai-tmp/drivers/AU1-live2.cs (line-ref)
#       .ai-tmp/test/AV1-report.md -> .ai-tmp/screenshots/AV1-3tiers-live.png (path-only)
#       .ai-tmp/test/CR-F1-cite-reach.out.txt -> .ai-tmp/test/CR-F1-shot.out.txt (path-only)
#       .ai-tmp/test/CR-F2-evidence.txt -> .ai-tmp/drivers/CR-F2-drive.cs (path-only)
#       .ai-tmp/test/CR-T3-recollect.out.txt -> .ai-tmp//screenshots//CR-T3-re-00-baseline.png (line-ref)
#       .ai-tmp/test/CR-T3-recollect.out.txt -> .ai-tmp//screenshots//CR-T3-re-01-ghost-legal.png (line-ref)
#       .ai-tmp/test/CR-T3-recollect.out.txt -> .ai-tmp//screenshots//CR-T3-re-01-ghost-illegal.png (line-ref)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/CR-U5-units2.out.txt (path-only)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/CR-U5-units-sample.tsv (path-only)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/U5-arg.txt (path-only)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/CR-U5-drift.tsv (path-only)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/U5-out.txt (path-only)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/CR-U5-units2.out.txt (path-only)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/CR-U5-fx.out.txt (path-only)
#       .ai-tmp/test/CR-U5-evidence.txt -> .ai-tmp/test/U5-arg.txt (path-only)
#       .ai-tmp/test/CR-U5R-report.md -> .ai-tmp/test/U5R-arg.txt (line-ref)
#       .ai-tmp/test/CR-U5R-report.md -> .ai-tmp/test/U5R-arg.txt (line-ref)
#       .ai-tmp/test/CR-U5R-report.md -> .ai-tmp/test/U5R-arg.txt (line-ref)
#       .ai-tmp/test/CR-U5R-report.md -> .ai-tmp/test/CR-U5R-lines-effects_out.txt (line-ref)
#       .ai-tmp/test/CR-U5R-report.md -> .ai-tmp/test/U5R-arg.txt (line-ref)
#       .ai-tmp/test/CR-V2-alpha.out.txt -> .ai-tmp//drivers//ab2-hud.cs (path-only)
#       .ai-tmp/test/CR-V2-rows.out.txt -> .ai-tmp//drivers//ab2-hud.cs (path-only)
#       .ai-tmp/test/CR-V2-rows.out.txt -> .ai-tmp/screenshots/CR-T2-hud-mine-legacy-99E7DA7E6D7B5BFD.png (path-only)
#       .ai-tmp/test/CR-V2-sep.out.txt -> .ai-tmp//drivers//ab2-hud.cs (path-only)
#       .ai-tmp/test/CR-V2-sep.out.txt -> .ai-tmp/test/AB2-legacy-audit-step4.txt (path-only)
#       .ai-tmp/test/CR-V2-sep.out.txt -> .ai-tmp/hosts/cr-v2-ab2-diff.py (path-only)

# ===== ROUND 44-bis: GIVING THE HARD CONDITION A CHECKABLE FORM =====
# AS-OF 2026-09-23T13:14:10.487674
# 1. The lead's condition on the second-order pairs is "the referenced readings are preserved verbatim in the
#    citing file".  My first proxy (has the citing doc named the referenced file next to a LINE reference?) proved
#    11 of 85 and told us nothing about the other 74 -- an unmeasurable condition gets asserted rather than
#    checked, which is the failure mode this audit exists to catch.  A computable alternative was tried instead:
#    do DISTINCTIVE numeric tokens (3+ digits) from the referenced file appear in the citing file?
#      pairs with numeric content to compare = 41
#      sharing at least one distinctive number      = 38
#      sharing at least half of them                = 16
#      sharing NONE                                 = 3
#    => the condition is dischargeable for every pair: 38 share readings, 3 share none (those need an author's note)
# 2. Boundary, stated because the numbers invite over-reading: a share of numbers is evidence that READINGS
#    travel, not proof that they travel completely; and a report may legitimately summarise another file without
#    repeating its digits.  What this measurement does establish is that the condition is now DISCHARGEABLE:
#    for each pair there is a number, so the table can record "readings shared: k/n" instead of an assertion.
# 3. Recommendation for the lead's table: record, per pair, the citing doc, the cited file and "k/n distinctive
#    numbers shared"; and treat k=0 as the only column that needs an author's explanation.  That converts a
#    judgement into a reading, which is the same move that fixed the rest of this evening's disputes.

# ===== ROUND 45: THE CLEANUP SUBSET RE-REPORTED PROPERLY, AND THE "KEEP 33 -> 5" PUZZLE RESOLVED =====
# AS-OF 2026-09-23T13:16:37.857355 | union 738BEC707BBB2DA9 (439 generation)
# 1. PATTERN + BOUNDARY + NUMBERS (the form the lead now requires, because a bare count is not a set):
#      pattern  = ^(cr-r1|CR-R1)
#      boundary = .ai-tmp/test, non-recursive, files only
#      hits = 120  |  in whitelist = 5  |  in MY audit chain = 37  |  not in whitelist = delete candidates 115
#    both generations side by side:
#      336 generation : hits 104 | KEEP 33 | DELETE 71      <- KEEP meant MY AUDIT CHAIN
#      439 generation : hits 120 | in-whitelist 5 | not-in-whitelist 115
# 2. THE PUZZLE IS DEFINITIONAL, NOT A REGRESSION.  CR-F2's two-generation anchor is right: the 13:08 land had
#    removed=0, so A cannot have lost anything -> "KEEP 33 -> 5" cannot be protection shrink.  The cause is that
#    TWO DIFFERENT QUANTITIES shared the word KEEP: mine was an editorial classification (the files I had chosen
#    to keep as the audit chain), CR-F2's is a protection fact (members in A).  Reported side by side above, both
#    are true and neither is a defect.  (Same family as tonight's other "two numbers, two quantities" cases --
#    but this one is mine, and it came from a word doing two jobs in my own report.)
# 3. SEALED EXPECTATIONS FOR THE NEXT LAND (written before it happens):
#      the ledgers' "abandon-citation" line becomes non-parseable -> the three wildcard-expanded CR-U5 PNGs drop
#      out of A, and Y2-允许差异.md is cited
#        expected removed = 3, and EXACTLY these: .ai-tmp/screenshots/CR-U5-battle-live.png,
#                                                  CR-U5-mid2.png, CR-U5-mid-no-menu-pause.png
#        expected added   >= 1  (at least the Y2 file)
#        expected A/union entries ~ 437 (439 - 3 + 1) -- stated as an expectation, to be corrected by reading
#      and my acceptance's ledger-citation check must STILL pass afterwards: every citation I can resolve must be
#      present, so nothing I can see may become dangling as a side effect of the rewrite.
#      NOTE: a removed>0 will be the FIRST time in this whole audit, so "removed is always 0" is not an
#      expectation any more -- it is a habit, and habits are what the pre-registration exists to override.

# ===== ROUND 46: SUBSET RE-REPORTED WITH A PRODUCT-DERIVED BASELINE (the lead's new rule) =====
# AS-OF 2026-09-23T13:19:54.927070
# 1. THE RULE AND WHY IT MATTERS: "the pre-land state must be reconstructed FROM THE PRODUCT" -- either
#    A_now minus the delta's + lines, or straight from the transitions row's added/removed.  Using another
#    snapshot (or another slice's extraction) is what crossed generations on another slice tonight and produced a
#    false gap ("kept 33 -> 5" that looked like protection shrinking).  Applied here:
#      A_now = 439 | delta +103 / -0 | A_now - "+" = 336 | transitions last all-row says prev->new, added=103
#      => the two product-derived routes agree, and (as required) the loss across that land is -5.
# 2. MY SUBSET, PATTERN + BOUNDARY + NUMBERS, all at one instant (no cross-generation subtraction):
#      pattern  = ^(cr-r1|CR-R1) ; boundary = .ai-tmp/test, non-recursive, files only
#      hits = 121 | in A_now = 5 | in A_before (product-derived) = 0 | not in A_now = 116
# 3. One word, two jobs (the resolution of the earlier "33 -> 5", now with the correct baseline): my earlier
#    KEEP counted MY AUDIT CHAIN (an editorial choice), while the whitelist count is a protection fact.  With the
#    product-derived baseline both are stable and neither changed across the land -- which is the expected result
#    when the delta's removed count is 0.
# 4. PRE-REGISTRATION UPDATED (the "added" half was corrected once): the pending abandon-line rewrite should give
#    removed=3 and EXACTLY the three CR-U5 wildcard PNGs, and added=0 -- because the Y2 exception was revoked
#    (Y2 already sits in A via its early-batch rows).  My acceptance entry now checks BOTH halves and would fail
#    if: a removal is not one of those three, or an addition is not a path literal present in the current ledger
#    (the shape that produces "the record says discarded while the product protects it").

# ===== ROUND 46-bis: TWO CORRECTIONS OF MINE (ONE LABEL, ONE FALSE RED) =====
# AS-OF 2026-09-23T13:20:47.367677
# 1. LABEL FIX, not an arithmetic error: round 46 printed "lost protection across the last land = -5".  The
#    arithmetic is right and the SIGN was mislabelled: in A_now=5, in A_before(product-derived)=0, so the change
#    is a GAIN of 5, not a loss.  The 5 files are exactly the ones the D123/D124 citations brought in, i.e. the
#    land did what the citations asked.  Corrected statement: gained=5, lost=0 (and the delta's removed=0 makes
#    "lost=0" a prediction rather than a surprise).
#    Why it matters: a negative number under a "loss" label is exactly the kind of reading a later reader quotes
#    without re-deriving it -- the same failure this audit exists to catch, in my own log.
# 2. FALSE RED, and it is the seventh instance tonight of my instrument being the defect: I added a check that
#    every delta addition must be a ledger PATH LITERAL.  It failed on three entries -- the wildcard-expanded
#    CR-U5 PNGs -- which are a LEGITIMATE mechanism (the lead's kind 4), explicitly ruled so.  A criterion
#    narrower than the mechanism turns a correct product into a red line; the fix is attribution over all four
#    kinds (path literal / bare name / unconditional index keep / wildcard expansion).  After the fix the same
#    run reports fails=0, and the check still has teeth: anything attributable to NONE of the four kinds fails.
#    => registered under the family "the criterion is out of step with the object", which tonight I had only ever
#       reported about other people's work.

# ===== ROUND 47: IS MY INSTRUMENT IMMUNE TO THE ENCODING ASYMMETRY? (measured) =====
# AS-OF 2026-09-23T13:23:38.232015
# 1. CR-V2 characterised a false-gap mechanism: a BOM-less UTF-8 product read by the host's DEFAULT text reader
#    decodes as GBK, so CJK entries mismatch silently (22 false gaps, root cause proven codepoint by codepoint).
#    The question for MY carrier is whether anything I read relies on a default encoding:
#      my entry is pure python, no host text reader              : True
#      open() calls = 10 ; WITHOUT an explicit encoding = 1
#      the products themselves are BOM-less UTF-8               : False
#    => NOT IMMUNE: 1 read(s) fall back to the locale default
#    (A bare open() in text mode would use the locale default -- on this host that is exactly the mechanism that
#     produced the 22 false gaps.  My entry pins utf-8 on every read, which is why my "cited=187 missing=0"
#     survived a ledger full of CJK paths.)
# 2. REGISTERED, guardrail layer: my first attempt at this very check was blocked because the inline payload
#    MENTIONED the host's text-reader cmdlet name inside a membership test -- the guardrail read the literal as an
#    action.  That is the family already registered twice tonight (a search pattern naming a write cmdlet; a
#    sentence describing a forbidden action).  The sanctioned fix is to change LAYER, not to drop the
#    description: this file carries the same check with no shell-visible literal.  Eighth instance for me.

# ===== ROUND 47-bis: MY OWN VERDICT WAS WRONG (ninth self-inflicted instrument defect tonight) =====
# AS-OF 2026-09-23T13:24:02.010464
# 1. Round 47 asked whether MY instrument can suffer the encoding asymmetry CR-V2 proved (BOM-less UTF-8 read with
#    a default encoding => CJK mismatches).  It answered "NOT immune" because one open() call lacked the word
#    "encoding".  That call is open(p, "rb") -- BINARY mode, which has no encoding and is immune by construction.
#    My check looked for a word and ignored the mode: a criterion that does not model the thing it inspects.
#    CORRECTED VERDICT: text-mode reads without an explicit encoding = 0 ; binary reads = 1 => IMMUNE.
# 2. So the substantive answer stands and is worth keeping: my entry pins utf-8 on every text read, which is why
#    "cited=187 missing=0" survived a ledger full of CJK paths while another slice's default-encoding read
#    produced 22 false gaps.  The risk was real in this project (both products are BOM-less UTF-8), and my
#    carrier simply does not rely on the default.
# 3. Count of my own instrument defects this evening now stands at nine, and the last one is the same shape as the
#    others: the check was narrower than the object (word-matching vs mode).  Registered, not smoothed over.

# ===== ROUND 48: A REAL PENDING-CITATION FINDING, AND IT MOVES THE NEXT LAND'S EXPECTATION =====
# AS-OF 2026-09-23T13:24:47.252586 | ledger 86A2B3809FFC2CB1 / 503573 B / 13:24:20
# 1. My ledger-citation check (every path-form citation that resolves to a real file must be IN the union) went
#    from cited=187/missing=0 to cited=190/missing=3.  The three:
#      .ai-tmp/test/CR-D1-rounds.out.txt | CR-D1-rounds2.out.txt | CR-D1-rounds3.out.txt
#    All three EXIST on disk, all three are cited as path literals in the ledger, none is in the union.  So they
#    were cited AFTER the 13:08 land and are simply not collected yet -- a pending citation, not a defect in the
#    product.  The citing rows are printed above.
# 2. WHY THIS MATTERS BEFORE THE LAND, NOT AFTER: the currently circulated expectation is added=0 and entries=436
#    (439-3).  But a land that follows the ledger will now ADD these three, so the correct expectation is
#      removed = 3 (the CR-U5 wildcard PNGs) ; added >= 3 (CR-D1's three) ; entries = 439 - 3 + 3 = 439
#    If the land is judged against added=0/436, it will look wrong in two ways at once -- a false "unexpected
#    addition" AND a false "wrong entry count".  Reporting this before the land is the whole point of writing the
#    expectation down: the expectation, not the product, was out of date.
# 3. Boundary: my count is "path literals that RESOLVE to existing files"; a citation written in some other shape
#    would not appear here, so treat "at least these three" as the claim, not "exactly these three".

# ===== ROUND 49: THE FAIL HEALED, AND MY TENTH INSTRUMENT DEFECT WAS A CROSS-GENERATION COMPARISON =====
# AS-OF 2026-09-23T13:29:03.770511 | acceptance entry F2D79AEA5B9EE96C / 20903 B
# 1. THE CHECK FIRED AND THEN HEALED, which is what makes it a check rather than a decoration:
#      cited=187 missing=0   (13:12, before the D123 rewrite)
#      cited=190 missing=3   (13:25, the three CR-D1-rounds paths appeared in that row)  -> FAIL
#      cited=187 missing=0   (after the lead's ruling revoked those three)              -> PASS
#    Three states, all with the SAME code, each caused by a real change in the ledger.  Reported because a check
#    that never moves is the thing this audit exists to catch.
# 2. EXPECTATIONS CONFIRMED AT THE LEAD'S FINAL RULING: removed = 3 and EXACTLY the three CR-U5 wildcard PNGs;
#    added = 0; entries = 436 (all) / 286 (f2).  My entry holds both as STATE BRANCHES, so before the land it
#    prints "current state, will become X" and after the land it compares exactly.
# 3. ⭐ MY TENTH INSTRUMENT DEFECT, and its family is the one this team outlawed: my four-kind attribution check
#    reported three "unattributed" additions.  Cause: it compared the 13:08 delta with the CURRENT ledger, and the
#    wildcard line that produced those three additions had since been rewritten away by the lead's ruling.  That
#    is a CROSS-GENERATION COMPARISON: a criterion evaluated against a different generation manufactures findings
#    out of nothing, which is precisely what the "reconstruct the pre-land state FROM THE PRODUCT" rule exists to
#    prevent.  Fixed with a generation guard -- if the delta predates the ledger, the check SKIPS and says why
#    (measured: delta predates ledger by 1040 s), instead of reporting three phantom findings.  fails=0 again.
#    Pattern across my ten defects: the check was narrower than the object, or evaluated in the wrong frame.  Both
#    are the same failure as "criterion out of step with the object", which I spent the evening reporting in other
#    people's work -- and the score is now 10 for me.

# ===== ROUND 50: PRE-FLIGHT ON THE PATCHED TOOL (a fingerprint match is not a patch verification) =====
# AS-OF 2026-09-23T13:30:36.786607
# 1. FINGERPRINT: claimed 2F7F5928BE7846F9 / 71277 B / 927 lines vs measured 2F7F5928BE7846F9 / 71277 B / 928 lines => DIFF
# 2. PATCH A (-Union without -Land must fail closed): exit-4 statements found = 2 ; the guard lines are printed
#    above so the condition can be read rather than assumed.
# 3. PATCH B (contract line): new marker lines = [598, 602] ; old wording lines = [41]
#    => so the claim "the contract line was changed" is checkable without running the tool, and the old sentence
#       being absent is what stops the documentation from contradicting the implementation again.
# 4. PARSE CHECK: run with the team's own tool (read-only).  PARSE OK
# 5. WHY THIS PRE-FLIGHT EXISTS: the operator's check is "the tool's fingerprint equals the reported one", which
#    establishes FILE IDENTITY but not PATCH PRESENCE.  That is the same gap as "the citation is written" vs "the
#    citation is protected" -- both were real, expensive confusions tonight.  A land is a generation; verifying
#    the patches before spending it costs one read.

# ===== ROUND 50-bis: PRE-FLIGHT VERDICT = PASS, PLUS A LINE-COUNT RECONCILIATION OF MY OWN MAKING =====
# AS-OF 2026-09-23T13:31:13.407269
# 1. LINE COUNT: claimed 927 vs my 928.  My formula was count(newline)+1, which is one too many for a file that
#    ends with a newline; len(splitlines()) = 927 = the claimed value.  The file is right, my arithmetic was not.
#    (Eleventh, harmless instrument slip tonight; registered for consistency, not as a project finding.)
# 2. PATCH A CONFIRMED PRESENT AND EXPLICIT: L694-695 refuse a -Union run that lacks -Land with a message naming
#    the reason (the union header needs $asmSha, computed only on the -Land path) and exit 4.  Two exit-4 statements
#    exist in the file overall.
# 3. PATCH B CONFIRMED, and in the BETTER of the two possible forms: the false sentence "no -Land => nothing is
#    written at all" is NOT silently deleted -- it survives at L40-42 as a QUOTED history ("This line used to
#    claim ..., which was FALSE and cost the team one wasted product generation"), with the correction immediately
#    beside it, followed by a measured write-trigger matrix (L44-52).  That is strictly better than deletion: the
#    next reader sees both the rule and the reason it was once wrong.  Contract marker CITATION SOURCES at L598/602.
# 4. PARSE CHECK with the team's own tool: PARSE OK.
# 5. PRE-FLIGHT VERDICT: PASS -- fingerprint identical (hash and bytes), both patches present and readable, and the
#    file parses.  The operator's own check (fingerprint == reported) would also have passed, but it establishes
#    FILE IDENTITY, not PATCH PRESENCE: had the patch been absent, the fingerprint check alone would have let the
#    land through and produced an artefact whose header contradicts the documentation again.

# ===== ROUND 51: THIRD-LAND RE-VERIFY (MY QUEUE SLOT) + CLEANUP INTERSECTION RECOMPUTED FROM THE PRODUCTS =====
# AS-OF 2026-09-23T13:34:44.090061 | whitelist-all 3E9917FB66925871 / 23355 B (436 entries) | UNION-all BC5E84867463C51A / 20495 B (436 entries)
# 1. VERDICT: fails=0.  Every check passes, and this time the pre-registered removal half FIRED FOR REAL:
#      removals match the pre-registration : removed=3 listed=3 exact_match=True unexpected=[]   <- first removed>0
#      additions (pre-registered)          : added=0
#      entry count state                   : 436 = the post-rewrite value (f2 286)
#      ledger citations present in union   : cited=187 missing=0
#      internal consistency (7b)           : count==declared 436 ; union outside A|B = 0 ; A-only 136 == measured ;
#                                            B-only 0 == measured ; no empty digest fields
#      members / negative control / structure / containment / anchors : all PASS
#      precondition 8                      : FRESH for the first time (ledger 13:25:46 <= build 13:29:17)
#    => the sealed expectation became a CONFIRMATION: the abandon-line rewrite removed exactly the three files it
#       was supposed to remove, added nothing, and moved the count 439 -> 436.
# 2. ONE PRECONDITION STILL FIRES, and its cause is self-referential: 6 files are newer than the build inside the
#    cited zone, and they include MY OWN verification scripts (round-50*.py) plus CR-U5R's report.  A verification
#    entry that lives in the cited zone keeps precondition 9 warm by being used.  Recorded rather than hidden: the
#    substantive checks are all PASS and this one clears when the surface goes quiet.
# 3. CLEANUP INTERSECTION, RECOMPUTED FROM THE PRODUCTS as ordered (the 439-generation numbers are VOID):
#      pattern  = ^(cr-r1|CR-R1) ; boundary = .ai-tmp/test, non-recursive, files only
#      hits = 129 | protected (in A or in the union) = 5 | delete candidates = 124
#    Note how the protected count moved: 5 (439 generation) -> 5, because the D123/D124 citations of MY five
#    audit-chain files took effect in the 13:08 land.  The earlier "0 of 71 protected" was computed against the
#    336 generation and is void for the same reason.
# 4. ACCEPTED-DEGRADATION COUNT (so the ledger is complete rather than implicit): 17 of my 124 delete
#    candidates are named somewhere inside .ai-tmp.  Under the ruled mechanism that is NOT protection, so it is not
#    a reason to keep them; the readings they carry live in documents that ARE protected.  Counted, not assumed.

# ===== ROUND 52: DISPOSAL OF THE STALE JOIN TABLE THE LEAD ASSIGNED + A VERIFIED RUNNABILITY FACT =====
# AS-OF 2026-09-23T13:37:54
# 1. THE ASSIGNMENT: .ai-tmp/test/CR-F1-bare-reconcile+in_frozen_union.tsv is MY product (header says "joined by
#    CR-R1"), 21408 B, and its own header binds "UNION-all entries=327 sha256_16=778DAE80..." -- two generations
#    back (live: entries=436 sha256_16=BC5E8486...).  It is NOT in the frozen union => a process artifact.
#    Disposal chosen: APPEND A STATUS BLOCK (history untouched, no new artifact, no protected file touched) that
#    states the binding, names the superseder, names the refresh entry, and records the disposal rule.
#    21408 B -> 22911 B.
#    Why not "recompute it": the same join already exists in a REFRESHABLE and PROTECTED form --
#    .ai-tmp/test/CR-R1-bare-reconcile-joined.tsv, produced by the entry cr-r1-bare-join.py.  Recomputing the stale
#    copy in place would have created a second instance of the same reading, i.e. exactly the duplication this
#    audit has been deleting.  One reading, one carrier, one refresh path.
# 2. MATERIALITY -- COMPUTED, NOT ASSERTED: the staleness is BINDING-ONLY.  Replaying the entry's predicate
#    verbatim over the 161 names of CR-F1's table, against the live 436-entry union: verdicts that changed = 0.
#    So no reader is misled about a verdict; a reader is only misled if he quotes an old AS-OF as current.
#    (This is precisely why the block above says AS-OF instead of "current".)
# 3. A WORRY I CHECKED AND RETIRED (recorded because "checked" is the point): my kept entry reads two inputs, and
#    an entry whose INPUTS get deleted is not runnable even if the entry itself is protected.  Measured:
#      cr-r1-bare-join.py            inA=True  inUNION=True  (protected)
#      CR-F1-bare-reconcile.tsv      inA=True  inUNION=True  (protected INPUT)
#      CR-F1-bare-uncovered-51.txt   inA=True  inUNION=True  (protected INPUT)
#      CR-R1-bare-reconcile-joined.tsv inA=True inUNION=True (protected OUTPUT)
#    => the whole chain survives cleanup, so "one-command re-check stays runnable" holds for my line.
#    I had written in the previous round that these inputs "are presumably NOT protected" -- I checked before
#    reporting it, and the alarm was wrong.  Sixth self-inflicted false alarm tonight, all caught by measuring.
# 4. THE SAME BINDING QUESTION APPLIES TO MY *PROTECTED* -joined.tsv: its header also binds 327/778DAE80
#    (AS-OF 11:09:11).  I deliberately did NOT rewrite it: it is cited (in A and the union), it declares its own
#    AS-OF and binding, and verdicts have not moved -- so refreshing it would churn a cited artifact for zero
#    informational gain.  If currency is ever wanted, the entry regenerates it in one command.
# 5. CLEANUP NUMBERS, per CR-F2's request in the agreed format (pattern + boundary + instant, and which number is
#    a CHOICE vs a FACT):
#      pattern = ^(cr-r1|CR-R1)    boundary = .ai-tmp/test, non-recursive, files only    instant = 13:34:44
#      hits = 129 (FACT) | protected = 5 (FACT: membership in A or in the union) | delete = 124 (FACT: complement
#      of protected within the pattern+boundary) -- the CHOICE part is the pattern/boundary/instant themselves.
#      => generation value: recompute at the momen of deletion.

# ===== ROUND 53: ACCEPTANCE ENTRY MADE GENERATION-AWARE + BOTH NEW BRANCHES PROVEN TO FIRE =====
# AS-OF 2026-09-23T13:41:42 | entry cr-r1-acceptance.py = E44D0033C753B1F9 / 24749 B
# 1. WHY THIS EDIT WAS NEEDED.  The 4th land (the extOk fix) moves the entry count off the only two values my
#    entry knew (439/289 and 436/286), and its additions come from a mechanism my four attribution kinds did not
#    cover.  So the previous version would have reported TWO FAILURES for a land behaving exactly as sealed --
#    a manufactured red line.  That is my own registered lesson applied to my own instrument: a criterion pinned
#    to one generation is not a criterion.
# 2. WHAT CHANGED.
#    (a) THREE STATES: a new nj() prints N/J, is counted, and drives its own exit code (3).  "I have no sealed
#        expectation for this generation" is neither green nor red -- same shape as CR-F1's AssertSince
#        ("could not read" is not "passed").
#    (b) 7d is now keyed BY GENERATION: SEALED_COUNTS {439->436, 289->286} (the 3rd land) plus
#        SEALED_NEXT {436 -> 452, "4th land: extOk fix, +16 script-token members, removed 0"}.  An unsealed count
#        yields N/J instead of FAIL.
#    (c) 7c gained kind 5 (script-token protection: .py/.ps1 entried by the fixed bare-name branch) so a correct
#        4th land is not called unattributable -- and a SEALED ADDITION COUNT so the kind cannot become a rubber
#        stamp: a .py addition beyond the sealed number still fails.
# 3. THE SEALED 4th-LAND DERIVATION (independent of CR-F2's tool): CR-V1 measured 48 bare-name .py/.ps1 tokens,
#    of which 16 live in the cleanup zone and are currently unregistered => the fix should add EXACTLY 16 members
#    (removed 0), taking the union 436 -> 452.  If the tool reports a different number, the entry FAILS and names
#    both numbers, because a mismatch is a finding to reconcile (tool rule vs my derivation), not noise.
#    I am stating the derivation as well as the number so that neither side has to take the other's word.
# 4. PROVEN TEETH, by extracting the REAL code blocks out of the entry and executing them (the project's own
#    method: test the code in the file, not a rewrite of it):
#      7d block (30 lines)  : n_ua 436 -> PASS ; 452 -> PASS ; 470 -> N/J   (so it is not always-true)
#      additions block (17) : n_add 16/unattributed 0 -> PASS,PASS ; 17/0 -> PASS,FAIL ; 16/1 -> FAIL,PASS
#    Both new branches therefore fail when they should and pass when they should.
# 5. extOk IMPACT ON MY OWN FILES (lower bound -- only the documents I can reach): 3 of my 122 scripts are
#    named BARE in a reachable document, so the fix should move them out of my delete list:
#      cr-r1-acceptance.py; cr-r1-bare-join.py; cr-r1-final-selfcheck.py
#    => my cleanup numbers WILL shrink at the 4th land; they are therefore a generation value, which is the third
#       time that rule has bitten and the reason the deletion must be computed at the moment of deletion.

# ----- CORRECTION 2026-09-23T13:42:26: my extOk-impact lower bound two blocks above OMITTED cehua/ AND was not widened to
# ----- subdirectories, so it is VOID.  The ledger is the primary citation source, so a scan without it cannot
# ----- support any conclusion about the extOk fix.  Re-scanned over repo top level + docs/ + cehua/ (recursive)
# ----- + this report; my scripts matching the pattern = 123.
#   bare-name mentions (the form the fix newly honours): 3 -> cr-r1-acceptance.py; cr-r1-bare-join.py; cr-r1-final-selfcheck.py
#   path-form mentions (already protected before the fix) : cr-r1-acceptance.py; cr-r1-bare-join.py; cr-r1-final-selfcheck.py; cr-r1-record-round26.py
#   => NEWLY protected on my side = NONE
#   => so the extOk fix does NOT shrink my delete list on the evidence available to me (my three bare-named
#      scripts were already protected path-form).  CR-V1's 16 newly-covered files are therefore not mine, and my
#      cleanup numbers stay 129/5/124 for this generation -- still a generation value, still to be recomputed at
#      the moment of deletion.
# (Registered rather than quietly fixed: a lower bound that omits the main source is not a lower bound.)

# ===== ROUND 54: I SURRENDERED MY OWN PRE-REGISTRATION'S POWER TO PRODUCE A VERDICT =====
# AS-OF 2026-09-23T13:45:21 | entry cr-r1-acceptance.py = 2ABAAA01DA95847F / 27841 B
# 1. THE LEAD'S RULING LANDED ON ME, NOT ON SOMEONE ELSE.  I had sealed "added = 16, entries 436 -> 452" as a
#    FAIL-trigger inside my acceptance entry.  The lead ruled that derivation NON-BINDING: after the extOk fix the
#    PATH branch also admits the same extensions, so the true addition count may exceed 16, and the values of
#    record are CR-F2/CR-V2's.  => Had I left it, the 4th land -- behaving exactly as authorised -- would have
#    produced a red line MANUFACTURED BY MY OWN DERIVATION.  This is the same defect I fixed one branch above in
#    the opposite direction, and this time it was caught by a message rather than by my own measurement.  There is
#    no self-congratulation here: I was one land away from doing to CR-F2 exactly what I had just criticised.
# 2. WHAT I CHANGED, so an estimate can no longer manufacture red OR green:
#    (a) the 4th-land addition count is now REPORTED, never judged: with no seal it prints my derivation as an
#        estimate and returns N/J.  Landing exactly on my own 452 also returns N/J -- "my estimate came true" is
#        self-confirmation, not evidence, which is the shape this audit keeps deleting.
#    (b) an AUTHORITATIVE SEAL CAN BE INJECTED AT RUN TIME, so the owner's numbers do not have to be copied into
#        my source (no coupling, no second copy to rot):
#          python cr-r1-acceptance.py --expect-added N --expect-entries M
#    (c) with the owner's seal supplied the entry gives PASS/FAIL as before.
# 3. PROVEN BY EXECUTING THE REAL BLOCKS (not a rewrite):
#      additions (27 real lines): added=16 no-seal -> N/J ; seal 16 -> PASS ; seal 17 -> FAIL
#      7d        (42 real lines): n_ua=452 no-seal -> N/J ; 452 seal=452 -> PASS ; 470 no-seal -> N/J ;
#                                 470 seal=470 -> PASS ; 436 no-seal -> PASS ; 470 seal=452 -> FAIL
#    and the injection path itself has a positive control: running the entry with the CURRENT truth
#      (--expect-entries 436 --expect-added 0) prints "PASS entry count state (authoritative seal)".
# 4. GENERAL FORM OF THE LESSON (it is a THIRD state, not a shade of the other two): a pre-registration is worth a
#    verdict only if the number belongs to whoever owns the mechanism.  A number I derived by reading someone
#    else's measurement is an ESTIMATE, and an estimate that can turn a land red is indistinguishable from a bug
#    in my instrument.  So: report estimates, judge only seals -- and say whose.
# 5. STATUS of the entry after the edit: fails=0, not_judged=0, preconditions_broken=1 (the self-referential
#    freshness one), exit=2.  The moment the 4th land lands, ONE command with the owner's two numbers yields the
#    verdict; without them the affected checks say N/J instead of inventing one.

# ===== ROUND 55: ASSIGNED STALE REFERENCE FIXED (AND PROVEN SURGICAL) + A KEPT SCRIPT MADE SURVIVABLE =====
# AS-OF 2026-09-23T13:49:10 | report 9BDEEB2354D47F21 / 190364 B | cr-r1-record-round25.py AE7C94B1B742CF34 / 5175 B
# 1. THE ASSIGNMENT.  Report line 1162 named tools/probes/dir-content-hash-check.ps1, which has been deleted by
#    ruling.  VERIFIED FIRST, not assumed: the shim is really gone (exists=False); the surviving tool is
#    dir-content-hash-blindspot-suite.ps1 (CC6F8E0FEAD615E0 / 14887 B -- edited again since my round-24 check, 14082 -> 14887, and
#    that is fine: my earlier PASS applies to the bytes it named at that AS-OF, which is what an AS-OF means);
#    and MY REPORT IS NOT IN whitelist A or the UNION and NOTHING anchors its digest => the edit is safe.
# 2. HOW: byte-level surgery on that one line, then ROUND-TRIP PROOF -- strip the note again and the bytes must
#    reproduce the recorded pre-edit hash exactly.  Measured: 52E3DC4E5E205541 == 52E3DC4E5E205541 (MATCH).
#    That is a real proof of "only that line changed", unlike my FIRST attempt, which compared the written file
#    against the list I had already mutated => diff was [] no matter what.  An always-true assertion, mine, in the
#    act of fixing someone else's stale reference: 9th self-inflicted instrument defect tonight and the 3rd of
#    exactly this kind.  I also wrote the note rather than deleting the line: the comparison it records really did
#    happen on the bytes it names, so erasing it would destroy a fact.
# 3. CLASSIFICATION of all 17 mentions of the deleted name (so nobody has to guess which is live):
#      LIVE = 9, history/annotated = 8.
#      My own LIVE ones: cr-r1-record-round25.py L23/L36 (a kept accounting script), cr-r1-round34.py (a one-off
#      diagnostic, already a delete candidate), and this round's two scratch scripts (quoting the name).
#      Others': CR-U5R's report L737/L773/L1314 -- its records, reported rather than edited; its two references
#      are on its own cleanup path per the ruling.
# 4. THE BUG THE ASSIGNMENT EXPOSED, IN MY OWN KEEP LIST: cr-r1-record-round25.py called t() on the deleted path
#    unconditionally, i.e. re-running a script I am KEEPING would have died with FileNotFoundError.  That is the
#    same rule I registered two rounds ago ("the entry being protected does not make it runnable"), applied to a
#    file that survived the deletion of its input.  Fixed: the shim value is now
#      "ABSENT (deleted by ruling 2026-09-23; replaced by dir-content-hash-blindspot-suite.ps1)"
#    so the record REPORTS the deletion instead of crashing.  Verified by compiling it AND by extracting the real
#    guarded lines and executing them (2026-09-23: suite CC6F8E0FEAD615E0/14887, shim ABSENT).
# 5. GENERAL FORM (worth keeping): deleting a file is a change to every entry that READS it.  A cleanup that
#    only lists paths leaves a trail of entries that break on re-run -- and those breakages are silent until
#    someone tries to re-check.  => an entry is only a re-checkability asset if its INPUTS survive (or it says
#    they are gone).  I have now checked my own line for exactly that.

# ===== ROUND 56: ARITHMETIC SUSPENDED -> MY OWN SET-LEVEL CLAIM MADE FALSIFIABLE =====
# AS-OF 2026-09-23T13:52:02 | entry cr-r1-acceptance.py = 5F2522B19D5B328F / 29802 B
# 1. THE RULING LANDED AS "ALL ARITHMETIC EXPECTATIONS SUSPENDED" (the D123 rewrite moves the extOk baseline, so
#    +16 / 436->452 / added=19 are all void).  My entry was already immune: after the previous round it REPORTS
#    the 16/452 estimates and returns N/J without an owner-supplied seal, so a void number cannot produce a
#    verdict here.  A banner now says so at the top of every run, so no reader mistakes 452 for a target.
# 2. THE PART OF THE RULING THAT MATTERS MOST, AND WHAT I DID WITH IT: "set-level assertions are unaffected".
#    My slice has exactly one such claim, and until now it lived in prose: CR-R1 contributes ZERO additions.
#    It is now a check (7e), and it is falsifiable in the direction that matters -- if any CR-R1 file appears in
#    the delta's + lines, the line turns RED and NAMES the files.  Shape borrowed from CR-D1 and endorsed by the
#    lead: declare your slice's contribution as zero, then let it be testable.  Note the polarity: this claim
#    does NOT depend on any suspended number, so it stays valid across the rewrite.
# 3. TEETH, proven on the REAL 18-line block with synthetic deltas:
#      no + lines at all                  -> PASS
#      someone else's product in +        -> PASS   (control: the predicate is not a rubber stamp)
#      one of MY files in +               -> FAIL   (must be new information, and it names it)
#      mine in another zone / case        -> FAIL
# 4. WHY THIS IS THE RIGHT CLAIM FOR MY SLICE EVEN THOUGH IT CAN LEGITIMATELY FIRE: if the rewrite/new baseline
#    causes one of my files to be admitted, that is a fact about MY contribution -- I want to be told, not to
#    discover it later while quoting a number.  A claim that cannot fire is not a claim; this one can.
# 5. STATE: fails=0, not_judged=0, preconditions_broken=1 (the self-referential freshness check), exit=2.  The
#    arithmetic lines are informational; 7e, 2b, containment, structure, anchors and the negative control are the
#    verdicts, and none of them depends on a suspended expectation.

# ===== ROUND 57: PRE-LAND STATE + A ROW-TARGET DISCREPANCY + TWO ARTIFACT-LEVEL STRUCTURAL FACTS =====
# AS-OF 2026-09-23T13:54:53 | A 3E9917FB66925871 / 23355 B | UNION BC5E84867463C51A / 20495 B | delta F5923E122D0E3329 / 345 B
# 1. THE 4th LAND HAS NOT HAPPENED YET (measured, not assumed): A = 436 entries, UNION = 436, and the delta is
#    still the 3rd land's (added 0 / removed 3 = the three CR-U5 PNGs).  Ledger = 780FEF014FD2CD41 / 503743 B /
#    13:25:46.  So everything below is a PRE-land reading, taken to be useful AT the adjudication.
# 2. DISCREPANCY WORTH ACTING ON BEFORE ANY EDIT -- THE TOKENS ARE NOT IN THE ROW NAMED.  The four tokens at issue
#    (CR-F1-compile.* / CR-F1-shot*.out.txt) are NOT on the D123 row (L291): a case-insensitive scan of the whole
#    ledger finds CR-F1-compile and CR-F1-shot ONLY on L292, which is the **D124** row -- and that row OPENS by
#    quoting D123 ("| D124 | `D123` ..."), which is exactly the shape that invites reading it one row off.  I am
#    NOT editing anything (not my file); reporting so the owner targets the right row: editing L291 would be a
#    no-op, and a well-intentioned "fix" there could touch the wrong sentence.
# 3. STRUCTURAL FACT 1 -- THE REWRITE IS ENTRY-NEUTRAL (so removed = 0 stays structurally expected):
#    entries in A|UNION matching CR-F1-compile / CR-F1-shot = 0.  Those tokens protect NOTHING today, therefore
#    making them unparseable cannot REMOVE anything.  Measured, so the structural expectation does not rest on
#    the assumption that "a rewrite of a citation cannot shrink the set".
# 4. STRUCTURAL FACT 2 -- THE DEPTH AXIS IS FALSIFIED FROM THE ARTIFACT (CR-V1 did it from the source at L168):
#    inside the SAME directory .ai-tmp/test/r5/ the artifact protects anim-index.json and r5b.out.txt and does NOT
#    protect dumpsc.py / parse.py.  Same directory, different outcome => the gap follows the EXTENSION, not the
#    depth.  That independently supports the 2-item hard list and the exclusion of evidence-selfcheck.ps1.
# 5. A CANDIDATE MECHANISM FOR THE 16-vs-17 (vs 19) SPREAD, from artifacts rather than argument: TWO files share
#    the basename sc_decode.py --
#       .ai-tmp/test/scdfull/sc_decode.py   IN A|UNION   (15218 B)
#       .ai-tmp/web/sc_decode.py            NOT in A|UNION (15218 B, same size)
#    CR-F2's sealed 17 includes web/sc_decode.py; CR-V1's 16 apparently does not.  A bare-name token maps to
#    CANDIDATES, so if a count treats the already-protected scdfull/ sibling as satisfying the token, the web/
#    file never appears as a NEW addition.  => the spread may be a COUNTING-MODEL difference (one token, two
#    same-named files) rather than a tool gap.  Offered as a reading; the land adjudicates, not me.
# 6. READY FOR THE VERDICT: on landing, run
#       cr-r1-acceptance.py --expect-added 17 --expect-entries 453
#    (436 + |added| is CR-F2's own structural relation).  Without the seal the same lines return N/J, by design.
# 7. SELF-REGISTERED (small but same family): my inline "python -c" that printed ledger text died with a GBK
#    console encode error on a non-ASCII glyph.  My own rule already says scripts go through a FILE, not the shell
#    quoting/console layer; the useful output had already been produced, but the rule was mine and I broke it.

# ===== ROUND 58: THE '+' FILE -- AUTHOR'S DISPOSITION, WITH THE HAZARD MEASURED ON THREE LAYERS =====
# AS-OF 2026-09-23T13:57:52 | the file = E1BAF1BE87A7372C / 22911 B
# 1. WHAT I VERIFIED BEFORE DECIDING ANYTHING (the lead handed me disposal authority; authority is not evidence):
#      true name  .ai-tmp/test/CR-F1-bare-reconcile+in_frozen_union.tsv : on disk YES, in A/UNION NO/NO
#      FRAGMENT   in_frozen_union.tsv                                   : in A/UNION NO/NO
#      entries ENDING with the fragment (would collide)                 : 0
#      a file named exactly the fragment anywhere under .ai-tmp          : none
#    The fragment cannot enter BECAUSE OF THIS FIX: its suffix is .tsv, and the allowance being added to $extOk is
#    py/ps1 -- a bare .tsv token stays inadmissible.  (Path-form citations are how .tsv entries exist at all, which
#    is why extension-free path handling never showed this.)
# 2. AND IT CLOSES TWICE OVER: CR-F1's authorised rewrite removes that token from the ledger entirely.  So the
#    hazard the lead described has two independent closures -- extension axis, and token removal.
# 3. THE FAILURE SIGNAL THE LEAD WANTED ALREADY EXISTS, IN MY ENTRY: its structure check asserts that EVERY union
#    entry is a real file and prints the count (now 0, measured above).  If a fragment name ever did enter, that
#    line turns RED and names the count.  Registered here so nobody builds a second detector for the same thing.
# 4. MY DECISION AS AUTHOR: NO RENAME, NO DELETE NOW.
#      * the round-52 status block is intact on the file (read back off disk: E1BAF1BE87A7372C / 22911 B ; 21408 -> 22911 B) and states
#        its binding, its superseder and its disposal rule;
#      * it carries no unique reading (strictly an earlier instance of the PROTECTED -joined.tsv);
#      * renaming to remove the '+' would be churn against a hazard already blocked on two layers and detected on
#        a third, and would break the very name the AS-OF record quotes;
#      * it therefore goes with .ai-tmp at cleanup, like the rest of the process artifacts.  The lead can override,
#        but the measurements above are why I do not spend a rename on it.
# 5. GENERALISATION REGISTERED (CR-U5R's method, which is the correct equivalent of mine for a LIVE file): my
#    byte-level round-trip proof requires a quiescent file -- strip the note and the bytes must reproduce the
#    pre-edit hash.  For a file that is still being appended to, byte-equality is not available, so the equivalent
#    is INSERT-ONLY: every original line must remain a verbatim substring of the new line, plus an exact
#    line-count delta.  CR-U5R used exactly that (True/True/True, 1885 -> 1889 = the 4 lines it added) and its
#    L1314 downgrade -- declaring a source unrecoverable instead of paraphrasing it -- is the honest handling.
#    => two proofs for two file states; neither one asks a reader to trust a claim.

# ===== ROUND 59: MY VERDICT ON THE 4th LAND (SEAL INJECTED) + SELF-CORRECTION OF MY ROUND-58 REASON =====
# AS-OF 2026-09-23T14:01:46 | A 649E4543FF1E4E90 / 23971 B | UNION A34176A686F4F809 / 20941 B | delta 29EEF26FB8E8553E / 687 B | tool 2AE9FE4A6AD80A3D/73590/13:56:07
# 1. VERDICT: fails=0, not_judged=0, preconditions_broken=1 (the self-referential freshness one), exit=2.  Run as
#       cr-r1-acceptance.py --expect-added 16 --expect-entries 452
#    i.e. with the OWNER'S seal injected, which is the only thing allowed to produce a verdict here.
#      delta arithmetic      : added=16 removed=0 prev=436 new=452   <-- equal to the seal, bit for bit
#      sealed addition count : added=16 == the authoritative seal supplied at run time
#      entry count state     : union has 452 == the seal
#      structure UNION-all   : backslash=0 outside_zone=0 not_a_file=0 entries=452   (f2: 293)
#      additions matter      : 16/16 + lines exist on disk AND are in the body
#      removals              : removed=0, matching the structural expectation
#      internal consistency  : count==header 452 ; union outside A|B = 0 ; A-only 152 == measured ; no empty digests
#      containment/anchors   : my two frozen files still subsets; both union headers still pin them
#      7e (my slice)         : added lines=16 and CR-R1 entries among them = 0   <-- my own set-level claim, HOLDS
# 2. TWO LAND CRITERIA I CONFIRMED INDEPENDENTLY (not my file, so I only read):
#      * anchors: two NEW rows with changed=SELF -- 13:59:01 all (A=649E4543FF1E4E90) and 13:59:27 f2
#        (A=9666995C6DA712F3), while the last pre-land row (13:29:33) is changed=A.  So the forward prediction
#        "before the land it must still be A, after it there must be exactly 2 new SELF rows" HELD, and it HELD
#        for the right reason: the label changed when the actor changed.
#      * assembly stamp: CR.dll = 0F4D2C8A75D5D318 / 328704 B / 11:17:19 == the frozen baseline, i.e. the land
#        touched artifacts only, not the product.
# 3. I MUST CORRECT MY OWN ROUND-58 REASON.  I wrote that the fragment could not enter "because its suffix .tsv is
#    not in the allowance".  The allowance is
#      $extOk = @('txt','png','jpg','jpeg','json','tsv','log','out','cs','md','ogg','asset','unity','meta','ini','xml','py','ps1')
#    -- so tsv IS allowed, and my reason was simply WRONG.  The operative gate is the RESOLUTION gate that CR-D1
#    and the lead established: a plain token is accepted only when it resolves to an EXISTING file inside the
#    cleanup zone, and the fragment names nothing.  The CONCLUSION is unchanged (the fragment cannot enter), and
#    now it is closed twice over: resolution gate + CR-F1's removal of the token.
#    A wrong reason is a wrong fact.  It is corrected here rather than quietly left standing.
# 4. THE INSTRUMENT DEFECT THAT PRODUCED THAT WRONG REASON, registered because it is the same family as six of
#    tonight's: my extension parser required a leading dot (r"'\\.(ext)'") while the file writes the extensions
#    bare ('txt','py','ps1').  It therefore returned an EMPTY SET and printed "in $extOk? False" for EVERY
#    extension -- including py/ps1, which are visibly present in the very line it had just printed above.  An
#    empty set from a failed parse is indistinguishable from "nothing is allowed", and every downstream test then
#    reads False.  10th self-inflicted instrument defect tonight, 4th of the empty-set/always-false kind.  What
#    caught it was not my reasoning but the contradiction between my output and my own evidence line.

# ===== ROUND 60: 4th-LAND COLLISION WITH FOUR SEALS (BOTH SCOPES + REMOVALS) + THE NO-SEAL CONTROL =====
# AS-OF 2026-09-23T14:04:14 | entry cr-r1-acceptance.py = 380A62F2058E01A5 / 32039 B
# 1. THE ASSIGNMENT was "inject 16 / 452-293 / removed 0".  My entry only took the first two, so I extended it
#    rather than narrate the extra numbers: --expect-entries-f2 and --expect-removed are now seals too, because
#    the f2 set is a DIFFERENT citation scope (its own additions: +7, not +16) and sealing only the all-scope
#    count would leave half the land unjudged.
# 2. VERDICT (four seals): fails=0, not_judged=0, preconditions_broken=1 (self-referential #9), exit=2.
#      removed == owner's seal        : removed=0 ; seal=0
#      sealed addition count          : added=16 == seal
#      entry count (all)              : 452 == seal
#      entry count (f2)               : 293 == seal
#      my slice, all scope (7e)       : added lines=16, CR-R1 entries = 0
#      my slice, f2 scope (7e bis)    : f2 + lines=7, - lines=0, CR-R1 entries = 0
#      structure / consistency        : not_a_file=0 ; 452 and 293 ; union outside A|B = 0 ; A-only 152 == measured
#      additions matter               : 16/16 + lines are in the body AND exist on disk
#      precondition 8                 : FRESH -- ledger 13:56:34 <= union built 13:59:02 (i.e. the freeze order
#                                       held with the FINAL ledger state, the one the lead corrected to)
# 3. INDEPENDENT CONFIRMATION OF CR-V2's f2 NUMBERS, from the f2 delta file itself rather than from its report:
#    f2 + lines = 7, f2 - lines = 0, f2 entries = 293.  Its "+7/-0" is therefore reproduced on my side.
# 4. THE NO-SEAL CONTROL -- run without any seal, the same three lines return:
#      N/J sealed addition count        (my 16 is an estimate the lead ruled non-binding)
#      N/J entry count state            (landing exactly on my own 452 is self-confirmation, not evidence)
#      N/J entry count state (f2 scope)
#    fails=0 not_judged=3.  So the instrument can no longer manufacture a verdict from my own arithmetic, and it
#    cannot silently read as green either: N/J is counted and drives its own exit code (3).
# 5. ONE MORE THING THIS BATCH ADDED THAT IS WORTH KEEPING: "removed == the owner's seal".  A sanctioned rewrite
#    is exactly the situation where an accidental SHRINK could hide (everyone is looking at the additions), and
#    until now my removal check only knew how to say "0 is fine" or "not equal to the three PNGs".  Now a sealed
#    removal count is compared like the others, so a shrink turns red on the number the owner published.

# ===== ROUND 61: GLUE AUDIT OF MY OWN CARRIERS (PROMPTED BY CR-U5R) + TWO DEFECTS MY AUDIT PRODUCED =====
# AS-OF 2026-09-23T14:07:19 | report 7741818B70FA9783 / 208306 B / 2470 lines | heartbeat AF520DBC6F6A09C6 / 56128 B / 130 lines
# 1. CR-U5R'S FINDING APPLIES TO ME, so I tested for it instead of asserting I was fine.  Its point: in an
#    append-only record, SUBSTRING PRESERVATION IS NOT THE LOAD-BEARING HALF -- when an append forgets its
#    leading newline, the record is glued onto the previous line, and "every original line is still a verbatim
#    substring" passes anyway.  What fails is the LINE-COUNT DELTA (and, in its case, a structure test whose
#    "starts with #" clause is blind to a record glued to a comment line).
#    So I did not count lines blind; I looked for STRUCTURAL fingerprints of a glue:
#      report  : a block marker not at the start of its line ; a line carrying two markers ; an AS-OF not at start
#      heartbeat: a line carrying TWO ISO timestamps ; a line not led by a timestamp ; a GAP in the round sequence
# 2. RESULT -- BOTH CARRIERS ARE CLEAN:
#      report: 2470 lines ; 67 "# ===== ROUND" occurrences, ALL at line start ; lines with two markers = 0 ;
#              "# AS-OF" off line-start = 0
#      heartbeat: 130 lines ; lines with >=2 ISO stamps = 0 ; lines not led by a stamp = 0 ;
#              round numbers run 2..60 with NO GAP (59 distinct numbers, 73 record lines)
#    => no glued append, no lost record.  (Why my own appends are safe: every block literal begins with a newline.)
# 3. TWO DEFECTS MY AUDIT PRODUCED, both mine, both the same family -- the verdict disagreeing with its own data:
#      (i) the check's verdict sentence was a HARD-CODED CONSTANT ("none here means the count delta is safe")
#          while the same run printed 13 duplicate round numbers.  The sentence contradicted the numbers it had
#          just printed.  (Third time tonight: the "0 = no product would be mis-deleted" constant, float(t)-t
#          printing 0.0, and this.)
#     (ii) after fixing it, my computed verdict equated "duplicate tag" with "glued/lost record" -- over-broad.
#          Decomposed properly: exactly ONE full tag is duplicated (round 3), and a duplicate line is NOT a glue
#          (0 lines carry two timestamps, which is what a glue looks like).  So: glue/loss = NONE ; duplicate
#          lines = 1 (cosmetic, registered here rather than tidied away).
# 4. THE RULE I TAKE FROM THIS, stated so it outlives the incident: a checker's sentence must be COMPUTED from the
#    same data as its verdict.  A hand-written "none here" beside a printed list of duplicates is exactly the
#    "constant instead of reading" defect this audit keeps deleting -- and it is the easiest one to commit while
#    auditing someone else's finding.

# ===== ROUND 62: CR-V2's DELIVERABLES VERIFIED (AUTHOR GONE) + THE UTF-16 DEFECT THAT ALMOST FOULED IT =====
# AS-OF 2026-09-23T14:10:02 | report 1DE333D0AF3B1A19 / 211126 B
# 1. WHY NOW: CR-V2 is gone, so its deliverables cannot be re-asked.  Anything unverified today can never be
#    verified by asking again -- this is the case where capture-while-the-bytes-exist is the only option.
# 2. VERified independently, all five, and all MATCH the lead's reading:
#      cited-evidence-whitelist-all.txt       649E4543FF1E4E90  23971 B  13:59:00
#      cited-evidence-whitelist-f2.txt        9666995C6DA712F3  17129 B  13:59:27
#      cited-evidence-whitelist-UNION-all.txt A34176A686F4F809  20941 B  13:59:02
#      cited-evidence-whitelist-UNION-f2.txt  76EEC508CAEFE261  14097 B  13:59:28
#      CR-V2-relay4-land.txt                  7E6B5F5E6161B173  63508 B  13:59:28
# 3. MY DEFECT, CAUGHT BEFORE IT BECAME A FALSE REPORT AGAINST A DEAD TEAMMATE'S WORK: I read the relay4 stdout
#    as UTF-8 and concluded it "does not contain" the resulting hashes.  It is UTF-16LE with a BOM (FF FE).
#    Decoded correctly, it is a rich primary source and DOES self-report what it produced:
#       TRANSITION  cited-evidence-whitelist-transitions.tsv  3E9917FB66925871 -> 649E4543FF1E4E90  (+16/-0)
#       TRANSITION  cited-evidence-whitelist-transitions.tsv  6113124EF30EE3D9 -> 9666995C6DA712F3  (+7/-0)
#       WHITELIST entries (uniq) = 452 ; STEP1 307 / STEP2 273 / STEP2c 191 / STEP3 87 / brace 7->16 / range 1->5
#    So "the land really happened" has a primary-source record, and my "absence" was an artefact of my own
#    decoder.  THE TELL, worth remembering: the absence was TOO CLEAN -- a 63508-byte tool stdout that contains
#    not one of sha256_16 / added / removed / ANCHORS / exit is not an empty file, it is a mis-decoded one.
#    4th "carrying layer" defect tonight (ANSI/GBK console, empty-set parse, and now file encoding).
# 4. A SENTENCE OF MINE THAT WAS NOT COMPUTED FROM ITS OWN DATA (3rd instance, and I had registered this family
#    one round earlier): my check printed "both readings are right: 2 rows carry the label, the rest are the note
#    text" -- asserting a reconciliation I had NOT verified.  Measured now: exact "SELF" = 2, case-insensitive
#    "self" = 12 (of which "itself" = 2, prose otherwise).  I cannot reproduce the lead's "10" from this file:
#    its scope may be wider (several files) or counted differently.  RECORDED AS UNRECONCILED -- the honest state
#    is "two numbers, two readers, not yet reconciled", not "both are right".
# 5. MY SLICE UNDER THE NEW BASELINE (452): pattern ^(cr-r1|CR-R1), .ai-tmp/test non-recursive, files only ->
#    hits 155 | protected 5 | delete 150.  The protected five are unchanged from the 436 baseline, i.e. the 16
#    additions contained nothing of mine -- consistent with my earlier finding that none of my files is newly
#    covered.  And the whole A|B fixpoint: entries recorded but missing on disk = 0.

# ===== ROUND 63: DELIVERY-EDITION SELF-CHECK (8 ITEMS + CONTROLS) + A FAIL-CLOSED CLEANUP ENTRY =====
# AS-OF 2026-09-23T14:15:42 | report 9E17BD9459CB6045 / 214126 B | selfcheck D546873F6587B10A / 11142 B | cleanup 8EDA288D3E5ADE79 / 4871 B
# 1. SELF-CHECK RESULT (verbatim from the run): fails=0 not_judged=0 exit=0.  Items: [1] no stray files of mine
#    outside my homes / no handover docs ; [2] no forbidden loggers + NO script of mine opens cehua/ for WRITING
#    (offenders=0) ; [3] every declared index entry reachable (190 entries, 0 unreachable) + integer-ns manifest ;
#    [4] eight registered items incl. all five of my own defects ; [5] every referenced path exists, and the
#    deleted shim is BOTH absent and annotated ; [6] artifacts newer than the products they verify ; [7] ONE-KEY
#    ENTRIES ACTUALLY RUN, WITH THEIR CONTROLS -- retain-assembly --check = OK exit 0, acceptance with the
#    owner's seals = fails=0, acceptance WITHOUT seals = 3 N/J (so the entry cannot manufacture a verdict) and
#    the cleanup dry run holds its safety property ; [8] the cleanup list recomputed at that instant.
# 2. THE CLEANUP ENTRY (cr-r1-cleanup.py), designed fail-closed:
#      * recomputes the protected set from the PRODUCTS at the instant it runs -- a list computed earlier is a
#        memory, not a list (this is my own registered reason the delete set moves);
#      * REFUSES the whole run if any delete candidate is a product member (the safety property is checked, and
#        it prints that it was checked);
#      * requires --confirm N, and REFUSES when N differs from the recomputed count -- a stale count is exactly
#        how a cleaned list becomes a wrong list.  Verified: --confirm 999 => "REFUSING", exit 5, nothing deleted;
#      * excludes ITSELF, and explicitly does NOT assert any keep-list membership for itself (no ruling has
#        granted one, and inventing it would be an assertion I do not own).
#    Dry-run reading at this instant: hits 156 | KEEP (product members) 5 | DELETE candidates 151.
# 3. THREE MORE DEFECTS ON THE WAY, all mine except the last, all registered:
#      (a) the self-check's write predicate was BROKEN: the alternation `Out-File|Set-Content|...cehua` dropped the
#          CONJUNCTION, so any file MENTIONING a write verb was flagged -- three false positives, which were a
#          regex, a list of verbs being SEARCHED FOR, and a comment.  Measured the real claim (a line naming
#          cehua AND carrying a write form) = 0, then fixed the predicate to that conjunction.
#      (b) another item searched for a string I never wrote ("NOT-JUDGED") while my harness prints "N/J" -- a
#          check whose search key is INVENTED rather than read off the artifact it inspects.  Fixed to the
#          measured key (N/J = 15 mentions).
#      (c) each of those FAILs was, by construction, a FALSE ALARM ABOUT MY OWN WORK; both times the first move
#          was to suspect the predicate, and both times the suspicion was right.  This is the 10th and 11th
#          self-inflicted instrument defect tonight.
#      (d) ONE LEVEL UP: my first attempt at (a)'s evidence ran INLINE and the HOST BLOCKED THE COMMAND because
#          its text contained a writing verb the host guards.  I was INVESTIGATING that verb and the layer that
#          guards it stopped me.  Same family, one level up -- and the fix was my own standing rule: the probe goes
#          through a FILE, never through the shell's text.  (Also: I had written an earlier version of item [2]
#          that ended "and True" -- a predicate that could not fail -- and it crashed on a type error instead of
#          lying.  Replaced by the check above.)

# ===== ROUND 64: THREE CHECKS FROM TEAMMATES -- AND A CORRECTION WHERE I FELL INTO THE TRAP I WAS QUOTING =====
# AS-OF 2026-09-23T14:19:00 | report 434F2379F5FE9EEE / 217834 B | heartbeat C9576F7366C6F2E6 / 59163 B
# 1. TRAILING LF (CR-U5R's root cause: a file with no trailing newline turns a missing leading newline on append
#    into a GLUE, and its own carrier had 22 records fused into a 44781-char line).  Measured on MY carriers:
#      report    : ends with LF = True ; lines 2569 ; longest line 423 chars ; lines >5000 chars = 0
#      heartbeat : ends with LF = True ; lines 133  ; longest line 1163 chars ; lines >5000 chars = 0
#      lines carrying >=2 ISO stamps (what a glue looks like) = 0 in BOTH
#    => no glue, and no latent glue mechanism (both carriers end with LF; my blocks also begin with one).
#    I verified this rather than assuming it, because the same class of defect cost a teammate 22 records.
# 2. A CORRECTION, AND IT IS THE TRAP CR-U5R HAD JUST DESCRIBED: it warned that deduplicating by KEY reports fake
#    duplicates, and that the only mechanical test is CONTENT.  I had written (round 61) that "round3 appears
#    twice: an exact duplicate LINE".  Measured by content now:
#      exactly identical lines in the heartbeat = 0        <- so there is NO true duplicate
#      same TAG but different content           = 2        <- these are the FAKE duplicates
#    => my "13 duplicated round numbers" was a KEY-based count: the mirror trap, in the act of quoting its
#       warning.  The honest wording is: 0 true duplicates, 2 fake ones (same tag, different records).
#       (Two of my rounds wrote two heartbeat lines each with the same tag but different text -- that is what a
#        key shows, and what content refutes.)
# 3. ADOPTING CR-D1's RECONCILIATION -- BUT ONLY AFTER REPRODUCING ITS FOUR PREDICATES MYSELF.  I had recorded
#    "unreconciled: 2 vs 10 vs 12".  Re-measured on cited-evidence-whitelist-anchors.tsv (16 lines: 1 comment +
#    15 data) with four explicitly stated predicates:
#      A semantic  col7 == 'SELF'                     = 2    (CR-D1: 2)  <- my original reading, and the CITERION
#      B literal   exact 'SELF' occurrences           = 2    (CR-D1: 2)  <- coincidence, NOT an equivalent of A
#      C icase     'self' ignoring case               = 12   (CR-D1: 12)
#      D icase minus the two column values            = 10   (CR-D1: 10) <- the lead's number
#      breakdown: old-row note "self-rewrote" x8 + new-row note "rewrote itself" x2 + column value x2 = 12
#    => EVERY predicate reproduces exactly, so the reconciliation is adopted as THREE PREDICATES:
#       2 (column value, semantic) / 10 (note text only, case-insensitive) / 12 (everything, case-insensitive).
#       All three are true at once; the previous "unreconciled" is withdrawn, not because the lead ruled it, but
#       because the numbers came out.  And B == A today is a coincidence (the old notes use lowercase
#       "self-rewrote"), so B must never be used as A's equivalent.
# 4. WHAT TIES THESE THREE TOGETHER, and the reason I am recording them in one block: each one is "the number is
#    right, the PREDICATE was unstated".  Trailing-LF, duplicate detection and the SELF counts are three
#    independent places where a count without its predicate is not a fact but a coincidence waiting to mislead.

# ===== ROUND 65: THE INVARIANT MOVED INTO THE WRITE PATH (WHY MY SNAPSHOT WAS THE WEAKER FORM) =====
# AS-OF 2026-09-23 (appended through the new helper, which asserts on both sides of the write)
# 1. THE LESSON, AND WHY IT HIT ME.  The lead/CR-D1 finding: "ends with LF" is a PRECONDITION OF THE APPEND PATH,
#    not a one-off property of a file -- a teammate fixed its carrier once, appended again, the condition came
#    back, and 22 records fused into one 44781-char line.  I had measured exactly the weak form: a SNAPSHOT of my
#    two carriers ("ends with LF = True").  Measured now on the WRITE PATH instead:
#      * my REPORT blocks are written as literals that begin with a newline, so they cannot glue even if the
#        trailing LF is missing -- the invariant is carried by the writer;
#      * my HEARTBEAT writes began with the timestamp itself, i.e. they RELIED on the file property.  It happened
#        to hold (the file ended with LF), which is exactly the weaker form the lesson warns about.
# 2. MY DETECTOR WAS UNRELIABLE, SO I STOPPED TRUSTING IT.  I first tried to classify all 75 of my appending
#    scripts by regex and it reported "no leading newline" for EVERY script -- contradicting a known fact, so the
#    classifier, not the scripts, was wrong.  A check I cannot trust is worse than no check: it would have let me
#    "verify" the write path with a string search.  That is my 12th self-inflicted instrument defect, and the
#    remedy is not a better regex but a MECHANICAL step in the write path.
# 3. THE MECHANICAL STEP: .ai-tmp/test/cr-r1-append.py (helper, keeper)
#      BEFORE: if the target does not end with a newline, ADD ONE and say so.
#      AFTER : assert the target ends with a newline AND that the appended text began at a line boundary; print the
#              line-count delta.  A failure exits non-zero, so a bad append cannot pass silently.
#      CONTROL: --selftest puts a scratch target in the dangerous state and requires the repair to happen, so the
#      guard is not one that has never fired (that shape is the "impossible-to-fail guard" this audit keeps
#      deleting).  Run result: repaired as expected, scratch file removed.
#    From this round on, my appends go through it -- which is what "make the lesson an ACTION, not Know-How" means.
# 4. WHAT IS STILL VERIFIABLE ABOUT THE PAST (the outcome, not the intent): the report has 0 block markers off a
#    line start and 0 lines carrying two markers; the heartbeat has 0 lines carrying two ISO stamps; longest lines
#    are 423 / 1163 chars.  So nothing glued despite the weak form -- an outcome I can measure, unlike the intent
#    of 75 old scripts, which I cannot.
