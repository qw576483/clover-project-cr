# G4 · 自审对比（对局 HUD + 结算 vs 原版基线图）

> **对照物**：`策划/参考图/02_对局HUD_1242x2208.jpg`（顶部 HUD 完整）、`09_对局_1242x2208.jpg`
> （顶部 HUD 完整、另一局）、另 6 张对局图 `03/04/05/06/08/10`（只用于确认底部覆盖与顶部一致性）。
> **我们的截图**：、（均 **1080×1920**）。
> **联络图（AI 只读这张）**： → **格 3**（HUD）/ **格 4**（A 02）/
> **格 5**（结算）/ **格 6**（A 09；⚠️ A **没有**结算整屏，格 6 只作"同一局画面"参考）。
> **我们的值出处**：`.ai-tmp/test/v4-audit-step7-Hud.txt`、`.ai-tmp/test/v4-audit-step9-Result.txt`
> （运行时节点审计；canvas 像素、原点左上）。
> **采集口径**：HUD 截图来自**真·人机对局**（生产链路：登录 → 人机对战），卡池是服务端真数据
> （审计里手牌是真卡名/真卡面），⛔ 不是注入的假数据；结算截图是把 `BattleEndNotify` 经**生产入口**
> `BattleUiHost.OnEnded` 注入（`win=true / crowns 3:1 / reason=king_destroyed`），并已在两点注明是构造值。
> **判据口径**：每项只写 `一致` / `不一致（差在哪）`；差 ≤1.0px 记 `一致`。
> **冻结**：两图 + 两份审计均晚于本片对面板文件的最后一处改动（本片只改了 `DeckEditPanel.cs`，
> **未动** `HudPanel.cs` / `ResultPanel.cs`）。Play 记账见 `.ai-tmp/test/play-log.tsv`。

## 一、对局 HUD（`V4-对局HUD.png`，来自真·人机对局）

| # | 项 | 结论 |
|---|---|---|
| 1 | 有无原版对应界面 | **一致**：02/09 就是对局中的整屏，含顶部 HUD |
| 2 | 朝向 / 画布 | **一致**：`Screen=1080x1920 portrait=True`、`referenceResolution=1080x1920`、`matchWidthOrHeight=0`、`scaleFactor=1` |
| 3 | 顶部左上块**位置** | **一致**：原版王冠盾徽 x 20..78 / y 25..120 @1242 ⇒ 17.4..67.8 / 21.7..104.3 @1080；我们 `CrownIcon` 画布 `(21,18)-(113,91.8)` ⇒ 左/上差 **+3.6 / −3.7px**（≤4px，落在读数容差 ±4.3@1080 内） |
| 4 | 顶部左上块**内容** | **不一致（差在哪）**：原版是「王冠盾徽 + 玩家名 `Blue King` + 部落名 `Clash Royale` + 奖杯 `2000`」四件；我们是「金冠图元 + `我方冠 0 : 对方冠 0`」。**图元与文案都不同**（原版该处没有"冠数"文字，冠数在结算） |
| 5 | 倒计时框**位置**（右上角、贴屏右边） | **一致**：原版框右边界 x≈1240 @1242（距右边缘 ≈2px）；我们 `TimerBox` 右 = **1080**（贴边），左 = 892 |
| 6 | 倒计时框**离顶距离** | **不一致（差在哪）**：原版框上边界 y≈7 @1242 ⇒ **6.1 @1080**（几何量取 B4）；我们 **14**（`TimerBoxTop`）⇒ **+7.9px** |
| 7 | 倒计时框**尺寸** | **不一致（差在哪）**：原版 ≈**208×129 @1242** ⇒ **180.9×112.2 @1080**（B3/B6）；我们 **188×113** ⇒ 宽 **+7.1**、高 **+0.8**（高在 1px 内，宽超容差） |
| 8 | 倒计时框**文字结构** | **一致**：都是「小标签 + 大数字」两行（原版 `Time left:` / `0:18`；我们 `剩余时间` / `02:50`，字号 24 / 48）。文字为**本项目中文 UI**（等价语义） |
| 9 | 「圣水加倍 x2」徽记 | **不一致（差在哪）**：原版在倒计时框**下方**有一枚 `x2` 药水瓶徽记（02 图 x≈1090..1190 / y≈140..250）；我们**没有这一件** |
| 10 | 圣水条三件套的**素材** | **一致**：轨道 = A 原版 `ui_out/516`、填充 = `ui_out/518`、描边框 = `ui_out/517`，全部 `Sliced`（审计 `ElixirTrack/ElixirFill/ElixirFrame` 三行命中）；⛔ 不是纯色条、⛔ 不是拉伸整图 |
| 11 | 圣水条的**位置与尺寸** | **不一致（差在哪）**：原版底部被宣传字覆盖 ⇒ 原版值**未量到**（几何量取 C2），无法判"一致"；我们 `ElixirTrack 1029.7×47 @ (25.1,1591)-(1054.9,1638)`（条宽 = 底排整排宽，本项目自定） |
| 12 | 手牌 4 张 + 「下一张」 | **不一致（差在哪）**：原版值**未量到**（C1/C3）；我们 4 张 `177×242` + 下一张 `109.7×150`，整排 `1029.7×242 @ (25.1,1654)-(1054.9,1896)`。尺寸/间距是"由竖版容纳约束解出的**本项目自定值**"（面板注释有推导） |
| 13 | 手牌卡面 | **一致**：卡槽底 = A 原版 `frame_043`（`SlotCard` 九宫格），卡面 = A 原版 `ui_spells_out` 帧（审计 `HANDArt*` 有 sprite）；⛔ 不再是"类型色底" |
| 14 | 手牌卡面**缺件** | **不一致（差在哪）**：`皮卡超人`/`亡灵大军` 等卡的 `key` 未登记帧号 ⇒ 该槽只画原版卡槽底 + 卡名（`HandArt0/HandArt3 sprite=NULL`）。与 G3 #13 同一**约定降级口径**，但相对原版仍是缺件 |
| 15 | 冠数（HUD 上的本方/对方冠数） | **不一致（差在哪）**：原版顶部**没有冠数**（02/09 顶部只有玩家块 + 对手王塔血量 + 倒计时）⇒ 原版值**未量到**（C4）；我们是 `我方冠 0 : 对方冠 0`（本项目新增一件） |
| 16 | 「暂停」按钮 | **不一致（差在哪）**：审计 `PauseButton sprite=NULL color=(0.1,0.17,0.28)` ⇒ **纯色块**，且原版 HUD 该处无暂停入口（原版是设置齿轮，位置未到镜）⇒ 既无原版对照、又是纯色件 |
| 17 | 阶段行 / 状态行 | **不一致（差在哪）**：`Phase`（常规时间/加时赛/已结束）与 `Status`（拖放提示 / 服务端拒因）是**本项目新增**文字条，原版无 |
| 18 | 有没有元素被顶出画布 | **一致**：`ASSERT offCanvasNodes=0 OK`（底排 `25.1..1054.9` 与圣水条同列同宽） |
| 19 | 九宫格 / 纯色块 | **一致**：`spriteImages=18 sliced=9 notSliced=9`；9 个非九宫格全部是 **A 原版图标 / 卡面**（按原比例原样贴）；屏幕上的纯色形状**只有** #16 的暂停按钮（另加 Loading 遮罩/幽灵卡，均为非常态件） |

**对局 HUD：一致 10 / 不一致 9**

## 二、结算（`V4-结算.png`）

| # | 项 | 结论 |
|---|---|---|
| 1 | 有无原版对应界面 | **不一致（差在哪）**：A 侧**没有取到**结算整屏（`参考图/清单.md` §2「结算(胜负/三冠) = 未取到」，试过 Fandom `victory/defeat/result` 文件名搜索 + App Store 全槽位）⇒ 比较对象退化为"A 的同类部件"（面板底 / 标题条 / 深色圆角框 / 按钮 / 皇冠图元）。**如实标注，非缺陷** |
| 2 | 朝向 / 画布 | **一致**：竖版 1080×1920 / `match=0` / `scaleFactor=1` |
| 3 | 面板框架（宽 / 贴顶 / 标题条高 / 内缩） | **一致**：`ResultBox 1000×1090 @ (40,167)` ⇒ 宽 1000、贴顶 167；标题条 92；内缩 40 —— 全部是 A 侧**实测过**的那套竖版口径（`CrUiStyle`，与 G2/G3b 同源） |
| 4 | 标题条素材 | **一致**：A 原版金色标题条 `frame_069` 九宫格（`border=(24,2,24,20)`） |
| 5 | 面板底素材 / 底色 | **不一致（差在哪）**：`frame_806` 羊皮纸（`Sliced`+`border=(2,2,2,2)`）；A 面板是浅灰蓝 ⇒ 同 `D14`（`CrUiStyle`，本片无权改） |
| 6 | 结局徽记 | **一致**：A 原版图元 `ui_battle_end_out/195`（金杯 + 橄榄枝，`OutcomeIcon 96×89`，`sprite=frame_195_0`）；**位置为本项目自定**（A 无对照） |
| 7 | 结局字（胜/负/平） | **不一致（差在哪）**：我们 `胜 利` 字号 **72**；A 无结算整屏 ⇒ 原版值**未量到**（C5） |
| 8 | 冠数行 | **一致**：A 原版皇冠图元 `frame_050` + 数字；按本机 `my_team` 换算成"我方/对方"（审计 `Crowns '我方冠 3 : 对方冠 1'`） |
| 9 | 两条数据条 | **一致**：A 原版深色圆角框 `ui_battle_end_out/108` 九宫格（`border=(24,24,24,24)`），`ReasonBar 920×96` / `DetailBar 920×76` |
| 10 | 两个动作**上下两行** | **一致**：竖版口径 ⇒ `再来一局`(110 高) 与 `回主菜单`(95 高) **上下排布**、⛔ 不再左右并排（A 无对照，按竖版重排） |
| 11 | 按钮素材 | **一致**：主按钮 = `frame_300` 金按钮、次按钮 = `frame_014` 深蓝灰块，均 `Sliced` |
| 12 | 说明行 / 状态行 / 提示行 | **不一致（差在哪）**：`Rules`（人机/房间两种模式的"再来一局"后果）、`Status`、`Hint` 三行是本项目**新增文案**（为"点了才知道"兜底），A 无对照 |
| 13 | 拿不到结算数据时的兜底页 | **一致**：非预期分支有 UI 出口（`ShowNoData`，⛔ 不留空白页），并打 `Error` 留痕 |
| 14 | 有没有元素被顶出画布 | **一致**：`ASSERT offCanvasNodes=0 OK` |
| 15 | 九宫格 / 纯色块 | **一致**：`spriteImages=26 sliced=15 notSliced=11`；非九宫格全部是 A 原版图标（皇冠/徽记）按原比例贴 |
| 16 | 字号档位 | **一致**：标题 56 / 结局 72 / 正文 32 / 小字 24 —— 与 G1–G3b 同一套（A 侧该套字号已实测登记） |

**结算：一致 12 / 不一致 4**

## 三、合计与"不一致"的可修性

**对局 HUD + 结算：一致 22 / 不一致 13**（HUD 10/9、结算 12/4）。

| 不一致项 | 本片能否改 | 说明 |
|---|---|---|
| HUD #4 左上块内容 / #15 冠数 | ⛔ 不能 | 这两件是**本项目既有面板契约**（冠数是本项目的 HUD 件，原版无）；改它 = 改玩法呈现形态，不在本片范围。且原版对应项**未量到** |
| HUD #6/#7 计时框离顶 / 宽 | ⛔ 本轮不改 | 实测差 +7.9 / +7.1px；改它要动 `HudPanel` 的几何常量并重跑 `offCanvas` 断言 —— 属可见的**微调**，已在 `对照表行` 记差值，留给"UI 像素级收口"片 |
| HUD #9 x2 徽记 缺失 | ⛔ 本轮不改 | 需要新接一件原版图元（`ui_out` 里的 x2 药水瓶）；图元是否已在 `原版资源/` 未核，另开片 |
| HUD #11/#12 圣水条/手牌 尺寸 | ⛔ 无法判 | 原版值**未量到**（C1/C2），只能记"未量到" |
| HUD #14 手牌卡面缺件 | ⛔ 本轮不改 | 同 G3 #13：帧号白名单是逐帧目视建立的，⛔ 不许猜 |
| HUD #16 暂停按钮是纯色块 | ⛔ 本轮不改 | 需给它接 A 原版按钮九宫格（`frame_014`），并确认 HUD 上是否真该有这个入口（原版无） |
| HUD #17 阶段/状态行 | ⛔ 不是缺陷 | 本项目新增的状态表达（原版无此信息），已在 `对照表` 记为"本项目新增" |
| 结算 #1 「原版无此界面」 | — | 如实标注项，不是缺陷（A 侧确实未取到） |
| 结算 #5 面板底色 | ⛔ 不能 | `CrUiStyle.ContentPanel` 共用件（本片 forbidden）；同 `D14` |
| 结算 #7 结局字号 | ⛔ 无法判 | 原版值**未量到**（C5） |
| 结算 #12 说明/状态/提示行 | ⛔ 不是缺陷 | 本项目新增的"后果说明"，原版无 |

⇒ 本轮**不可消除**的不一致全部在 `策划/验收表.md` 的「允许的差异」里有行（含 `D9/D10/D14/D15`），
每条写了「是什么 / 为什么 / 出处 / 何时消除 / 谁能改」；**本片没有未登记的差异**。

## 四、返工记录

本片**未改** `HudPanel.cs` / `ResultPanel.cs`（逐项审计 `offCanvasNodes=0 OK`、九宫格断言全过）。
HUD 截图来自**真·人机对局**（生产链路），不是注入的假数据；结算截图按要求把 `BattleEndNotify` 经
`BattleUiHost.OnEnded` 注入（构造值已在类注释与本节注明）。
另：本片在 `DeckEditPanel.cs` 上发现并修掉 3 处缺陷（见 `G3-自审.md` §四），与 HUD / 结算无关。
