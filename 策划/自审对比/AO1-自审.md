# AO1 自审：逐项 一致 / 不一致（⛔ 不写"基本一致 / 大致像"）

- 采集时刻 = **2026-09-22 15:46–15:49**；实现文件 = `MainMenuPanel.cs`（15:46 前） / `DeckEditPanel.cs`（15:46 前）
  ⇒ 证据全部**晚于**实现（`tools/verify.ps1` → `evidence-freshness` / `freeze-before-capture` 双 PASS）。
- 同机位并排图（左 = A 原版基线图，右 = 我们 1080×1920 实机）：
  - `.ai-tmp/screenshots/AO1-mainmenu-compare.png`（基线 `策划/参考图/12_主菜单_750x1334.png`）
  - `.ai-tmp/screenshots/AO1-deckedit-compare.png`（基线 `策划/参考图/07_卡组编辑_1242x2208.jpg`）
- 部件级读数（帧名/尺寸/文字色）= `.ai-tmp/test/AO1-step0-mainmenu.txt`（L3：运行时节点树 dump）

---

## 一、主菜单（`MainMenuPanel.cs`）

**一致 = 5**

| # | 项 | 依据 |
|---|---|---|
| 1 | 面板底 = 亮灰蓝实心 + 深色细描边 + 圆角 | 左图 Player Profile 弹窗底 vs 右图 → 运行时节点树 `MenuBox.sprite=Rounded:frame_014`、`MenuBoxBody.sprite=Rounded:frame_019`（实测色 (99,104,123) / (216,230,236)） |
| 2 | 标题 = **白字 + 黑描边** | 左图「Player Profile」vs 右图「主菜单」→ `Title.TXT col=1.00,1.00,1.00` + `Outline(黑,2px)` |
| 3 | 按钮 = **蓝底 + 白字** 圆角 | 左图 `Clan` / 蓝 ribbon vs 右图五颗按钮 → 5 颗全部 `sprite=Rounded:frame_165`、`Label col=1.00,1.00,1.00` |
| 4 | 名字条 = **深色盘 + 白字** | 左图 `ItsRafaXD` 条 vs 右图「克洛弗」条 → `PlayerBar.sprite=Rounded:frame_014`、`Nickname col=1.00,1.00,1.00` |
| 5 | 竖版 1080×1920（高>宽）、内容框宽 ≤ 1000 | 左图 750×1334（高>宽）vs 右图 Screen=1080x1920 |

**不一致 = 7**（每项都带登记号，⛔ 不写"基本一致"）

| # | 差在哪 | 登记 |
|---|---|---|
| 1 | **标题带**：A 没有独立色带（标题直接压在亮面上）；我们有一条**板岩色带** | D-AO1-2（白字压亮面对比度不足，只能垫板岩带） |
| 2 | **按钮蓝的代次**：A 是亮蓝 + 上缘高光；我们是 `ui_out` 165 的平蓝 | D-AO1-3 |
| 3 | **名字条底色**：A 是**深藏青**值块；我们是板岩（同族、不同色） | D-AO1-3 |
| 4 | **右上角红圆 X 关闭**：A 有；我们主菜单**没有**（本界面不是弹窗） | **原版该部件在本界面不适用**（A 的左图是"主菜单之上的 Player Profile 弹窗"，不是主菜单本体） |
| 5 | **底部主菜单栏**：A 是"深藏青方块 + 图标"；我们是竖排按钮列表 | 本项目竖版重排的既有登记（G2），非本片引入 |
| 6 | **字体**：A 是用自有加粗字体 + 粗描边；我们引擎默认字体 + 2px 描边 | 既有缺口（本项目无原版字体） |
| 7 | **圆角半径**：A 更大；我们取自 `ui_out` 014 件自身的圆角（24） | 本片新增，未量取 A 的精确圆角 |

## 二、卡组编辑（`DeckEditPanel.cs`）

**一致 = 4**

| # | 项 | 依据 |
|---|---|---|
| 1 | 卡池 **4 列**、每页 8 张 | 左图 4 列 vs 右图 4 列 → `PoolGrid 998x762`、`poolLabel='卡池：共 60 张 · 第 1/8 页（每页 8 张）'` |
| 2 | **每格都画原版卡面** | 运行时逐格 dump：60 格中 **59 格 `art_sprite=CardArt:frame_NNN`（197×251）**，唯一 `Card41`（`goblin-hut`）无帧（见不一致 #5） |
| 3 | 卡面 = **原版 `ui_spells` 帧**，按 `.sc` export 名对卡 | 逐格帧号与 `card.tsv` 顺序**逐行吻合**（knight=22 / archers=23 / goblins=49 / spear-goblins=53 / giant=55 / pekka=30 / minions=34 / minion-horde=33 …） |
| 4 | 竖版 1080×1920 | 同主菜单 |

**不一致 = 8**

| # | 差在哪 | 登记 |
|---|---|---|
| 1 | **面板底**：A 是**深蓝棋盘纹底**；我们是**米色羊皮纸**（`ui_out` 806） | 未做（本片范围外，`CrUiStyle.ContentPanel` 归别的片） |
| 2 | **标题条**：A 是蓝底白字标签条；我们是**棕金木色条**（`ui_out` 069） | 未做 |
| 3 | **按钮**：A 是蓝/金圆角按钮 + 图标；我们是深蓝灰(上一页/下一页/取消) + 金字"保存" | 未做 |
| 4 | **顶部 `Decks / Collection` 标签页**：A 有；本项目**没有**该部件 | **原版有此部件、本界面没有**（本项目卡组面板无标签页），已如实列出 |
| 5 | **`goblin-hut` 卡面无图元**（唯一空格子） | D-AO1-1（`ui_spells` 95 条 export 无 `goblin_hut` 命名） |
| 6 | **已选槽位**：A 是底部 8 个卡位 + 蓝色数字编号；我们是顶部一行小槽 + 中文名 | 本项目既有布局，未做 |
| 7 | **卡格底**：A 卡格是白框 + 金/蓝内框 + 等级带 + 左下圣水；我们只有白色卡片底（`ui_out` 043）+ 名字带 + 圣水数字 | 未做 |
| 8 | **字体** | 既有缺口（同主菜单 #6） |

---

## 三、计数汇总

| 面板 | 一致 | 不一致 | 其中"原版有此部件/本界面没有（或反之）" |
|---|---|---|---|
| 主菜单 | **5** | **7** | 1（红圆 X 关闭：属 A 的弹窗体，不属主菜单本体） |
| 卡组编辑 | **4** | **8** | 1（`Decks/Collection` 标签页：A 有、本项目无此部件） |
| 房间列表 / 房间内 | **未自审** | **未自审** | —— 本片未改这两个面板（见 `AO1-report.md` §5） |

⛔ 上表里所有"不一致"都**没有被处理成一致**，也没有被写成"基本一致" —— 它们是本片**没做完**的部分，
差异已登记，并由 `AO1-report.md` §5 给出接力接口。
