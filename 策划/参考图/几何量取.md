# 参考图几何量取（A = Clash Royale / 皇室战争）

> **本文件是"原版值"的唯一出处**，服务于 `策划/对照表.md` 的「原版值(出处)」列与各片自审的逐项比对。
> 每题给：**像素值 + 原点与参照边 + 占屏宽/高百分比 + 折算到 1080 宽画布的值 + 出处（哪张图 + 量哪条边）**。
> ⛔ **量不到的项一律写 `未量到`，并给「试过哪几张图 / 为什么量不到 / 消除条件 / 谁能给」**；⛔ 不填估计值。
> 采集时间 2026-09-21（V4 片）。**坐标原点**：图片左上角为 (0,0)，+x 向右、+y 向下（阅读图片的自然方向）。
>
> **折算口径**：基线图 `1242×2208` 与本项目竖版画布 `1080×1920` **同宽高比 0.5625** ⇒ 单一比例
> `k = 1080/1242 = 0.8696`（横向、纵向通用）。`@1080` 列 = 像素值 × 0.8696。
> **读数容差**：像素扫描/网格叠图读数 **±5px**（放大 4× 的裁图上逐格读）；超容差的差一律写成 `不一致`。

## 0. 图清单核对（先证明"拿到的就是清单里那张"）

`python tools/probes/measure-hud.py list` → 17 张全部 `ok`，实际分辨率与 `清单.md` 声明**逐张一致**（无 MISMATCH）。
本片用到的 3 张：

| 图 | 用途 | 尺寸 |
|---|---|---|
| `07_卡组编辑_1242x2208.jpg` | 卡组编辑（G3） | 1242×2208 |
| `02_对局HUD_1242x2208.jpg` | 对局 HUD（G4） | 1242×2208 |
| `09_对局_1242x2208.jpg` | 对局 HUD / 结算（G4） | 1242×2208 |

> 其余 6 张对局图（03/04/05/06/08/10）本片也各扫过底部一次，结论与 02/09 相同（该底部缺失已由 §1.3 用 18/20/21/23 补上）。

---

## 1. 量到的项

### 1.1 卡格阵列（出处：`07_卡组编辑_1242x2208.jpg`）

量法：`g3-measure.py seg2` —— 对每一列 x 统计 `y∈[340,690)` 内 `max(r,g,b)>150` 的像素数，`≥180` 视为"卡面列"。
卡格有亮边框、卡面为高饱和色，卡池底色为深蓝（`max≈120`）⇒ 该判据能把卡列与背景分开。

| # | 项 | 像素值 @1242 | 原点与参照边 | 占屏宽/高 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|---|
| A1 | 卡格宽（第 1 格，选中态含外发光） | **236**（x 82..317） | 左边界 = 图左边缘 | 19.0%W | **205.2** | 07 图，`seg2 x 0..1241 340..690 max 150 180` |
| A2 | 卡格宽（第 4 格，未选中） | **238**（x 960..1197） | 左边界 = 图左边缘 | 19.2%W | **207.0** | 同上（同一带的右端那格） |
| A3 | 卡格**列步进** | **292.7** = (960−82)/3 | 第 1 格左边 → 第 4 格左边 | 23.6%W | **254.5** | 由 A1/A2 的左边界算得 |
| A4 | 第 1 行卡格主体顶边 | **y 386** | 图顶边 | 17.5%H | **335.7** | 07 图，`seg2 y 320..1500 25..1210 max 150 300` |
| A5 | 第 1 行卡格主体高 | **294**（y 386..679） | 图顶边 | 13.3%H | **255.7** | 同上 |
| A6 | 卡格**行步进** | **549**（386→935） | 图顶边 | 24.9%H | **477.4** | 同 A4（第 2 行主体顶 = y 935） |
| A7 | 每页卡格布局 | **4 列 × 2 行 = 8 格** | 图左上 | — | — | 07 图，`grid` 叠网格逐格数（`.ai-tmp/screenshots/07-grid-row1.png`） |

### 1.2 对局 HUD 顶部（出处：`02_对局HUD_1242x2208.jpg`）

量法：`g3-measure.py grid`（叠像素网格）+ `measure-hud.py crop --scale 4`（4× 放大逐格读），
放大图 = `.ai-tmp/screenshots/02-timer-zoom.png`（crop=(1010,0,1242,140)）、`02-topleft-zoom.png`（crop=(0,0,420,140)）。

| # | 项 | 像素值 @1242 | 原点与参照边 | 占屏宽/高 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|---|
| B1 | 倒计时框左边 | **x ≈ 1032** | 图左边缘 | 83.1%W | **897.4** | 02 图，`02-timer-zoom.png` 读框体左描边 |
| B2 | 倒计时框右边 | **x ≈ 1240** | 图右边缘（贴边） | 99.8%W | **1078.3** | 同上，框右描边距右边缘 ≈2px |
| B3 | 倒计时框**宽** | **≈208**（1032→1240） | 框左描边 → 框右描边 | 16.7%W | **180.9** | 由 B1/B2 算得 |
| B4 | 倒计时框上边 | **y ≈ 7** | 图顶边 | 0.3%H | **6.1** | 同上，框上描边 |
| B5 | 倒计时框下边 | **y ≈ 136** | 图顶边 | 6.2%H | **118.3** | 同上，框下描边（读数 ±5px） |
| B6 | 倒计时框**高** | **≈129**（7→136） | 框上描边 → 框下描边 | 5.8%H | **112.2** | 由 B4/B5 算得 |
| B7 | 左上块（王冠盾徽） | x 20..78 / y 25..120 | 图左上 | 4.7%W / 4.3%H | 宽 **50.4** / 高 **41.2** | 02 图，`02-topleft-zoom.png` 读盾徽外框 |
| B8 | 左上块（玩家名行）起点 | x ≈ 78 / y ≈ 15 | 图左上 | 6.3%W / 0.7%H | x **67.8** / y **13.0** | 同上，读「Blue King」字块左边 |

> ⚠️ B7/B8 是"块级"读数（文字/图元边缘），只用于**位置**对齐；原版该块里是
> 「王冠盾徽 + 玩家名 + 部落名 + 奖杯数」四件（02 图可见 `Blue King` / `Clash Royale` / `2000`）。

---

### 1.3 对局 HUD 底部（出处：18_对局HUD_1080x1920 / 21 / 23）

量法：`.ai-tmp/test/Z1-measure.py`（复用 `tools/probes/measure-hud2.py` 的叠网格裁图）。
**18 图 = 1080×1920，与本项目画布同尺寸 ⇒ 本节的 @1080 列 = 像素值（k=1.0）**。

| # | 项 | 像素值 @18 | 原点与参照边 | 占屏宽/高 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|---|
| D1 | 圣水条外框上边 | y=1797 | 图顶边 | 93.6%H | 1797 | 18 图 dump col x=700 |
| D2 | 圣水条外框下边 | y=1841 | 图顶边 | 95.9%H | 1841 | 同上 |
| D3 | 圣水条高 | 44 | 上边→下边 | 2.3%H | 44 | 由 D1/D2 |
| D4 | 圣水条右端 | x=1044 | 图左边缘 | 96.7%W | 1044 | 18 图 dump row y=1819 |
| D5 | 圣水条左端 | 徽章遮挡，可见起 x≈88 | 图左缘 | 8.1%W | 88 | 同上 |
| D6 | 圣水填充(2/10)右端 | x≈292 | 图左缘 | 27.0%W | 292 | dump row y=1820 |
| D7 | 圣水数字徽章 | 心(63,1815) 径60 | 图左上 | 5.6%W | 60 | bb mag |
| D8 | 手牌张数 | 4 张 | — | — | — | 18/20/23 逐图数 |
| D9 | 手牌整排左边 | x=144 | 图左缘 | 13.3%W | 144 | 18 图 gaps |
| D10 | 手牌整排右边 | x=704 | 图左缘 | 65.2%W | 704 | 同上 |
| D11 | 单卡宽 / 卡间距 | 140 / 3 | 卡左→邻卡左 | 13.0%W | 140 / 3 | 由 D9/D10 |
| D12 | 卡顶 / 卡底 | y 1614 / 1785 | 图顶边 | 84.1% / 93.0%H | 1614 / 1785 | Y1 |
| D13 | 整排距画布底 | 1920−1785 = 135 | 卡底→图底边 | 7.0%H | 135 | 由 D12 |
| D14 | 「下一张」卡 | x 33..97 / y 1634..1716 | 图左上 | 6.0%W | 64×83 | c18_nextcard_zoom |
| D15 | 下一张标签 | x 10..115 / y 1718..1755 | 图左上 | 10.6%W | 105 | 同上 |
| D16 | 倒计时框 | x 882..1080 / y 0..100 | 图左上 | 18.3%W | 198×100 | z1_18_top_x2 |
| D17 | 顶中"4"紫徽 | x 490..572 / y 0..69 | 图左上 | 7.7%W | 83×70 | 同上 |

> ⚠️ **18 与 19 是同一张图**（MD5 相同，见 `.ai-tmp/test/Y1-report.md`）⇒ ⛔ 不许当两张图交叉验证。
> 21/23 是别的版本/别的对局（HUD 有聊天气泡、名字条位置不同），只作**顺序与锚定**旁证，
> ⛔ 不用它们的像素值。

---

### 1.4 卡组编辑页**分带与底色**（出处：`07_卡组编辑_1242x2208.jpg`）—— D146 补量，补上原 C7

**为什么这条以前量不到**：§2 的 **C7** 原文 = 「Tab 带 / 卡组槽位行的边界与其下方卡池底**同色系深蓝**，
边界不锐利；`rows` 扫描给的是宽区间（误差 >±10px）」。根因 = 旧量法按**整行均值**取色，
被带内**卡面内容**（高饱和色块）拉走。

**本片改口径**：取**整行中位数**（对局部内容鲁棒，只有"整行底色换了"才会跳）。
新增量法脚本 = `tools/probes/ref07-bands.py`（判据资产，随工程提交）。

```bash
# 复跑（读数全部由这几条产出；⛔ 用 C:/Python312/python.exe，它才有 PIL/numpy）
python tools/probes/ref07-bands.py bands --minsep 8 --thr 4      # 逐段中位色
python tools/probes/ref07-bands.py rows  --minsep 18 --thr 18    # 强横向边界
python tools/probes/ref07-bands.py cols  --y0 39 --y1 157 --minsep 30 --thr 40   # Tab 带内
python tools/probes/ref07-bands.py cols  --y0 157 --y1 332 --minsep 25 --thr 30  # 编号行内
```

| # | 项 | 像素值 @1242 | 原点与参照边 | 占屏宽/高 | 折算 @1080 | 出处（d = 中位色差，越大边界越硬） |
|---|---|---|---|---|---|---|
| E1 | 顶部深蓝底 | y 0..39 | 图顶边 | 2.0%H | 0..33.9 | `bands` 段 1 |
| E2 | **Tab 带**上边 | **y=39** | 图顶边 | 1.8%H | **33.9** | `rows` d=15.0 |
| E3 | Tab 带内**选中态**亮蓝区 | y 79..104 | 图顶边 | 3.6%H | 68.7..90.4 | `rows` d=99.0 / 82.0（选中 Tab 的亮块） |
| E4 | **Tab 带**下边 = 编号行上边 | **y=157** | 图顶边 | 7.1%H | **136.5** | `rows` **d=236.0**（全图最强边界之一） |
| E5 | Tab1（Decks）亮块左边界 | **x=117** | 图左边缘 | 9.4%W | **101.7** | `cols(y39..157)` d=242.0 |
| E6 | Tab1 亮块右边界 | **x=598** | 图左边缘 | 48.1%W | **520.0** | `cols(y39..157)` d=250.0 |
| E7 | **编号行**下边 = 卡池顶上边 | **y=332** | 图顶边 | 15.0%H | **288.7** | `rows` **d=103.0** |
| E8 | 选中的编号按钮（**金色**）左边界 | x=138 | 图左边缘 | 11.1%W | 120.0 | `cols(y157..332)` **d=253.0**（金→黄，最强的色相切换） |
| E9 | 卡格第 1 行顶（**含选中外发光**） | y=348 | 图顶边 | 15.8%H | **302.6** | `rows(x82..318)` d=194.5 |
| E10 | 卡格**主体**顶边 | y=386 | 图顶边 | 17.5%H | 335.7 | §1.1 **A4**（互证：发光比主体顶早 38px） |
| E11 | 卡格内**菱形圣水徽章**上缘 | y=437 | 图顶边 | 19.8%H | **380.0** | `rows` **d=244.0**（深蓝→品紫） |
| E12 | 卡格底装饰带起点 | y=692 | 图顶边 | 31.3%H | 601.7 | `rows` d=69.5 |

**底色（同一批量法的输出，逐条可复跑）**：

| # | 区域 | 中位色 rgb | 出处 |
|---|---|---|---|
| E13 | 顶部（安全区） | **(8, 39, 91)** | `bands` 段 1 |
| E14 | **Tab 带底**（未选中） | **(10..12, 54..55, 98..103)** | `bands` 段 2 / 段 5 |
| E15 | **Tab 选中亮蓝** | **(33, 124, 193)** | `bands` 段 3 |
| E16 | **编号按钮常态蓝** | **(47..60, 93..107, 180..195)** | `bands` y207..279 |
| E17 | **编号按钮（选中）金色** | **(182, 171, 134)** 过渡；中心 **(223,196,188)/(237,211,194)** | `bands` y218..226 |
| E18 | **卡池底深蓝** | **(2, 35, 90)** | `bands` 段 21 |
| E19 | **卡格菱形圣水徽章（品紫）** | **(220,136,240) / (157,75,167)** | `bands` y437 / y475 |
| E20 | 选中卡**外发光**（品紫） | **(187,122,241) / (226,89,253)** | `rows(x82..318)` y393 / y415 |

> ⚠️ **本节的读数容差**：边界值取自中位色差的**局部极大位置**，与 §0 口径一致按 **±5px @1242**；
> 底色取**段中位色**，JPEG 有压缩噪声 ⇒ 逐通道 **±6**。
> ⛔ E5/E6（Tab 亮块左右界）**只量到左右两条**，`Collection` Tab 的左界没有独立读出（它贴在 E6 右侧的缝上）
> ⇒ 按"两 Tab 等宽"**不外推**，登记在 §2 的 C7b。

---

## 1.5 卡组编辑页的**元件树**（出处：`原版资源/sc/ui_v215.sc` 的 `card_page_deck_special`）

**这是"原版版式"的另一条独立通道**：§1.4 量的是**截图像素**，本节解的是 **`.sc` 放置矩阵**。
两条互证 ⇒ 相对位置是硬结论（绝对屏幕坐标仍未解出，见 `策划/原版UI布局坐标.md` §5 F1）。

量法（新增判据资产 `tools/probes/sc-subtree.py`，`sc-layout.py` 的递归版）：

```bash
python tools/probes/sc-subtree.py --clip card_page_deck_special --depth 3
python tools/probes/sc-subtree.py --clip card_page_collection --depth 3
```

| 元件名（原版） | 类型 | x | y | 图元帧 | 本项目的对应物（现状） |
|---|---|---|---|---|---|
| `panel` | clip | 0 | 156.8 | **frame_537**（1×1 渐变块，九宫格拉伸 ⇒ **卡池底**） | ⛔ **无**（现状用亮面体纯色） |
| `battle_cards_area` | ? | -280 | 200.4 | —（`.sc` 内**无内容** ⇒ 运行时挂卡） | 有（但排布不同） |
| `title` | ? | -269.35 | 78 | — | 有（「卡 组 编 辑」） |
| `small_title` | ? | -210 | 48 | — | ⛔ 无 |
| `elixar_total_title` | ? | -115.25 | 595 | — | ⛔ 无（**平均圣水**那一行的标题） |
| `elixir_icon` | clip | 138.75 | 605.1 | **frame_404**（94×115 品红水滴/眼形） | 有（`IconElixirDrop`） |
| `info_button` | clip | 231.5 | 93 | **frame_292**（金黄问号）+ frame_033/034（金圆角件） | ⛔ 无 |
| `random_button` | clip | 246.15 | 604.3 | **frame_538**（渐变块）+ **frame_539**（蓝方块内金「?」） | ⛔ 无 |
| `<shape 1446/1447/1448>` | shape | ∓256.5 / 0 | 602.35 | **frame_372/373/374**（黑竖长条，圆角） | ⛔ 无 |

> ⛔ **本表的 x/y 是"界面 clip 的局部系"，不是屏幕像素**（出处见 `策划/原版UI布局坐标.md` §5 F1）。
> 它的用途 = 判**元件之间的相对次序与疏密**（这才是"版式"），⛔ 不许当绝对位置用。

---

---

## 1.6 卡组编辑页**整屏骨架**（出处：`07_卡组编辑_1242x2208.jpg`）—— D151 补量

**为什么还要补这一节**：§1.4 量的是"分带与底色"（Tab 带 y 区间、编号行 y 区间、卡格 y），
但要把这一页**照原版摆出来**，还缺三样：**Tab 块自己的上缘**、**编号行 7 颗按钮的横纵几何**、
**底行（平均圣水 pill + 工具钮）的位置与配色**。本节把这三样一次量完。

量法：① `tools/probes/ref07-bands.py`（行/列中位色差，§1.4 同一工具）；
② 纵向逐点采样（`x=40 / 300 / 620 / 1100` 四列，取 5×5 中位色）；
③ 带标尺的放大裁图（`.ai-tmp/test/D151-numrow-ruler.png` / `D151-bottom-ruler.png`，每 50px 一条、每 100px 标数）。
读数容差 ±5px@1242（与前几节同口径）。

### 1.6.1 Tab 块与顶部安全区

| # | 项 | 像素值 @1242 | 原点与参照边 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|
| E21 | 顶部安全区（整宽 y 0..157 的"背景"） | **(7, 38, 92)** | 图顶边 | — | `x=40` 与 `x=300` 列 y0..24 中位色一致；`x=60` 列 y0..32 同值 |
| E22 | **Decks Tab 块上缘** | **y=28** | 图顶边 | **24.3** | `x=300` 列：y24=(4,40,90) → y28=(53,196,232)（一条高光亮边） |
| E23 | Decks Tab 块上半（亮） | **(48, 148, 210)** | — | — | `x=300` 列 y32..64 |
| E24 | Decks Tab 块下半（暗） | **(13, 95, 173)** | — | — | `x=300` 列 y124..156 |
| E25 | **Collection Tab 块上缘** | **y=46** | 图顶边 | **40.0** | `x=1100` 列：y44=(3,27,76) 深 → y48=(10,52,92) 起稳定 |
| E26 | Collection Tab 块底色 | **(12, 55, 97)** | — | — | `x=1100` 列 y48..112（= §1.4 **E14** 互证） |
| E27 | Collection Tab 块下半（更暗） | **(8, 42, 88)** | — | — | `x=1100` 列 y112..156 |

> **结论（版式）**：两个 Tab 都是**上缘不齐的"翻页签"** —— Decks（选中）比 Collection（未选中）**高 18px@1242（15.7@1080）**，
> 两块底边同高（= y157 = §1.4 **E4**）。这正是"选中态抬起来"的原版读法。

### 1.6.2 编号行（1..5 + 交换 + 复制，共 7 颗）

| # | 项 | 像素值 @1242 | 原点与参照边 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|
| E28 | 编号行带**上亮边** | **(2..17, 117..188, 255)** | — | — | `x=40` 列 y160=(17,188,255) / y180=(0,117,255)；`x=620` 列 y170=(1,120,255) |
| E29 | 编号行带**带体**（渐变中段） | **(1, 96, 234)** | — | — | `x=40` 列 y240 |
| E30 | 编号行带**下亮边** | **(3, 130, 250)** | — | — | `x=40` 列 y320 |
| E31 | 金按钮「1」纵向 | **y 195..294**（高 **100**） | 图顶边 | **169.6..255.6**（高 **87.0**） | `x80..180` 列 gold 掩码（`r>150 & b<130`，行占比 >25%） |
| E32 | 金按钮「1」横向 | **x 75..184**（宽 **109**） | 图左缘 | **65.2..160.0**（宽 **94.8**） | 同 E31（gold 列掩码，两段 75..136 / 138..184，中间被白色数字「1」断开） |
| E33 | 金按钮填充色 | **(252, 200, 64)** | — | — | 采样 (170,280) 中位色（避开白色数字） |
| E34 | 蓝按钮「2..5」左边 | **236 / 400 / 563 / 727** | 图左缘 | 205.2 / 347.8 / 489.6 / 632.2 | `cols(y180..300)` 的 **dark→blue 跳变**（d=116/134/130/97） |
| E35 | 蓝按钮「2..5」右边 | **346 / 510 / 673 / 837** | 图左缘 | 300.9 / 443.5 / 585.2 / 727.8 | 同上（blue→dark 跳变） |
| E36 | **按钮步进** | **163.5**（236→400→563→727，差 164/163/164） | 相邻按钮左边 | **142.2** | 由 E34 算得 |
| E37 | **按钮宽** | **110**（= 346−236 = 510−400 = …） | 按钮左边 → 右边 | **95.7** | 由 E34/E35 算得；与金按钮 109（E32）**一致** |
| E38 | 第 1 颗按钮左边 | **73.5**（= 236 − 163.5） | 图左缘 | **63.9** | 由 E34/E36 **反推**，与 E32 实测的 75 **互证**（差 1.5px ≪ 容差 5px） |
| E39 | 蓝按钮常态色 | **(56, 107, 195)** | — | — | 采样 (260,205) 中位色 |
| E40 | 交换 / 复制两颗钮 | 与「1..5」**同宽同步进**（第 6/7 位 ≈ 891 / 1054） | 图左缘 | ≈ 774.9 / 916.7 | `D151-numrow-ruler.png` 标尺逐格读（⚠️ 这两颗的边界色差弱，取**同规格外推**，不是独立读数 —— 登记为 §2 的 **C8** 残余） |

> ⚠️ **E40 是外推，不是读数**：`cols(y180..300)` 在 x>850 之后的候选（879 / 948 / 1008 / 1043 / 1081 / 1129 / 1165）
> 既包含按钮边界、也包含按钮**内部图案**（⇄ 箭头 / 卡片图标）⇒ 无法把两者分开（这正是 §2 **C8** 记的口径问题）。
> 本工程按"5 颗数字钮已实测的**同规格**"摆第 6/7 颗（步进 142.2 / 宽 95.7），**标为外推**。

### 1.6.3 底行（平均圣水 pill + 三颗工具钮）

| # | 项 | 像素值 @1242 | 原点与参照边 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|
| E41 | **底行**纵向 | **y 1351..1469**（高 **118**） | 图顶边 | **1174.8..1277.4**（高 **102.6**） | `x800..880` 列亮掩码（`sum>330`，行占比 >25%） |
| E42 | 平均圣水 pill 横向 | **x 9..310** | 图左缘 | **7.8..269.6** | `D151-bottom-ruler.png` 标尺逐格读（圆角块左右缘） |
| E43 | pill 底色 | **(26, 66, 126)** | — | — | 采样 (150,1400) / (190,1410) 中位色 |
| E44 | 三颗工具钮横向 | **773..899 / 934..1058 / 1090..1205**（步进 ≈161，宽 ≈110） | 图左缘 | **672.2..781.7 / 812.2..920.0 / 947.8..1047.8** | 同上标尺图逐格读 |
| E45 | 工具钮底色 | **(58, 129, 188)** | — | — | 采样 (830,1450) / (1120,1450) 中位色 |

### 1.6.4 缺口的**残余**（如实登记，⛔ 不填估计值）

| # | 项 | 为什么仍量不到 | 本工程怎么处理 |
|---|---|---|---|
| E46 | 交换 / 复制钮的**独立**边界 | 见 E40：同色系 + 钮内图案色差更大（= §2 **C8**） | 按数字钮**同规格外推**，已标"外推" |
| E47 | Tab 块的**圆角半径 / 描边** | Tab 是渐变块 + 亮描边，中位色差在圆角处被邻域平均掉 | 本工程用**直角色块 + 顶部高光条**近似（差异登记 D151） |
| E48 | ~~`Collection` Tab 的**左边界**~~ | **★ D151 第二片已消除 ⇒ 见 §1.6.5 E50/E51/E52** | 已改为实测值；`TabGap` 不再是推定值 |

### 1.6.5 `Collection` 页签的左右缘（★ D151 第二片补量；消除 §2 **C7b**）

**为什么以前量不到**：未选中态页签的填充 `#0C3761` 与它下面的顶区面板（`#0C325E` 一带）
**只差约 9/通道** ⇒ ① 中位色差通道被淹没；② 颜色掩膜（试过 ±22）会把面板一起圈进来，
bbox 直接涨到 x520..1079 **不可用**。**能读出来的唯一途径 = 在原分辨率上逐像素看跳变**。

量法（可复跑，见 §3）：

```bash
# 在原分辨率（1242 宽）上逐像素打印 y=45/55/100 三行的颜色跳变，读 597→654 那条缝
python - <<'PY'
import numpy as np; from PIL import Image
a=np.asarray(Image.open(r'策划/参考图/07_卡组编辑_1242x2208.jpg').convert('RGB')).astype(np.float32)
for y in (45,55,100):
    prev=None
    for x in range(560,720):
        s='#%02X%02X%02X'%tuple(int(v) for v in a[y,x])
        if s!=prev: print(y,x,s); prev=s
PY
```

| # | 项 | 像素值 @1242 | 原点与参照边 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|
| E49 | Decks 页签**右缘** | **x=597**（x=598 起 `#1F7EAA`→`#003865` 掉暗） | 图左缘 | **519.1** | 原分辨率 y=45/55/100 三行**一致**；与 §1.6.1 的 **E6**（598）互证 |
| E50 | **Collection 页签左缘** | **x=654**（x=654 起 `#062856`→`#0C3761`；x=655..710 稳定为块体） | 图左缘 | **568.7** | 原分辨率 y=45/55/100 三行一致（三行都在 x=654 起跳）⇒ **硬读数**，不再是反解 |
| E51 | **Collection 页签右缘** | **x=1107** | 图左缘 | **962.7** | ① 缩到 1080 后 y=55 细扫：x=962 `#0E3761` → x=964 `#072A57` 掉背景；② 减背景放大图（×4）上可**目视**右上圆角：起弯 ≈1092、直边止于 ≈1107 |
| E52 | **两页签之间的缝** | **57**（= 654 − 597） | Decks 右缘 → Collection 左缘 | **49.6** | 由 E49/E50 算得 |
| E53 | Collection 页签**宽** | **453**（= 1107 − 654） | 左缘 → 右缘 | **394.0** | 由 E50/E51 算得 |

> **结论（版式）**：**两个页签宽度不同** —— Decks **418.3**（E6−E5）／ Collection **394.0**（E53）。
> ⛔ 旧值「两者同宽 418.3 + 缝 40」是**由对称性反解**出来的（原 §2 **C7b**），实测后缝应为 **49.6**
> （差 9.6@1080 = 11@1242，**超出** ±5px@1242 容差）⇒ C7b **消除**。
> 宽度差的原因也读得出来：原版 "Decks" 用的是**更大的字号**（基线图上可直接看出），页签宽 = 文字宽 + 内边距。

### 1.6.6 Decks 页**底部宣传区**（★ D151 第二片补量）

**为什么要量它**：用户 2026-09-24「**UI呢？？？？？ 你的UI都不是原版UI啊**」——
底行（E41）**之下**的整片区域，原版是一张**宣传插图**（两个人像 + 卡背 + 两行大标题
「百張卡牌 / 組建牌組」），而本工程原先在那里放的是**卡池** ⇒ 这才是最大的一条视觉差。

| # | 项 | 像素值 @1242 | 原点与参照边 | 折算 @1080 | 出处 |
|---|---|---|---|---|---|
| E54 | **宣传区上缘** | **y=1501** | 图顶边 | **1305** | 缩到 1080 后 y=1305 处行中位色差 d=41.0、下行中位色 `#066099`（底行之下第一条硬分带） |
| E55 | 宣传区底色（上段 / 中段） | **(1, 79, 140)** / **(1, 66, 108)** | — | — | 中位色：y1310-1360 = `#014F8C`、y1420-1500 = `#01426C` ⇒ 取中值 `#01477D` |
| E56 | 宣传语**第 1 行**中心 | **y ≈ 1817** | 图顶边 | **1580** | 缩到 1080 后：`x=300..780` 内 V>230 的亮像素行范围 **y1500..1665@1080** ⇒ 中心 1580@1080（= 1817@1242） |
| E57 | 宣传语**第 2 行**中心 | **y ≈ 2041** | 图顶边 | **1775** | 同口径：亮像素行范围 **y1700..1850@1080** ⇒ 中心 1775@1080（= 2041@1242） |
| E58 | 宣传区**底边** | **y=2208**（一直铺到图最下沿） | 图顶边 | **1920** | y=1918@1080 仍是底色 `#023666`（无安全区留白） |

> ⚠️ **插图本体未复刻**：`.sc` 里卡组页对应元件是 `<? 2769>`（kind=`?`、**无名**）
> ⇒ 定位不到具体帧，**没有图元可落地**。本工程只落地 E54–E58 这五项（底板块 + 两行标题的位置），
> 差异登记在 `策划/差异登记.tsv` **D152**。

### 1.6.8 页签**圆角 / 顶线 / 染色**与底行钮板外观（★ D155 第三片补量）

> **为什么要补这一节**：用户 2026-09-24 第 10 条「你的 UI 都不是原版 UI 啊」。D151 的前两片只量了
> **位置与尺寸**（E1–E58），页签与钮板的**外观**（圆角 / 顶线 / 填充色）一直是"量取底色画个直角色块"。
> 本节把外观也量出来，并把「哪一件是原版图元、哪一件定位不到」逐条钉死。
>
> **口径**：全部在**原分辨率 1242×2208** 上读；进代码时 × 0.8696 换到 1080。容差 = 本节末的 ±5px@1242。

| 编号 | 量什么 | 原版读数（@1242） | 参照 | 换算值（@1080） | 怎么读的（可复跑） |
|---|---|---|---|---|---|
| E65 | **页签底染色的分母** = 源帧 `ui_out` 166 的**内填像素** | `(76, 172, 255)` | 源帧 (c−1, c−1) = (18, 18)，c = `BlueCorner` = 19 | — | `d155-colorshift.py` 打印的"166 内填像素"行；⛔ **不是全帧不透明像素均值** `#449FE1`（那个含四角高光与描边，当分母会偏暗） |
| E66 | **选中页签顶部亮线**：行数 + 色 | **y27..y30 共 4 行**；色 `#34C4E7` / `#1FC2E3` / `#21B9E0` ⇒ 三行逐通道中位 **(33,194,227)** | 图顶边 | 高 **3.5**；色 `#21C2E3` | 原分辨率 x=358..362 逐行读（避字）。⚠️ 旧读数 `(48,148,210)` 取的是 y32..64 的**中位**——那一段已经**含渐变**，所以偏暗；⛔ 不是那条高光本身 |
| E67 | **未选中页签顶部暗带**：行数 + 色 | **y27..y38 共 12 行**；色 `#041C4B`(y30) / `#031949`(y36) ⇒ 中位 **(3,26,74)** | 图顶边 | 高 **10.4**；色 `#031A4A` | 原分辨率 x=880 列逐行。判定依据：它比**顶区面板自身**（y24 的 `#06245A`）**更暗** ⇒ 是内阴影，⛔ 不是背景 |
| E68 | **页签圆角半径** | **r ≈ 25** | 页签左上角 | **21.7** | 逐行找最左"亮蓝签体"像素：x140(y28) → 136 → 132 → … → **118(y52)** 收口 ⇒ dx=25、dy=24 自洽 |
| E69 | **底行工具钮钮板**（外观四项） | 圆角 **r ≈ 25**；深边 **4px** `#002075`；顶亮线 `#62B4E2`；填充 `#4E96CC`(渐变 `#4E97CC`→`#3A80BB`) | 钮板左上角 / 顶边 | 圆角 **21.7**、深边 **3.5** | 逐行最左亮蓝像素 x803(y1350) → 776(y1374) ⇒ r≈25；x=860 列 y1346..1349 = `#002275/#032177/#05207B/#002070`（深边 4px）、y1351 = `#62B4E2`（顶亮线）、y1360 之后填充 |
| E70 | **原版 `full_page_button_tab` 的六件实测剖面** | 265 = **1×36 竖向渐变条** `#4EAFFF`(前 17 格平直) → `#1F89FF` → `#0055A9`，末 4 格透明；266 = **39×1** 均匀 `#4EAFFF`；267 = **38×1** `#4EAFFF`（其剖面 == 166 内侧行，Δ=10）；196 = **247×56 纯白**；239 = **19×19 白圆点** | 源帧 bbox | — | `d155-tab-pieces.py`（逐像素）+ `d155-tab-gradient.py`（把 265 拉伸后逐行比参考图） |

> **结论（外观，三条）**：
> 1. **页签底 = 原版图元能复刻到"横向"**：把 `ui_out` 166 的**内侧行**当作横边，与原件 266/267 **逐像素 Δ=5**
>    ⇒ 代码里的"四角镜像九宫格"（`MakeRounded`）在**横向上就是原版拼法**。
> 2. **纵向复刻不到**：原件竖边是**独立件 265**（1×36 渐变条），与 166 内侧列尾段 Δ=58 —— 它就是页签的
>    **竖向渐变**来源。但 **265 不能直接拿来铺**：它的剖面**有"膝"**（前 17 格平直 → 10 格内陡降），
>    与参考图页签的**平滑**剖面不符（逐行 tint 比值 0.51~1.24 摆动）⇒ 本工程**不铺**竖向渐变，
>    页签是单色（= 页签均值 `#217CC1`）。理由与判据 = `d155-tab-gradient.py`（**反向断言**：绿 = 已证伪"能用 265"）。
> 3. **圆角有 12% 的缺口**：源帧 166 自身圆弧 ≈13px ⇒ 镜像拼出来的圆角上限 = `BlueCorner` = **19@1080 = 21.9@1242**，
>    而参考图实测 **25@1242**（E68）⇒ **偏小 3.1px**。⛔ 不写"一致"，登记在 D155。
>
> **口径互证（一条独立证据）**：参考图与图集之间存在**同向色偏** —— 三处已知同族部位（选中页签 / 未选中页签 /
> 底行钮板）的 G、B 通道比值**一律 ≤ 0.90**（`d155-colorshift.py`，反控 = 166 对自己的比值 1.000）
> ⇒ 「原版图元 + 实测 tint」这条口径（本仓既有约定 `CrUiStyle.ButtonBlueTint` 同口径）成立。
> ⚠️ 因此**本节的色值以参考图为唯一真源**，⛔ 不用图集原色顶替。

**复跑命令**（全离线，⛔ 不需要 Unity）：

```bash
C:/Python312/python.exe tools/probes/d155-tab-pieces.py       # E70：六件剖面
C:/Python312/python.exe tools/probes/d155-tab-gradient.py     # 265 能不能用来铺渐变（反向断言）
C:/Python312/python.exe tools/probes/d155-colorshift.py       # E65 + 色偏同向性
C:/Python312/python.exe tools/probes/d155-tabband-cmp.py      # 页签带并排图（目视）
```

---

## 2. 未量到的项（如实登记；⛔ 不填估计值）

| # | 未量到的项 | 试过哪几张图 | 为什么量不到 | 消除条件 | 谁能给 |
|---|---|---|---|---|---|
| C4 | 对局 HUD **冠数**（本方/对方冠数位置与图元） | `02/09` + 本片 `18/19/20/21/23` 五图顶部各扫一遍 | 02/09 的顶部只有「左上玩家块 + 顶部对手王塔血量 4824 + 右上倒计时」；**冠数没到镜**；18/20/21/23 亦未见冠数控件 | 取得含冠数的截图 | 用户 |
| C4b | **阶段（加时）标识** | 同 C4 五图 + 02/09 | 未见 | 同 C4 | 用户 |
| C4c | **暂停按钮** | `18/20/21/23` | 四图未见暂停/齿轮按钮 | 同 C4 | 用户 |
| C5 | **结算界面**整屏（胜/负/平字样、皇冠、按钮） | `02..10`（8 张对局图）+ Fandom 文件名搜索（victory/defeat/result） | A 侧**没有取到**结算整屏（`清单.md` §2「结算(胜负/三冠) = 未取到」）；8 张对局图都停在对局中 | 取得原版结算页竖版截图 | 用户 / 社区图源 |
| C6 | 卡格第 2、3 列（深色卡面）的左右边界 | `07` | `07` 图第 2 格（蓝龙）、第 3 格（箭雨）**卡面本身是深蓝**，与深蓝卡池底色的 `max(r,g,b)` 差 < 30 ⇒ 阈值扫描无法给出确定边界（试过 thr=150/170/200/235 四档 + `colblobs`/`seg2` 两种模式） | 换用"卡格亮边框"通道（需原版卡框帧做模板匹配），或人工在 4× 放大图上逐格标 | 本片可再补（人工标）；或用户给矢量/切图 |
| ~~C7~~ | ~~`07` 顶部 Tab 带与卡组槽位行的**精确**上下边界~~ | — | **已消除（D146 补量）**：根因是旧口径按**整行均值**取色、被带内卡面内容拉走；改用**整行中位数**后三条硬边界全部读出（E2 d=15.0 / E4 **d=236.0** / E7 **d=103.0**），读数见 §1.4 | ✅ 已消除 | — |
| ~~C7b~~ | ~~`Collection` Tab 的**左边界**~~ | `07` | **已消除（D151 第二片，见 §1.6.5 E49–E53）**：根因是未选中态填充 `#0C3761` 与顶区面板 `#0C325E` **只差约 9/通道** ⇒ 中位色差通道淹掉、颜色掩膜也分不开。改用**原分辨率逐像素看跳变**后读出：左缘 **x=654@1242**（E50）、右缘 **x=1107@1242**（E51）⇒ 缝 **49.6@1080**（不是原推定的 40）、Collection 宽 **394.0@1080**（≠ Decks 的 418.3） | ✅ 已消除 | — |
| C8 | `07` 编号行 7 个元件（`1..5` + 交换 + 复制图标）的**逐个**左右边界 | `07` | `cols(y157..332)` 给了 21 条候选，但按钮是**同色系蓝**，中缝色差小、按钮**内部图案**（数字/图标）产生的色差更大 ⇒ 无法把"按钮边界"与"按钮内图案"分开 | 用按钮的**亮描边**做边检测，或按 §1.5 的 `random_button`/`info_button` 图元做模板匹配 | 本片可再补 |

> C1–C3 已由本片 18/20/21/23 四张未覆盖截图解决（见 §1.3）；原文"同一根因"另起一行看。本片用 `02-bottom.png` / `09-bottom.png`
> 两张放大图把它从"文字结论"升级成"可复核的图证"。

---

## 3. 量法命令清单（可一键复跑；判据资产随工程提交）

```powershell
# 4) 底部 HUD（18/20/21/23，Z1 补；§1.3 的读数全部由它复跑）
python .ai-tmp/test/Z1-measure.py dump 18_对局HUD_1080x1920.jpg col 700 1786 1852 2
python .ai-tmp/test/Z1-measure.py gaps 18_对局HUD_1080x1920.jpg 0 1640 1080 1800 0.6
# 0) 图清单与分辨率核对
python tools/probes/measure-hud.py list
# 1) 卡格阵列（07）
python tools/probes/g3-measure.py seg2 策划/参考图/07_卡组编辑_1242x2208.jpg x 0 1241 340 690 max 150 180
python tools/probes/g3-measure.py seg2 策划/参考图/07_卡组编辑_1242x2208.jpg y 320 1500 25 1210 max 150 300
python tools/probes/g3-measure.py grid 策划/参考图/07_卡组编辑_1242x2208.jpg .ai-tmp/screenshots/07-grid-row1.png 0 320 1242 700 25
# 2) HUD 顶部放大读数（02）
python tools/probes/g3-measure.py grid 策划/参考图/02_对局HUD_1242x2208.jpg .ai-tmp/screenshots/02-grid.png 0 0 1242 320 50
python tools/probes/measure-hud.py crop --img 02_对局HUD_1242x2208.jpg --box 1010,0,1242,140 --scale 4 --step 20 --out 02-timer-zoom.png
python tools/probes/measure-hud.py crop --img 02_对局HUD_1242x2208.jpg --box 0,0,420,140 --scale 3 --step 20 --out 02-topleft-zoom.png
# 3) 底部"宣传字覆盖"图证（C1/C2）
python tools/probes/g3-measure.py grid 策划/参考图/02_对局HUD_1242x2208.jpg .ai-tmp/screenshots/02-bottom.png 0 1450 1242 2208 100
python tools/probes/g3-measure.py grid 策划/参考图/09_对局_1242x2208.jpg .ai-tmp/screenshots/09-bottom.png 0 1450 1242 2208 100
```

**新增的量法脚本**：`tools/probes/v4-measure.py`（按"带内亮像素计数"分段，补 `g3-measure.py` 的
`colblobs/rowblobs` 单阈值在深蓝底上的误分）—— 属**判据资产**，随工程提交。
