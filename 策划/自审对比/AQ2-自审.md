# AQ2 自审（对局 HUD / 暂停 / 结算 3 面板）

> **本版（AU1 串行重采，2026-09-22 17:22–17:27 同一条 Play 链）**：AQ2 上一版"我方实机"列全是 **未采到**
> （当次 Play 链卡在主菜单）。AU1 用一条干净 Play 链把三个界面**真采到**并出了同机位并排图，因此本表把每一行的
> "我方实机"列**逐项改判**为 `一致` 或 `不一致（差在哪）`。⛔ 没有一行写"基本一致 / 大致像"。
>
> - 实机图（均为 1080×1920；驱动 `.ai-tmp/drivers/AU1-drive.cs`，读数 `.ai-tmp/test/AU1-evidence.txt` + `.ai-tmp/test/AU1-run.txt`）：
>   `AQ2-hud-mine.png`（17:23:27）/ `AQ2-pause-mine.png`（17:23:34）/ `AQ2-result-mine.png`（17:26:21）；
>   并排图 `AQ2-{hud,pause,result}-compare.png`（左=原版 A 缩放；右=实机）。
> - 判据不只靠眼睛：每行尽量给**实机像素读数/节点文本**（探针 `.ai-tmp/test/AU1-pix.py`，原始输出 `.ai-tmp/test/AU1-pix.txt`）。
> - ⛔ 本片**未改**任何 `.cs`（纯取证）；下面列出的 `不一致` 全部**只登记、不改**，各自归属写在「归属」列。
> - 详细换帧表见 B 段；本片取证过程与缺口见 `.ai-tmp/test/AU1-report.md`。

## 暂停面板（基线：`策划/参考图/24_设置_499x1080.jpg` 的原版功能面板语言）

| # | 判据项 | 原版（出处） | 我方（换帧后） | 代码层判定 | 实机并排（AU1 重采判） |
|---|---|---|---|---|---|
| 1 | 面板底 | 板岩外框 + 亮灰蓝面（`24_设置` 实测 (99,104,123)/(229,236,242)） | 014 外框 + 019 亮面（`SettingsPopup`） | 素材一致 | `一致`（实机框 (104,116,132) / 面 (220,232,240)；Δ≤12/255，在原图 JPEG 量级内） |
| 2 | 标题 | 板岩带 + 白字黑描边（`24_设置`） | `BandTitle`（同） | 素材一致 | `一致`（实机 y530-577 为整幅板岩带 (104,116,132)，白字压其上） |
| 3 | 标题条**不该**是棕木条 | 原版没有木色标题带 | 旧 069（棕木条）已移除 | 已消除旧差异 | `一致`（实机标题带为板岩色，无 069 木色） |
| 4 | 面板底**不该**是米色纸 | 原版没有米色纸面板 | 旧 806（米黄撕纸）已移除 | 已消除旧差异 | `一致`（实机面为 (220,232,240) 冷灰蓝，无 806 米黄） |
| 5 | 4 颗按钮底 | 蓝底白字（`24_设置` 蓝钮实测 (48,112,224)） | **`ui_out/166`**（`BlueButton` = `ResPaths.ButtonBlueCornerAlt`，出处 `client/Assets/Scripts/UI/CrUiStyle.cs:373`；⚠️ 本节原文写 `ui_out/165` = AF3 换帧**前**的旧帧） | 素材一致 | **`一致`**（AV1 标定 + **AD1 直采** 2026-09-22 18:44）：实机 4 钮填充 **mode=(44,108,224)**、边唇行 (48,111,224) ⇒ Δ vs (48,112,224) **≤ 4/255**（改前为 (48,156,255)，ΔG=+44）。读数出处 `.ai-tmp/screenshots/AD1-pause-btn-after.png` + 。⚠️ 该读数**绑定当时（AF3 前）的 `CrUiStyle.BlueCorner=10`**（九宫格中心像素取自帧 (c−1,c−1)）⇒ AE1（2026-09-22）据此把它定回 **10**（取 14 时实机变 (72,122,224)，见 ）。⚠️ **该前提已过时（AG1 片 2026-09-22 订正）**：AF3/AF4 已把源帧 165→**166**、`BlueCorner` 10→**19**（`client/Assets/Scripts/UI/CrUiStyle.cs:955`）、`ButtonBlueTint` 按新中心像素重标定（`CrUiStyle.cs:914-915`）；AF4 最终态（21:23:03）实机蓝面 = **(45,108,224)**（Δ(−3,−4,0)） |
| 6 | 按钮**不该**是亮黄金 3D 块 | 原版功能面板不用金色商店钮（300） | 旧 300 已移除 | 已消除旧差异 | `一致`（实机 4 钮均为蓝 (48,156,255)，无 300 金块） |
| 7 | 提示框底四角 | 原版圆角块四角对称 | 014 **四角镜像拼**（旧写法三角是错的） | 口径已修正 | **`不一致`**：本轮只有整屏图（提示框 y≈638，(108,112,116) 边框），**四角对称未逐角裁切验证 ⇒ 未达"一致"判定**（归属：取证口径，非代码缺陷） |
| 8 | 亮面上的说明字可读 | 原版亮面用暗字 | `TextOnLightDim`(110,118,132)（旧 `TextDim` 亮字 ⇒ 亮面上看不见） | 已按 AP1 实测口径改 | `一致`（实机说明字为暗字 (110,118,132) 压亮面 (220,232,240)，对比可读） |
| 9 | 遮罩 | 原版进设置类界面压暗下层 | **未做**：Popup 层的模态遮罩由 `UIManager` 统一提供（本面板不铺全屏底，`UI.cs:143-147`） | 本项目既有机制 | `一致`（实机面板外整幅遮罩，实测 (6,9,17)） |
| 10 | 「对局不真暂停」提示块 | 原版无此块 | 原版 014 板岩块 + 本项目文案 | 原版**无此部件**（本项目新增语义） | —（原版无此部件，不可比；实机存在，文案「注意：本作的对局不会真正暂停…」） |

## 结算面板（基线：**原版未取到**，`策划/参考图/清单.md:40`；并排图左侧用 `24_设置` 的同类部件语言）

| # | 判据项 | 原版（出处） | 我方（换帧后） | 代码层判定 | 实机并排（AU1 重采判） |
|---|---|---|---|---|---|
| 1 | 面板底 | 原版功能面板 = 014 + 019（借 `24_设置` 语言） | 014 + 019 | 对齐同类部件 | `一致`（实机框 (100,108,124) / 面 (220,232,240)，与暂停面板同值） |
| 2 | 标题 | 板岩带 + 白字黑描边 | `BandTitle` | 对齐同类部件 | `一致`（实机 y299-352 整幅板岩带 (100,108,124) vs 原版 (99,104,123)） |
| 3 | 结局大字可读 | 原版文字白字黑描边 | `Outlined`（无描边白字压亮面＝看不见） | 已按原版文字语言改 | `一致`（实机「失败」白字带黑描边，压 (220,232,240) 亮面可读） |
| 4 | 冠数行字色 | 原版亮面用暗字 | `TextOnLight` | 同上 | `一致`（实机「我方冠 0：/ 对方冠 2：」为暗字压亮面） |
| 5-6 | 2 颗按钮 | 原版功能面板蓝钮（**结算界面无直接对照物**） | **166**（AF3 换帧后；本节原文写 165） | 对齐同类部件（登记 D-AQ2-2） | **`一致`**（AE1 2026-09-22 **直采**）：`.ai-tmp/screenshots/AE1-result-mine.png`（1080×1920 @19:53:29）已采到结算面板两颗钮（读取窗口 = 运行时 dump 的屏幕矩形：PlayAgainButton `x=80..1000 / screen y=645..755`、MainMenuButton `x=80..1000 / screen y=534..629` ⇒ PNG `x=80..1000 / rows=1164..1274` 与 `rows=1290..1385`）。⚠️ **该图采于 `BlueCorner=14` 那一态，实测 mode=median=(72,122,224)**（Δ vs 原版 =(**+24,+10,0**) 超容差）⇒ AE1 据此判定 **14 是错的、把 `BlueCorner` 回退到 10** 并重编译；当时最终态（c=10，**AF3 换帧前**的口径）的同 helper 读数 = 实机 **(44,108,224)**（AD1 暂停面板直采，Δ≤4/255）+ 离线九宫格复刻预测 **(44,109,224)**（`AE1-tile.txt`，与实机差 1/255）⇒ **Δ ≤ 4/255 = `一致`**。⚠️ 当时最终态(c=10，**AF3 换帧前**的口径)的**结算**面板未再直采（一次自然结算 ≈ 14 分钟游戏时长），依据 = 已验证模型 + 同 helper 实机读数，详见 `AE1-量取.md` §4 |
| 7 | 两条数据条 | `ui_battle_end_out/108` 深色圆角空框 | 未动 | 判定不改 | **`不一致`**：实机两条为**亮面带 + 暗字**（「结算原因：加时结束，比冠数」/「我方塔血 46% 对方塔血 100% 人机对局…」），未见原版 108 深色圆角空框 |
| 8 | 结局徽记 | `ui_battle_end_out/195` 金杯+橄榄枝（`AQ2-end-frames.png` 肉眼核对） | 未动 | 判定不改 | `一致`（实机标题下方可见金杯+橄榄枝徽记） |
| 9 | 冠数图标 | `ui_out/50` 金色五尖皇冠 | 未动 | 判定不改 | **`不一致`**：实机冠数行两侧**未见**金色五尖皇冠；可见的是 5 个蓝底图元（金币/宝石/奖杯/卡牌/另一图标），与原版 50 不对应 |
| 10 | **胜负字牌** | 原版结算有（本片**未识别到对应帧**） | **无** | ⛔ 缺帧，未做（D-AQ2-3） | **`不一致`（实机确认）**：整屏无胜负字牌/缎带，只有「失败」大字 |
| 11 | **奖励条** | `win_reward_bar` 在原版 `.sc` 是**空 clip**（Y2 登记 D3） | **无** | ⛔ 无依据，未做 | **`不一致`（实机确认）**：整屏无奖励条 |
| 12 | **双方头像框** | 原版结算有（未识别到帧） | **无** | ⛔ 缺帧，未做（D-AQ2-3） | **`不一致`（实机确认）**：整屏无双方头像框 |
| 13 | 面板贴合 | 原版弹窗居中、占屏宽 86.6%@499 | 居中、占屏宽 92.6% | 差 +6.0pp（D-AQ2-1） | **`不一致`**（实机量得 92.6% vs 86.6%，+6.0pp，与 D-AQ2-1 一致） |

## 对局 HUD（基线：`策划/参考图/18_对局HUD_1080x1920.jpg`，与本项目 1080 画布同尺寸）

| # | 判据项 | 原版（出处） | 我方（换帧后） | 代码层判定 | 实机并排（AU1 重采判） |
|---|---|---|---|---|---|
| 1 | 圣水徽章（条左侧大图标） | `ui_out/159`（原版 `elixirBarLeft` 94×115，索引 §3.1） | **AV2 改**：159 + 徽章内**圣水整数**（白字黑描边），条上文字停用 | 素材一致（几何未动） | **`一致`（AV2 重采）**：实机徽章中心 (0,0,0)=数字**黑描边**、周边品红；原版 18 图同点 (255,251,255)=**白字**、原版条上**无文字**（`AV2-base-badge.png`：水滴内是一位整数「2」）。读数见 `AV2-pix-new.txt` |
| 2 | 圣水条填充色 | 原版 18 图底部为**玫红/品红**填充 | `ui_out/157`（品红细线，原版 `bar_body`） | 素材一致 | `一致`（实机条体 (210,34,216)/(237,120,241) 品红；原版 18 图同段同为品红） |
| 3 | 圣水条轨道 | 原版 `bar_bg` = `ui_out/155`（1×74） | 未动（155） | 素材一致 | **`不一致`（AV2 低圣水档复采后定论）**：**圣水 4 档**实测 x700=(145,176,65)、x1000=(153,180,69) = **竞技场原色**；原版 18 同点 (33,55,96)/(48,70,111) **深蓝** ⇒ 155 的半透明底（alpha 94）在实机**没画出来**（同帧同行只在最右 x≈1031..1036 有 37% 黑：x1035=(91,113,42)）。**根因已排除帧数据/压缩**（`tex=RGBA32`、`LoadAll<Sprite>=1`）⇒ 指向 `Image.type=Sliced`+1px sprite 口径（属 `CrUiStyle`，⛔ 本片无权改）；**归**：D-AV2-1 / 下一片（Simple A/B 一次即可定论） |
| 4 | 圣水条端头 | 原版 `bar_end` = `ui_out/158`（10×59） | **AV2 改**：轨道右端 = `156`（原版同族深色圆头件）；填充右端 = `158`（品红圆头，跟着 fill 走） | 见 `AV2-量取.md` B 段 #4 | **`一致`（AV2 重采）**：实机 x1040=(34,34,34) 深色端件（原版 18 同点 (37,37,39)）；节点 `ElixirTrackEnd sprite=frame_156_0` / `ElixirFillEnd sprite=frame_158_0` 实测在树 |
| 5 | 手牌卡槽底 | 原版 `slots` = `ui_out/200`（96×137） | **AV2 改**：切边**量到** ⇒ `Sliced` + border (10,8,10,8)（左,下,右,上）；卡面比例 0.86/0.70 → **1.0/1.0** | 形变已除；卡框仍缺（D-AV2-2） | **`不一致`（形变已除，卡框仍缺）**：实机卡面 (32,32,48) 是 `ui_spells` 卡面像素，原版 18 是**银灰卡框** (210,208,211) ⇒ 卡框在 `ui_spells` 裁剪区之外，裁剪常量与 `ArtFrames` **与 `DeckEditPanel` 同源**（⛔ 本片无权改那个文件）⇒ **归**：D-AV2-2 / 下一片统一裁 |
| 6 | 冠数徽记 | 原版 `starPlayer`/`starEnemy` = `ui_out/187`/`188` | 未动（本片**改不掉**） | 素材一致 | **`不一致`（改不掉，归世界层）**：原版 18 图**该位置只有竞技场**（`AV2-base-topleft.png`，无任何 HUD 控件）——原版冠数画在**竞技场内塔上方（世界空间）**，HUD 层无此控件；且该位置几何**未量到**（几何量取 §2 C4）⇒ 属布局/世界层，⛔ 本片不许改 HUD 几何 ⇒ **归**：D-AV2-3 / 世界表现片 + 位置量取片 |
| 7 | 计时条底板 | 原版 `HUD_topRight` 底板 = `ui_out/193` | **AV2 改**：按原版结构 = `193` 外框 + **`177`(1×1) 实心底**（新落地）；两行字改**白字黑描边** | 见 `AV2-素材补充.md` | **`一致`（AV2 重采）**：实机板内 x1000=(0,0,0) **实心近黑** vs 原版 18 同点 (11,3,0)；计时字 (255,255,255) 白 vs 原版 (203,201,204) 白（原版即白字黑描边，`AV2-base-topright.png`） |
| 8 | 暂停按钮 | 原版战斗内**无独立命名元件**（索引 §1 第 2 条）⇒ 用最接近的 `163` + `171` | 未动 | 推断件（既有登记保留） | **`一致`（等比不形变）**：`163` 219×219 → 48×48 = **两轴同比 0.219×** ⇒ 本来就没有"整幅非等比拉伸"（AQ2 该条口径**订正**，见 D-AV2-5）；推断件性质（原版该件属回放 HUD）保留既有登记 |
| 9 | 冠数/计时**位置** | 「未量到」（`策划/参考图/几何量取.md` §2 C4） | 沿用现状（本片未动） | 不可比 | —（原版该位置未量到，**不可比**） |
| 10 | 手牌 4 张 / 下一张 | 原版 4 张 + 下一张 | 一致（张数、位置未动） | 结构一致 | `一致`（实机 4 张手牌 + 左侧「下一张」+ 下一张图标；节点 `HandText0..3`/`NextText` 文本齐全） |

## 计数（⛔ 不含"基本一致/大致像"）

> **AV2 片重采后（2026-09-22 17:56–17:58 同一条 Play 链）**：HUD 列**逐行改判**（另两面板未动，行数不变）。
>
> **AE1 片（2026-09-22）重算计数（汇总与表体自洽）**：暂停 #5（AD1 直采）与结算 #5-6（AE1 直采）两行已改判 `一致`
> ⇒ 汇总随表体更新（上面两张表体里那两行的 `实机并排` 列已同步改写，不再留旧判）。
> ⚠️ 计数口径：结算 #5-6 在表体里是**一行**（与 AV2 的写法 `结算 7（#5-6,#7,…）` 同口径）⇒ 本次是 12−2=10，
> **不是** 12−3=9（AD1 报告里写的「不一致 9 / 一致 21」把 #5-6 记成了两行，算术上多减了一行）。
> **`一致`：20 行** —— 暂停 8（#1,#2,#3,#4,#5,#6,#8,#9）、结算 6（#1,#2,#3,#4,#5-6,#8）、**HUD 6（#1,#2,#4,#7,#8,#10）**。
> **`不一致`：10 行** —— 暂停 1（#7）、结算 6（#7,#9,#10,#11,#12,#13）、**HUD 3（#3,#5,#6）**。
> **`不可比`：1 行**（HUD #9，原版位置未量到）。
> **`原版无此部件`：1 行**（暂停 #10）。
> 合计 33 行 = 20 + 10 + 1 + 1 + 1（暂停 #10 同时计入"原版无此部件"）。
> 全表扫查（AE1 2026-09-22）：表体 32 行 + 归属表 8 行逐条对过，除本节计数外**没有**别的「计数与内容不符」；
> 归属表 #1 行（暂停 #5 / 结算 #5-6）的 `一致` 结论成立的前提是**当时（AF3 换帧前）的** `CrUiStyle.BlueCorner=10`（AE1 已定回 10）；⚠️ **该前提已过时（AG1 片 2026-09-22 订正）**：AF3/AF4 已把源帧 165→**166**、`BlueCorner` 10→**19**（`client/Assets/Scripts/UI/CrUiStyle.cs:955`）、`ButtonBlueTint` 按新中心像素重标定（同上 `:914-915`）⇒ 本行 `一致` 的**目标值**不变（仍是原版读数 (48,112,224)），但**绑定关系**以 `CrUiStyle` 现值为准。
> HUD 行改判证据：`.ai-tmp/screenshots/AV2-hud-mine.png`（17:58:44，圣水 7）/ `AV2-elixir-low.png`（17:56:55，**圣水 4**）+
> 并排图 `AV2-hud-compare.png` / `AV2-elixir-low-compare.png`；逐点读数 `.ai-tmp/test/AV2-pix-new.txt`；
> 逐条改动/切边/根因 ；素材落地断言 `AV2-素材补充.md`。

### `不一致` 的归属（⛔ 本片只登记，不改任何 `.cs`）

| 不一致行 | 归属 | 依据 |
|---|---|---|
| 暂停 #5 / 结算 #5-6（按钮蓝偏亮 (48,156,255) vs 原版 (48,112,224)） | **已消除 → 改判 `一致`（AD1 片，2026-09-22 18:43–18:44 同一条 Play 链补采）**。AV1 片把 `CrUiStyle.BlueButton` 的常态 face tint 标定到原版读数后，实机填充读数：**暂停面板常态钮 mode=(44,108,224)**（边唇行 (48,111,224)），Δ vs (48,112,224) = **(−4,−4,0)** / (0,−1,0)，改前为 **ΔG=+44**；残差 ≤4/255 归**素材帧拉伸带自身色 ≈(44,150,255)**（tint 是按帧单像素 (9,9)=(48,156,255) 标定的），落在既有色彩容差口径内（`策划/差异登记.tsv` D106「每通道 ≤13」）。运行时 `Image.color` dump = `FFB7E0` = tint(1.000,0.718,0.878)×255，证明 tint 真的生效。**结算 #5-6 未直采**（本轮链只到暂停），按同一 `CrUiStyle.BlueButton`（face tint 在 `CrUiStyle.cs:913-914` 硬编码，调用方只能改标签色）一并改判 `一致` | 实机像素读数：`.ai-tmp/screenshots/AD1-btn-after.png`（主菜单 18:43:55）+ `AD1-pause-btn-after.png`（暂停 18:44:11）；逐点量法 `.ai-tmp/test/AD1-measure.py` → `AD1-量取-raw.txt`；坐标与窗口 |
| 结算 #7/#9/#10/#11/#12（两条数据条未用 108 / 冠数图标无 50 / 胜负字牌·奖励条·头像框缺失） | **AQ2 范围**（`ResultPanel.cs`；#10-12 已在 D-AQ2-3 登记为"缺帧"） | 实机整屏 + 原版帧出处 |
| 结算 #13（面板宽 92.6% vs 86.6%） | D-AQ2-1（已登记为允许差异/待改） | 实机量取 |
| HUD #1/#4/#7/#8（徽章数字·条端·计时板实心底+白字黑描边·暂停钮等比） | **AV2 片：已改判 `一致`**（1 改帧 + 1 补帧 + 描边 + 数值搬家） | `AV2-量取.md` B 段 #1/#4/#7/#8 + `AV2-pix-new.txt` |
| HUD #3（轨道底半透明不渲染） | **D-AV2-1**（根因指向 `CrUiStyle.NineSlice` 的 `Image.type=Sliced` + 1px sprite，⛔ 不在本片范围） | `AV2-允许差异.md` D-AV2-1 |
| HUD #5（手牌缺银灰卡框） | **D-AV2-2**（卡框在 `ui_spells` 裁剪区之外，裁剪常量与 `DeckEditPanel` 同源，本片无权改） | `AV2-允许差异.md` D-AV2-2 |
| HUD #6（冠数胶囊在 HUD 层，原版在世界层塔上） | **D-AV2-3**（布局/世界层，本片⛔不许改 HUD 几何） | `AV2-允许差异.md` D-AV2-3 |
| 暂停 #7（提示框四角对称未逐角验证） | **本片取证口径**（非代码缺陷） | 需更细裁切，本轮未做 |
