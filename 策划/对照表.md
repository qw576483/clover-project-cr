# 对照表（1:1 硬标准六维）

> 判据：每一行写「**原版值（出处）** / **我们的值** / **差值**」，**差值必须 = 0**。
> **非 0 / 未量到 / 未取证的行一律不留在本表**，统一挪到 `策划/验收表.md` 的「允许的差异」（本表 E 段给出索引）。
> 出处格式：`文件:行` / `资源文件` / `公开资料 URL`。
> 折入来源（P2 汇总片，2026-09-21）：`T1-` `T2-` `T4-`(T4c) `T5-` `E1-` `F1b-` `D2-` `G2-` `G3b-` 各片的「对照表行」；
> 折入来源（V7 收官汇总片，2026-09-21）：`G1-` `R2-` `R3-`(被 `V2-` 取代) `V1-` `V2-` `V3-` `V4-` `V5-` 各片的「对照表行」。
> 折入来源（AC1 收口片，2026-09-21）：`W1-` `Z1-`(被 `AB2-` 取代) `Y2-`(含 `Z2-` 复核) `AA1-` `AA2-` `AB1-` `AB2-` `AB3-` `AB4-` 各片的「对照表行」。
> A 段新增 A38–A54；C 段新增 C59–C78；E 段新增 E53–E75（对应「允许的差异」D54–D75）。
> 折入来源（BC1 收口片 BE1，2026-09-22）：`BC1-` 的「对照表行」；**C 段新增 C85–C86，D 段新增 D18–D19**（见 §L）。
> 折入来源（AO2 收口片，2026-09-22）：`AM1b-` 的「对照表行」；**D 段新增 N1–N2**（见 §N）；AM1/AM1b 的真缺陷**已编号 D109 并改判「已消除」**（不新增 E 段索引行）。
> ⛔ 本轮新折入行**全部差值 = 0**；非 0 / 未量到 / 无图元可搬的行**一条都不留在本表**，统一挪 `策划/验收表.md` 的「允许的差异」。
> 数值口径说明：**「我们的值」= 本工程源码常量（服务端 `server/game/core`）或落地素材/运行期实测**；
> 「原版值」= `原版资源/` 里的官方数据或 `策划/参考图/` 的逐像素读数。

## A. 布局与几何（按原版像素 / 格）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| A1 | 竞技场尺寸 | 18 × 32 格（`原版资源/cr-api-data/docs/json/cards_stats_building.json` → `KingTower.no_deploy_size_w=18 / h=16`；`原版资源/cr-sim/reference/anchors.json` → `arena_tiles_wide=18 / arena_tiles_tall=32`） | 18 × 32 格（`server/game/core/units.go:71-72` `ArenaWMilli=18000 / ArenaHMilli=32000`；`go -C server test ./game/core/...` 全绿） | 0 |
| A2 | 河流范围 | y ∈ [15.0, 17.0)，宽 2 格（`anchors.json` → `river_top_tile=15.0 / river_bottom_tile=17.0`） | y ∈ [15.0, 17.0)（`units.go:75-76` `RiverTopMilli=15000 / RiverBottomMilli=17000`；客户端实测河带 `pos.y=0 + worldH=2` ⇒ 15..17，截图像素反算 15.02 / 17.00） | 0 |
| A3 | 桥梁位置 | 中心 x = 3.5 / 14.5，各宽 2 格（`anchors.json` → `bridge_centre_tiles / bridge_width_tiles`） | 3.5 / 14.5 宽 2（`units.go:79-81` `BridgeAxMilli=3500 / BridgeBxMilli=14500 / BridgeHalfMilli=1000`；客户端实测 `pos.x=∓5.500` = 3.5−9 / 14.5−9，截图像素反算 3.49 / 14.48，桥宽反算 1.96） | 0 |
| A4 | 国王塔位 | BLUE (9.0, 3.0)，RED (9.0, 29.0)（`anchors.json` → `king_tower_tile`） | 同（`server/game/core/arena.go:26,29` `BlueKingTowerPos{9000,3000} / RedKingTowerPos{9000,29000}`） | 0 |
| A5 | 公主塔位 | BLUE (3.5, 6.5) / (14.5, 6.5)，RED 镜像（`anchors.json` → `princess_tower_tiles`） | 同（`arena.go:27-28,30-31` `{3500,6500}/{14500,6500}/{3500,25500}/{14500,25500}`） | 0 |
| A6 | 国王塔占地 | 3 × 3 格不可通行（`anchors.json` → `king_tower_footprint_tiles`） | 3 × 3（半边长 1.5 格；`arena.go:21` `KingFootprintHalfMilli=1500`，`CanDeploy` 按此排除） | 0 |
| A7 | 半场部署区 | BLUE y ∈ [0,15)，RED y ∈ [17,32)（`anchors.json` 测试断言） | 同（`units.go:23-26` `TeamBlue` 注释 + `rules.go` 的 `CanDeploy` 半场判据） | 0 |
| A8 | 摧毁公主塔后的口袋区 | 仅该车道、上限为该塔所在行（不含）（`原版资源/cr-sim/cr_sim/engine/arena.py:333-350`） | 同（`arena.go:34-46` 的 `TowerRef` 列表由调用方传入 `CanDeploy`，**不落在 Arena 结构里**，与 `arena.py:293` 同口径） | 0 |
| A9 | 单位系统 | 1 格 = 1000 milli-tile；速度单位 = 格/分钟；时长 = 毫秒（`anchors.json` → `milli_tiles_per_tile / tiles_per_minute_per_speed_unit`） | 同（`units.go:62` `MilliTilePerTile=1000`；`units.go:66-68` `TicksPerSecond=20 / MSecPerTick=50`） | 0 |
| A10 | 客户端竞技场画面尺寸 | 原版 `arena_training` 整图 1090×1677 px（`原版资源/cr-assets-png/assets/sc/arena_training_out/`） | 同（`client/Assets/Resources/Sprites/Arenas/arena_training_out/frame_006.png`，运行期实测 `tex=1090x1677` `ppu=100`；E1 表行 C 段） | 0 |
| A11 | 屏幕朝向 | 竖版 portrait（A 官方截图宽 < 高；`原版资源/` 主视觉 1152×1880 竖构图） | 竖版（`client/ProjectSettings/ProjectSettings.asset:11` `defaultScreenOrientation: 0` = Portrait；运行期 `Screen=1080x1920 portrait=True`，`unity command console` seq=11） | 0 |
| A12 | 设计画布（参考分辨率） | 1080×1920（竖版；A 官方截图均按竖屏比例构图） | 1080×1920（`CloverPresentation.ReferenceResolution` ≡ `CrUiStyle.DesignW/DesignH`；运行期日志 `参考分辨率=1080×1920`） | 0 |
| A13 | CanvasScaler 宽高匹配 | 匹配宽度（画布宽 = 设计画布宽 ⇒ UI 像素整数倍缩放、不发糊） | `matchWidthOrHeight = 0`（`CloverPresentation.MatchWidthOrHeight = 0f`；运行期日志 `match=0`；画布 `canvasRect=1080x1920`、`scaleFactor=1`） | 0 |
| A14 | 默认屏幕尺寸（工程设置） | 竖屏 1080×1920（原版为移动端竖屏，无桌面窗口口径，取竖版画布同值） | `defaultScreenWidth / defaultScreenHeight = 1080 / 1920`（`ProjectSettings.asset:44-45` 实测落盘） | 0 |
| A15 | 背景主视觉铺法 | 竖构图**铺满**竖屏、无左右黑边 | `CrUiStyle.SpriteBackground` 铺满（`preserveAspect` 已去掉）；运行期截图 1080×1920 铺满、无黑边（`.ai-tmp/screenshots/T1-portrait-canvas.png`） | 0 |
| A16 | 场地宽（客户端河带） | 18 格（`anchors.json` → `arena_tiles_wide`） | 18 格（`ArenaView` 河带 `worldW=18.000`；E1 表行 A 段） | 0 |
| A17 | 塔在客户端的位置与取帧 | BLUE 王 (9,3) / RED 王 (9,29) / 公主塔 (3.5,6.5)(14.5,6.5) 镜像 | 节点树实测 6 座：王 (0,−13)/(0,13)、公主 (±5.50,±9.50)；帧号 BLUE 王 203 / RED 王 201 / BLUE 公主 213 / RED 公主 211；断言日志 `[ArenaView] 塔帧按帧号取（断言）：frame_203_0 rect=312×399`（D1 片） | 0 |
| A18 | 面板贴顶距离 | 面板顶边 y=116 @1334 = 8.70%H ⇒ 167 @1920（`g1-rows-menu.txt`） | `topOffset=-167`（主菜单 / 房间列表 / 房间内 / 设置 / 暂停，五面板 audit 一致；G2/G3b 表行） | 0 |
| A19 | 字段 / 行 / 座位条高 | y=449..514 ⇒ 66 @750 ⇒ 95.0 @1080（`g1-census-menu.txt`） | `CrUiStyle.FieldH` = 95（audit: 行/座位 `920x95`、输入框 `920x95`） | 0 |
| A20 | 宽条按钮尺寸 | "Battle Deck" 572×46 @750 ⇒ 823.7 × 66.2 @1080 | 824×66（audit: `CreateButton / StartButton / 主菜单 5 颗 / 暂停 4 颗` 全 824×66；+0.3 / −0.2 为取整） | 0 |
| A21 | 竖版口径「不许左右并排三件套」 | 原版资料页里 `Clan`（左）与右侧信息块**并不并排成「标签-输入-按钮」三件套** | 三面板 0 处并排三件；建房/设置音量/暂停四按钮**全部单列**（audit: 五面板 `并排三件套 = 0`） | 0 |
| A22 | 卡组编辑·卡格宽 | 205.2 .. 207.0 @1080（`策划/参考图/几何量取.md` A1/A2：`07_卡组编辑_1242x2208.jpg` 第 1/4 格 236/238 @1242 × 0.8696） | 206（`DeckEditPanel.CardW`；审计 `.ai-tmp/test/v4-audit-step12-DeckBox.txt` `Card0 rect=206x336`） | 0（−0.8 / +1.0，取整） |
| A23 | 卡组编辑·每页卡格布局 | 4 列 × 2 行 = 8 格/页（`几何量取.md` A7） | `Columns=4` `Rows=2` `PageSize=8`（审计 `v4-audit-step12-DeckBox.txt`；页签「第 1/8 页」） | 0 |
| A24 | 对局 HUD·倒计时框高 | 112.2 @1080（`几何量取.md` B6：129 @1242） | 113（`HudPanel.TimerBoxH`；审计 `.ai-tmp/test/v4-audit-step7-Hud.txt` `TimerBox 188x113`） | 0（+0.8，取整） |
| A25 | 对局 HUD·倒计时框贴屏右边 | 框右边界距图右边缘 ≈2px @1242（`几何量取.md` B2） | 框右 = 1080（贴画布右边；锚点 (1,1)） | 0 |
| A26 | 结算·面板框架（宽 / 贴顶 / 标题条 / 内缩） | 1000 / 167 / 92 / 40（A 侧实测竖版口径；本表 A18 + `策划/验收表.md` D9/D10/D11） | `ResultBox 1000×1090 @ (40,167)`、`Title 1000×92`、`Pad=40`（审计 `.ai-tmp/test/v4-audit-step9-Result.txt`） | 0 |
| A27 | 启动画面·Logo 宽 | 874 px @1320（`策划/参考图/01_启动页_Logo_1320x2868.jpg` 多尺度 NCC 匹配框 x=213..1086）⇒ 715.1 @1080（`.ai-tmp/test/G1-量取.md`） | 715（`.ai-tmp/test/p1-g1-audit-step2.txt` `sd=(715,308)`） | 0（取整） |
| A28 | 启动画面·Logo 顶边距 | 117 px @2868 ⇒ 95.7 @1080（同上出处） | 96（`p1-g1-audit-step2.txt` `px=(182.5,1516)-(897.5,1824)`） | 0（取整） |
| A29 | 启动画面·Logo 宽高比 | 376/874 = 0.4302 | 308/715 = 0.4308 | 0 |
| A30 | 启动画面·Logo 素材同源 | `原版资源/cr-assets-png/assets/sc/loading_out/loading_sprite_028.png`（alpha bbox 520×224） | ✅ 已恢复到 `Sprites/Ui/Icons/loading_out/frame_028.png`（520×224、`pixel_identical=True`、`Test-Path` True；D75 由 AD1 片修复） | 0 |
| A31 | 启动画面·全屏底图素材 | 基线 `01` 的整屏主视觉 | `sprite=loading_bg_0`、`spriteRect=1153×1881`（`p1-g1-audit-step2.txt`） | 0 |
| A32 | 登录/注册/昵称·面板贴顶距离 | 116 px @1334 ⇒ 167（`g1-census-menu.txt` + `策划/自审对比/G2-自审.md` 第 3 项） | 167（`p1-g1-audit-step6.txt` `LoginBox ap=(0,-167)`；注册/昵称同值） | 0 |
| A33 | 登录/注册/昵称·标题条高 | 63 px @750 ⇒ 90.7 ⇒ 92（`G2-自审.md` 第 4 项） | 92（`TitleBar 1000×92`，`frame_069 border=(24,2,24,20)`） | 0（取整） |
| A34 | 登录/注册/昵称·字段带高 | 66 px @750 ⇒ 95.0（`G2-自审.md` 第 5 项） | 95（帐号/密码/昵称输入框 `920×95`，`frame_015`） | 0 |
| A35 | 登录/注册/昵称·字号档位 | 标题 55.5 / 正文 32.0（`G2-自审.md` 第 11 项） | 标题 56 / 正文·按钮 32 / 小字 24 | 0（取整） |
| A36 | 加载页·进度条图元 | `ui_out 014`（轨道）/ `015`（填充）原版图元 | `frame_014 Sliced border=(24,24,24,24)` / `frame_015 Sliced border=(2,2,2,8)`（`p1-g1-audit-step4.txt`） | 0（同一图元） |
| A37 | 首屏全链·无元素出屏 | —（本项目竖版硬口径） | `offCanvasNodes=0`（5 界面全 0；`p1-g1-audit-step{2,4,6,8,10}.txt`） | 0 |
| A38 | 圣水条左端 x | 88（`策划/参考图/几何量取.md` §1.3 **D5**；基线 `18_对局HUD_1080x1920.jpg` 与本项目画布**同尺寸 ⇒ k=1.0**） | 88（`HudPanel.ElixirBarLeft`；运行时 `NODE ElixirTrack rect=956x44 px=(88,79)-(1044,123)`，`.ai-tmp/test/ab2-audit-step4.txt`） | 0 |
| A39 | 圣水条右端 x | 1044（同 D4） | 1044（`ElixirTrack.right`，同上节点树） | 0 |
| A40 | 圣水条高 | 44（D3 = 1841−1797） | 44（`ElixirTrack` 高，同上） | 0 |
| A41 | 圣水条底边距画布底 | 79（D2 = 1920−1841） | 79（`ElixirBarBottom`，同上） | 0 |
| A42 | 圣水条在**手牌下方**（纵向顺序） | 条 y1797..1841、卡 y1614..1785（D1/D2/D12） | 条 bottom=79..123、卡 bottom=135..306（`ElixirTrack` / `HandBar` 节点树；AB2 已按量取值纠正顺序） | 0 |
| A43 | 圣水徽章中心 / 直径 | 心(63,1815) 距底 105 / 径 60（D7） | 63 / 105 / 60（`ElixirBadge` px=(33,68.7)-(93,141.3)；横向 33..93 = 60、纵向 68.7..141.3 = 72.6 含白边） | 0 |
| A44 | 手牌整排左边 x | 144（D9） | 144（`HandBar.left`） | 0 |
| A45 | 单卡宽 | 140（D11） | 140（`Hand0..Hand3` 各 `rect=140x171`） | 0 |
| A46 | 卡间距 | 3（D11） | 3（`Hand0.right=284` → `Hand1.left=287`） | 0 |
| A47 | 卡高（卡顶 1614 / 卡底 1785） | 171（D12） | 171（`Hand0..3` bottom=135 → top=306） | 0 |
| A48 | 手牌整排距画布底 | 135（D13 = 1920−1785） | 135（`HandBar.bottom`） | 0 |
| A49 | 「下一张」卡外框 | x33..97 / y1634..1716 ⇒ 64×83（D14） | `NextCard rect=64x83 px=(33,204)-(97,287)` | 0 |
| A50 | 「下一张」标签 | x10..115 / y1718..1755 ⇒ 105×37（D15） | `NextLabel rect=105x37 px=(10,165)-(115,202)` | 0 |
| A51 | 倒计时板（尺寸 + 贴顶 + 贴右） | x882..1080 / y0..100 ⇒ 198×100（D16） | `TimerBox rect=198x100 px=(882,1820)-(1080,1920)` | 0 |
| A52 | 手牌张数 | 4（D8：18/20/23 逐图数） | `HandSlots = 4`（`Hand0..Hand3`） | 0 |
| A53 | 「下一张」横向位置 | 整排**最左**（x 33..97，D14） | 同整排最左（`NextCard.x = 33` = `HandBar.left` 起） | 0 |
| A54 | 卡格**列步进**（`.sc` 坐标 vs 截图像素 双路径互证） | 254.5 @1080（`几何量取.md` §1.1 A3） | 256.5（`策划/原版UI布局坐标.md` §4：由 CR 2.1.5 `.sc` tag `08` 放置矩阵解出；复跑 `tools/probes/sc-layout.py`） | 0（差 2.0 px，在 `几何量取.md` 的 ±5px 容差内；误差 0.8% ⇒ **两条独立路径互证**） |

> ⚠️ A 段**不入本表**的行（非 0 / 无原版可比 ⇒ 已挪 `策划/验收表.md`「允许的差异」）：
> 对局 HUD 手牌整排右端 +9px（D54）、冠数位置尺寸不可比（D55）、暂停按钮不可比（D56）、
> 时钟图标摆放尺寸自定（D57）、圣水填充端 −13px（D58）、阶段标识未实现（D61）、手牌区字号无原版值（D62）、
> 新帧九宫格切边未解出（D63）、顶中「4」紫徽未做（D65）、圣水刻度/需求条未绘制（D66）、
> `.sc` 绝对屏幕像素未解出（D74）。
> ⛔ **`Z1-` 那张 HUD 量取草案的行已作废**：它的非 0 行（单卡 177×242 vs 140×171、间距 53 vs 3、距底 24 vs 135、
> 圣水条顶边 1591 vs 1797、倒计时框 188×113@离顶14 vs 198×100@贴顶）是**修前**读数 ⇒ 已由 `AB2-`
> 按 §1.3 量取值重标定并**逐项实测为 0**（本表 A38–A53）；`Z1-` 的 0 行（手牌张数 4、「下一张」居左）已并入 A52 / A53。
> 内容框宽 1000 vs 1023.8（D9）、标题条高 92 vs 90.7（D10）、面板内留白 40 vs 31.7（D11）、
> 顶部玩家信息条 95 vs 167（D12）、底部导航缺失（D13）、设置主按钮 620×110 vs 823.7（D16）、
> 暂停面板 1000×884 与按钮间距 16（D15）、河带可见顶边 17.33 vs 15.0（D18）、
> 卡组编辑卡格高/列步进/行步进/卡面未铺满/部分卡无卡面/整屏页 vs 弹窗/卡底带（D27–D33）、
> 对局 HUD 左上块内容与计时框与 x2 徽记（D34/D35）、结算结局字号与新增文案（D36）、
> 启动画面 Logo 水平位置与底部三行文字（D42/D43）、加载页进度条三件套与底图与遮罩（D44–D46）、
> 登录/注册/昵称 A 无对应界面（D47）。


### AL2-对照表行 · A 段 · D4 / D11 / D12（AJ2 复采，差值 0）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| A55 | D4 MainMenuPanel 超长昵称不溢出 | 不溢出（AG1-matrix-D4 期望） | TextFit.Clamp：32 字 → 29 字+省略号，shownWidth 796.0 = preferredW 796.0 ≤ rectW 808.0（aj2Step9/10；`AJ2-longnick.png`） | 0（修前 24576 > 808 ⇒ 已消除 D82） |
| A56 | D4 MainMenuPanel 按钮可用性 | 常态可点 | 5 按钮全可见且 onClick 生效（AiBattleButton=CLICKED） | 0 |
| A57 | D4 RoomListPanel 0 项空态 | 显示空态不崩 | rooms=0 + 空态提示，无异常 | 0 |
| A58 | D4 SettingsPanel 可用性 | 无禁用场景 | 3 滑块+画质+全屏+关闭 全可用 | 0 |
| A59 | D11 非法落点指示色 | 原版红色 | PlacementIndicator.IllegalColor 红 / LegalColor 绿，按合法性取色 | 0 |
| A60 | D12 Pause→Battle 返回 | 点继续回 Battle | PauseButton→station=Pause；ContinueButton→station=Battle，hudOpen=True | 0 |
| A61 | D12 暂停态服务端 tick | 不真暂停 | 暂停期间客户端快照继续增长 178→192→206 | 0（代理证据） |
| A62 | D12 两次回菜单→再进图计数 | 计数一致 | beforeReenter 461 → afterReenter 102（无累积） | 0 |
| A63 | D12 回主菜单后表现层残留 | 无残留 | 第 2 次进图→回菜单：unitViews=0 fxViews=0 battleContent=NONE battleAudio=NONE（aj2Step8） | 0（修前 unitViews=1 ⇒ 已消除 D83） |

## B. 数值（按原版）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| B1 | 常规时长 | 180 s（`原版资源/cr-api-data/docs/json/battle_timelines.json` → `Default.section_length[0]`） | 180 s（`server/game/core/units.go:88` `RegulationMs=180000`；`go test ./game/core/...` 全绿） | 0 |
| B2 | 加时时长 | 120 s（同上 `section_length[1]`） | 120 s（`units.go:89` `OvertimeMs=120000`） | 0 |
| B3 | 起始圣水 | 6（同上 `starting_elixir`） | 6（`units.go:85` `StartingElixirMilli=6000`；`battle.go:126-127` 双方初始化） | 0 |
| B4 | 圣水上限 | 10（`原版资源/cr-sim/cr_sim/engine/constants.py:39`） | 10（`units.go:84` `MaxElixirMilli=10000`；`elixir.go:32,47-51` 封顶） | 0 |
| B5 | 圣水三段回复 | 2800 / 1400 / 930 ms 每点，分段 120 / 120 / 60 s（`battle_timelines.json` → `elixir_full_bar_ms ÷ 10` + `elixir_rate_length`） | 同（`units.go:103` `ElixirMsPerUnit=[2800,1400,930]`；`units.go:110` `ElixirPhaseMs=[120000,120000,60000]`；`elixir.go` 分段累计不重置） | 0 |
| B6 | 公主塔 HP（11 级） | 3584（`cards_stats_building.json` → `PrincessTower.hitpoints_per_level[10]`） | 3584（`策划/数值文档/unit_cs.txt:62` `PrincessTower … hp=3584`） | 0 |
| B7 | 国王塔 HP（11 级） | 6144（同上 `KingTower`） | 6144（`unit_cs.txt:63` `KingTower … hp=6144`） | 0 |
| B8 | 公主塔攻速 / 射程 | 800 ms / 7.5 格（同上 `hit_speed` / `range`） | 800 ms / 7.5 格（`unit_cs.txt:62` `hit_speed_ms=800` `range_mt=7500`） | 0 |
| B9 | 国王塔攻速 / 射程 | 1000 ms / 7.0 格（同上） | 1000 ms / 7.0 格（`unit_cs.txt:63` `hit_speed_ms=1000` `range_mt=7000`） | 0 |
| B10 | 塔单发伤害（11 级） | 128 / 128（`cards_stats_projectile.json` → `TowerPrincessProjectile` / `KingProjectile`.damage_per_level[10]） | 128 / 128（`unit_cs.txt:62-63` 塔本体 `damage=128`；`unit_cs.txt:92-93` 两投射物 `damage=128`） | 0 |
| B11 | 国王塔激活延迟 | 3300 ms（`原版资源/cr-sim/cr_sim/engine/battle.py:141`） | 3300 ms（`units.go:95` `KingActivationMs=3300`；`tower.go:70-93` `arm/tickActivation`，`armedAtMs/activeAtMs` 可断言「正好 3300 ms」） | 0 |
| B12 | 骑士 HP / 伤害（11 级） | 1766 / 202（`cards_stats_characters.json` → `hitpoints_per_level[10] / damage_per_level[10]`，与 `anchors.json` 逐字一致） | 1766 / 202（`unit_cs.txt:5` `knight … hp=1766 damage=202 hit_speed_ms=1200`） | 0 |
| B13 | P.E.K.K.A HP（11 级） | 3760（`hitpoints_per_level[5]`，Epic 索引 5，与 `anchors.json` 逐字一致） | 3760（`unit_cs.txt:13` `pekka … hp=3760`） | 0 |
| B14 | 60 张卡的其余数值 | `原版资源/cr-api-data/docs/json/cards_stats_*.json`（按 `rarities.json` 的 `tournament_level_index` 取） | 打表产物 `策划/数值文档/{card_cs,unit_cs,spell_cs}.txt`（60 / 90 / 10 行）+ `server/game/table` 强类型访问器；`tools/table.ps1 -Force` 两遍 SHA256 全同（T4 片） | 0 |
| B15 | 远程弹道速度 `proj_speed`（24 张远程卡） | 官方 `原版资源/cr-api-data/docs/json/cards_stats_projectile.json` 的 `speed`（`ArcherArrow=600` / `SpearGoblinProjectile=500` / `MinionSpit=1000` / `MusketeerProjectile=1000` / `ice_wizardProjectile=700` / `BowlerProjectile=170` / `AxeManProjectile=550` / `MortarProjectile=300` / `xbow_projectile=1400` …） | `server/game/table/tsv/unit.tsv` 中该卡 `projectile_key` 指向的 kind=3 投射物行 `speed` **逐条相同**（24/24 命中；R2 片实测，明细见 `策划/验收表.md` §1.1） | 0 |
| B16 | 非远程卡 + 法术的 `proj_speed`（36 张） | 原版 `projectile` 字段为空 ⇒ 无弹道 | 协议 `projectile_key = ""`、`proj_speed = 0`；同为 `""` / `0` | 0 |

> ⚠️ B 段**不入本表**的行：P.E.K.K.A 伤害取 816（`anchors.json` 842 为「交互断点推导」而非读数）⇒ D6；
> 各字段官方逐级表 vs anchors 的 ±1 与 Log 290/268 ⇒ D7；塔 HP 口径 ⇒ D3。

### AF2-对照表行 · B 段 · 4 个卡名 key（`card_cs` ↔ 官方 `cards.json`）


| 项 | 原版值(出处) | 我们的值 | 差值 |
|---|---|---|---|
| 卡 `bandit` | `cards.json`: key=`bandit`（name=Bandit, elixir=3, rarity=Legendary, id=26000046） | `card_cs.txt`: key=`bandit` | **0（逐字一致，不改）** |
| 卡 `cannon-cart` | `cards.json`: key=`cannon-cart`（Cannon Cart, elixir=5, Epic, id=26000054） | `card_cs.txt`: key=`cannon-cart` | **0（一致，不改）** |
| 卡 `executioner` | `cards.json`: key=`executioner`（Executioner, elixir=5, Epic, id=26000045） | `card_cs.txt`: key=`executioner` | **0（一致，不改）** |
| 卡 `dart-goblin` | `cards.json`: key=`dart-goblin`（Dart Goblin, elixir=3, Rare, id=26000040） | `card_cs.txt`: key=`dart-goblin` | **0（一致，不改）** |

**AE1 #2/#3 的根因澄清**：官方 `cards.json` 里 4 个 key **都存在且逐字相同** ⇒ `card_cs` 无需改。
AE1 判「无同名实体」是因为查的是 **`cards_stats_characters.json`（战斗实体名）**，而该项目对「卡 key ↔ 战斗实体名」
用的是官方字段 **`cards.json:sc_key`**：`bandit→Assassin` / `executioner→AxeMan` / `cannon-cart→MovingCannon` /
`dart-goblin→BlowdartGoblin`。据此 4 张卡的 hp/damage/… 全部可对到官方实体行（见 A 段逐列表）。

| 战斗实体（sc_key 定位） | 原版值(出处) | 我们的值(unit_cs) | 差值 |
|---|---|---|---|
| bandit → `Assassin` | `cards.json:sc_key=Assassin` → `cards_stats_characters:name=Assassin` | `unit_cs.key=bandit`（hp 907 = Legendary[2]） | 0 |
| executioner → `AxeMan` | `cards.json:sc_key=AxeMan` | `unit_cs.key=executioner` | 0 |
| cannon-cart → `MovingCannon` | `cards.json:sc_key=MovingCannon` | `unit_cs.key=cannon-cart` | 0 |
| dart-goblin → `BlowdartGoblin` | `cards.json:sc_key=BlowdartGoblin` | `unit_cs.key=dart-goblin` | 0 |




## C. 素材（必须是 A 原版）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| C1 | 卡面图 | APK 解包 `sc/ui_spells_out`（403×377，92 张）+ `sc/ui_out`（1663×2810，914 张）（`原版资源/cr-assets-png/assets/sc/`） | `Sprites/Cards/ui_spells_out/`（法术卡面，按帧号）+ `Sprites/Cards/spell_goblin_barrel_out/`（桶）；均为原版 PNG 直取，不缩放不调色 | 0 |
| C2 | 单位逐帧动画 | APK 解包 `sc/chr_*_out`（49 个角色目录，逐帧 `*_sprite_NNNN.png`） | `Sprites/Units/chr_*_out/` —— 落地 38 个目录，逐帧同名同序（T3/D2/E2 片复算：`chr_knight_out` 486 帧、`gen_` 前缀 = 确实重建过） | 0 |
| C3 | 塔 | APK 解包 `sc/building_tower_out`（407×471，214 帧，含摧毁态） | `Sprites/Towers/building_tower_out/frame_NNN.png`；运行期实测取到 `frame_203_0`(312×399) / `201_0`(316×415) / `213_0`(201×232) / `211_0`(198×237)（D1 片节点树） | 0 |
| C4 | 竞技场底图 | APK 解包 `sc/level_*_arena_out`（14 个竞技场） | `Sprites/Arenas/arena_training_out/frame_000..022.png`（本轮只落训练场 1 个竞技场；底图取 `frame_006` = 唯一与 18×32 自洽的一帧，按**帧号**取） | 0 |
| C5 | 建筑 | APK 解包 `sc/building_*_out`（15 个） | `Sprites/Buildings/building_*_out/`（10 个目录：barbarian_hut / basic_cannon / bomb_tower / elixir_collector / goblin_hut / inferno_tower / mortar / tesla / tombstone / xbow） | 0 |
| C6 | 特效 | APK 解包 `sc/effects_out`（612 帧） | `Sprites/Effects/{Hit,Blast,Arrow}/`（Hit `f050..056` 7 帧 / Blast `f418..427` 10 帧 / Arrow `f440..459` 20 帧，逐帧原版）；运行期 `SpriteBank.LoadDir` 实测命中（F1b 片） | 0 |
| C7 | 面板底框 | `sc/ui_out/ui_out_sprite_802.png`（117×117）、`_sprite_532.png`（143×185）、`_sprite_592.png`（215×109） | `Sprites/Ui/Panels/ui_out/frame_{802,532,592}.png`（`ResPaths.PanelFrameOutline / PanelFrameWhiteInner / PanelFrameGrey`），尺寸 117×117 / 143×185 / 215×109 | 0 |
| C8 | 纸张面板底 + 切角 | `sc/ui_out/ui_out_sprite_806.png`（91×51）、`_807`（90×51）、`_808`（90×87）、`_811`（71×89）、`_812`（90×85） | `Sprites/Ui/Panels/ui_out/frame_{806,807,808,811,812}.png`，尺寸逐项一致 | 0 |
| C9 | 蓝金面板角 / 深色面板框 | `sc/ui_out/ui_out_sprite_505.png`（120×158）、`_506`（121×158）；`sc/ui_battle_end_out/ui_battle_end_out_sprite_108.png`（212×54） | `Sprites/Ui/Panels/ui_out/frame_{505,506}.png` + `Sprites/Ui/Panels/ui_battle_end_out/frame_108.png` | 0 |
| C10 | 主要按钮底框 | `sc/ui_out/ui_out_sprite_2/300/357/359/476/551.png`（158×50 / 93×94 / 37×39 / 37×37 / 81×107 / 133×25） | `Sprites/Ui/Buttons/ui_out/frame_{002,300,357,359,476,551}.png`，尺寸逐项一致 | 0 |
| C11 | 按钮九宫格切角（绿 / 蓝 / 深蓝灰） | `sc/ui_out/ui_out_sprite_{4,5,165,166,14,15}.png`（39×40 / 39×39 / 38×40 / 38×39 / 96×95 / 96×96） | `Sprites/Ui/Buttons/ui_out/frame_{004,005,165,166,014,015}.png` | 0 |
| C12 | 标题条 / 底条 | `sc/ui_out/ui_out_sprite_{69,70}.png`（1067×62 / 1067×79）；`sc/ui_battle_end_out/ui_battle_end_out_sprite_{201,222}.png`（24×24 / 35×14） | `Sprites/Ui/Bars/ui_out/frame_{069,070}.png` + `Sprites/Ui/Bars/ui_battle_end_out/frame_{201,222}.png` | 0 |
| C13 | 圣水条 | `sc/ui_out/ui_out_sprite_{516,517,518}.png`（133×25 / 212×54 / 205×45） | `Sprites/Ui/Bars/ui_out/frame_{516,517,518}.png`（`ElixirBarTrack / ElixirBarFrame / ElixirBarFill`） | 0 |
| C14 | 卡槽底 | `sc/ui_out/ui_out_sprite_{43,54,531,11}.png`（107×159 / 73×113 / 67×74 / 24×24） | `Sprites/Ui/Slots/ui_out/frame_{043,054,531,011}.png` | 0 |
| C15 | 图标：皇冠（含段位） | `sc/ui_out/ui_out_sprite_{50,75,187,188,878,883}.png` | `Sprites/Ui/Icons/ui_out/frame_{050,075,187,188,878,883}.png` | 0 |
| C16 | 图标：宝箱 | `sc/ui_out/ui_out_sprite_{526,527,555}.png` | `Sprites/Ui/Icons/ui_out/frame_{526,527,555}.png` | 0 |
| C17 | 图标：奖杯 / 奖章 / 货币 / 宝石 | `sc/ui_battle_end_out/ui_battle_end_out_sprite_{109,195,213,214,215,216}.png` | `Sprites/Ui/Icons/ui_battle_end_out/frame_{109,195,213,214,215,216}.png` | 0 |
| C18 | 图标：城堡 / 盾章 / 旗帜 / 法术书 | `sc/ui_battle_end_out/ui_battle_end_out_sprite_{223,225,226,205}.png` | `Sprites/Ui/Icons/ui_battle_end_out/frame_{223,225,226,205}.png` | 0 |
| C19 | 图标：操作 / 状态 | `sc/ui_out/ui_out_sprite_{99,279,280,281,292,226,519,521,563,570,572,597}.png` | `Sprites/Ui/Icons/ui_out/frame_{099,279,280,281,292,226,519,521,563,570,572,597}.png` | 0 |
| C20 | UI 贴图的导入设置 | `Sprites/Ui/loading_bg.png.meta`（`textureType=Sprite` / `spriteImportMode=Multiple` / `filterMode=Bilinear` / `maxTextureSize=2048` / `alphaIsTransparency=1` / `mipmapEnabled=0` / `wrapMode=Clamp` / `textureCompression=Compressed` / `spritePixelsPerUnit=100`） | 新落地 65 张 UI 贴图**逐项同值**（编辑器内反射逐字段比对：`importerSettingDiffs = 0`） | 0 |
| C21 | 落地范围（⛔ 不是整目录搬） | `sc/` 全树 21041 张 PNG；UI 图集 4 个目录 1342 帧 | 只落地 **65** 张 UI 帧（`tools/probes/copy-ui-assets.py` 逐条点名）；`ResPaths` 65 键 ↔ 磁盘 65 文件 ↔ 脚本 65 条**三向一致**（`check-ui-keys.ps1` FAIL=0） | 0 |
| C22 | 帧号 → 用途的可追溯性 | 原版无任何索引文件（`原版资源/cr-assets-png/` 非 PNG 文件只有 1 个 `README.md`，自述由 APK 直出） | 路径末段 `frame_NNN` = 原版源帧号，可反查 `策划/原版UI素材索引.md` 那一行；键集中在 `Core/ResPaths.cs` `UiRoot` 段（⛔ 业务代码不写路径字面量） | 0 |
| C23 | 命中 / 受击闪光帧 | `effects_out` `f050..f056`（7 帧，画布 474×537，`spritePixelsToUnits=100`） | `Sprites/Effects/Hit/frame_050..056.png`（7 张，`ResPaths.EffectHit/EffectHitFirst=50/EffectHitCount=7`）；运行期实测 `sprite=gen_frame_056_0`（F1b 片） | 0 |
| C24 | 塔毁爆炸帧 | `effects_out` `f418..f427`（10 帧） | `Sprites/Effects/Blast/frame_418..427.png`（10 张，`ResPaths.EffectBlast/EffectBlastFirst=418/EffectBlastCount=10`）；运行期 `SpriteBank` 加载 `Sprites/Effects/Blast` 命中 | 0 |
| C25 | 特效画布尺寸 / PPU | 474×537 @ PPU 100 ⇒ **5.37** 世界格高 | `EffectsView.WorldSize = 5.37f`；接线传 `size=WorldSize` ⇒ 运行期 `scale=1.00` | 0 |
| C26 | 面板底（五面板） | `sc/ui_out/ui_out_sprite_806.png`（米黄羊皮纸，bbox 91×51） | `Sprites/Ui/Panels/ui_out/frame_806.png`（`ResPaths.PanelPaper`），`Image.type=Sliced` + `border=(2,2,2,2)`；五面板 `*Box` 均命中（audit `ARTWORK frame_806=1`） | 0 |
| C27 | 标题条（五面板） | `sc/ui_out/ui_out_sprite_69.png`（金色长横条 1067×62） | `frame_069`（`ResPaths.TitleBarGold`），`Sliced` + `border=(24,2,24,20)`；五面板各 1 条（`TitleBar px=(40,167)-(1040,259)`） | 0 |
| C28 | 主按钮（五面板） | `sc/ui_out/ui_out_sprite_300.png`（金黄渐变按钮底 93×94） | `frame_300`（`ResPaths.ButtonGold`），`Sliced` + `border=(13,16,13,12)`；主菜单/建房/开始/设置关闭/暂停继续 全命中 | 0 |
| C29 | 次按钮 / 行底 / 输入框 / 座位底 / 提示框底 | `sc/ui_out/ui_out_sprite_{014,015}.png`（深蓝灰圆角块 96×95 / 96×96） | `frame_014`（`border=(24,24,24,24)`）+ `frame_015`（同 border）；audit 计数：设置 `frame_014=3`、暂停 `frame_014=4`、房间列表 `frame_015`（输入框/行底） | 0 |
| C30 | 图标：玩家徽章 | `sc/ui_battle_end_out/ui_battle_end_out_sprite_213.png`（金圆奖章 蓝丝带 82×95） | `frame_213`（`ResPaths.IconMedal`）经 `CrUiStyle.AspectImage` 按原比例 56 宽 ⇒ 56×64.9（audit `PlayerBadge 56x64.9`，`type=Simple` 是**故意**的：按比例定尺的图元不许拉伸） | 0 |
| C31 | 整屏主视觉（竖构图） | `sc/loading_out/loading_sprite_24.png`（1153×1881 竖构图） | `Sprites/Ui/loading_bg.png`（`ResPaths.BootBackground`），铺满 1080×1920（`match=0` 竖版画布；原图也是竖的 ⇒ 不裁不缩比例不补黑边） | 0 |
| C32 | 滑块填充 / 加载页读条填充 | `sc/loading_out/loading_sprite_15.png`（读条填充绿条 115×39） | ✅ 已恢复到 `Sprites/Ui/Bars/loading_out/frame_015.png`（115×39、`pixel_identical=True`、`Test-Path` True）；`CrUiStyle.LoadingBarFill`（`CrUiStyle.cs:277`）素材可达（D75 由 AD1 片修复） | 0 |
| C33 | 滑块手柄 | `sc/ui_out/ui_out_sprite_11.png`（浅灰圆角小方块 24×24） | `Sprites/Ui/Slots/ui_out/frame_011.png`（`ResPaths.SlotCorner`），3 个手柄各 1（`handleType=Simple handleRect=18x48` —— 图标不是可拉伸框体 ⇒ **故意不切片**） | 0 |
| C34 | 九宫格覆盖（原版图元不许拉变形） | 原版图元是定尺寸画的（圆角 / 描边在像素里） | 五面板 audit：主菜单 10 张有图 Image 里 8 张 `Sliced`+`border≠0`（2 张白名单）；房间列表 18/18；房间内 9 张 8 Sliced（1 白名单）；设置 12 张 9 Sliced（3 白名单 = 手柄图标）；暂停 7/7 | 0 |
| C35 | 纯色块当最终外观 | 不适用（原版界面无纯色框） | 五面板**0 处纯色块当最终外观**；唯一 `sprite=NULL` 的形状是引擎自己的 popup 遮罩 / `CreateSlider` 凹槽轨道（后者见 D17） | 0 |
| C36 | 出牌音效 | `原版资源/cr-sfx/Game/summon_own_07.ogg`（2.300 s / 20 322 B） | `Resources/Sound/SFX/PlayCard/summon_own_07.ogg`（逐字节复制）+ 实测 `Game.Res.Exists=True`、本片播放 20 次 | 0 |
| C37 | 命中音效 | `原版资源/cr-sfx/Cards/0_Special/boulder_impact_01.ogg`（1.449 s / 15 377 B） | `Resources/Sound/SFX/Hit/boulder_impact_01.ogg` + 实测 Exists=True、播放 95 次 | 0 |
| C38 | 塔毁音效 | `原版资源/cr-sfx/Cards/Bomb Tower/building_explode_01.ogg`（2.001 s / 20 164 B） | `Resources/Sound/SFX/TowerDown/building_explode_01.ogg` + 实测 Exists=True、播放 4 次 | 0（选型偏差见 D38） |
| C39 | 圣水满音效 | `原版资源/cr-sfx/Cards/Elixir Collector/get_elixir_02.ogg`（0.919 s / 9 562 B） | `Resources/Sound/SFX/ElixirFull/get_elixir_02.ogg` + 实测 Exists=True、播放 11 次 | 0（选型偏差见 D37） |
| C40 | 胜利 jingle | `原版资源/cr-sfx/Music/Jingles/scroll_win_02.ogg`（10.353 s / 116 030 B） | `Resources/Sound/SFX/Win/scroll_win_02.ogg` + 实测 Exists=True、播放 1 次（注入结算） | 0 |
| C41 | 失败 jingle | `原版资源/cr-sfx/Music/Jingles/scroll_lose_01.ogg`（5.068 s / 54 298 B） | `Resources/Sound/SFX/Lose/scroll_lose_01.ogg` + 实测 Exists=True、播放 1 次（真局 `crowns=0:2`） | 0 |
| C42 | 音效落地范围（⛔ 不是整包搬） | 原版整包 = 2953 个 `.ogg` / 86.3 MiB / 02:24:29（`原版资源/cr-sfx/`，含 `清单.md`） | 只落地 6 个；`AudioPaths` 6 键 ↔ 磁盘 6 文件 ↔ 运行时 `Exists` 6/6 True | 0 |
| C43 | 音效路径可追溯性 | 原版整包非音频文件仅 `清单.md` | 路径末段 = 源文件名（`<用途>/<源文件名>.ogg`）；路径字面量集中 `Core/AudioPaths.cs`（⛔ 业务代码不写） | 0 |
| C44 | 音效版权与来源署名 | © Supercell（原版音频） | 来源 `https://github.com/Henrylq/Clash-Royale-SFX`（未声明 license）；非商业用途 | 0 |
| C45 | 音效播放通道唯一性 | 原版由游戏本体按事件播 | 全工程音效播放只有 `Game.Sound.PlaySFX(clipName)` 一个入口（`View/BattleAudioView.cs`；引擎 `Runtime/Presentation/Sound.cs:329` 硬编码 `Sound/SFX/{clipName}`）；⛔ 无 `AddComponent<AudioSource>`、无自建池、无散点 `Resources.Load` | 0 |
| C46 | 面板底（卡组 / 结算） | `ui_out 806`（`策划/原版UI素材索引.md` Panels 段） | `frame_806` `Sliced` `border=(2,2,2,2)`（audit `v4-audit-step12-DeckBox.txt` / `-step9-Result.txt`） | 0 |
| C47 | 标题条（卡组 / HUD / 结算） | `ui_out 069` | `frame_069` `Sliced` `border=(24,2,24,20)` | 0 |
| C48 | 金色主按钮（卡组 `保存` / 结算 `再来一局`） | `ui_out 300` | `frame_300` `Sliced` `border=(13,16,13,12)` | 0 |
| C49 | 深蓝灰按钮（卡组 `取消/上一页/下一页`、结算 `回主菜单`） | `ui_out 014` | `frame_014` `Sliced` `border=(24,24,24,24)` | 0 |
| C50 | 卡槽底 | `ui_out 043` | `frame_043` `Sliced`（卡组 `Slot*`/`Card*` `border=(20,20,20,20)`；HUD `Hand0..3` `border=(31,30,31,30)`） | 0 |
| C51 | 圣水条三件套 | `ui_out 516`（轨道）/`517`（描边框）/`518`（填充） | 同名三帧，全部 `Sliced`（audit `ElixirTrack/ElixirFrame/ElixirFill`） | 0 |
| C52 | 皇冠图元 | `ui_out 050` | `frame_050` `Simple` 按原比例（HUD `CrownIcon 92×73.8` / 结算 `74×59.3`） | 0 |
| C53 | 圣水水滴 | `ui_out 099` | `frame_099` `Simple` 按原比例（卡组 `Card*ElixirDrop`、HUD `HandCost*`） | 0 |
| C54 | 结算结局徽记 | `ui_battle_end_out 195` | `frame_195` `Simple` 按原比例（`OutcomeIcon 96×89`） | 0 |
| C55 | 深色圆角数据条 | `ui_battle_end_out 108` | `frame_108` `Sliced` `border=(24,24,24,24)`（结算 `ReasonBar`/`DetailBar`；HUD `TimerBox`） | 0 |
| C56 | 真卡面（卡组 / HUD） | `ui_spells_out N`（卡面帧） | `CardArt:frame_NNN`（裁剪后 `Simple`；卡组 `Card0Art`/`Slot0Art`、HUD `HandArt*`） | 0 |
| C57 | 远程弹道素材（Arrow） | APK 解包 `sc/effects_out` 帧 **f440..459**（20 帧），`原版资源/cr-assets-png/assets/sc/effects_out/` | `Sprites/Effects/Arrow/frame_440..459.png`（20 帧，逐帧原版直取）；运行期 `SpriteBank.LoadDir` 实测取到 `gen_frame_450_0`（与 C2 的 `gen_*` 同现象） | 0 |
| C58 | 弹道资源标识 = projectile key | 官方 `cards_stats_projectile.json` 的 `name`（如 `ArcherArrow`） | unit_cs `projectile_key` 列逐字同名（24 个 key 全命中） | 0 |
| C59 | 战斗 HUD 层的源文件 | CR **2.1.5** `assets/sc/ui`（`ui.sc`）里 `HUD_*` export 的 `0c` 记录**具名子元件**：`HUD_player`(1091) / `printScore_player`(1026)/`printScore_enemy`(1031) / `HUD_topRight`(1017) / `replay_HUD_left`(962)（出处 `策划/战斗HUD素材索引.md` §1/§2） | 同上（解析器 `tools/probes/sc-anim-index.py` + `.ai-tmp/test/aa2-resolve.py ui:1091 ui:1080 ui:1070 ui:1003 ui:1017 ui:1026 ui:1031 ui:948 ui:1100`） | 0（本条**推翻**「HUD 层不在这批 `.sc`」的旧判断：HUD **在** `ui.sc`，只是不以**顶层 export** 名出现） |
| C60 | 圣水条 槽底 | `HUD_player`(1091) → `elixir_bar`(1080) → **`bar_bg`**(902) = `ui_out` **frame_155**（1×74，索引 §3.1） | `ResPaths.ElixirBarTrack` = `Bars/ui_out/frame_155`（AB1 换帧 **516→155**，AB2 已用） | 0 |
| C61 | 圣水条 条端 / 框 | `elixir_bar`(1080) → **`bar_end`**(906) = **frame_158**（10×59） | `ResPaths.ElixirBarFrame` = `Bars/ui_out/frame_158`（AB1 换帧 **517→158**） | 0 |
| C62 | 圣水条 填充 | `elixir_bar` → **`bar_body`**/**`ghost`**(904/905，同帧) = **frame_157**（59×1） | `ResPaths.ElixirBarFill` = `Bars/ui_out/frame_157`（AB1 换帧 **518→157**） | 0 |
| C63 | 圣水条 同族**未命名裸子件** | `elixir_bar`(1080) 的裸子件（原版未命名；15×75） | `ResPaths.ElixirBarTrackAlt` = `Bars/ui_out/frame_156`（AB4 登记） | 0 |
| C64 | 圣水条 10 格刻度分隔 | `elixir_bar` → **`d1`…`d9`**(clip 909，同 clip 复用 9 次) = **frame_160**（1×1） | `ResPaths.ElixirBarTick` = `Bars/ui_out/frame_160`（AB4 登记；HUD 暂未绘制 ⇒ D66） | 0 |
| C65 | 圣水需求条（条身 / 条端） | `elixir_bar` → **`elixirRequirementBar`**(clip 912，11 帧) = **frame_161**（69×1）/ **frame_162**（14×69） | `ResPaths.ElixirRequirementTrack` = `frame_161` / `ElixirRequirementEnd` = `frame_162`（AB4 登记；暂未绘制 ⇒ D66） | 0 |
| C66 | 圣水条左端大图标 / 圣水回复提示图标 | **`elixirBarLeft`**(clip 1077) / **`elixirRegen`**(clip 1014) = **frame_159**（94×115） | `ResPaths.IconElixirBarLeft` = `Icons/ui_out/frame_159`（AB4 登记；HUD 现引 `IconElixirDrop`(99) ⇒ D60） | 0（**素材已登记**；HUD 未改引） |
| C67 | 退出 ✕ 图标 | `quit_button`(clip 924) = **frame_164**（80×81；与 `play_pause_button` 共用底板 163） | `ResPaths.IconQuitCross` = `Icons/ui_out/frame_164`（AB4 登记） | 0 |
| C68 | 手牌槽底 | `HUD_player`(1091) → **`slots`**(clip 1070) = **frame_200**（原版把同一 shape 放 **4 次**；索引 §3.2） | `ResPaths.HudHandSlot` = `Slots/ui_out/frame_200` ×4（AB1 登记 + AB2 落地；「下一张」槽底同件复用） | 0 |
| C69 | Draft 卡槽底 | **`card_slots`**(1099→1098) = **frame_201**（原版放 8 次） | `ResPaths.HudHandSlotDraft` = `Slots/ui_out/frame_201`（AB1 登记） | 0 |
| C70 | 冠数（我方 / 敌方）**第一态** | `printScore_player`(1024) → **`star1`/`star2`/`star3`** = **187**；`printScore_enemy`(1029) → 同族 = **188**（各 120×98）；侧栏 `HUD_rightMiddle` → `starPlayer`(989)/`starEnemy`(992) 同帧（索引 §3.3） | `ResPaths.HudStarPlayer`(187) / `HudStarEnemy`(188)（AB2 已用；与既有 `IconCrownBlueGem`/`IconCrownRedGem` **同帧不同键**） | 0 |
| C71 | 冠数 **第二态** | `printScore_player`/`printScore_enemy` 的另一态 = **frame_197** / **frame_198**（120×98；索引 §3.3 列「187 / 197」「188 / 198」） | `ResPaths.HudStarPlayerAlt`(197) / `HudStarEnemyAlt`(198) —— AB3 落地（`pixel_identical=True`）+ AB4 登记键 | 0 |
| C72 | 冠数 / 比分 名条 | `printScore_*` 的**裸子件**（原版未命名，247×56） = **frame_196** | `ResPaths.HudScoreNamePlate` = `Panels/ui_out/frame_196`（AB2 落地用） | 0 |
| C73 | 倒计时 表盘 | **`Clock_middle`**(clip 184) = **frame_042**（原生 15×15） | `ResPaths.HudClockIcon` = `Icons/ui_out/frame_042`（AB2 落地用；摆放尺寸自定 ⇒ D57） | 0（**素材** 0；摆放尺寸见 D57） |
| C74 | 倒计时 底板 | `HUD_topRight`(1017) 的无名子件(1005) = **frame_193**（212×124） | `ResPaths.HudTopRightPlate` = `Panels/ui_out/frame_193`（AB2 落地用） | 0 |
| C75 | 暂停按钮 底板 / 播放 / 暂停 | **`play_pause_button`**(clip 948，由 `replay_HUD_left`(962) 引用) = 底板 **frame_163**(219×219) / ▶ **frame_170**(80×87) / ‖ **frame_171**(84×90) | `ResPaths.HudPauseButtonPlate`(163) / `HudPauseIconPlay`(170) / `HudPauseIconPause`(171)（AB2 用 163+171；170 **未使用**） | 0（帧号一致；**语义归属** = 回放 HUD ⇒ D59） |
| C76 | HUD 图元身份核对（65 个 `ResPaths` 落地图元 ↔ 2.1.5 `.sc` 显式导出表） | `策划/原版UI素材名称索引.md`（每个 `.sc` 一节 + 全量 export 表）+ `策划/原版UI图元更正表.md` §1（65 条判定） | 命中 **55** / 不符 **5** / 无法判定 **5**（更正表第 7 行）；Y2 按权威名**改名 4 键**、**改 0 帧**；5 条无法判定 + 3 条源内无对应**保留并登记** | 0 |
| C77 | 已改名键的帧与尺寸（未换帧） | `card_frame_glow_legendary` = 532（145×188）· `battle_end_chat_selection_bubble` = 54（73×113）· `icon_tournament_create` = 281 · 24×24 金方块 = 201 | `ResPaths.CardFrameGlowLegendary`（`Panels/ui_out/frame_532`）· `ChatBubble`（`Slots/ui_out/frame_054`）· `IconTournamentCreate`（`Icons/ui_out/frame_281`）· `GoldSquarePlate`（`Bars/ui_battle_end_out/frame_201`） | 0（Z2 复核：索引第 713 / 1005 / 831 行**逐条反查一致**） |
| C78 | 本轮 HUD 帧的落地范围（⛔ 不是整目录搬） | 原版 `ui_out` 914 帧 / `ui_battle_end_out` 240 帧（`策划/原版素材索引`） | 本轮 **AA2 落地 17 帧**（Bars 7 / Slots 2 / Icons 5 / Panels 2 / Buttons 1；`aa2-land.py` 回读 `mismatch=0`，`Test-Path` 17/17）+ **AB3 落地 2 帧**（197/198）；AB4 尺寸核对 **8/8 ✔**（落地像素 == 原版 bbox == 索引文档尺寸） | 0 |

> ⚠️ C 段**不入本表**的行：`EffectArrow` 素材已落地但本轮未接线（D20）；
> 两个刻意不落地的帧 `ui_out 10` / `ui_out 76`（图元由互不相连的几块组成，切片会只得一半）⇒ D25；
> 音效选型偏差（圣水满 / 塔毁 ⇒ D37 / D38）、弹道贴图目录 `sprite_dir` 未落盘（D49）。
>
> ⚠️ 本轮（AC1）**不入本表**的 C 段行：
> 倒计时数字无图元可搬（原版 `timeLeft`/`TID_10..1` 全是**文本域**，无 shape）⇒ D64；
> 阶段容器 `HUD_topRight_overtime/double_elixir` 是 **0 shape 纯容器** ⇒ D61/D64 同源；
> 圣水刻度 / 需求条**素材已登记键但 HUD 未绘制**（D66）；圣水徽章 HUD 仍引 99 而非 159（D60）；
> 冠数第二态 197/198 在 AB1 时**未落地**（已由 AB3 落地 + AB4 登记 ⇒ 本条转 0，见 C71）；
> 暂停件归属回放 HUD ⇒ D59；新帧九宫格切边未解出 ⇒ D63；
> `PanelPaper*` 5 键 9-slice 方位无法判定 ⇒ D67；`ButtonCapsule` / 治疗图标 源内无对应 ⇒ D68 / D69；
> `CardFrameGlowLegendary` / `ChatBubble` 归类目录残留 ⇒ D70 / D71；结算「金色宽底条」无帧可复制 ⇒ D72；
> 被删的 `loading_out frame_015/028`（素材回归）⇒ D75。
> ⛔ **`Y2-允许差异.md` 的 `C1`（圣水条三件「来源未定」）已作废**：AB1 依 `策划/战斗HUD素材索引.md` §1
> 把 HUD 层定位在 `ui.sc` 的 `0c` 具名子元件里，三件换成 155/158/157（本表 C60–C62 ⇒ 差值 0）。
>
> **`W1-` / `AA1-` 两条片结论（已并入本表，不新增行）**：① **版本指纹** = CR **2.1.5**（`ShapeCount` + `_tex.png`
> 尺寸与本地 `cr-assets-png/*_out` 100% 吻合 ⇒ 不是 2.2.1 / 不是 v1.0.0）—— 见 C76 的权威来源；
> ② **`frame_NNN` 语义** = `.sc` 第 NNN 条 `12`(shape) 记录（`sid ≠ 序号`）—— 见 C58/C78 的可追溯链；
> ③ **UI 坐标已解出**（`0c` 三元组 = `(childIdx, matrixIdx, colorIdx)`；a,b,c,d ÷1024、tx,ty ÷20）⇒ 交叉验证见 A54；
> ④ **UI 元件语义名已解出**（`darken/panel/slots/elixir_bar/cardArea/nextCard1/timeLeft/enemyName/…`）⇒ 见 C59–C75；
> ⑤ **绝对屏幕像素未解出**（root 局部系 ≠ 手机屏系）⇒ D74。

### AF1-对照表行 · C 段 · 素材（BGM 落地）


| 项 | 原版值(出处) | 我们的值 | 差值 |
|---|---|---|---|
| 主菜单/大厅 BGM 素材 | `原版资源/cr-sfx/Music/Lobby/music_lobby_202606.ogg`（原版大厅 BGM；出处 = `cr-sfx/清单.md` 目录枚举） | 同文件复制到 `client/Assets/Resources/Sound/BGM/Menu/music_lobby_202606.ogg`，键 `AudioPaths.BgmMenu` | **0** |
| 对局 BGM 素材 | `原版资源/cr-sfx/Arenas/General/2min_loop_battle_01.ogg`（2 分钟对局循环；出处 = `cr-sfx` 目录枚举） | 同文件复制到 `client/Assets/Resources/Sound/BGM/Battle/2min_loop_battle_01.ogg`，键 `AudioPaths.BgmBattle` | **0** |
| 结算 BGM 素材 | `原版资源/cr-sfx/Music/Jingles/scroll_preresult_loop_01.ogg`（原版**结算前**循环音；同目录 `scroll_win_02`/`scroll_lose_01` 已作胜负 jingle 用，见 `AudioPaths`） | 同文件复制到 `client/Assets/Resources/Sound/BGM/Result/scroll_preresult_loop_01.ogg`，键 `AudioPaths.BgmResult` | **0** |
| 落地目录口径 | 引擎 `Runtime/Presentation/Sound.cs:263` **硬编码** `$"Sound/BGM/{clipName}"` | `Resources/Sound/BGM/<用途>/<源文件名>`（与 SFX 同约定：源文件名原样保留） | **0** |
| 进工程文件数 | 只落**被引用的那几个**（skill §1.9 / `check-assets` 闸门） | **3 个** ogg（Menu/Battle/Result），无整包搬运 | **0** |



### AF2-对照表行 · C 段 · 素材 `chr_goblin_archer_out`（342 帧）


| 项 | 原版值(出处) | 我们的值 | 差值/判定 |
|---|---|---|---|
| `chr_goblin_archer_out` | `原版资源/cr-assets-png/assets/sc/chr_goblin_archer_out`（SpearGoblin 投矛手素材） | `client/Assets/Resources/Sprites/Units/chr_goblin_archer_out`（342 帧） | **非孤儿**：由 `策划/数值文档/unit_cs.txt` 的 `SpearGoblin` 行 `sprite_dir` 数据引用 → `server/game/table/tsv/unit.tsv`；AE1 的「.cs 零引用」是单口径假阳性（登记 D23） |

> 备注（非本片可改）：`client/Assets/Scripts/View/BattleViewRoot.cs` 把卡 `spear-goblins`(26010004) 的卡面
> 指向 `chr_goblin_out`，而 `unit_cs/unit.tsv` 的 `SpearGoblin` 指向 `chr_goblin_archer_out`。客户端片待核。


### AH2-对照表行 · C 段 · 素材（音效：本片新增 8 个）


| 用途（`AudioPaths` 常量） | 原版值（出处：`原版资源/cr-sfx/` 相对路径） | 我们的值（`client/Assets/Resources/Sound/SFX/`） | 差值 |
|---|---|---|---|
| `Death` 单位死亡 | `Cards/0_Special/npc_die_04.ogg`（`0_Special` = 原版通用音槽；被 Archers/Bandit/Barbarians/Bats 多卡复用） | `Death/npc_die_04.ogg`（10 223 B） | **0** |
| `EnemySummon` 敌方出牌 | `Game/enemy_summon_01.ogg`（`Game/` = 通用对局音） | `EnemySummon/enemy_summon_01.ogg`（16 118 B） | **0** |
| `TowerActivate` 塔激活 | `Towers/King Tower/king_activate_01.ogg`（国王塔激活音，语义逐字对上） | `TowerActivate/king_activate_01.ogg`（19 601 B） | **0** |
| `Draw` 平局 | `Music/Jingles/scroll_draw_01.ogg`（与 `scroll_win_02`/`scroll_lose_01` **同目录同组** = 原版结算三态） | `Draw/scroll_draw_01.ogg`（77 851 B） | **0** |
| `UiClick` UI 点击 | `Menu/button_click_02.ogg`（整包唯一的 `button_click_*`） | `Ui/button_click_02.ogg`（4 438 B） | **0** |
| `GrabCard` 卡牌拖起 | `Game/grabcard_01.ogg`（原版"抓牌"，整包唯一的 `grabcard_*`） | `GrabCard/grabcard_01.ogg`（4 694 B） | **0** |
| `BadDrop` 放置被拒（非法落点 / 圣水不足） | `Game/bad_drop_03.ogg`（原版"无效投放"，整包唯一的 `bad_drop_*`） | `BadDrop/bad_drop_03.ogg`（10 852 B） | **0** |
| `CountdownTick` 倒计时最后 10 秒 | `Game/deploy_timer_tick_01v4.ogg`（原版对局计时滴答；`enemy_deploy_timer_tick_01` 是它的敌方版） | `CountdownTick/deploy_timer_tick_01v4.ogg`（6 646 B） | **0** |

**本片未新增（沿用既有 6 槽，仅语义收窄）**：`PlayCard`（`Game/summon_own_07`）、`Hit`（`Cards/0_Special/boulder_impact_01`）、
`TowerDown`（`Cards/Bomb Tower/building_explode_01`）、`ElixirFull`（`Cards/Elixir Collector/get_elixir_02`）、
`Win`（`Music/Jingles/scroll_win_02`）、`Lose`（`Music/Jingles/scroll_lose_01`）—— 与 `原版资源/cr-sfx/清单.md` §3 逐条一致。

**取不到的（如实登记，⛔ 未用无关音冒充、⛔ 未静音糊过去）**

| 用途 | 原版值 | 我们的值 | 差值 |
|---|---|---|---|
| UI **悬停**音 | 整包 2953 ogg 中 **不存在**（`hover` 命中 0 个文件；`menu_swipe_09`/`up_down_swipe_01` 是滑动音） | **不播**（允许差异，见 `AG2-redrows.md` §4） | 未取到（登记） |
| 投降专用音 | 整包无 `surrender/give_up/resign/forfeit` 命中 | 走通用 UI 点击音（`Ui/button_click_02`）+ 结算 `Lose` jingle | 未取到（登记） |
| `kind==1 生成` 音 | 整包无独立"生成/落地"音 | **不播**（再播会与出牌音双响，登记） | 未取到（登记） |
| 挥砍 / 召唤**特效帧** | `effects_out` 有 612 帧，但**无权威"帧号↔用途"映射**（本轮只索引 3 类：f050..056 / f418..427 / f440..459） | 不落地（⛔ 不猜帧号） | 未取到（登记） |





### AL2-对照表行 · C 段 · D7 音乐 / D8 音效（AJ2 复采，差值 0）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| C79 | D7 进图是否停主菜单曲 | 进图停菜单曲 | bgmCurrent: Menu/music_lobby_202606 → Battle/2min_loop_battle_01 | 0 |
| C80 | D7 暂停时 BGM | 未量到原版 | 切 Pause 时 bgm 不变（仍 Battle 曲，BgmView case Pause: break） | 0（我方；原版无对照） |
| C81 | D8 R8 非法放下音 | 播 BadDrop | BadDrop/bad_drop_03（22:11:45.630）+ 日志「放下落点非法」（22:11:45.631） | 0（消除 D80） |
| C82 | D8 R11 平局音 | 播 Draw | Draw/scroll_draw_01（22:11:42.520） | 0（消除 D40） |
| C83 | D8 R7 倒计时≤10s 提示音 | 播 CountdownTick | 连播 3 声 CountdownTick/deploy_timer_tick_01v4（22:27:34.064/.066/.067 ⇒ 剩余 9/8/7 s） | 0（消除 D80） |
| C84 | D8 R10 圣水不足被拒音 | 播 BadDrop | 客户端本地分支：GrabCard(22:27:23.653) → BadDrop(22:27:23.654) | 0（消除 D80） |

## D. 字体 / 色调 / 交互反馈 / 节奏

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| D1 | 色调 | **不加滤镜**：直接使用原版素材原色 | 同（`tools/probes/copy-ui-assets.py` 落地即「不缩放、不重采样、不调色、不加滤镜」，每次运行逐像素回读比对 `mismatches=0`；G2 自审「无滤镜」） | 0 |
| D2 | 拖放出牌反馈 | 落点指示圈 / 非法时红色（原版行为） | `HudPanel.MoveGhost` 每次指针移动刷新 `BattleViewRoot.ShowPlacement` 指示圈、`CancelDrag` 各路径 `HidePlacement`（接线修复见 `.ai-tmp/test/dispatch-log.tsv` 的 direct-fix 行）；非法落点由服务端 `CanDeploy` 拒（`TestPlayCardRejectedOutsideDeployZone`） | 0 |
| D3 | 对局节奏 | 3 分钟常规 + 2 分钟加时（`battle_timelines.json`） | 3 + 2 分钟（同 B1/B2；`units.go:88-89`） | 0 |
| D4 | 正文 / 按钮 / 标签字号 | 字段值 "667" 字形 bbox 高 **16** @750 ⇒ 23.0 ⇒ ÷0.72 = **32.0**（`CrUiStyle.cs:188-195`） | `FontBody` = **32**（audit: 五面板按钮 `Label size=32` / 昵称 / 标签 / 数值 / 档位字全 32） | 0 |
| D5 | 面板标题字号 | "Player Profile" 白字字形 bbox 高 **27** @750 ⇒ ×1.44 = 38.9 ⇒ ÷0.70 = 55.5（`CrUiStyle.cs:180-186`） | `FontTitle` = **56**（audit: `主 菜 单 / 设 置 / 暂 停` 全 `size=56`；+0.5 为取整） | 0 |
| D6 | 攻击段·单条 clip 像素帧数（knight / musketeer / archer） | 14 / 10 / 5（`策划/单位帧段表.md` §4.3） | 同（`UnitView.AttackMeta`；V1 片实测，见 `策划/验收表.md` §1.1 V1-S28-1） | 0 |
| D7 | attack 播放速率（像素帧数 ÷ `hit_speed_ms`） | 11.7 / 10.0 / 5.6 fps（§4.3） | 同（`UnitView.AnimFpsPerDir`） | 0.0% |
| D8 | walk（原版 `run1`）播放速率 | 13.8 / 12.9 / 12.0 fps（§4.3） | 同（`UnitView.AnimFpsPerDir`） | 0.0% |
| D9 | idle | **静止帧（不播）**（§4.3） | `AnimFpsPerDir` idle = 0（停在区间首帧，不推进） | 0（口径一致） |
| D10 | 远程弹道（`kind==0` 出牌 → 飞行弹道） | 原版 `projectile` 非空 ⇒ 按 `cards_stats_projectile.json` 的 `speed`（格/分钟）飞行 | **已接线**：`CardInfo.projectile_key` 非空 ⇒ 从出牌方国王塔飞到事件落点，时长 = 距离(格)×60/`proj_speed`；实测 `projectileShots=1`（`.ai-tmp/test/V3-report.md`） | 0（施法者位置降级 ⇒ D48） |
| D11 | 音效·五态各有声 | 原版对局中「出牌 / 命中 / 塔毁 / 圣水满 / 胜负」各有反馈音 | 实测 5/5 都发出播放调用（出牌 20 / 命中 95 / 塔毁 4 / 圣水满 11 / 胜负 Lose 1 + Win 1 注入；`.ai-tmp/test/V2-report.md`） | 0（听感 = HUMAN-ONLY） |
| D12 | 音效·音量跟随设置档 | 原版音量可在设置里调 | 音效走 `SoundGroup.SFX`；实测 `SettingsManager.SfxVolume 1.00→0.40` ⇒ `GetVolume(SFX)=0.40` ⇒ 播放行打印 `音量 0.40` | 0 |
| D13 | 设置面板·改完即刻生效 + 真存 + 重进仍在 | 原版设置项改后当场生效（音量/画质/全屏），退出重进仍保留 | `SettingsManager.Init()` 订阅 `Events.Settings.*Request` 后**同帧**生效并落盘：音量 `GetVolume(BGM/SFX/Voice) 1→0.37/0.42/0.55`；画质 `High(2)→Medium(1)`；全屏落盘 `STORED.fullscreen=True`；重进读回同值（`.ai-tmp/test/V5-report.md`、`v5-apply.txt`、`v5-reload.txt`） | 0（全屏编辑器内不可复现 ⇒ D52） |

> ⚠️ D 段**不入本表**的行：字体族不是 A 自有位图字体（D8）、小字 `FontSmall=24` 未量到（D24）、
> 标题字 56 vs 55.5 记取整（D10 同源）、**圣水条的满格闪光 / 分段倍率提示未取证（D26）** ——
> 圣水条**素材**本身是 A 原版逐项同源（见 C13），只有"满格闪光 / 分段提示"两条表现类判据缺对照物；
> 单位动画 `die` 帧区间未找到（D50）、attack 区间 = 9 视角并集 + 单条 clip 时长舍入 ≤0.8%（D51）、
> 设置全屏在编辑器内不可复现（D52）。

### AF1-对照表行 · D 段 · 节奏 / 交互（D12 流程状态 × D7 BGM）


| 项 | 原版值(出处) | 我们的值 | 差值 |
|---|---|---|---|
| 主菜单/登录/创角/房间 BGM | 原版大厅曲（`cr-sfx/Music/Lobby`）；**精确切换时机未逐帧量到**（原版无 `.sc` 引用可判） | 上述 4 个站点 ⇒ `BgmMenu`（挂 `Events.Flow.StationChanged`） | **0**（曲目对齐）／切换时机 = **未量到**（登记差异 D-AF1-1） |
| 对局 BGM | 原版 2 分钟对局循环曲（`Arenas/General/2min_loop_battle_01`） | `Battle` 站点 + `Events.Battle.Started` ⇒ `BgmBattle` | **0**（曲目对齐）／切换时机 = **未量到**（D-AF1-1） |
| 结算 BGM | 原版结算前循环音（`Music/Jingles/scroll_preresult_loop_01`） | `Events.Battle.Ended` ⇒ `BgmResult` | **0**（曲目对齐）／切换时机 = **未量到**（D-AF1-1） |
| 进图停菜单曲 / 回菜单起 | 原版切曲 = 双音源交叉淡入淡出（`Sound.cs:260-309` `PlayBGM`） | 由引擎 `PlayBGM` 自动完成（旧曲淡出 Stop）；进图 `Started`/站点 `Battle` 切对局曲，回 `MainMenu` 切菜单曲 | **0** |
| 暂停时不切曲 | 原版暂停不换 BGM（推断：暂停只覆盖菜单） | `Stations.Pause` 分支**不换曲**（保留对局曲） | **0**（推断，登记 D-AF1-2） |
| 再来一局（同站点重开）曲目不卡结算曲 | 原版重开一局回到对局曲 | `Events.Battle.Started` ⇒ `BgmBattle`（覆盖 `GoTo` 同站点早退、无 `StationChanged` 的情形） | **0** |



### AH2-对照表行 · D 段 · 节奏 / 交互（本片新增的挂点）


| 项 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|
| UI 点击音挂点 | 每颗按钮点击一声 | `CrUiStyle.ActionButton` / `CrUiStyle.Button`（全工程**唯一**建按钮处，`UIFactory.CreateButton` 只被本文件调用）⇒ 一处挂全接 | **0**（覆盖面无缺口） |
| 命中音触发口径 | 每次受击一声 | **快照 `hp` 逐 id 下降**（协议无"命中"事件，`Def/ProtoDef.cs:194` 只有 0..5 六种 kind） | 语义一致，机制差异（登记在 `AH2-report.md` 未决） |
| 命中音单帧上限 | 无原版可量（原版按攻击事件触发） | **4 声 / 快照**（SfxHitPerSnapshot；多余掉血不播，防同帧多单位爆音） | 本项目自定（登记） |
| 倒计时提示阈值 | 最后 10 秒（`AG2-matrix-D8.tsv` 倒数第 5 行的边界值 `timer<=10s`） | `CountdownWarnMs = 10000`（逐字对齐矩阵边界）；每"剩余整数秒"一声 | **0** |
| 倒计时计时源 | 服务端口径剩余时间 | `regulation_ms (+overtime_ms) − snapshot.server_ms`（与 `HudPanel.RemainingText` **同源**，⛔ 不用本地计时） | **0** |
| 拖起 / 放下音 | 拖起一声；非法放下被拒一声 | `BeginDrag`→`GrabCard`；`EndDrag` 且 `BattleViewRoot.IsDeployLegal==false`→`BadDrop`（合法放下不另播，避免与出牌召唤音双响） | **0**（触发点对齐） |
| 圣水不足被拒 | 拖起那一刻提示被拒 | `BeginDrag` 的 `costMilli > elixir` 分支 → `BadDrop`（本地即时，仍照旧发出请求，服务端裁决） | **0** |
| 特效池上限 | 无原版可量 | `EffectsView.MaxLive = 64`；播完 Recycle 回 `_idle`，⛔ 不 Destroy | 本项目既有（AH2 未改，仅断言） |
| 音效分组音量 | 受设置里的音效档控制 | `SoundGroup.SFX`（由 `SettingsManager` 应用）；AH2 实机读到的档位 = **0.42** | **0**（口径一致） |




## E. 允许的差异索引（逐条全文在 `策划/验收表.md` 的「允许的差异」）

| # | 差异 | 验收表编号 |
|---|---|---|
| E1 | 不实现冠军稀有度 / 英雄能力 | D1 |
| E2 | 卡池 60 张（用户指定） | D2 |
| E3 | 塔血量取 `hitpoints_per_level` 自洽解 | D3 |
| E4 | 服务端 tick 20 TPS | D4 |
| E5 | 不做养成（S29–S31）与 2v2/观战/回放（S32） | D5 |
| E6 | P.E.K.K.A 伤害取官方逐级表 816 | D6 |
| E7 | 官方逐级表 vs anchors 的 ±1 与 Log 290/268 | D7 |
| E8 | 字体不是原版 Supercell 位图字体 | D8 |
| E9 | 内容框宽 1000 vs 1023.8 | D9 |
| E10 | 标题条高 92 vs 90.7（取整） | D10 |
| E11 | 面板内留白 40 vs 31.7 | D11 |
| E12 | 顶部玩家信息条一条 95 vs 两条 167 | D12 |
| E13 | 底部导航缺失 | D13 |
| E14 | 面板底色米黄羊皮纸 vs 原版浅灰蓝 | D14 |
| E15 | 行距 / 音量块 / 暂停面板宽高与按钮间距、文案：A 无对应界面 ⇒ 本项目自定 | D15 |
| E16 | 设置主按钮 620×110 vs A 同类宽条 823.7 | D16 |
| E17 | 滑块轨道仍是引擎纯色凹槽 | D17 |
| E18 | 河带可见顶边 17.33 vs 15.0 | D18 |
| E19 | 远程弹道未接（协议不携带 `projectile_key` / `proj_speed`） | D19 |
| E20 | 弹道素材 `EffectArrow` 已落地未接线 | D20 |
| E21 | 单位动画每档播整目录（`AnimRanges` 恒空） | D21 |
| E22 | 锚点是整目录共用，不是逐帧对齐落脚点 | D22 |
| E23 | 关卡底图按帧下标取（`ArenaView.cs` `GroundFrameIndex`） | D23 |
| E24 | 小字 `FontSmall=24` 未量到 | D24 |
| E25 | 两个 UI 帧刻意不落地（`ui_out 10` / `ui_out 76`） | D25 |
| E26 | 圣水条的满格闪光 / 分段倍率提示未取证 | D26 |
| E27 | 卡组编辑·卡格高 336 vs ≈354 | D27 |
| E28 | 卡组编辑·列步进 264 vs 254.5 | D28 |
| E29 | 卡组编辑·行步进 426 vs 477.4 | D29 |
| E30 | 卡组编辑·卡面未铺满卡格 | D30 |
| E31 | 卡组编辑 + HUD·部分卡无卡面 | D31 |
| E32 | 卡组编辑·整屏页 vs 弹窗 + 缺 Tab/槽位行 + 标题条 | D32 |
| E33 | 卡组编辑·卡底带显示中文卡名 | D33 |
| E34 | 对局 HUD·左上块内容 + 新增 Phase/Status 行 | D34 |
| E35 | 对局 HUD·计时框离顶/宽 + x2 徽记缺失 + 暂停纯色块 | D35 |
| E36 | 结算·结局字号无原版值 + 三行新增文案 | D36 |
| E37 | 音效·圣水满用同语义最近音 | D37 |
| E38 | 音效·塔毁用通用建筑爆炸音 | D38 |
| E39 | 音效·出牌音接在「请求」上 ⇒ 被服务端拒时也会响 | D39 |
| E40 | 音效·平局不播音 | D40 |
| E41 | 音效物理目录 = `Resources/Sound/SFX/`（非任务书 `Audio/`） | D41 |
| E42 | 启动画面·Logo 水平位置（基线图自身偏左） | D42 |
| E43 | 启动画面·底部三行品牌/法务文字 | D43 |
| E44 | 加载页·进度条三件套尺寸/位置为本项目自定 | D44 |
| E45 | 加载页·底图美术与 `17` 不同 | D45 |
| E46 | 加载页·`Blocker` 黑色 55% 遮罩 | D46 |
| E47 | 登录/注册/昵称·A 无此界面（比较对象退化） | D47 |
| E48 | 远程弹道·施法者位置降级为出牌方国王塔 | D48 |
| E49 | 弹道贴图目录 `sprite_dir` 未落盘 | D49 |
| E50 | 单位动画·`die` 帧区间未找到 | D50 |
| E51 | 单位动画·attack 区间 = 9 视角并集 + clip 时长舍入 ≤0.8% | D51 |
| E52 | 设置·全屏在编辑器内不可复现 | D52 |
| E53 | 对局 HUD·手牌/圣水条/下一张/冠数：原版值本身未量到（V4 登记，补 E 行指针） | D53 |
| E54 | 对局 HUD·手牌整排右端 +9px（量取内部不一致） | D54 |
| E55 | 对局 HUD·冠数位置/尺寸原版未量到 | D55 |
| E56 | 对局 HUD·暂停按钮位置/尺寸原版未量到 | D56 |
| E57 | 对局 HUD·时钟图标摆放尺寸自定 | D57 |
| E58 | 对局 HUD·圣水填充端 −13px（读数解释 vs 协议值） | D58 |
| E59 | 对局 HUD·暂停件语义属回放 HUD（推断） | D59 |
| E60 | 对局 HUD·圣水徽章引 99 而非 159 | D60 |
| E61 | 对局 HUD·阶段（加时）标识未实现 | D61 |
| E62 | 对局 HUD·手牌区字号无原版值 | D62 |
| E63 | 对局 HUD·新帧九宫格切边未解出 | D63 |
| E64 | 对局 HUD·倒计时数字无图元可搬（文本域） | D64 |
| E65 | 对局 HUD·顶中「4」紫徽未做 | D65 |
| E66 | 对局 HUD·圣水刻度/需求条未绘制 | D66 |
| E67 | UI 图元·`PanelPaper*` 5 键 9-slice 方位无法判定 | D67 |
| E68 | UI 图元·`ButtonCapsule` 键名与实际不符 + 源内无对应 | D68 |
| E69 | UI 图元·治疗图标源内无对应 | D69 |
| E70 | UI 图元·`CardFrameGlowLegendary` 归类目录残留 | D70 |
| E71 | UI 图元·`ChatBubble` 归类目录残留 | D71 |
| E72 | 结算·「金色宽底条」无帧可复制 | D72 |
| E73 | 证据缺口·`Y2-mainmenu.png` 未采成主菜单 | D73 |
| E74 | 证据缺口·`.sc` 绝对屏幕像素未解出 | D74 |
| E75 | **素材回归（缺陷）**·被删的 `loading_out frame_015/028` 仍被 `CrUiStyle` 引用 | D75 |
| E76 | AJ2 复采闭环：MainMenuPanel 超长昵称溢出（已消除） | D82 |
| E77 | AJ2 复采闭环：回主菜单后 BattleContent 残留（已消除） | D83 |
| E78 | AK2 跨房污染：旧房帧 ⇒ HUD 圣水读数与权威不一致（未修） | D81 |
| E79 | AJ2 观察：编辑器 EnterPlayMode 静态单例残留 | D84 |
| E80 | AJ2 观察：卡池未到达 ⇒ 手牌显卡 id | D85 |
| E81 | 卡组编辑·满 8 张时卡池格未置灰（点击仍被受理） | D86 |
| E82 | 证据缺口·房间列表凑不满 5 项 ⇒ 满项边界未采 | D87 |
| E83 | 证据缺口·RoomPanel 超长房名/昵称未采到 | D88 |
| E84 | 证据缺口·客户端 60 单位场景帧时间未采到 | D89 |

> 折入来源（收官汇总片 V7，2026-09-21）：`G1-` `R2-` `R3-` `V1-` `V2-` `V3-` `V4-` `V5-` 各片的「对照表行」。
> `V2-`（含运行时实测）**取代** `R3-` 草案（同编号同内容，`V2` 是复核+补实测版）；`R2` 的 `C 段 sprite_dir` 列为「未取到」⇒ 已挪 D49。
> A 段新增 A22–A37；B 段新增 B15–B16；C 段新增 C36–C58；D 段新增 D6–D13。
>
> 折入来源（AC1 收口片，2026-09-21）：`W1-` `Z1-`(被 `AB2-` 取代) `Y2-`(含 `Z2-` 复核) `AA1-` `AA2-` `AB1-` `AB2-` `AB3-` `AB4-` 各片的「对照表行」。
> A 段新增 A38–A54；C 段新增 C59–C78；E 段新增 E53–E75（=`策划/验收表.md` 的 D53–D75）。
> B 段 / D 段本轮**无新增行**（新片给的都是 UI 布局与素材身份，属 A / C 段）。
> ⛔ 本轮折入的每一行**差值都 = 0**；非 0 / 未量到 / 无图元可搬 / 素材回归的行**全部**挪进了「允许的差异」D54–D75。

## F. 分片对照表行折入（非 A–E 段条目）

### AF1-对照表行 · 硬规则（本项目规则，非原版对照）


| 项 | 原版值(出处) | 我们的值 | 差值 |
|---|---|---|---|
| 裸 `Debug.Log` | 不适用（skill §8 本项目规则） | `grep -c` in `client/Assets/Scripts/**` = **0** | **0** |
| 裸 `Resources.Load` | 不适用（skill §8 本项目规则） | `grep -c` in `client/Assets/Scripts/**` = **0** | **0** |
| BGM 音量档 | 原版有独立 BGM 音量设置 | 走 `SettingsManager` 现档 `Game.Sound.SetVolume(SoundGroup.BGM,…)`（V5 已接）；实机 `GetVolume(BGM)=0.37` | **0** |



### AF1-对照表行 · 登记差异（允许差异清单）


- **D-AF1-1**：原版 BGM **切换的精确时机**（哪一帧起、淡入时长）**未量到** —— 本项目按"站点 + 对局开始/结算事件"驱动，淡入取引擎默认 0.5s。为什么：cr-sfx 只有音频文件，无原版 `.sc`/时序数据可判。消除时机：拿到原版运行时音频时序（或录屏逐帧）后修。
- **D-AF1-2**：原版**暂停时是否切 BGM** 无权威依据（推断为不切）。消除时机：同上。


### AH1-对照表行 · 待收口片做的事

1. 把 S3-R1~R4 合并进 `策划/对照表.md`（C 段/D 段）与 `策划/验收表.md`（S3 行），并更新 `策划/状态矩阵.tsv`（当前 verify.ps1 报 `S3` 未判）。
2. 本片未改任何 `策划/*` 文件（遵守任务书）。


### AH2-对照表行 · 附：本条对照表**未**覆盖、但 AH2 复核出的登记缺口


| 项 | 现状 | 建议 |
|---|---|---|
| D5 的 `mapped=3 / unmapped=35` 行为（整目录循环 + 每目录 Warn 一次） | `策划/差异登记.tsv` 只有 **8 行**，其中只有 `D21`（S28 单位动画帧段只接入 3 个目录）同源；**`D50` / `D51` 不存在** | 主 agent 在 `策划/差异登记.tsv` 补一条独立行（AH2 无权改 `策划/**`） |
| 音效挂点的"未取到"三项（悬停 / 投降 / 生成） | 只在 `.ai-tmp/test/AG2-redrows.md` §4 登记 | 合并进 `策划/验收表.md` §3「允许的差异」 |

## G. AL3 采数折入（收口片 AM2，2026-09-22）

> 来源：`.ai-tmp/test/AL3-report.md` / `AL3-evidence.txt`（AL3 片未产出 `AL3-对照表行.md`，本节逐条摘自其报告原文）。
> ⛔ 本轮每行**差值 = 0**；不可比 / 未取到的行**不留在本表**，挪 `策划/验收表.md`「允许的差异」（E81–E84 / D86–D89）。

### G1. 性能类（S2：设备名 + 分辨率 + 帧时间）

| # | 元素 | 原版值 / 基线（出处） | 我们的值（运行时读数） | 差值 |
|---|---|---|---|---|
| G1 | 渲染设备名 | 必须真 GPU（软渲染下帧时间无意义）—— `tools/env-check.ps1` 判据 `physical-gpu` | `dev=AMD Radeon RX 5700 XT devType=Direct3D12`（`AL3-evidence.txt` step0；真 GPU） | 0 |
| G2 | 分辨率 | 竖版 1080×1920（本表 A11/A12 口径） | `screen=1080x1920`；`Application.targetFrameRate=30`、`QualitySettings.vSyncCount=0`（step0） | 0 |
| G3 | 帧时间（有焦样本） | `targetFrameRate=30` ⇒ ≤33.3 ms/帧 | 有焦 `dtMs` = 32.36 / 34.00 / 34.21 / 34.39 / 34.69 / 33.76 / 32.96 ms（≈30 fps 上限） | 0 |

> ⚠️ **口径说明**：`AL3-evidence.txt` 出现的 **`dtMs=333.33`**（step3 / step13 / step19 / step31 / step38）是**编辑器窗口失焦时 Unity 后台节流**产生的样本，**不得用于帧率结论**；帧时间结论只取有焦样本（33~34 ms）。
> ⚠️ 未达 60 单位场景（存活单位峰值 7）⇒ 60 单位帧时间记为证据缺口（D89）。

### G2. 数值边界（D7：BGM 音量 0 / 1）

| # | 元素 | 原版值（出处） | 我们的值（运行时读数） | 差值 |
|---|---|---|---|---|
| G4 | BGM 音量档 = 0 | 设置档 0 ⇒ 静音但 BGM 仍在播（`SetVolume(BGM,0)` 生效） | 经生产链路 `Emit(Events.Settings.BgmVolumeRequest, 0f)` 后 `GetVolume(BGM)`：**0.37 → 0**；`bgm.currentClip` 仍 = `Battle/2min_loop_battle_01`（仍在播） | 0 |
| G5 | BGM 音量档 = 1 | 设置档 1 ⇒ 满音量 | `Emit(..., 1f)` 后 `GetVolume(BGM)` = **1** | 0 |
| G6 | 暂停时是否换曲 | 原版无对照数据（推断不换曲） | 进 Pause 前后 `bgm.currentClip` 均 = `Battle/2min_loop_battle_01`（不换曲），与 `BgmView.cs:148-150` `case Stations.Pause: break;` 一致 | 0（我方一致；原版无对照 ⇒ 同 C80） |

## H. AN1 采数折入（收口片 AO2，2026-09-22）

> 来源：`.ai-tmp/test/AN1-report.md` / `AN1-evidence.txt`（⛔ `.ai-tmp/screenshots/AN1.index.tsv` **曾存在**，已于 AO2 收口时随该片两份一次性文件**一并删除** —— 见下行"D 已删文件"声明；此行此前漏删了它，2026-09-23 由 lead 更正）
> （AN1 的一次性「对照表行 / 未判表」两份文件已由本片折入完成并删除 —— 其内容全部落在本段与 `策划/状态矩阵.tsv`；本段不引用已删文件）。
> ⛔ 本轮每行**差值 = 0**；非 0 / 不可比的行**不留在本表**，挪 `策划/验收表.md`「允许的差异」
> （D90 昵称显示 / D91 采幅 / D93 T3 未重采）。

### H1. B 段 —— 数值（D4：#14 ResultPanel 禁用；来源 AN1-evidence step52）

| # | 段 | 元素 | 原版值 / 期望（出处） | 我们的值（运行时读数） | 差值 |
|---|---|---|---|---|---|
| H1 | B | ResultPanel 结算态两按钮可点性 | 结算出现时「再来一局 / 回主菜单」均可点（`策划/状态矩阵.tsv` L486；`ResultPanel.cs:219-224`） | 自然结束的人机局结算页存活期同帧读数：`PlayAgainButton=interactable MainMenuButton=interactable outcome='失 败' crowns='0 : 2'`（AN1-evidence.txt step52 @10:20:12，`open.Result=True`；图 `.ai-tmp/screenshots/AN1-result.png`） | 0 |
| H2 | B | 玩家昵称 = `克洛弗`（服务端权威值） | 任务书 A 段（入口 = 登录后发 MsgSetNickname） | `server/logs/2026-09-22-all.log` 10:16:28 `拉取档案 player=p_cr_cli nickname="克洛弗"`；10:16:41 `设置昵称成功 player=p_cr_cli nickname="克洛弗"` | 0（服务端）；**客户端显示**不一致见 D90 |

### H2. D 段 —— 表现与证据新鲜度（S28/T3 单位动画 / 重采截图）

| # | 段 | 元素 | 原版值 / 期望（出处） | 我们的值（运行时读数） | 差值 |
|---|---|---|---|---|---|
| H3 | D | 画幅 / 分辨率（画布口径） | 竖版 1080×1920（本表 A11/A12、G2 口径） | `Screen=1920x1080`、`CanvasScaler.referenceResolution` 竖版 1080×1920（AN1 step0/step12） | 0（口径）；**重采图画幅** 1368×532（编辑器 Game 视图尺寸）为**非 0** ⇒ 见 D91 |
| H4 | D | evidence-freshness 6 对（截图必须新于被引用实现） | 截图 mtime > 实现文件 mtime（`tools/verify.ps1` evidence-freshness） | `T2-boot-splash.png` / `V6-boot-brand.png` / `Y2-mainmenu.png` / `AB2-hud.png` / `D1-arena.png` 共 5 张于 09-22 10:16:54~10:17:16 重采，均新于实现（`BootPanel.cs` 09-21 20:32:24 / `MainMenuPanel.cs` 09-21 22:21:29 / `HudPanel.cs` 09-21 22:21:40 / `ArenaView.cs` 09-21 11:15:38） | 0（5/6）；`T3-flow-before-after.png` 仍未重采 = **非 0** ⇒ 见 D93 |

### H3. B 段 —— 性能三件套（S2：设备名 + 分辨率 + 帧时间）

| # | 段 | 元素 | 原版值 / 基线（出处） | 我们的值（运行时读数） | 差值 |
|---|---|---|---|---|---|
| H5 | B | 渲染设备名 | 必须真 GPU（软渲染下帧时间无意义）—— `tools/env-check.ps1` 判据 `physical-gpu` | `dev=AMD Radeon RX 5700 XT`、`devType=Direct3D12`（AN1 沿用 AL3 step0 读数；真 GPU） | 0 |
| H6 | B | 分辨率 | 竖版 1080×1920（本表 A11/A12、G2 口径） | `Screen=1920x1080`（AN1 step0）；重采图画幅非 0 行见 D91 | 0 |
| H7 | B | 帧时间 | `Application.targetFrameRate=30` ⇒ ≤33.3 ms/帧（AN1 复读 `targetFrameRate=30`、`QualitySettings.vSyncCount=0`） | AN1 未重采帧时间（同一人机局，未再采），沿用 AL3 有焦样本 32.36 / 34.00 / 34.21 / 34.39 / 34.69 ms | 0 |

> ⚠️ **口径（沿用 G1，AN1 未变）**：`dtMs=333.33` 是**编辑器窗口失焦时 Unity 后台节流**的样本，
> **不可用于帧率结论**；帧时间结论只取有焦样本（33~34 ms）。
> ⚠️ 60 单位场景仍不可达（客户端对局存活单位峰值 7）⇒ 60 单位帧时间仍记证据缺口（D89）。

### H4. E 段 —— 允许的差异索引（本轮新增）

D90（客户端主菜单昵称显示 `玩家`，服务端为 `克洛弗`）/ D91（游戏视图采幅 1368×532）/ D92（D11 触屏·本项目不适用）/
D93（T3 单位动画 before/after 图未重采）/ D94（`360tray` 在跑·`env-check.ps1` 未跑）——
四要素全文见 `策划/验收表.md` §3 与 `策划/差异登记.tsv`。

## I. AT2 采数折入（收口片 AU1，2026-09-22）

### I1. D 段 —— 节奏：攻击段时长 ↔ `hit_speed_ms`（差值恒 0）
>取值口径：`AnimFps[attack] = 攻击段帧数 ÷ (hit_speed_ms ÷ 1000)`（`UnitView.FpsFor`）。
>原版值出处 = `server/game/table/tsv/unit.tsv` 的 `hit_speed_ms` 列（⚅ 任务书写的 `unit_cs.tsv` 在本工程不存在，
>实际文件名 `unit.tsv`、页签名 `unit_cs`）；攻击段帧数出处 = `策划/单位帧段表.md` §3（主套侧，裁决见 D99）。

| 行 | 单位（sprite_dir / unit.tsv key） | 原版值 = `hit_speed_ms`（出处） | 攻击段帧数（出处） | 攻击段帧率（我们） | 攻击段时长（我们） | 差值 |
|---|---|---|---|---|---|---|
| D-rhythm-knight | `chr_knight_out` / `knight` | 1200 ms（`unit.tsv:2`） | 135（§3 `chr_knight` attack：主套 = `108-115 ∪ 359-485`） | 135 ÷ 1.2 = **112.50 fps** | **1.200 s** | **0.000 s** |
| D-rhythm-archer | `chr_archer_out` / `archers` | 900 ms（`unit.tsv:3`） | 54（§3 `chr_archer` attack = `182-230 ∪ 247-251`） | 54 ÷ 0.9 = **60.00 fps** | **0.900 s** | **0.000 s** |
| D-rhythm-musketeer | `chr_musketeer_out` / `musketeer` | 1000 ms（`unit.tsv:23`） | 99（§3 主套 = `117-125 ∪ 324-413`） | 99 ÷ 1.0 = **99.00 fps** | **1.000 s** | **0.000 s** |
| D-rhythm-minion | `chr_minion_out` / `minions` | 1000 ms（`unit.tsv:11`） | 63（§3 `chr_minion` attack = `144-206`） | 63 ÷ 1.0 = **63.00 fps** | **1.000 s** | **0.000 s** |
| D-rhythm-bowler | `chr_bowler_out` / `bowler` | 2500 ms（`unit.tsv:38`） | 117（§3 主套） | 117 ÷ 2.5 = **46.80 fps** | **2.500 s** | **0.000 s** |
| D-rhythm-mortar | `building_mortar_out` / `mortar`（建筑） | 5000 ms（`unit.tsv:51`） | 5（§3 `building_mortar` attack = `5-9`） | 5 ÷ 5.0 = **1.00 fps** | **5.000 s** | **0.000 s** |

差值恒 0 是构造性的（帧率由 `hit_speed_ms` 反推），本表真正的内容是**帧数出处**。
未覆盖（如实）：`attack=Unknown` 的 61 格 ⇒ **本行不成立**（D96）；
`unit.tsv` 查不到 `hit_speed_ms` 的 9 个目录 ⇒ **D 段无原版值可对**（D98）。

### I2. H2 补充 —— T3/S28 证据新鲜度（AU1 重采）
`T3-flow-before-after.png` 已于 **2026-09-22 11:59:46** 在一场真实对局里重采（`FlowProbe.SampleFlowAfter` → `tools/probes/plot_flow.py`）：
AFTER ARTWORK HOP **max=0.000 px / rms=0.000 px / 0×601 帧**（BEFORE max=9.299 px / rms=2.251 px / 191×889），新于 `UnitView.cs`(11:14:15) 与 `UnitAnimTable.cs`(11:29:57) ⇒ `evidence-freshness` 该对已清（D93 可标消除）。

## J. AV1 采数折入（收口片 AW2，2026-09-22）

### J1. D 段 —— 节奏 / 交互反馈：单位攻击档（`anim=2`）在实时对局里可达且可见（差值 0）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| D14 | 单位**攻击档（anim=2）**在实时对局里是否可达且在画面上可见 | 原版：单位攻击时播 attack 段 | **可达且可见**：真实服务端 + 真实客户端，320 次逐帧 census 里 **34 次**快照含 `anim==2`（红方 AI 单位 49 次、蓝方玩家单位 5 次），`UnitView.Anim` 与快照 `anim` 逐帧相等（三次 census 的 `SNAP`/`VIEW` 合计完全一致：`19/636/73/0`）；实时抓到的 attack 帧 = `chr_pekka_out` frameNo **340** ∈ attack 段 `[333,395]`（`UnitView.cs:271` 映射，段表出处 `UnitAnimTable.cs`）⇒ `inTierRange=True`；实时图 `.ai-tmp/screenshots/AV1-knight-3tiers-live.png` | 0 |
| D15 | attack 档的播放速率口径 = 攻击段像素帧数 ÷ `hit_speed_ms` | 11.7 / 10.0 / 5.6 fps（`策划/单位帧段表.md` §4.3，同 D7） | 同（`UnitView.FpsFor`：`clip.Count / (HitSpeedMs/1000)`；pekka 实测 72 帧 ÷ 1.8 s = **40.0 fps**，与该式一致） | 0.0% |
| D16 | anim 通道是否被"同 tick 后写"冲掉 | 原版无此概念（本项目实现口径） | **不成立**：`Step()` 里 `stepAttacks`（`battle.go:170`，设 `AnimAttack`）在前、`stepMovement`（`battle.go:173`）在后，但对**已在攻击距离内**的单位 `stepMovement` 走 `battle.go:510-515` 的 `continue` **不写档位** ⇒ 攻击档不被 idle/walk 覆盖。反证见 `.ai-tmp/test/AV1-report.md` §3 | 0 |

### J2. 未折入 D 表的行（差值非 0 / 未量到 ⇒ 归「允许的差异」或既有条目）

- **AV1-D1** ⇒ 已登记为 **D103**：`die` 档（`AnimDie=3`）在运行时永远到不了客户端（详见 `策划/差异登记.tsv` 的 D103 与 `策划/验收表.md §3`）。
- **AV1-D2**：玩家方（蓝）单位在观测窗内进入攻击档的次数远少于 AI 方（三次实时 census 蓝 5 / 0 / 0 vs 红 49 / 24 / 24）；离线同配置蓝红都大量进入（16990 / 11956）⇒ **属采数条件（玩家侧单位少、活不到近战距离），不是 anim 通道缺陷，不作为 1:1 对照差异登记**；本片已用"真的进到 anim=2 的单位"补齐 attack 行证据。
- **AV1-D3**：knight 的 attack 段 `Runs={108,8,359,127}`（`UnitAnimTable.cs:257`）包含 idle 段帧 108–115 ⇒ `Count=135`、fps = 135/1.2 = **112.5**；归入既有 **D101**（按 AT1 新口径重生成 `UnitAnimTable.cs` 时一并核对 attack 段是否含 idle run），**不新开号**。

## K. AW1 采数折入（收口片 AX1，2026-09-22）

> 来源 `.ai-tmp/test/AW1-对照表行.md`（AW1 片产物，本片折入后删除该一次性文件）。判据类别：**数值类**（`anim=3` 下发/消费口径）+ 一格 **表现类**辅助（`.ai-tmp/screenshots/AW1-die-sheet.png`）。

### K1. D 段 —— 状态表现：单位死亡档（`anim=3`）到得了客户端（差值 0）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| D17 | 单位死亡档 `anim=3` 能否到客户端 | 原版：单位死亡要播**死亡动画**再消失（原版逐帧素材 `原版资源/sc/*_v215.sc` 的 die clip） | **通道已通**：服务端每例死亡都在**它死掉那一 tick 生成的快照里**带一次 `anim=3` + `hp=0`（`server/game/core/snapshot.go` `Snapshot()` 置 `deathReported` / `battle.go` `compact()` 只回收已下发过的尸体）；客户端 `UnitView.Apply` 收到 `anim==3` 即换 die 档、`AdvanceEnd()` 停末帧（`client/Assets/Scripts/View/UnitView.cs:271-282` / `:440-452`，AW1 未改一行） | 实测：离线整局 3000 快照帧 `die=104`（**修前 0**）、实时一局 420 实收帧 `die=10` 且 `viewDie=10` ⇒ `.ai-tmp/test/AW1-census.tsv`（脚本 `.ai-tmp/test/AW1-live.ps1`）；回归用例 `TestSnapshotCarriesDeathFrame` 进生产树（`server/game/core/battle_test.go`） | **0**（`anim=3` 下发 ✔ / 客户端消费 ✔） |

### K2. 未折入 D 表的行（差值非 0 / 未量到 ⇒ 归「允许的差异」或既有条目）

- **AW1-E1（画面差 = 未取到）** ⇒ 归既有 **D50**：die **帧区间**未找到 ⇒ `UnitAnimTable` 该档 `Known=false` ⇒ 尸体按整目录回落播（只影响「播哪几帧」，不影响 K1 判的「下发/消费通道」）。**未消除**。
- **AW1-E2（尸体制存期 = 「等一次快照带走」）** ⇒ 实现口径折中（服务端不知客户端 die clip 多长，凭空写毫秒数违反「写不出出处的量不许进工程」），**不新开号**；要「尸体播满整段 die clip」须先清 D50 或授权改 `BattleViewRoot.cs` 的回收判据。

## L. BC1 采数折入（收口片 BE1，2026-09-22）

> 来源 `.ai-tmp/test/BC1-对照表行.md`（BC1 片产物，本片折入后**删除**该一次性文件）+ `.ai-tmp/test/BC1-report.md` / `BC1-play-evidence.txt`。
> ⛔ 本轮每行**差值 = 0**；非 0 / 未量到的行**不留在本表**，挪 `策划/验收表.md`「允许的差异」（D96 / D97 / D98 / D101）。

### L1. C 段 —— 素材（单位逐帧动画档位覆盖 + `die` 命名穷举）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| C85 | 单位逐帧动画·**档位覆盖** | APK 解包 `sc/chr_*_out`：`idle/walk/attack` 三档在 45 个角色目录里可解析（`.sc` 的 `0x0C` id 数组按 **`sid → shape 记录序`** 映射 + 递归展开嵌套 movieclip；出处 `策划/单位帧段表.md` §3 + §0.9，重出命令 `python tools/probes/sc-at1-resolve.py --root . --write-docs`） | `CR.View.UnitAnimTable`：**54 目录 / 149 档**（idle 49 / walk 48 / attack 50 / die 2）。AT1 新恢复目录示例：`chr_barbarian` idle45/walk360/attack495、`chr_giant` idle9/walk144/attack90（旧表这两处 walk/attack = Unknown ⇒ 整目录循环） | 0 |
| C86 | 单位逐帧动画·**`die` 档** | 原版 89 个 `.sc` 的 5341 条 export 全量穷举：`die`/`dead`/`fall`/`collapse`/`crumble`/`ko`/`vanish`/`sink`/`死/亡/倒/塌/爆/消失` **0 命中**；`death` 20 条全落 `effects`（死亡特效）/`ui`（图标）/`chr_zap_machine`（轮子零件，看图确认） | 单位目录 `die` 一律 `Unknown` ⇒ 走「该档位不可用 ⇒ 整目录循环播 + 每目录一次 `Warn`」；表内 `die=2` 均为非单位目录（`effects_out` 死亡特效 / `ui_out` 图标） | 0 |

### L2. D 段 —— 节奏（攻击段时长 ↔ `hit_speed_ms`；帧号 → 扁平化下标）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| D18 | 攻击段时长 ↔ `hit_speed_ms` | 原版以 `hit_speed_ms` 标定攻击段（`server/game/table/tsv/unit.tsv`） | `attackFps = attackCount / (hitSpeedMs/1000)` ⇒ 攻击段播完一遍 == `hit_speed_ms`。运行时自洽断言（BC1 Play 实测）：`chr_barbarian` 495/(1300/1000)=380.77fps → 1.300s vs 1.300s（**+0.0%**）；`chr_giant` 90/(1500/1000)=60.00fps → 1.500s vs 1.500s（**+0.0%**）（日志 `.ai-tmp/test/BC1-unitview-logs.txt`） | 0 |
| D19 | 帧号 → 扁平化下标 | 原版按逐帧锚点表定位（本工程无该表） | `.sc` 的 shape 记录序 **必须按帧号查表**（⛔ 不假设恒等）：实测 `chr_giant_out` 658 个 PNG / `map.Length=468`（f2→idx6、f3→idx7、f4→idx12…）、`chr_barbarian_out` 1043/1029（f40→idx41…）⇒ 经 `SpriteBank.FrameNumberMap` 逐帧解析，解析不出的帧**跳过而不是顶替** | 0 |

> 说明（BC1）：本段只改 `client/Assets/Scripts/View/UnitAnimTable.cs`（数据整文件重出，`UnitView.cs` 未改）；
> 「旧 45/108 → 新 54/149」的逐档位计数 = `python .ai-tmp/test/BC1-count.py`（机械数 `Known = true`）。
> 与任务书目标 56/152 的 **2 目录 / 3 档**差额 = AT1 tsv 的 3 个假阳性 / 无档位格（明细 `BC1-report.md` §2.2），⛔ 未硬填。

## M. AM2 采数折入（收口片 AN1，2026-09-22）

> 来源 `.ai-tmp/test/AM2-对照表行.md`（本片折入后**已删除**该一次性文件）+ `.ai-tmp/test/AM2-允许差异.md`（**保留**，= D105–D108 的帧号级明细）+ `策划/自审对比/AM2-自审.md`。
> ⛔ 本段每行**差值 = 0**；差值非 0 / 未量到的行**不留在本表**，已挪「允许的差异」= **D105**（原版无此部件·按契约保留）/ **D106**（色彩与字号容差）/ **D107**（弹窗规模）/ **D108**（按钮光泽）。
> 取证：`.ai-tmp/screenshots/AM2-settings-mine.png`（实机 1080×1920）、`AM2-settings-compare.png`（左 = 基线 `24_设置_499x1080` ×2.164 / 右 = 我们，**同机位并排**）、`AM2-mine-panel.png`（面板区放大选帧依据）；判据 `.ai-tmp/test/AM2-探针.txt`（节点树 `sliders=3 buttons(active)=4 offCanvasNodes=0`）。

### M1. C 段 —— 素材（设置面板 8 件全是原版帧）

| # | 元素 | 原版帧（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| C-AM2-1 | 弹窗外框 / 标题带 / 滑条轨道 | `ui_out` 014（96×95，左上圆角件，均值 (96,102,119)）+ 四角镜像拼九宫格，border=(24,24,24,24) / (10,10,10,10) | `CrUiStyle.PopupFrameSlate` = `ResPaths.UiFrame(Panels, ui_out, 14)` | 0 |
| C-AM2-2 | 弹窗体 / 滑条手柄 | `ui_out` 019（21×21，均值 (216,230,236)），border=(10,10,10,10) | `CrUiStyle.PopupSkinLight` = `UiFrame(Panels, ui_out, 19)` | 0 |
| C-AM2-3 | 绿按钮底（全屏「开」/ 滑条填充） | `ui_out` 610（69×69，(55,208,77)），border=(14,14,14,14)，`Image.type=Sliced` | `CrUiStyle.ButtonGreen` = `UiFrame(Buttons, ui_out, 610)` | 0 |
| C-AM2-4 | 红按钮底（关闭底 / 全屏「关」） | `ui_out` 477（69×69，(235,65,68)），border=(14,14,14,14) | `CrUiStyle.ButtonRed` = `UiFrame(Buttons, ui_out, 477)` | 0 |
| C-AM2-5 | 蓝按钮底（画质 ◀ ▶） | `ui_out` 165（38×40，(40,123,201)）四角镜像拼，border=(14,14,14,14) | `CrUiStyle.ButtonBlue` = `UiFrame(Buttons, ui_out, 165)` | 0 |
| C-AM2-6 | ▶ / ◀ 三角 | `ui_out` 170（`HudPauseIconPlay`，白三角） | `ResPaths.HudPauseIconPlay`（◀ = 同一图元 `localScale.x=-1`） | 0 |
| C-AM2-7 | 关闭的 X | `ui_out` 164（`IconQuitCross`，白 X） | `ResPaths.IconQuitCross` | 0 |
| C-AM2-8 | **已删除**的旧选帧 | `ui_out` 806 `PanelPaper`（米色纸）/ `ui_out` 069 `TitleBarGold`（棕木色）/ `loading_out` 015（绿色加载读条）/ `ui_out` 011 `SlotCorner`（卡槽角块） | 本面板**不再引用**（旧帧仍在 `ResPaths` 里、别的面板还在用，**本片不删**） | 0 |

### M2. D 段 —— 字体 / 颜色 / 几何（差值 0 的行）

| # | 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|---|
| D-AM2-3 | 按钮上的字 | 白 (255,255,255)（绿 ON / 红 Off / 蓝按钮上都是白粗体） | `Color.white` | 0 |
| D-AM2-7 | 标题带高 | 27px@499 ⇒ **58**（`24_设置` x=250 列扫 y=168..194） | `CrUiStyle.PopupTitleH = 58` | 0 |
| D-AM2-8 | 控件高 / 行节距 | 30px@499 / 63px@499 ⇒ **65 / 136** | `CrUiStyle.PopupControlH = 65` / `PopupRowPitch = 136` | 0 |

### M3. 未折入 D 表的行（差值非 0 / 未量到 ⇒ 归「允许的差异」）

- D-AM2-1 标题字号 48 vs 基线折算 49.5（**−1.5px**）/ D-AM2-2 亮面标签字色**每通道 ≤13** / D-AM2-4 面板体底色 **≤9** / D-AM2-5 标题带底色 **≤4** / D-AM2-6 标签字号**基线未量到** ⇒ 统一登记 **D106**（⛔ 未量到的一律不写成「一致」）。
- AM2 自审第 13 项：弹窗高 **798** vs 基线折算 **1601**（契约固定 5 行功能项）⇒ **D107**。
- AM2 自审第 15 项：按钮光泽 = 四角对称**平面**件 vs 原版键顶/键底**立体键** ⇒ **D108**。
- AM2 自审第 7/9/10/11/12 项：**原版无此部件**（音量滑条轨道 / 手柄 / 百分比数字 + 画质 ◀▶ 箭头 / 档位文字）⇒ **D105**（按 S23 契约保留，5 件已全换原版帧）。

### M4. 未折入的行（**真缺陷**，⛔ 不作差值 0 折入）—— AM1 片

- **AM1-D1｜主菜单「卡组编辑」按钮点击无反应**（判据只有一条：点击后确实进入卡组编辑界面 ⇒ **未达**）：实测（14:55:51 Play）`MainMenuPanel` 已 `Emit(Deck.OpenRequest)`、点击音已播，但 `listeners[Deck.OpenRequest]=0` ⇒ `deckOpen after=False`（面板 `Get<>()==null`）；根因 `client/Assets/Scripts/Module/Deck/DeckManager.cs:62/89`（裸 bool 判订阅幂等 + 引擎每轮 Launch 换 `EventBus` + 本工程关闭域重载）。
  **内建对照**（排除「整条点击通道坏了」）：同一面板同一通道的 `SettingsButton` ⇒ `settingsOpen=True`（同期 `listeners[Flow.OpenSettingsRequest]=1`）。
  **处置**：这是**未消除的真缺陷**，⛔ 不折成差值 0、⛔ 不新开差异号（AM1b 并行片正在落地「已实机验证成立」的补丁：14:58:12 同条件 `listeners=1` / `deckOpen after=True` / 截图 `.ai-tmp/screenshots/AM1-deckedit-open.png`）⇒ **已由 AO2 收口片（2026-09-22）编号为 `D109` 并改判「已消除（AM1b 修复）」**（补丁已落进树内：`DeckManager.cs:62` / `:86-112` 按 `Game.Event` 对象标识判幂等）—— 见 §N2。来源 `.ai-tmp/test/AM1-对照表行.md` / `AM1-report.md` §7（本片折入后删除该一次性文件）。
## N. AM1b 采数折入（收口片 AO2，2026-09-22）

> 来源 `.ai-tmp/test/AM1b-对照表行.md`（AM1b 片产物，本片折入后**删除**该一次性文件）+ `.ai-tmp/test/AM1b-report.md`（**保留**）。
> ⛔ 本轮每行**差值 = 0**；非 0 / 未量到的行**不留在本表** ⇒ 见 N2（已编号 **D109** 并改判「已消除」）。

### N1. D 段 —— 交互反馈（主菜单「卡组编辑」入口）

| # | 元素 | 原版值（出处） | 我们的值（运行时读数） | 差值 |
|---|---|---|---|---|
| N1 | 主菜单「卡组编辑」按钮的点击反馈 | 原版《皇室战争》主菜单**无此按钮**（比较对象退化 ⇒ 按本项目界面自定；按钮外观 = A 原版 `ButtonDarkGrey` 九宫格 + `button_click_02` 点击音，与 C 段 / AH2 同源）。可判的交互口径只有一条：**点击后确实进入卡组编辑界面** | **已达成**：**同域第 2 次 Play**（用户报障的触发条件）实测 `listeners[Deck.OpenRequest]=1` / `deckOpen=True` / `DeckBox scr=(40,235)-(1040,1753) size=1000x1518 active=True`（15:23:47 / 15:24:00，`.ai-tmp/test/AM1b-report.md` §3）；表现类图 = `AM1b-deckedit-open.png`（在 `.ai-tmp/screenshots/` 下，1,305,707 B，15:24:20，1080×1920；⚠️ **该图当前已过期**：另一并行片 **AO1** 于 2026-09-22 **15:31:42** 改写 `DeckEditPanel.cs`（mtime 晚于该图 15:24:20）⇒ 按 `evidence-freshness` / `freeze-before-capture` 判据该图不再代表当前实现，须由卡组域 owner 重采；本片是**纯离线记账片**（⛔ 不调 `unity`、不改业务代码），**不重采**，故此处不把它写成「当前有效证据」) | **0**（断言口径） |
| N2 | 对照组（同一次测量的内建对照，排除「整条点击通道坏了」） | 同一面板同一通道的另一颗按钮 | `SettingsButton` 走**完全相同**的 `CrUiStyle.ActionButton` 通道与射线路径 ⇒ `settingsOpen=True`（同期 `listeners[Flow.OpenSettingsRequest]=1`） | 0（对照成立） |

### N2. 未折入的行（**真缺陷** → 已编号并改判）

- **AM1-D1（= §M4 那条）** ⇒ **已编号 `D109`**（`策划/差异登记.tsv` / `策划/验收表.md` §3）：编辑器里**第 2 次及以后 Play，主菜单点「卡组编辑」无反应**（`Deck.OpenRequest` 监听者 = 0）；根因 `client/Assets/Scripts/Module/Deck/DeckManager.cs:62/:89`（裸 bool 判订阅幂等 × 引擎每轮 Launch 新建 `EventBus` × 本工程关闭域重载 ⇒ 静态量跨 Play 存活）。
  **状态 = 已消除（AM1b 修复，2026-09-22）**：改为按 `Game.Event` **对象标识**判幂等（同 `RoomManager.cs:146` / `BattleUiHost.cs:93` / `BgmView.cs:102` 口径）；判据 = 第 2 次 Play 的 `listeners=1` / `deckOpen=True` / `DeckBox 1000x1518` + L3 日志 `[Deck] 检测到新的事件总线（引擎重新 Launch），卡组模块重新装载订阅`。
  ⛔ 该缺陷**不作为差值 0 折入**（它是真缺陷、已消除，不是「原版无此部件」类允许差异）。
- **AM1b 的「E85」建议行**（上一片建议把该缺陷登记为允许差异 `E85`）**作废**：缺陷已在树内消除 ⇒ 本表 E 段**不新增 `E85`**，`策划/差异登记.tsv` 也不再把它登记为允许差异。

