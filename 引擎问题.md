# clover-project-cr · 引擎问题

> 记录本项目在引擎侧遇到的**缺口 / bug**，以及 skill 说不清的地方。
> 三段固定：`## 引擎缺口` / `## 引擎 bug` / `## skill 问题`；每行 `现象 | 最小复现 | 影响 | 我们的绕法 | 建议`。
> ⛔ 本项目不改引擎源码（`clover-client-unity-engine/` / `clover-server-engine/`）；有缺口一律在这里登记 + 用绕法把活做完。

## 引擎缺口

| 现象 | 最小复现 | 影响 | 我们的绕法 | 建议 |
| --- | --- | --- | --- | --- |
| `SnapshotInterpolator` 的公开面只能读"正在渲染那一对"的载荷（`WindowStartPayload` / `WindowEndPayload`，`clover-client-unity-engine/Runtime/Presentation/SnapshotInterpolator.cs:381`、`:384`）与 `HistoryLength`（`:350`），**取不到任意历史槽的载荷**；而 `View/BattleViewRoot.cs:1891-1898` 要用"环里最旧的那个含该 id 的快照"当朝向基线（5 个间隔 = 500 ms，是 `UnitView` 去向档的输入；取 1 个间隔时实测朝向抽搐，见 `:1872-1882`） | `var it = new SnapshotInterpolator(SnapshotInterpolatorOptions.Default()); it.Push(0f, a); it.Push(100f, b); it.Push(200f, c);` 之后没有任何入口能取到下标 0（最旧）那份载荷 | 战斗插值的**时间轴**（时钟推进 / 速率律 / 窗口选择 / 首帧对齐 / 两条上限）与引擎件逐字一致、可下沉；但**六槽快照历史的载荷环**无法下沉 ⇒ 按"历史槽存储交给引擎"的切换口径**不可等价切换**（逐条对照见 `View/BattleViewRoot.cs:378-606` 与 `:1622-1842`） | 不切换：`BattleViewRoot` 自持渲染时钟与六槽历史 | 公开"按历史槽读载荷"：`int HistoryStartIndex` + `object PayloadAt(int slotFromOldest)`（或"离最新 n 个间隔取载荷"的 `PayloadAtIntervalsBehind(int n)`） |
| 引擎件的"只报一次"告警按**实例终身**记账（`Runtime/Presentation/SnapshotInterpolator.cs:314`、`:489`、`:498`、`:602`、`:635`），且 `Reset()` 明确不重置它们（`:694-699`）；`View/BattleViewRoot.cs` 的对应告警是**每局各报一次**（`_timeWarned` 在 `Build()` 里复位，`:801`），或"回到正常范围后复位、下次再报"（`_outOfWindowWarned` `:1837`、`_clockLeadCapped` `:1807`） | 同一实例 `Reset()` 之后再 `Push(-1f, payload)` ⇒ 不再产生 `push.noTimestamp` 告警；而 `BattleViewRoot` 在新一局 `Build()` 后会再报一次 | 切换后"无时间戳 / 乱序 / 时钟超前上限"三类非预期分支从第 2 局起不再留痕（"掉窗"可用 `OutOfWindow` 属性自记账绕掉） | 不切换：保留 cr 自己的 `_timeWarned` / `_outOfWindowWarned` / `_clockLeadCapped` | `Reset()` 增加"同时重置 WarnOnce 记账"的可选参数，或暴露 `bool ClockLeadCapped` 这类只读状态位 |
| 未就绪时 `WindowStartPayload` / `WindowEndPayload` 为 `null`（`Runtime/Presentation/SnapshotInterpolator.cs:710-713`）；而 `View/BattleViewRoot.cs:354-357` 的 `_prevIndex` / `_curIndex` 恒为非空字典，调用点直接 `foreach`（`:1846`、`:1588`）与 `TryGetValue`（`:1887`） | 任何快照到达之前读 `SnapshotInterpolator.WindowEndPayload` ⇒ `null` | 机械转发会让 `ApplyEntities` / `CasterWorld` 抛 NRE（离散事件可能早于首帧快照到达）；不改表现，属转发时必须补的 null 守卫 | 不切换 | 公开面注明"未就绪时 payload 为 null"，或提供只读空集合兜底 |

## 引擎 bug

| 现象 | 最小复现 | 影响 | 我们的绕法 | 建议 |
| --- | --- | --- | --- | --- |
| 按角色 ID 寻址的下行推送**全部静默丢失**（回包正常、`PushToPlayer` 返回 nil、网关无任何错误日志）：网关日志里缺 `added extra idIndex key p_<角色ID> for conn c-X` 的那些会话，之后每一条 `PushToPlayer(角色ID, …)` 都推不到该客户端；本机 2026-09-25 全天 25 个会话中，推送失败的会话与缺这行索引的会话**一一对应**（例：`bound conn c-18 -> cr_cli (reconnect=true)` 之后 14:41:53~14:43:29 连开 5 局、客户端一局都没收到 `PushBattleStart`；对照 `14:50:15 bound conn c-3` 之后紧接的 `added extra idIndex key p_cr_cli for conn c-3` 则两局都通） | 客户端反复重连 / 重启后登录（本机 `unity command editor_play` 多次进出 Play 即可）：登录成功、`auth: login ok`、业务回包全部正常，但收不到服务端推送；服务端日志出现 `bound conn c-X -> cr_cli (reconnect=true)` 而**没有**同 conn 的 `added extra idIndex key …` | 以角色 ID 为 target 的一切下行（业务推送、引擎自动实体同步、PlayerFullSync）在该会话里全丢；`onNotify` 在 `defaultRouter` 找不到路由时**不推也不报错**（只在 `sendByDeliveryMode` 失败时打 Errorf）⇒ 完全静默，只能靠网关 DEBUG 行的有无来区分 | 已在本工程侧绕开：`server/game/logic/player.go` 新增 `pushTarget`（角色 ID → 账号，即去掉 `p_` 前缀），8 个推送点（`battle.go:126/222/231/259`、`room.go:832/842/866`、`logic.go:100` 的 room.Module `Pusher`）全部改按**账号维度**寻址 —— 账号索引由网关在每条登录回包上重建（`bound conn` 行每次登录都有），不依赖角色索引 | 根因在 `Ctx.SetPlayerID`（`internal/transport/event/ctx.go:181-208`）：它**先**写 `connBag["player_id"]`、**后**判下行链路（`c.bag.ctlPub == nil || ctlSubject == ""` 时静默跳过发布），而后续每次调用又因 `ConnValue("player_id")` 已等于该值而早退（`:187`）⇒ 只要有一次调用落在"链路未就绪 / 会话尚未在网关建立"的窗口里，这条连接就**永远不会**补发 GWControlBind。建议任选其一：① 未成功发布时不写 connBag；② `defaultRouter`（`gwcore/session.go:1025`）在 `"p:"` 索引命中但会话已断（或为空）时回落 `"a:"`；③ 会话绑定 owner 成功时无条件重发一次该连接的 GWControlBind |

## skill 问题

| 现象 | 最小复现 | 影响 | 我们的绕法 | 建议 |
| --- | --- | --- | --- | --- |
| （无） | — | — | — | — |
