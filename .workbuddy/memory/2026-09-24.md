# 2026-09-24 工作日志

## D151 第三片 / D155：卡组编辑页「外观复刻 + 拖动判据修活」（离线，⛔ 未开 Unity）

### 用户原话
> `UI呢？？？？？ 你的UI都不是原版UI啊。 什么玩意就需要开unity啊！！！！！很多都有问题，你他妈的编辑卡组到底能不能拖动还不知道呢？ 接着处理！！！！！！！！！！！！！！！！！！`

### 做完的（都可离线复跑）

1. **拖动判据修活（第四条负控原来是死的）** —— 离线拖放路径审计
   - 现象：负控 `slotsnap` 注入 `s = 60px` 时 **P7 全绿** ⇒ 判据是死的。
   - 根因（算出来的）：判据 ③ 只喂**格心**，而「格心 → 自己」的灵敏度上限 = `padX = CardW·½·1.35 = 138.4`
     ⇒ 任何 < 138.4 的偏移都测不出。
   - 修法：新增判据 **③b「卡面 5×5 采样喂进落位判定必须返回该格」**（灵敏度 ≈ s > 56.4 必红）
     + 负控注入改成「列原点偏半格（0.5×CardStepX）」。现况 **四条负控全红 / 正控 P1–P7 全绿**。
   - ⛔ 教训：**判据太弱 ≠ 注入形式不对**；先算灵敏度再改注入。

2. **页签底从「纯色方块」→「原版图元九宫格 + 实测 tint」** —— `DeckEditPanel.BuildTab()`
   - 底 = `CrUiStyle.Skin(..., ButtonBlue = ui_out 166, BlueCorner = 19, tint = TabOnTint/TabOffTint)`
     （`MakeRounded` 四角镜像，一个像素都不是自造的）；顶线**两态分开**：选中 = 亮线 3.5 `#21C2E3`（E66）、
     未选中 = 暗带 10.4 `#031A4A`（E67）。
   - 订正两处旧读数：`TabEdgeH` 6 → **3.5**；`TabOnEdgeColor` `(48,148,210)` → **(33,194,227)**（旧值取的是含渐变的中位段）。

3. **四条新离线判据资产**（`tools/probes/`）
   - `d155-tab-pieces.py`：逐像素证明 266/267 == 166 内侧行尾段（Δ=5），而 **265 是独立件**（1×36 竖向渐变条，Δ=58）。
   - `d155-tab-gradient.py`：**反向断言** —— 证伪「用 265 铺页签竖向渐变」（265 剖面有"膝"，逐行 tint 比值 0.51~1.24 摆动）。
   - `d155-colorshift.py`：三处部位 G/B 比值一律 ≤ 0.90 ⇒ 参考图与图集**同向**色偏 ⇒ 「原版图元 + 实测 tint」口径成立（反控 1.000）。
   - `d155-tabband-cmp.py`：页签带并排图。
   - `d151-layout-preview.py` 增 `mirror9()`（按 `MakeRounded` + `Image.Type.Sliced` 逐行同口径复刻）。

4. **登记**：`几何量取.md` **§1.6.8 E65–E70**；`差异登记.tsv` **D153 / D154 / D155**（D153/D154 是上一轮计划了但**没写进去**的，本轮补上）
   + 给 D146/D151 加历史指针；`验收表.md` S7 行追加第三片小结 + **verify 的 3 条 FAIL 成因与消除条件**。

5. **回归**：`d151-band-diff.py` **14/14 PASS**；编译闸门 **0 警告 0 错误** + 负控能红（`CS1503`）。

### 残留差异（如实登记，⛔ 不写"一致"）
- 页签圆角 **19@1080 = 21.9@1242** vs 实测 **25@1242** ⇒ 小 3.1px（源帧 166 圆弧只有 ≈13px）。
- 页签底**无竖向渐变**（单色 = 均值 `#217CC1`）；原版由 265 提供，但 265 剖面与参考图不符 ⇒ 不采用。
- **顶区背景纹理**（原版斜纹深蓝）未落地：`UI_menu_background`（export #377）的 `frame_NNN` 是 `—`，定位不到。

### 坑（已写进 skill）
- ⛔ **`client/Temp/bin/Debug/` 不是纯临时目录**：里面还有 Unity/包编的程序集（`UnityEngine.UI.dll` /
  `UnityEditor.UI.dll` / `*TestRunner.dll`）。本轮 `rm -rf` 后闸门报 4 条 `CS0006`。恢复（**不需要开 Unity**）：
  `cd client && mkdir -p Temp/bin/Debug && cp -p Library/ScriptAssemblies/*.dll Library/ScriptAssemblies/*.pdb Temp/bin/Debug/`
  ⇒ 要**整目录拷**（115 个 dll），⛔ 不是只拷 `CloverEngine.*`。已补进
  `clover-engine/reference/fast-compile-loop.md` 的「编译循环坑表」第 7 条。

### 仍需开 Unity 才能做的（⛔ 不假称完成）
- 实机截图复采（`verify.ps1` 的 `evidence-freshness` 2 条 + `freeze-before-capture` 9 条因此转红）。
- 机器级 A1/A2 复采（D149/D150 末句）；运行时 `DeckEditPanel.VerifyDragPath()`。
